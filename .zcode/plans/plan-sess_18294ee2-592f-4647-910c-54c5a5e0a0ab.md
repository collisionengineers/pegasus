# EPIC-011 continuation — resume the UI replacement programme from the dead Claude session

Historical session evidence. This plan is retired from active execution; do
not recreate its worktrees, rerun its merges or treat it as release authority.
The owning v1 stream plans and current Kanmer tickets govern current work.
Configuration evidence does not establish a fresh process restart.

## Situation (verified)

Merged to `dev` already: Wave-0 docs (#582–#587), CASE-024 (#581), the shell/design-system/routes **PLAT-029 (#589)**, and AUTO-011 AI job ledger (#590). Local `dev` also carries two user commits (`99316c3c` zcode folder, `14a120e8` merge).

Open PRs (all target `dev`):
- **#591 PLAT-048** and **#592 TICK-061** — reviewed, all CI green, never merged (session died first).
- **#593 KANMER-005** — unit + sql-integration (1) red, **but the agent already pushed the fix** (`a8aec269` "seed the retained holder kind with the raw-SQL lease fixture" at 11:36, after the red run started); was told to wait for #592 before merging dev (migration/EF-snapshot order); checklist 10/11 (review's rollout-sentence item outstanding).
- **#594 TICK-058** — unit + sql-integration (1) red on head `b5b6e096`; review never finished; checklist 7/8.
- **#595 ENG-026** — sql-integration (1) + sql-integration-coverage red (likely the same bootstrap/migration-census class AUTO-011 hit); review never finished; checklist 0/10.
- **#588 UIIMP-005** — CONFLICTING with dev (post-#589); head `172e520e` is a commit **you pushed manually at 12:04**; CI reports no checks on it; the snapshot regen + verify loop was mid-run when the session died.

Wave-2 recovery: `task/case-025-cases-queues`, `task/mail-025-inbox-port`, `task/uiimp-008-work-centre` are pushed with **no PR** (agents died mid-ticket; tickets sit in `preparing`, never `take_ticket`-claimed). **CASE-012 has nothing pushed** — start fresh. Machine note: Claude ran as user `PC`; none of its worktrees exist here — everything is rebuilt from origin at the recorded paths.

Board: EPIC-011 = 43 tickets (25 backlog / 5 preparing / 2 implementing / 3 review / 8 verifying). MCP is correctly rooted at `.worktrees/kanmer`.

## Execution plan

### Phase 0 — environment recovery
1. `git fetch origin`; confirm local `dev` == `origin/dev` (if the zcode-folder commits are unpushed, surface to you rather than pushing myself).
2. Re-materialize worktrees at the recorded board paths from their remote branches (`git worktree add ../pegasus-worktrees/<slug> <branch>`) for the six in-flight tickets — this reuses the recorded root, not a second worktree.
3. Rebuild the serialized local verification loop (restore → build → test non-browser → browser → snapshot update/verify → catalogue) — `scratchpad/wave-loop.ps1` died with the PC session.

### Phase 1 — land the green PRs
- Merge **#592 (TICK-061)** first, then **#591 (PLAT-048)** (`gh pr merge --merge`); move both tickets → verifying; tick checklists.

### Phase 2 — finish the red PRs (order: 593 → 594 → 595 → 588)
For each: fresh `gh pr checks` (rerun DELIV-031-style SQL-timeout flakes once before treating as real), reproduce locally with the canonical commands, fix, complete the interrupted review, merge when green.
- **#593**: merge dev (post-#592) into the branch, resolve the EF snapshot re-merge, rerun CI (verify `a8aec269` fixed the VehicleWorkflowTerminalTests regression), write the review-required rollout sentence + tick the 11th checklist item, merge.
- **#594**: diagnose unit + sql failures on `b5b6e096` (branch already carries #592 + dev@5ca2572c), fix, finish the interrupted independent review (delta above #592 only), merge.
- **#595**: diagnose the census/coverage failures (expect `Invoke-AzureDatabaseBootstrap.ps1` census entry for `20260828112103_NamedEstimates`, mirroring AUTO-011's `a1eff8be`), fix, complete review + checklist, merge. Migration order preserved: #592 → #593 → #595.
- **#588**: merge dev into the branch (resolve #589 fallout: snapshots, site.css, catalogue), keep your `172e520e` commit, regenerate snapshots **once** on this branch, run `-Verify` + `Test-UiCatalogue.ps1`, complete the review's docs-housekeeping conditions, CI green, merge. Delete the temp snapshot-regen state after.

### Phase 3 — respawn Wave 2 (page ports)
- Dispatch three **completion** agents for CASE-025, MAIL-025, UIIMP-008: first action is auditing their own pushed branch against the ticket plan; keep sound work, repair/finish, `take_ticket`, update checklist, open PR.
- CASE-012: fresh agent, new worktree from current dev.
- Then launch the remaining lanes (INTK-046, CASE-026, ENG-025, INTK-047, PLAT-023, PLAT-028 — PLAT-028 already has research/plan/checklist docs) at ≤4 parallel lanes, ≤3 build-heavy PRs open at once; snapshot regen serialized one branch at a time on the branch about to merge.
- Independent review agent per PR before merge; I run all tests (subagents never run tests).

### Phase 4 — remaining Wave 3 backend
ENG-027 (valuations) → CASE-028 (timeline/action logs/rail counts) → MAIL-027 (outbound mail; `requires-live-approval` boundary respected — no live mailbox activation), one unmerged migration at a time. Reconcile AUTO-006 vs AUTO-010 against `waves.md` before that lane runs.

### Phase 5 — Wave 4 feature UIs
PLAT-049, PLAT-050, PLAT-051, AUTO-010, CASE-029, ENG-028, MAIL-026, CASE-030 — each blocked by its wave-2 page + wave-3 backend; same subagent/review/test protocol.

### Phase 6 — Wave 5 closeout + release
UIIMP-009 (delete the legacy CSS block + dead selectors, grep-proved), DELIV-030 (current-architecture/operations refresh), UIIMP-010 (final browser walk at 1580/1100/760 + screenshots; verify SDK 10.0.302 present on this machine), catalogue reclassification. Operator eyeball of the shell (dev-walk). Then — only with your explicit **MERGE AUTH GRANTED** — the exact-SHA `dev` → `main` promotion; proofs for verifying tickets are written on merged `main` afterwards.

## Standing rules (unchanged from the programme)
Subagents never run tests — I run the canonical commands per merge; one snapshot regen per merge; migration order strict; review by a non-implementing agent with recorded dispositions; Kanmer `take_ticket`/stage moves kept accurate (worktrees re-created at recorded paths); DELIV-031 flakes rerun once, then treated as real; `corpus/`, operator-notes, and protected docs untouched; nothing merges to `main` without your authority.

## First actions on approval
Fetch + verify dev, re-materialize the six in-flight worktrees, merge #592 then #591, and start the #593 diagnosis — reporting each merge to you as it lands.
import { BackLink } from '@pegasus/design-system';

/** Muted return link, arrow pointing back. */
export const ToCases = () => <BackLink href="#">Back to Cases</BackLink>;

/** Returning to a filtered queue. */
export const ToQueue = () => <BackLink href="#">Back to Not ready</BackLink>;

import { SendToClaudeButton } from '@pegasus/design-system';

/** The one recorded palette divergence: the Engineer assessment surface's control in the provider's terracotta. */
export const Default = () => <SendToClaudeButton />;

/** A custom label; the sparkle glyph and gradient stay. */
export const CustomLabel = () => <SendToClaudeButton>Send assessment to Claude</SendToClaudeButton>;

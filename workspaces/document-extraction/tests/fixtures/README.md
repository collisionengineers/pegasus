# Controlled fixtures

Only manifest-listed files under this directory may be read by automated test
corpus tooling. See `docs/testing/fixture-strategy.md` for provenance, privacy,
licensing and path-containment rules.

The first tests generate compact CFB byte arrays in memory, so there are no
binary fixtures yet. Empty product directories are not reserved in advance.

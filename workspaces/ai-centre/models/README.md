# Models

This folder stores model-family definitions, licences, model cards, inference contracts, promotion
manifests, and small configuration files.

Weights, checkpoints, adapters, exported binaries, run output, and caches remain in the approved
external artifact store and are referenced by immutable hash. No model is currently approved for
production.

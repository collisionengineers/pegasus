# Case-system connectors

Provider adapters for instructions, case status, estimates, reports, and audit events. Translate
vendor payloads into the canonical case contracts; never let a vendor schema become the domain
model. Live write-back requires an explicit capability and approval policy.

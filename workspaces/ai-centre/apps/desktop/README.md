# Windows desktop workstation

The imported desktop proposal is superseded for Pegasus. The existing Web application and
`design/` own product UI; `Pegasus.Core` owns business policy. Do not create a second case
application, runtime, navigation shell, evidence viewer, report preview, or connector surface here.

Historical desktop-stack investigations remain source evidence only. Any future workstation
capability requires a root product allocation, accepted architecture decision, and real caller.

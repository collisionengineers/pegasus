# Data pipelines

Deterministic code for inventory, secret blocking, extraction, OCR, hashing, deduplication, lineage,
source-role classification, minimisation, annotation export/import, split construction, and deletion
propagation belongs here. Pipelines must be resumable, auditable, and fail closed on missing approval.

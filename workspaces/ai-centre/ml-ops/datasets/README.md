# Dataset definitions

Version dataset recipes, schemas, label taxonomies, synthetic fixtures, manifests, and dataset cards
here. Private source inputs remain under ignored `corpus/ai-centre/`; generated dataset payloads
belong under `artifacts/`. Tracked manifests identify inputs and outputs by immutable hash. Dataset
splits must be by case and time after deduplication.

# Contributor guide

<!--
This is the user-owned portion of AGENTS.md. Keep it outside Kanmer's managed
marker-delimited instruction block. Replace every TODO below with facts about
this repository; delete guidance that no longer applies.
-->

## Commands

| Command | Purpose |
|---|---|
| `TODO` | Describe the deterministic command used to build, test, lint, or type-check this repository. |

<!-- TODO: Add every command a contributor needs, including its purpose and where to run it. -->

## Architecture map

<!-- TODO: Map the repository's main directories/modules. State what each owns,
which dependencies are intentionally one-way, and where cross-cutting behaviour
lives. Keep this a navigable map rather than a file inventory. -->

## Conventions

<!-- TODO: Record repeatable, normal rules: naming, formatting, module/API
boundaries, error handling, test placement, and branch/commit conventions. A
convention should tell a contributor what to do on ordinary work. -->

## Gotchas

<!-- TODO: Record surprising, consequential traps. Each entry should name the
failure mode and its mitigation, so it does not become a vague duplicate of a
normal convention. -->

## Verification

### Deterministic checks

- [ ] `TODO` — run the repository's automated checks first and record the result.

### Manual or environment-dependent checks

- [ ] `TODO` — list any manual, platform-specific, credentialed, or deployment verification separately.

<!-- TODO: Keep deterministic commands/checks above manual confirmation so the
fastest reproducible evidence is always run first. -->

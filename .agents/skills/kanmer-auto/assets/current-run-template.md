---
kind: auto-current
schema: 1
run_id: <run_id>
run_path: automation/runs/<run-id>.md
group: <EPIC-000 or HZN-000>
project_fingerprint: <get_status project identity>
controller: <controller slug>
status: running
updated_at: <ISO-8601 UTC timestamp>
---

# Current auto run — <group>

## Resume instruction

Open `<run_path>`, then re-read the group context, the live roster, and `get_doc_gates` for every ticket before dispatching. This pointer is written only after its complete run record has been written and read back.

# I3: auto-fill Vehicle type from the DVLA lookup

## Aim

Implement `C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/vehicle-type-autofill-plan.md` §1–§6 with
the operator decisions recorded there (L1e/L2e → `scooter`, other L-class → `motorcycle`; wheelplan +
revenue weight fallback; HGV/bus/tractor → `other`; code list unchanged). Anchors re-verified on dev.
One correction: the plan cites `EfValuationStore.cs:584` as the precedent for loading the owning
`CaseEntity`; no such precedent exists. Load it from `context.Cases` by id inside the same
transaction in `FillEmptyVehicleFieldsAsync`.

## Changes

1. `src/Pegasus.Core/Vehicle/LookupContracts.cs` (`VehicleDetails` at ~39–44): add `string? TypeApproval,
   string? Wheelplan, int? RevenueWeightKg`; `VehicleLookupResult.EnsureValidFor` treats blank strings and
   `RevenueWeightKg <= 0` as invalid, mirroring `FuelType`/`EngineCapacityCc`.
   `src/Pegasus.Core/Vehicle/VehicleLookupFillPolicy.cs` `Merge` (~34–51): DVLA-first for the three new
   members; extend the all-null → null check.
2. New `src/Pegasus.Core/Vehicle/VehicleTypePolicy.cs`: `public static class VehicleTypePolicy`
   (`MethodKey = "dvla-type-approval"`, `MethodVersion = 1`, `static string? Classify(VehicleDetails? vehicle)`),
   pure, normalised input (trim, upper-case, remove spaces), tiers exactly as the plan's §2 table:
   type approval (M1 car; M2/M3 other; N1 van; N2/N3 other; L1/L2 with or without e → scooter;
   L3–L7 → motorcycle; O1–O4 trailer; T/C/R/S → other; else tier 2), wheelplan (2-WHEEL/3-WHEEL →
   motorcycle; ARTIC/MULTI AXLE → other; RIGID BODY → tier 3; else null), revenue weight for rigid body
   (absent → car; 1–3500 → van; > 3500 → other). Every non-null output must be one of
   `AssessmentVocabulary.VehicleType` codes (`src/Pegasus.Core/Assessment/AssessmentContracts.cs:208–209`).
3. `src/Pegasus.Infrastructure/Vehicle/DvlaDvsaProductionAdapter.cs` `ReadDvlaAsync` (~171–176): also read
   `Text(root,"typeApproval")`, `Text(root,"wheelplan")`, `Number(root,"revenueWeight")`; `ParseDvsaVehicle`
   (~377–383) passes nulls. Replay fixtures (`DvlaDvsaAdapters.cs`) may carry the members; confirm
   `JsonUnmappedMemberHandling.Disallow` still passes.
   `src/Pegasus.Infrastructure/Persistence/VehicleEntities.cs` `VehicleLookupObservationEntity`: add
   `TypeApproval`, `Wheelplan`, `RevenueWeightKg`; lengths in `VehicleModelConfiguration.cs`; set in
   `EfVehicleLookupWorkStore.RecordOutcomeAsync` (~184–188); read back in `MapObservation` (~406) if the
   Core observation exposes vehicle details. Additive migration (three nullable columns) plus snapshot;
   no grants, no data movement.
4. `EfVehicleLookupWorkStore.FillEmptyVehicleFieldsAsync` (~308–404): after the CaseDataFields fills,
   `var code = VehicleTypePolicy.Classify(result.Vehicle)`; when non-null load the existing
   `CaseAssessmentFields` row for `AssessmentVocabulary.VehicleType`; apply
   `VehicleLookupFillPolicy.Fills(hasFact: false, hasConfirmed: existing?.ConfirmedBy is not null)`; an
   unconfirmed earlier lookup value is re-stamped only if the code changed (same skip-if-unchanged rule as
   `AssessmentWriteSet.Apply`, `AssessmentFieldWriter.cs:154–165`); write through
   `AssessmentFieldWriter.Write(context, owningCase, caseId, existing, path, code, ActorKind.Automation, "vehicle-lookup", recordedAtUtc, confirmedBy: null)`
   (signature at `AssessmentFieldWriter.cs:21–31`); count it in `filled` so report generation goes stale
   in the same transaction. Nothing in Web changes (`_CaseVehicle.cshtml:152–176` already pre-selects
   the code and shows the Lookup chip at :160; readiness `AssessmentPolicy.cs:296` unchanged).
5. No backfill. `docs/frd/frd-06-vehicle-and-engineering-evidence.md` § Look up DVLA & MOT (~166–191):
   extend "fills Make, Model, Year, or Mileage" to include the derived Vehicle type, name the rule
   (type approval → wheelplan → revenue weight; mopeds → scooter; heavy/PSV → other) and its Lookup
   provenance / staff re-stamp on Save (D34 amended 2026-09-16). `docs/current-architecture.md` only if it
   enumerates Core Vehicle policies.
6. Tests: new `tests/Pegasus.Core.Tests/Vehicle/VehicleTypePolicyTests.cs` (table over M1, N1, L1e, L3e,
   N3, O2, T1; wheelplan-only 2-WHEEL, "2 AXLE RIGID BODY" with 0/1800/7500 kg, NON STANDARD → null;
   casing/whitespace; null → null; every non-null output in the vocabulary); `VehicleLookupFillPolicyTests`
   Merge carries the members DVLA-first; `ProductionVehicleLookupTests` VES stub with the three fields;
   `VehicleLookupGapFillTests`: RecordOutcome writes `vehicle.vehicle_type = "car"` as Automation/unconfirmed,
   a staff-confirmed row survives re-lookup, an unconfirmed row is re-stamped only when the code changes,
   a null classification writes nothing. `CaseRecordGapsV26WebTests.TheVehicleSectionEditsVinTypeAndBodyOutsideTheEngineerSections`
   keeps covering the Save path; migration/architecture tests pick up the snapshot.

## Acceptance

- Contract, merge, policy, adapter, entity/config/migration/snapshot, fill step and FRD-06 edit all present.
- Staff-confirmed values are never overwritten; unchanged unconfirmed values are not re-stamped.
- `VehicleTypePolicy.Classify` is pure and every non-null result is a vocabulary code.
- Migration is additive only; snapshot updated; no grants.
- New and extended tests exist as listed; no existing assertion removed.

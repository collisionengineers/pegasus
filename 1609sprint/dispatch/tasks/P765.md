# P765: repair the failing tests on PR 765 (`task/audit-original-report-current`)

## Aim

PR 765 "Require original Audit report outcome before Case/PO allocation" fails these tests in CI
run 35090133458 (`sql-integration (3)`):

- `Pegasus.IntegrationTests.CustodyOutboxIntegrationTests.UnconfirmedSuppliedMailboxReportRetriesWithoutAllocationThenRecoversFromItsRealCustodyIntent(custodyDisposition: Pending, expectedCustodyState: Pending)`
- the same theory with `(custodyDisposition: Failed, expectedCustodyState: Failed)`
- `Pegasus.IntegrationTests.CustodyOutboxIntegrationTests.ConfirmedSuppliedMailboxAuditReportReevaluatesToOneRepairableAuditCase`

The dispatcher has reproduced the failures at the current head; the log tail is appended below.

## Required change

The PR's contract (its own description): an Audit awaiting its original report stays an
Unidentified item without a Case, PO or reserved sequence; supplying a readable original report
establishes Repairable or Total loss and custody must be confirmed before the existing allocation
path issues the identity; existing custody and Worker retry paths handle the supplied report;
exact idempotent replay. Read FRD-01, FRD-02 and FRD-05 in `docs/frd/` and the PR's own changes
(`git log origin/dev..HEAD`, `git diff origin/dev...HEAD`). Fix whichever side is wrong: the
custody outbox recovery path, the re-evaluation path, or the test's expectation. Do not weaken
assertions; if a test expectation contradicts the PR's stated contract, change the test and
explain.

## Acceptance

- The three failing cases pass and the rest of `CustodyOutboxIntegrationTests` still passes.
- Replay stays exactly idempotent (no second allocation, no duplicate custody rows).
- No unrelated file changes; documentation only if the contract wording in FRD-01/02/05 needed the correction.

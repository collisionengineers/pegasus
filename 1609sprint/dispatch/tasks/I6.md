# I6: estimate document (PDF) with the specialist-hours calculation fixes

## Aim

Implement `C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/estimate-generator/PLAN.md` in full:
§4 document design, §5 backend, §6 frontend (Phase 1), §8 documentation, §9 verification and §10
calculation fixes 1–3 (operator direction of 16 Sep). Phase 2 (§7) stays gated. Read the review
`.../estimate-generator/pegasus-estimate-document-review-2026-09-16.md` and apply its corrections
E01–E12. The sample and extracted text: `.../estimate-generator/evaestimate.pdf`, `evaestimate-extracted.md`,
`evaestimate-page1.png`, `reference-repairable-report-p1.png`. This branch is stacked on
`task/report-generation-freshness`; its layout changes are already in your base.

Operator decisions for §11 (use these, do not re-ask): A "Estimate PDF" button, "Estimate document" in
FRD/code; B print no signature; C omit part-number column; D one Labour box; E omit £ discount amounts;
F read-mode actions row; G new capability id RPT-07; H Phase 2 waits for the v27 `attach` letter;
I header "Your Ref" prints the claim number; J leave the editor unchanged (`specialist_wu` reachable via
JSON/AI routes only).

## Changes

1. `ReportChrome` extraction from `src/Pegasus.Infrastructure/Reports/AssessmentReportLayout.cs` (primitives
   `Footer:116`, `Letterhead:470`, `Title:501`, `Badge:521`, `Tile:529`, `Section:549`, `HeaderCell:581`,
   `DataTable:604`, `BodyCell:637`, `CostTable:671`, `Slug:990`; the fee note is a section at `:316`). The
   assessment report must render byte-for-byte the same content: render the existing renderer test fixtures
   before and after and save both PDFs plus a pixel/text comparison under `artifacts/estimate-document/` in this
   worktree (gitignored) and describe the comparison in `checks` (the verifier runs the tests).
2. Core `src/Pegasus.Core/Reports/EstimateDocumentRendering.cs` per §5.1 (snapshot, `Validate` fail-closed rules of
   §4.5, column mapping §4.3, summary blocks §4.4 with every figure's owner). Per review E02 the snapshot must
   carry `PaintMaterials` and `OtherCosts` seeds (`Estimates.cs:301–302`); per E03 `AssessmentWorkspace.CanOpen`
   (`:41–48`) is a lifecycle gate, not staff Case-read authority: authorise the document with the staff Case-read
   rule the Report handler uses; per E05 respect `BlocksAcceptance` (`Estimates.cs:67–68`); per E06 define the
   Accepted → Superseded → Discarded case explicitly (use `RecordedTotals` for Accepted/Superseded, never
   recompute a once-accepted figure).
3. Infrastructure `QuestPdfEstimateDocumentRenderer` per §5.2 (QuestPDF 2026.8.0, fonts as the report renderer);
   `OnGetEstimateDocumentAsync` in `Pages/Cases/Details.cshtml.cs` per §6.3; read-mode `est-actions` row in
   `_CaseEstimate.cshtml` (currently only under `editing`, ~419–422) per §6.1–6.2 with `data-no-inplace`
   like the report preview (`_CaseReport.cshtml:91`, `case-workspace.js:1773`). Button copy exactly "Estimate PDF".
4. §10 fixes in `src/Pegasus.Core/Assessment/Estimates.cs` and `RepairSpecifications.cs`:
   Fix 1 `specialist_wu` `WorkUnits` join panel hours priced at the estimate rate, its `Price` off-pattern;
   `RepairSpecificationPolicy.PolicyVersion` 3 → 4 with the one-line history note; only editable versions
   recompute (`ForProjection` keeps frozen `RecordedTotals`). Fix 2 `specialist_fixed` with non-zero
   `WorkUnits` adds `EstimateAnomaly(Position, "hours", value, "Hours on a fixed-price Specialist line are retained but not priced.")`.
   Fix 3 new pure `EstimateHours` record (exact shape in §10) consumed by `EstimateTotals.Compute`,
   `ReportRepairCosts.For`, `Details.Report.cs` `LabourHours` and the document's Hours row; this also corrects
   the report's printed hours (`AssessmentReportRendering.cs:241–243` sums all lines' work units today).
   Core test pins `Raw.PanelLabour + Raw.PaintLabour == PricedTotal × HourlyRate × (1 − overall discount)`.
5. Golden fixture (§9): EVA sample typed as `specialist_fixed` (tyre £180.00, alignment £112.42) and
   `check_labour` (1.00 h operations); its correct net is **£2,617.99** (the plan's £2,618.23 is an arithmetic
   error: 1,415.76 + 510.58 + 160.63 + 531.02), VAT and gross accordingly; hours × rate reproduces £1,832.16.
6. Documentation (§8): FRD-11 Labour row rewrite, capabilities `RPT-07`, ADR-0050 note if it lists renderers,
   `docs/current-architecture.md` if it lists Core Reports files. Prose only; no new Markdown file unless
   the index routes it.
7. Tests (§9): Core `EstimateDocumentRendering` validate/snapshot/summary tests; the calculation-fix tests;
   renderer tests under `tests/Pegasus.IntegrationTests/Reports/`; a `Category=SqlServer` web test for the
   handler (authorised staff 200 with PDF content type; lifecycle-refused and unauthorised cases).

## Acceptance

- Assessment report output unchanged after `ReportChrome` extraction (before/after evidence described).
- Estimate document renders every §4 block from owned figures; `Validate` fails closed per §4.5; E02/E03/E05/E06 handled.
- §10 fixes present: PolicyVersion 4, `specialist_fixed` hours anomaly, single `EstimateHours` classifier used by all four consumers; accepted totals never recomputed.
- Golden fixture net £2,617.99; hours × rate £1,832.16.
- Button/handler/read-mode row present; copy exactly "Estimate PDF"; no Phase 2 retention or delivery.
- FRD-11, capabilities RPT-07 updated; tests as listed.

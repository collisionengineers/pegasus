# I1: estimate import route — visible refusals, dialog mechanics, Glass's rounding

## Aim

Implement the operator-approved plan in
`C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/import-test-and-fix/draft-plan.md` (items A–G).
The line anchors below were re-verified on `dev` on 16 Sep 2026 and supersede the plan's where
they differ. Deep-audit context: `C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/deep-code-audit/2026-09-16-deep-code-audit.md` §5.

## Changes

A. `src/Pegasus.Web/wwwroot/js/site.js`, `bindDialogOpeners` (~1303–1315): the click listener at
   ~1314 is `control.addEventListener('click', function () { open(control); });`. When the control is
   an `<a>` with `href`, call `event.preventDefault()` before `open(control)`. The href stays the
   no-script fallback. (`bindNativeDialogs` at ~126–146 already prevents default for native `<dialog>`; leave it.)

B. `src/Pegasus.Web/Pages/Cases/Shared/_CaseEstimate.cshtml`: the four backdrop dialogs render
   `hidden="@(Model.OpenDialog == "…" ? null : "hidden")"` at lines ~695, ~729, ~776, ~808. Replace
   with the pattern at `src/Pegasus.Web/Pages/_WorkCentreBody.cshtml:273`
   (`Html.Raw(" data-dialog-open-on-load=\"true\"") : Html.Raw(" hidden")`), so a server-opened dialog
   gets `open()` via `bindBackdropDialogs` (Escape, inert, focus trap) while a no-script load still
   renders it open. Ten other pages already use the attribute; follow them.

C. `src/Pegasus.Web/wwwroot/js/case-workspace.js`, `swap()` (~654–663): immediately before
   `current.replaceWith(next)` (~662), for each `[data-dialog]:not([hidden]), [data-reason-dialog]:not([hidden])`
   inside `current`, call its `pegasusClose()` so the `inertOutside` release runs, `openDialogStack`
   pops and the keydown listener is removed. Only roots actually replaced are touched.

D. `case-workspace.js` ~700–704 currently toasts only `[data-case-notices] [data-confirmation]`.
   Also toast the text of a swapped-in `[data-case-notices] [role="alert"]` with
   `window.pegasusToast(text, 'danger')` (`site.js` ~1911–1922; `.toast--danger` exists at `site.css:784`).
   `showActionError` (~763–774) toasts its message the same way after writing the notice. The
   page notice remains as the persistent record. Write no new copy.

E. `src/Pegasus.Web/wwwroot/css/site.css` ~211: extend the existing `.btn.is-disabled,.btn:disabled{…}`
   selector list with `form[aria-busy="true"] button[type="submit"]` so the in-flight submit
   (`submitInPlace` sets `aria-busy`, ~735/758) takes the disabled treatment. No JS change.

F. `src/Pegasus.Infrastructure/Assessment/GlassEstimatePdfParser.cs`, `Complete()` (~308–325): the
   predicate at ~320 `own.Sum(item => item.Labour ?? 0) != sum.Labour` demands the sum of per-row
   rounded labour; Glass's prints section labour as `round(SummaryHours × SummaryRate, 2)` once.
   Replace that one term with
   `decimal.Round(sum.SummaryHours.Value * sum.SummaryRate.Value, 2, MidpointRounding.AwayFromZero) != sum.Labour`.
   Keep every other check (per-row `round(hours × rate) == row labour`, hours sum, material sum,
   `Labour + Material == Total`, `sum.Labour == SummaryLabour`, document totals). Update the class
   summary comment. In `docs/frd/frd-06-vehicle-and-engineering-evidence.md` § Glass's calculation PDFs
   (~340–347), amend the reconciliation sentence: section labour reconciles to the printed rate × section
   hours as the source computes it. Prose-only doc edit; no new file.

G. Tests.
   - `tests/Pegasus.IntegrationTests/GlassEstimatePdfParserTests.cs` (model on `AnUnreadableAmountRefusesTheWholeGlassTable`, ~34):
     a synthetic `VisualRow[]` complete document (identity rows, one Body row, one Auxiliary row, Paint
     rows 0.80/1.60/1.70/0.30/0.30 h at £83.28 whose per-row labour sums to 391.41 while the printed
     section labour is 391.42) that now parses with `SourceTotals.Net` reconciled; and a sibling where one
     row's labour is off by 1p that still refuses with "main rows disagree". These two synthetic facts
     must not carry the class's `Category=Corpus` trait if that trait is per-method; if it is on the
     class, keep them in the class (the verifier sets `PEGASUS_REFERENCE_PACK_ROOT` so the four
     reference oracles run too).
   - `AssessmentEstimateImportWebTests.ImportDialogHasAStaticTargetWhenJavaScriptIsUnavailable` (~447):
     keep `DoesNotContain("hidden=")`, add `Contains("data-dialog-open-on-load=\"true\"")`.
   - There is no JS test harness; do not add one.

Optional local proof, never committed: if `C:/Users/Alex/Desktop/calc.pdf` exists, a throwaway
check that `new PdfEstimateDocumentParser().Parse(File.ReadAllBytes(...))` yields 22 rows, Net 3,034.39,
VAT 606.88, Gross 3,641.27. Do not run it (host-slot rule); describe it in `checks` for the verifier.

Out of scope: the 5.9 s server round trip; the Audatex "Estimate Report" layout; adding calc.pdf to the private pack.

## Acceptance

- Anchor openers open their dialog in place with no navigation; a `?dialog=` server-rendered dialog carries `data-dialog-open-on-load="true"` and no `hidden`.
- An in-place swap closes any open backdrop dialog inside the replaced root before replacement; nothing outside stays `inert`.
- Refusal alerts and action errors are toasted as danger toasts; success toasts unchanged.
- `form[aria-busy="true"] button[type="submit"]` shares the disabled rule.
- Glass's parser accepts printed section labour equal to `round(SummaryHours × SummaryRate)` and still refuses a 1p row disagreement; all other reconciliation checks intact; FRD-06 sentence amended.
- The two new parser facts and the import-dialog assertion exist; no assertion removed.

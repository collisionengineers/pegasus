# I4: codebase audit quick fixes (16 Sep)

## Aim

Fix the verified, decision-free findings of
`C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/codebase-audits/codebase-audit..md` as corrected by
`.../codebase-audits/pegasus-audit-review-addendum-2026-09-16.md`. Items H1/H2/H4/H5, the dead-code
deletion tranche and the bootstrap verification account in `appsettings.json` are out of scope
(operator decisions pending); do not touch them.

## Changes

A1. Upload outcome "Open case" link 404: `src/Pegasus.Web/Presentation/UploadOutcome.cs:212,240` build
    `$"/Cases/Details/{caseId:D}"` but the page is `@page "/Cases/{id:guid}"` (`Pages/Cases/Details.cshtml:1`).
    Point the link at the real route (reuse whatever helper other pages use for Case links). Extend
    `UploadOutcomeQueriesTests` / `UploadConfirmationWebTests` to assert the href.
A2. Mail "Move to folder": `IRetainedMailFolderMover` (`src/Pegasus.Core/Intake/RetainedMailFolderMove.cs:41`)
    has `IsAvailable`; only `UnavailableRetainedMailFolderMover` is registered (`DependencyInjection.cs:108`)
    and `RetainedMail.cs:730` already consults `folderMover?.IsAvailable`. Verify whether
    `Pages/Mail/Message.cshtml` (~495–498) still renders the control when the mover is unavailable. If it
    does, hide it on that same fact (no new copy); if it is already hidden, record "A2 already correct"
    in your summary and change nothing.
A3. Heartbeat handlers return different status codes for the same outcomes:
    `Administration/Accounts/Index.cshtml.cs` (~304 `BadRequest()`, ~312 `NoContentResult`) vs
    `Administration/Glass/Index.cshtml.cs` (~246 `ConflictObjectResult`, ~253 `OkResult`). Align both on one
    convention (choose the one the client script expects; check the JS caller) and add a web test per page.
A4. Five POST-only page models have no `OnGet`: `Pages/Cases/{Workflow,Vehicle,Custody,Tasks,Closure}.cshtml.cs`.
    Add `public IActionResult OnGet() => NotFound();` to each (do not mirror `Export.cshtml.cs`, which
    redirects), with one web test per page asserting 404 on GET.
A5. Two Worker timers share `%PendingWorkRecoverySchedule%` (`src/Pegasus.Worker/IntakeFunctions.cs:26` and `:49`).
    Give the second timer its own setting name, add it beside the existing one in `infra/*.bicep` and any
    local settings template, and extend `WorkerActivationReleaseContractTests` (or the closest contract
    test) so both names are required.
D1. `src/Pegasus.Core/Actors/ActorDisplayNames.cs:44–51` reads one staff account per id. Add a batch
    read (`GetManyAsync`) to the staff account query port in `src/Pegasus.Core/Identity/StaffAccountAdministration.cs`
    (~186–198), implement it in the EF adapter, use it here, and add a test that counts SQL commands
    (not fake-call counts) to prove one round trip.

## Acceptance

- A1 link resolves to the Case page route; test asserts it.
- A3 both heartbeat handlers share one status-code convention; tests cover both.
- A4 all five pages return 404 on GET; tests cover each.
- A5 the two timers use distinct schedule settings, present in bicep and contract tests.
- D1 display names resolve with one query for N staff ids; command-count test proves it.
- No changes to `appsettings.json` bootstrap block, `OperatorLabels.cs` deletions, or any H-item.

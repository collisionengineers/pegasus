# I5: report and fee-note generation — command identity, freshness, footer, culture

## Aim

Implement work packages 1–2 and the deterministic parts of package 5 from the review
`C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/docs-generation/pegasus-report-fee-note-audit-review.md`
(§1, §2, §3 N1, §6, §9) of the audit `.../docs-generation/report-and-fee-note-audit.md`. Two operator
decisions are already recorded in the audit: (1) a separate fee-note request attaches to the Case's
current generation rather than superseding it; (2) workspace Save stales the generation only when a
printed fact actually changed. Out of scope: A5 replacement wording (copy needs approval; leave the
current text), A4 printed hours (delivered with the estimate document task), packages 3–4 (N2/B2/A8/N3/N4).

## Changes

1. A3 + N1 (package 1). `src/Pegasus.Infrastructure/Persistence/EfCaseReportGenerationStore.cs` ~75–89 replays
   on `OperationKey` alone and returns the stored artifact whatever the requested Kind/IncludeFeeNote;
   `Pages/Cases/Shared/_CaseReport.cshtml:64` and `:100` post the same `GenerateReportOperationKey` from two
   forms. Bind replay to command identity (kind + packaging choice): a replay with the same key and a
   different command is a conflict, not a silent reuse (follow the existing `IntakeAllocation` command-hash
   pattern in Core). Give each form its own operation key. Carry the browser's expected Case version through
   generation/preparation so a stale form is refused rather than silently re-read (N1).
2. A1 + A2 + B3 (package 2). One Core-owned change classifier (explicit, narrow, returns stale/not-stale plus a
   reason code) shared by every accepted-write route: workspace Save (`EfCaseWorkspaceStore.cs:259–268`
   currently stales unconditionally), the assessment save route and Case-data Save (B3 parity; neither
   invalidates today). Compare normalised effective values, cover the sign-off engineer change explicitly,
   and preserve FRD-11's invalidation on source confirmation/removal and signatory changes. Commit the
   accepted change and its invalidation atomically. A separate fee-note request identifies the current
   generation, verifies it is still current/usable and the caller has edit authority, and renders from its
   frozen snapshot (frozen report date and fee facts) without superseding it (`EfCaseReportGenerationStore.cs`
   ~130–145, 865–896: `MaterialOf`, `SupersedeCurrentAsync`, the `IncludeFeeNote` forcing). Define and test
   the no-current-generation and stale cases explicitly (refuse with the existing reason vocabulary; invent no rule).
3. Package 5 deterministic items: A6 footer keyed on the page's artifact, not the request
   (`src/Pegasus.Infrastructure/Reports/AssessmentReportLayout.cs:116–120`); A7 invariant culture for the mileage
   figure (`AssessmentReportProjection.cs:259–261`); B7 one VAT constant shared by `AssessmentReportRendering.cs:506`
   and `AssessmentReportLayout.cs:419`; D `tests/Pegasus.IntegrationTests/Reports/AssessmentReportRendererTests.cs`
   builds a LocalDB context (`:288`) without a `SqlServer` trait: replace with a renderer-only registration if
   the tests never query SQL, otherwise add the trait.
4. Documentation: FRD-11 (`docs/frd/frd-11-*.md`) sentences that describe replay, fee-note attachment and
   staleness must match the new behaviour; prose-only, no new file.
5. Tests with each package (targeted failing tests first): same-key/different-kind replay conflict; changed
   `IncludeFeeNote`; stale expected version refused; note-only save does not stale; printed-fact change does;
   sign-off change does; source confirmation/removal does; fee note attaches to the current generation and
   is refused when none/stale; footer per page; culture-invariant mileage.

## Acceptance

- Replay is bound to command identity; the two forms carry distinct keys; stale expected versions are refused.
- One Core change classifier decides staleness with reason codes and is used by every accepted-write route; FRD-11 safeguards preserved.
- Fee note attaches to the current generation from its frozen snapshot; refusals use existing reason vocabulary.
- Footer, culture and VAT-constant corrections present; renderer tests need no LocalDB or carry the trait.
- No wording changes to the invented fee fallback (A5) and no printed-hours change (A4).

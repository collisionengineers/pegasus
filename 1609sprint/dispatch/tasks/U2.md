# U2: six-shard SQL CI and the Case test split (PR 764)

## Aim

The branch already carries, as its latest commit, the operator-approved remediation: six SQL
shards in `.github/workflows/ci.yml` (matrix, execution and partition verification all 6),
`scripts/Test-TestShard.ps1` exercising 3 and 6 runners, a runbook note, and the former
16-file partial `CaseDetailsWebTests` replaced by 15 concrete `Case*WebTests` classes plus the
test-free `internal static partial class CaseWebTestSupport` (new file
`tests/Pegasus.IntegrationTests/CaseWebTestSupport.Recording.cs`). Your job is the independent
correction pass on that commit: fix what the review found, apply the addendum items below, and
change nothing else. No application source may change.

## Inputs (read-only)

- Approved plan and progress: `C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/ci-six-shards/PLAN.md`, `.../PROGRESS.md`.
- Review addendum: `C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/audit-reference-change/Six_Shard_SQL_CI_Review_Addendum.md`.
- Author's method map (method → original file → new file, body/attribute hashes): `C:/Users/Alex/Documents/GitHub/pegasus/.git/worktrees/performance-next/caseweb-test-split-map.json`.
- Baseline discovery (2,349 non-corpus rows, 188 under `CaseDetailsWebTests`): `C:/Users/Alex/Documents/GitHub/pegasus/artifacts/performance/implementation-20260916/ci-timeout-artifacts/test-shard-1/listed-1.txt`.

## Required changes

1. Address every real finding from the independent review pasted below this brief (helper
   accessibility, per-test state, inherited or duplicated tests, missing `[Trait("Category", "SqlServer")]`
   on any of the 15 classes, shared mutable static state introduced by the helper extraction).
2. Move `SendPageRendersItsChoiceInReviewAndWithEngineer` from `CaseCustodyWebTests.cs` (currently ~line 906)
   to `CaseWorkflowWebTests.cs`: lifecycle/EVA behaviour belongs to Workflow per the approved plan.
   Move the method body and its attributes verbatim; do not alter its assertions.
3. In `CaseWorkflowWebTests.cs`, the comment above `SendToEvaRendersInReviewAndWithEngineer` (~line 160)
   still says Review-only although the theory data covers Review and WithEngineer. Reconcile the
   comment with the behaviour; do not change the data.
4. `scripts/Test-TestShard.ps1`: add negative cases (inconsistent inventories between shards, a
   duplicate or missing assignment, an assignment set that is not a partition) that must fail,
   and a six-runner fixture with at least 13 classes including tied sizes and a reversed input
   order, so the snake allocation crosses both direction changes. Keep the existing positive
   cases. Add one cheap consistency assertion that reads `.github/workflows/ci.yml` and fails
   unless the matrix list length, the `-ShardCount` on the execution step and the `-ShardCount`
   on the partition step agree.
5. Wording: where the runbook or comments say "188 parameterized rows", say "188 discovered test
   cases" (the set includes ordinary facts).

Do not add a duration scheduler, a shared fixture or collection, a timeout increase, or any
change under `src/`. Preserve every method, assertion, theory row and skip condition. Each test
keeps its own store, factory, database, temporary directory and disposables.

## Verifier commands (run by the dispatcher, not by you)

- `dotnet build Pegasus.slnx -c Release`
- `pwsh scripts/Test-TestShard.ps1`
- discovery listing compared to the baseline: same 2,349 names overall; the 188 former
  `CaseDetailsWebTests` cases appear exactly once each under one of the 15 classes
- `dotnet test tests/Pegasus.IntegrationTests -c Release --no-build --filter "<the 15 classes>"` with TRX retained
- documentation links and Markdown placement

## Acceptance

- No file under `src/` changed; only tests, `scripts/Test-TestShard.ps1`, `.github/workflows/ci.yml` (still 6/6/6) and `docs/runbook.md`.
- All 15 classes are `public sealed class` with an explicit `[Trait("Category", "SqlServer")]`; none is `partial` with tests, none inherits tests, no test is duplicated or lost relative to the method map.
- `SendPageRendersItsChoiceInReviewAndWithEngineer` lives in `CaseWorkflowWebTests` unchanged; the `SendToEva…` comment matches its data.
- `Test-TestShard.ps1` passes with the new negative cases, the 13+-class six-runner fixture and the ci.yml consistency assertion.
- The shared helper owner holds only immutable helper definitions; no mutable static state, no process-wide environment/culture/current-directory changes.

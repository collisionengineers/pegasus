# I8: one Audit reference prefix (`a.`) for both assessment outcomes (stacked on PR 765)

## Aim

Operator ruling (16 Sep 2026): the Audit prefix no longer encodes the assessment outcome. Every new
Audit Case/PO reference uses `a.`; `ap.` is retired. New-only: no migration or renumbering, because the
test estate is wiped at release (the release procedure runs the intake-data wipe; do not run it). The
legacy same-record Engineer-Finding path (`EfRecordEngineerFinding.cs`, `Workflow.cshtml.cs` handler,
`DependencyInjection.cs:581`) stays untouched and is flagged in the PR. Inputs:
`C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/audit-reference-change/investigations/investigation-1.md`,
`investigation-2.md` (line anchors drifted; content right), and
`.../audit-reference-change/pegasus-audit-reference-review-addendum.md` (AR-01…AR-05).
This branch is stacked on PR 765 (assessment-gated allocation); do not re-do its work.

## Changes

1. `src/Pegasus.Core/Cases/CaseContracts.cs:106–120` `AuditIdentity.Create(caseReference, assessment)`: emit
   `a.` for Repairable and TotalLoss alike; keep the `assessment` parameter and its invalid-enum validation
   (the assessment still gates creation per PR 765). Remove the only `"ap."` literal in `src/` (line ~114).
2. AR-02 codec: `EfCreateAuditCaseStore.cs` (~121, ~171) persists `Assessment.ToString()` while
   `EfLinkedCaseReplacementStore.cs` `ParseAssessment` (~506–512) accepts only `repairable|total_loss`.
   Use one Core-owned code mapping for both reads and writes (reuse `EfCaseAcceptanceStore`'s `ToCode` if that
   is the existing owner).
3. Corrected-principal replacement (`EfLinkedCaseReplacementStore.cs:152–153`) writes an unprefixed primary
   `Reference` with a prefixed `AuditReference`. A replacement Audit's primary reference must carry the
   prefix exactly like standalone acceptance (`EfCaseAcceptanceStore.cs:229–234`); keep the shared-sequence,
   two-record rule.
4. AR-05: the dedicated Case reference filter (`EfCaseQueryStore.cs:172`) searches `Reference` only while global
   search covers both; make the filter match either field.
5. Documentation: `CONTEXT.md:44,48`; `docs/capabilities.md:68` (CASE-04); `docs/frd/frd-01:16,21`,
   `frd-02:181`, `frd-05:53`, `frd-09:82`, `frd-11:47`; `docs/adr/0051-*.md:18,26,45–46,52` (amend the numbering
   clause only; keep the two-record, shared-sequence, nested-custody decisions); `docs/adr/0002-*.md:20`;
   `docs/prd/pegasus-product.md:175`; `docs/principal-profiles/qdos.md:120`; `docs/runbook.md` where it
   names the prefixes. State the rule once: an Audit reference is `a.` + the original reference; the
   assessment outcome is recorded on the Case, not in its identity. Prose only; no new file.
6. Tests: `tests/Pegasus.Core.Tests/Lifecycle/CreateAuditCaseTests.cs:17,55,119`;
   `tests/Pegasus.IntegrationTests/CaseRecordFrameV26WebTests.cs:189,200,453` (on this base the file still
   declares `partial class CaseDetailsWebTests`; edit in place, do not restructure);
   `ProviderApiSubmissionTests.cs:482` (`Assert.StartsWith("ap.", …)`). Add a replacement-path test proving the
   prefixed primary reference, a codec round-trip test, and a filter test for AR-05. Regenerate any synthetic
   fixture that embeds `ap.`; leave historical exports under `reference/` untouched and list them in the summary.

## Acceptance

- No `"ap."` literal remains in `src/`; `AuditIdentity.Create` still validates the assessment.
- Codec unified; replacement primary reference prefixed; dedicated filter matches both fields.
- All listed docs updated with one consistent rule; ADR-0051 decisions other than numbering intact.
- Tests updated and added as listed; legacy Engineer-Finding path unchanged and named in the summary.
- No migration; no data script; no change to PR 765's allocation gating.

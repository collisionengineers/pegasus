# P767: repair the failing tests on PR 767 (`codex/case-linking-pdf`, stacked on PR 766)

## Aim

PR 767 "File matched email PDFs and photographs on existing Cases" failed ten tests in CI run
35096092350:

- `MultiFormatIntakeWebTests.LowTextPdfWithoutDominantRasterDoesNotSelectOcr`
- `MultiFormatIntakeWebTests.MostlyOffPageRasterUsesVisibleIntersectionAndDoesNotSelectOcr`
- `MultiFormatIntakeWebTests.TwoDiscretePdfImageObjectsRemainSeparateInReceipt`
- `MultiFormatIntakeWebTests.PdfImageObjectBudgetIsSharedAcrossEmailAttachmentsAndConfirmingBodyFailsClosed`
- `MultiFormatIntakeWebTests.FullPageRasterWithLowTextProducesExactlyOneScannedPageOcrCandidate`
- `UnidentifiedRecordWebTests.APdfWithSelectedPhotographsShowsOnlyThosePhotographsAndTheirCustodyState`
- `UnidentifiedRecordWebTests.AGroupOfProcessedPdfReceiptsShowsEachOriginalAndSelectedPhotograph`
- `ImageCaseCustodyIntegrationTests.PdfParentRetainsItsSourceAndEachSelectedEmbeddedPhotographExactlyOnce`
- `ImageViewingWebTests.ImageCasePageAndCaseEvidenceTabRenderTheGallery`
- `QdosAllocationRecoveryTests.UniqueExistingCaseAssociationBypassesNewAllocationExactlyOnce`

The dispatcher has merged the repaired base branch (`origin/codex/u50-pdf-image-intake`) into this
branch and reproduced the remaining failures at HEAD; the log tail is appended below. Some of the
ten may already pass after the merge; fix only what still fails.

## Required change

The PR's contract: file the original email, PDF and selected photographs against the uniquely
matched Case through existing custody and retry mechanisms with stable operation identities;
clear "Images required" only after every planned file has confirmed custody; reasoned
re-evaluation of associations; the Inbox shows the linked Case as destination while keeping the
independent classification. Read FRD-01, FRD-02, FRD-05 and FRD-08 and the branch diff
(`git diff origin/codex/u50-pdf-image-intake...HEAD`). Fix the wrong side per test; never weaken
an assertion silently; explain each test-side change.

## Acceptance

- All ten named tests pass; no regression in the five classes of the verifier filter.
- Custody rows and operation identities stay exactly-once under retry.
- No changes outside `src/` and `tests/` except FRD wording corrections the fix requires.

# I7: Pegasus UI guardrails skill and root routing rule (docs only)

## Aim

Land the mandatory UI guardrail skill from the operator's pack
`C:/Users/Alex/Documents/GitHub/pegasus/1609sprint/pegasus-ui-agent-guardrails/` and the root routing
rule. Nothing else from the pack: no nested `src/Pegasus.Web/**/AGENTS.md|CLAUDE.md` (the placement gate
does not admit them), no gate exception, no mechanical CI lint, no screenshot baselines.

## Changes

1. Copy `.agents/skills/pegasus-ui-guardrails/SKILL.md` and `references/case-workspace.md` from the pack into
   `.agents/skills/pegasus-ui-guardrails/` (an allowed root). Fix relative links so they resolve from the new
   location. One factual correction: the pack lists `Send to Claude` as retired operator copy; it is live
   approved copy (`src/Pegasus.Web/Presentation/OperatorLabels.cs:2107`), so remove it from any
   retired/forbidden list. `Add valuation` is genuinely retired (PR 758) and stays.
2. Add the routing rule to root `AGENTS.md` and root `CLAUDE.md` (both exist), in the Pegasus repository
   instructions section, verbatim from the pack README:
   > Any change under `src/Pegasus.Web` must follow `.agents/skills/pegasus-ui-guardrails/SKILL.md` and any nearer local `AGENTS.md`/`CLAUDE.md`. A Web feature or bug fix is not permission to redesign the UI.
3. Do not modify `scripts/Test-MarkdownPlacement.ps1`, `src/`, or `docs/design/README.md`.

## Acceptance

- Skill files exist under `.agents/skills/pegasus-ui-guardrails/` with working relative links and the v26 facts unchanged (`--content-max:1580px`, 1440/760 breakpoints, self-hosted Inter Variable).
- `Send to Claude` is not listed as retired copy anywhere in the skill.
- Root `AGENTS.md` and `CLAUDE.md` carry the routing rule once each.
- No other files changed.

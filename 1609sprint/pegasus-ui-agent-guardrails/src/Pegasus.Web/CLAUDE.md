# Pegasus Web UI local instructions

Before changing any file at or below this directory, read and follow:

- `../../.agents/skills/pegasus-ui-guardrails/SKILL.md`
- the repository-root `CLAUDE.md`
- the relevant Razor UI skill(s)
- the relevant portion of `docs/design/README.md` and FRD-12

The UI guardrail skill is mandatory, not optional guidance.

Unless the current operator task explicitly requests a redesign or layout/style change, preserve the
current v26 shell, visual language, component vocabulary, action hierarchy and page geometry.
A feature, bug fix, backend change or refactor is not design authorization.

If these local instructions conflict with an explicit current operator instruction, follow the
operator instruction and update the owning design/FRD contract when the change is durable.

# TKT-310 — corpus/baseline read, 2026-07-21

Read-only inspection at the time of the inbound-triage rebuild investigation.

- `scripts/evaluation/email/baseline-v1.json` and `baseline-v2.json` exist; no `baseline-v4.json`.
  The classifier ships taxonomy v4 (per `email_classifier.py`'s Rule `0a` website-enquiry, the v4
  delta over v2), so the last generated baseline predates the currently-shipped rule set.
- `emailevals/to-sort/` contains 130 `.eml` files, named descriptively (claimant/vehicle/ref) but
  not filed into the taxonomy leaf structure.
- `emailevals/{general,received,sent}` contain roughly 60 category/subcategory directories; a
  directory-walk found only 3 with `.eml` files placed directly in them at the time of this read —
  the great majority of leaf folders are empty, consistent with the "6% populated" figure carried
  from the originating investigation.
- `emailevals/AGENTS.md` and `emailevals/PLAN.md` exist and describe the sorting workflow this
  ticket follows (batch staging, one work-log per batch, human review) — read those directly
  rather than duplicating their content here; they are the working method, this ticket is the
  acceptance.
- `scripts/evaluation/email/run_eval.py` exists and is the tool named for regenerating the
  baseline.

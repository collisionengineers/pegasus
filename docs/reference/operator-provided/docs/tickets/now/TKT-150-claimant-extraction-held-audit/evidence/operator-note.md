# Operator note — claimant-name gaps

Cases in Held and other queues are missing claimant names. QDOS26079 is a named example with many missing details. Determine why claimant extraction or persistence is failing, fix the owning seam, inspect all held cases for the same problem, and rerun affected cases. A missing claimant must keep the case out of Review.


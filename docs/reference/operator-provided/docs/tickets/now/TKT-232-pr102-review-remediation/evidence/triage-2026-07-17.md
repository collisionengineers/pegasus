# PR #102 review-comment triage — 2026-07-17

All 23 inline comments + 3 review bodies (Codex bot) verified against HEAD `94544380` by three
read-only per-file passes (code quoted) plus an architecture pass on the risky fixes. The
dev-mode `case_ref` finding appears 4× (3 review bodies + inline) and the curl-parsing finding
3×; de-duplicated below. Verdicts: **19 STILL-VALID, 1 INVALID**. None were already fixed by
TKT-225–231.

| # | Comment id(s) | Sev | Finding | Verdict → Change |
|---|---|---|---|---|
| F1 | 3597536135 | P1 | recovery.ts folder guard precedes allowCasePoMint — dev mode never mints | VALID → C4 |
| F2 | 3598638058 + 3 review bodies | P1 | Note claims discovered PO recorded as case_ref; `\|\|` chain stores externalRef | VALID → C5 (honesty + structured audit; precedence kept by design) |
| F3 | 3597888606 | P2 | Related-link overwrites existing deterministic classification | VALID → C3 |
| F4 | 3597888615 | P2 | `linked` counted when upsert returned null | VALID → C1 |
| F5 | 3599105116 | P2 | currentInboundCaseId not mailbox-qualified | VALID → C2 |
| F6 | 3600586563 | P2 | Ambiguous-suggestion row lookup not mailbox-qualified | VALID → C2 |
| F7 | 3600586565 | P1 | TOCTOU: ON CONFLICT `COALESCE(EXCLUDED.case_id, …)` re-points concurrent links | VALID → C1 |
| F8 | 3597536143 | P1 | Claimant-only Outlook hits lack provider corroboration | VALID → C6 |
| F9 | 3598034168 | P1 | 500-deep external-ref sweep lacks provider corroboration | VALID → C6 |
| F10 | 3598638062 | P1 | Weak-key related mail linked on subject match alone | VALID → C6 |
| F11 | 3597888611 | P2 | Related searchMessages omits onTruncated | VALID → C7 |
| F12 | 3597926399 | P2 | 25-cap before exclusions — reruns cannot advance | VALID → C7 (route-side new-link cap) |
| F13 | 3600586564 | P2 | One getMessageIdentity failure kills the whole backfill | VALID → C7 |
| F14 | 3600227945 | P2 | Images classified before parsed VRM available | VALID → C8 |
| F15 | 3597926402 | P1 | Located folder discarded when fetch fails → outlook_only/none | VALID → C9 |
| F16 | 3598034162 | P2 | Failure stamp WHERE has no mailbox predicate | VALID → C2 |
| F17 | 3599709151 | P2 | `force` restarts successful (created/linked) drains | VALID → C10 |
| F18 | 3600227936 | P1 | Refused Box original falls back createFromOutlook(false) — Box identity lost | VALID → C9 |
| F19–21 | 3597926393, 3599105110, 3599709145 | P1 | curl `-XDELETE` / `--json` / attached `-d`,`-F`,`-T` classified read-only | VALID → C12 |
| F22 | 3599709148 | P2 | `retro_related` staff-selectable via SUBTYPES_BY_CATEGORY | **INVALID** — that map drives the inbox FILTER (`inbox.view.tsx:111-123`); the reclassify control is the separate hardcoded `RECLASSIFY_TAGS` (`inbox-panels.tsx:15-16`), which never contained it |
| F23 | 3600586567 | P1 | AND-guarded `after::jsonb` cast can 22P02 the case queues | VALID → C11 |

Key verification facts (quoted in full in the session triage):

- `persistence.ts:151` was `COALESCE(EXCLUDED.case_id, inbound_email.case_id)` — incoming
  wins; every upsert caller was enumerated and none legitimately re-points (merge/detach use
  their own guarded direct UPDATEs), so first-link-wins is safe globally.
- `recovery.ts:123-143`: the `archive_identity_requires_review` return runs before the
  `allowCasePoMint === false` check; retro dev inserts box_folder_id with no case_po, so the
  mint path was unreachable. A plain reorder was rejected: normal intake attach also calls
  with `allowCasePoMint: true` and must NOT mint over a real archive folder.
- The Box arm already required principal-sender agreement for weak keys
  (`retro-activities.ts:227-234`); the Outlook and related arms had content-only corroboration.
- `-XDELETE`/`--json`/`-dDELETE` were live-tested against the classifier regexes: all three
  classified read-only before the fix.

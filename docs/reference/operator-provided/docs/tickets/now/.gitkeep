# Keeps the `now` status directory tracked when it is momentarily empty (git does not track
# empty directories, and scripts/checks/check-tickets.mjs requires every status directory to exist).
# Safe to leave: check-tickets skips non-directory entries; check-doc-links ignores non-.md files.

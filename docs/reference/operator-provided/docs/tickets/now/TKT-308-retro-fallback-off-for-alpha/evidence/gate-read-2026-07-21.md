# TKT-308 — gate read + live incident summary, 2026-07-21

## Code default

`packages/domain/src/gates.ts`:

```ts
retroCase: (): boolean => process.env.RETRO_CASE_ENABLED === 'true',
```

Default off (unset/anything but the literal string `'true'`). The comment above it: "retroCase is
the master switch (read by the orchestration retro activities AND the Data API's
`/api/internal/retro/*` routes — set it on BOTH apps)".

## Live incident this ticket exists to stop recurring

A companion diagnosis (Box terminal-retry-loop investigation, same day) found the retro fallback
fired live on `cespk-orch-dev` on 2026-07-21: case `13f1c47f-f337-48e7-8a2d-a43b3ff9e40e`
(Case/PO `A.QDOS26229`) was reconstructed with `reconstructionSource: box_eml` from a
staff-forwarded QDOS instruction that never minted through normal intake, and was stamped with a
live-Box folder id (`401801654393`) outside the pinned write root
(`BOX_ALLOWED_ROOT_ID`/`BOX_FOLDER_ROOT_ID` = `392761581105`). The audit event:

```json
{"casePo":"A.QDOS26229","discoveredArchivePo":"A.QDOS261819","reconstructionSource":"box_eml",
 "boxFolderId":"401801654393"}
```

Since the code default is off, this means the live `RETRO_CASE_ENABLED` app setting is currently
`true` on at least `cespk-orch-dev` (and possibly `cespk-api-dev`) — a live/code divergence this
ticket asks an operator to correct for the duration of the alpha.

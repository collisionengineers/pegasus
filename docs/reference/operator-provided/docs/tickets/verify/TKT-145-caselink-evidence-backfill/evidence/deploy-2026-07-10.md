# TKT-145 — deploy + smoke evidence (2026-07-10)

## Pre-deploy verification (verify, don't assume — all live-read 2026-07-10)

- Queue account confirmed **`cespkorchstdev01`**: orch `AzureWebJobsStorage__accountName=cespkorchstdev01`
  (identity-based host storage) and api `OUTLOOK_MOVE_QUEUE_SERVICE_URL=https://cespkorchstdev01.queue.core.windows.net`.
- RBAC (ARM REST — `az role assignment` MissingSubscription gotcha routed around):
  - api MI `51dcdd5f-7802-40e6-9388-18b66e006e71` holds roleDefinition `c6a89b2d-59bc-44d0-9896-0f6e12d7b80a`
    (**Storage Queue Data Message Sender**) at the **account scope** on cespkorchstdev01 → covers the new
    queue, **no new grant needed** (as the brief predicted).
  - orch MI `ad47e928-c6b5-4b24-895a-0500a9f5b22d` holds Storage **Queue Data Contributor**
    (`974c5e8b…`) + Blob Data Owner + Table Data Contributor at account scope → the consumer trigger reads fine.
- `RETRO_OUTLOOK_SEARCH_ENABLED=true` live on cespk-orch-dev (the $search fallback's gate).

## Queue provisioning (the TKT-091 QueueNotFound lesson — provision BEFORE deploy)

```
az storage queue create --name evidence-backfill --account-name cespkorchstdev01 --auth-mode login
→ { "created": true }
az storage queue list … → evidence-backfill, outlook-move (both present)
```

## Build + local smoke (Windows, per docs/operations/deployment.md)

- `tsc -b` green: packages/domain, api, orchestration.
- Test suites green: domain 50 files/1076 · api 38 files/395 · orch 18 files/271.
- `node scripts/build/build-orchestration.cjs` / `node scripts/build/build-api.cjs` → "orch bundle OK" / "api bundle OK" (import.meta.url banner intact).
- `npm install --prefix deploy/{orch,api} --omit=dev` → node_modules shipped.
- Local registration smoke (`node -e "require('./main.cjs')"`): **orch 73** registrations
  (incl. `evidence-backfill`), **api 95** — no `createRequire`/`ERR_INVALID_ARG_VALUE` crash.

## Publishes (Windows `func`, orch FIRST then api)

| app | before | after | new function |
|---|---|---|---|
| cespk-orch-dev | 72 | **73** (az functionapp function list) | `evidence-backfill` [queueTrigger] |
| cespk-api-dev | 94 | **95** (az functionapp function list) | `internalInboundEvidenceBackfill` [httpTrigger] |

## Post-deploy smoke

- Both apps `Running` (ARM `properties.state` — the plain `az functionapp show --query state`
  returns empty on these FC1 apps).
- App Insights (cespike-parser-ai-dev, union exceptions+traces, severity ≥ 3, 20-min window,
  cloud_RoleName in Data API/orchestration): **0 rows** — no error burst.
- Graph subscriptions: `VERIFY_LIVE=1 node verify-all.mjs` → **"PASS — LIVE_FACTS.json matches live"**
  (the drift check diffs the subscription count (3) + mailbox set against Graph; no drift).
  Full gate: 12 passed / 1 failed / 8 skipped — the single FAIL is the **pre-existing
  Windows-environmental parser pytest** (memory: windows-parser-test-preexisting-failures; no parser
  change in this ticket).

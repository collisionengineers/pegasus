If an evidently different vehicle is added to a case  (mainly different reg, different colour), then these images need to be set to exclude by default with a warning.

This could be because they are third party pics, or maybe someone just made a mistake, or even a bug in the automations. Either way it needs to be caught.


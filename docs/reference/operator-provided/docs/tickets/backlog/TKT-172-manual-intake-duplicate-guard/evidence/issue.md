Needs to autocheck for duplicate registration (considering both with and without space e.g. AB12 ABC or AB12ABC) when entering a manual case (both an image only, AND an instruction based case). Note that If an opposite type of case exists it should propose a merge. Ie if someone tries to upload an images only case, and instruction case is already logged, it should prompt the user to merge. 



If the user tries to log images only case and an image case already exists for the registration, it would prompt the user with a warning (not a blocker - can be dismissed, as mentioned elsewhere could be same car in 2 accidents).


---
name: ccc-plans-restructure
description: "CCC docs/plans reorganisation into broad workspaces, the development spine, and the agreed working order"
metadata: 
  node_type: memory
  type: project
  originSessionId: ec0f13f7-be83-463c-92d5-90d309cf6bad
---

Reorganising the 22 flat `docs/plans/` workspaces into **10 broad groups** (foundation, parser, bridge, casework, intelligence, mcp-tooling, agent-skills, ai-platform, business, unified-platform), tagged by **wave + layer**. Authoritative map: `docs/plans/_groups.md`.

**Development spine:** parser (G1) → bridge / Outlook+Box+EVA (G2) → … → unified-platform (G6, later). **mcp-tooling** and **agent-skills** are **parallel tracks**. `../collisionplugin` feeds those two (vehicle-valuation/roadworthy/rebuttal/total-loss skills + DVSA-MOT/Autotrader connectors).

**Decisions (user, 2026-05-29):**
- **Planning-first**: do the logical grouping + per-workspace brainstorming interviews now; the **physical folder consolidation is DEFERRED** to a later scripted/committed migration (task #1). Reason: the verifier/bootstrap are deeply wired to the legacy folders (`initial-repo-setup`, `operational-core`) — high churn. See [[ccc-scaffold-tooling]].
- Each broad workspace gets a full doc set: `context.md` (info) + expanded `plan.md` + `open-questions.md` + a dated spec in `docs/superpowers/specs/`.
- Interview order: parser → bridge → mcp-tooling → agent-skills.

**Status:** Phase-2 grouping overlay done (`_groups.md`, verifier green). **Parser interview DONE** — spec `docs/superpowers/specs/2026-05-29-parser-iteration-design.md`; parser iteration = local OCR via **ocrmypdf** + provider coverage parity for the 4 (real, confirmed) principals ACSP/OAK-AX/PRINCIPAL/WOODLANDS + table-layout hardening + Tk confidence/provenance + batch/throughput; **keep Tk UI, deterministic-first, local-only (no cloud/AI)**. New presets go in the versioned fixture `parser_provider_presets_v1.json`, NOT the raw 26-pinned `providers.json`. **Bridge interview DONE** — spec `docs/superpowers/specs/2026-05-29-bridge-design.md`; staged: build read-only Outlook intake (delta-query polling + app-only `Mail.Read` scoped via App RBAC to desk@/engineers@/info@/digital@; preserve originals; dedup; route attachments through existing parser triage), keep Box `package_only` + EVA export-only; governed option-papers (not built) for live Box upload, EVA/Sentry submit, spreadsheet bridge. Outlook design grounded in Microsoft Learn MCP. **mcp-tooling interview DONE** — registry/security-gateway design + connector inventory (build deferred); DVSA-MOT to be rebuilt as a first-party MCP (⚠️ live bearer token sits in plaintext at `../collisionplugin/connectors/dvladvsa/connectorurl.md` — must be ROTATED); Autotrader stays a Codex-wrapping connector (standalone MCP not viable without Autotrader API cost). **agent-skills interview DONE** — portable catalogue + lifecycle on the `SKILL.md` contract; adopt collisionplugin skills (vehicle-valuation, rebuttal, roadworthy, total-loss, ce-style) + new `ce-branding` skill + case-summary/missing-info; expert/legal outputs require human sign-off. **All 4 priority interviews complete; scaffold verify green throughout.** Priority interviews committed on branch `docs/workspace-grouping-and-interviews` (commit 6877da3, 32 files, not pushed). **casework interview DONE** (uncommitted) — full work-item state model (lifecycle + missing-info + dedup/merge/link/split + historical search) + local **SQLite** store + separate **WinUI 3** Windows app (build via the `winui-dev` agent + winui skills) + minimal audited automation; cross-language Python(parser/bridge)+.NET(casework UI); store access boundary (shared SQLite vs Python service) is an open option-paper; foundation ticket `p1-work-item-store-and-service.md` unblocks bridge intake. **ALL 10 broad-group interviews complete** (parser, bridge, mcp-tooling, agent-skills, casework, intelligence, ai-platform, business, foundation, unified-platform) — each with context.md + dated spec in docs/superpowers/specs/ + expanded plan.md + open-questions.md + option-papers. Multi-folder groups got new group-home folders (docs/plans/{intelligence,ai-platform,business,foundation,unified-platform}/) — additive, NOT a physical move of the existing sub-area folders. Foundation authored governance drafts needing sign-off: `docs/security/role_model.md`, `docs/security/data_retention_policy.md` (placeholder periods), vendor_register update, and `docs/plans/foundation/option-papers/cross-cutting-gaps-backlog.md`. Casework UI = WinUI 3 (winui-dev agent) over a SQLite work-item store. All pushed to **PR #1** (github.com/collisionengineers/ccc/pull/1) on branch `docs/workspace-grouping-and-interviews`: 6877da3 (priority-4), 095252e (casework), 69fce6a (remaining-5 + foundation governance), 307dc21 (**Phase-4 migration** — collisionplugin skills now in **`src/skills/`**: vehicle-valuation, total-loss, rebuttal, roadworthy, ce-style, ce-branding; DVSA token file excluded), 110e277 (follow-ups: `pyproject.toml` `[skills]` extra; shared templates+brand consolidated into `ce-branding/assets/` with `_pdf_common.py` repointed via `CE_BRANDING_DIR`; rebuttal/roadworthy SKILL.md wrappers). verify_scaffold green throughout; valuation scripts py_compile-clean. Remaining: physical folder consolidation (deferred, task #1); lifecycle-substrate (ai-platform) + tool-gateway (mcp-tooling) wiring; **ROTATE the DVSA-MOT token**; run a valuation render once `[skills]` deps install (couldn't run here — pytest + render deps absent in C:\Python314).

**How to work:** the user prefers efficient batched decisions; heavy `AskUserQuestion` previews once stalled — keep questions lightweight.

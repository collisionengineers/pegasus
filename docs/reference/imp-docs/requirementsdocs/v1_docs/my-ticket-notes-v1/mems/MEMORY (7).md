- [PyMuPDF licensed](pymupdf-licensed.md) — AGPL concern resolved; never raise it for cedocumentmapper again
- [az appsettings @file gotcha](az-appsettings-file-gotcha.md) — KV-ref/conn-string app settings must go via a JSON @file (ASCII array), not inline; az.cmd mangles ( ) ; on Windows
- [Live-services boundary](live-services-boundary.md) — 2026-06-20 OVERRIDE: Claude now wires activations directly (incl. live flow edits via byte-identical-trigger); only secrets-Claude-lacks, live email sends, Entra consent + live-confirm stay the operator's
- [Provider corpus analysis](provider-corpus-analysis.md) — EVA principal/garage/location findings + reproducible scripts in raw/principalandrepairersheets/outputs
- [Flow webhook trigger provisioning](flow-webhook-trigger-provisioning.md) — Office 365 flow triggers can't be armed via Dataverse clientdata API; rebuild the trigger in the designer (concurrency gotcha)
- [Code App CSP → use connectors](codeapp-csp-use-connectors.md) — Code Apps enforce connect-src 'none'; route external calls through a custom connector, never raw fetch
- [API-key connector connection](codeapp-apikey-connector-connection.md) — connector must define the api_key param first (download→edit apiProperties→update→BAP PUT→add-data-source); key lives on the connection
- [Parser connector base64 double-encode](powerplatform-connector-base64-double-encode.md) — gateway re-encodes the document param + DRIFTS; flow passes the RAW base64 string (NEVER base64ToBinary — it 400s; NEVER format:byte); the tolerant parser decode is load-bearing
- [Queue & case model](queue-case-model.md) — 2026-06-20 redesign: 3 queues (Not Ready/Review/Held) + Case/PO per instructions case + auto-merge-by-reg + AX inspection=Image Based Assessment
- [Enrichment activated](enrichment-activated.md) — 2026-06-20 flipped ENRICHMENT_ENABLED=true (the only blocker); chain was already built+verified; mileage is an MOT-history estimate so near-new vehicles get none
- [Inspection image-based detection](inspection-image-based-detection.md) — FIXED 2026-06-21: parser now emits canonical "Image Based Assessment" for image-based/desktop docs (all providers), deployed + live-verified
- [Parser vendored divergence](parser-vendored-divergence.md) — functions/parser VENDORS cedocumentmapper_v2 and has diverged from the sibling repo (vendored=B2 contact extraction, sibling=image-based fix); edit the right copy and reconcile
- [Job-sheet provider rules](jobsheet-provider-rules.md) — CE Job Sheet Principals sheet mapped to cr1bd_workproviders (applied 2026-06-21); inspection TYPE (desktop, always on) ≠ LOCATION (image-based vs address) — desktop-% is NEVER a modality signal; 0 genuine contradictions after 2026-06-21 ruling (RJS is address-based, not image-based)
- [Intake repo trails live](intake-repo-trails-live.md) — live CS Intake has Run_enrich + Run_case_resolve wired (verified 2026-06-21 via clientdata); repo intake.definition.json lacks them → reconcile before any solution re-import; reviewing the repo alone false-positives "auto-merge never runs"
- [Box integration pivot findings](box-integration-pivot-findings.md) — dossier at repo-root box-integration-pivot/; verdict=additive hybrid (Dataverse stays authoritative, not cheaper); KEY: any Box automation beyond plain upload needs a CUSTOM Box REST connector (CCG/JWT) — first-party connector is file-only+OAuth-only; File Request API is copy-from-template only; floor = base Box Business (Business Plus only for the optional metadata field)
- [Box test account](box-test-account.md) — throwaway Box test creds stored OUT-OF-REPO (path in the note); FREE account so CCG does NOT work + no File Requests/metadata (Business+); dev-token REST only; overrides "Claude never holds a Box credential" for THIS test account only
- [Box scope guard](box-scope-guard.md) — live Box build hard-scoped to test folder 392761581105; blocking PreToolUse hook + tools/box-scope.json liveReady toggle; arm via /hooks
- [Loc export artifact / no address matcher](loc-export-artifact-no-address-matcher.md) — `Loc` is an EVA-export artifact, not an intake input; inspection address = offline-derived FULL-address suggestions + manual confirm; partials are an offline backlog, never live; NO runtime matcher (ripped out 2026-06-23, ADR-0013)
- [Code App lookup writes need @odata.bind](codeapp-lookup-write-odata-bind.md) — Dataverse WRITES bind a lookup via `<Nav>@odata.bind` (e.g. cr1bd_Caseid@odata.bind), NEVER the read-only `_<rel>_value` form; the seam bridge forwards raw to the SDK so tsc+tests pass over a write that 400s live (fixed env-var/note/provenance/merge 2026-06-23)
- [Code App env-var tables not wired](codeapp-envvar-tables-not-wired.md) — getBoxGates + getHoldNewCasesDefault return false LIVE (env-var data sources not bridged); pac generates the GENERIC MicrosoftDataverse connector for system tables, so wiring needs a custom adapter — deferred follow-up bundled with Box go-live
- Junk-case cleanup — blank/identity-less `error`-status cases are junk; clear via the in-repo `/junk-case-cleanup` skill + `dataverse/.build/delete-junk-cases.ps1` (dry-run-first, blank-guarded). The `error`/Held split is INTENDED (a case with an unparseable attachment is a genuine exception), not a bug.
- [Box pivot Phase 7 merged](box-pivot-phase7-committed.md) — PR #13 MERGED to main 2026-06-22 after a Codex review pass (webhook now inline + 503-retry, NOT fire-and-forget-202; writes cr1bd_boxfileid + acceptedforeva; canonical audit no cr1bd_detail; purge image-only; dedup stays on the box:file tag); Dataverse schema LIVE (gates OFF) + Code App redeployed (Box dormant); Function/connector/flows deferred to the base-Business-account phase; ListFolder sweep documented-not-built; added docs/roles-and-permissions.md
- [Audit case-type](audit-case-type.md) — 2nd independent CE inspection auditing a 3rd-party engineer's original report; A. Case/PO prefix; NOT the engineer-report overlay; parser layer built 2026-06-23 (both copies, new `audit` envelope field), Dataverse/flows/Code App planned (ADR-0014)

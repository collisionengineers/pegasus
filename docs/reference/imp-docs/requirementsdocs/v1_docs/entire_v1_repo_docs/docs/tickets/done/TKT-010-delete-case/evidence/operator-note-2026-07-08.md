# Operator direction — 2026-07-08 (workstream item 13)

Change "delete case" to "Close case" and make it available to ALL users (not just superuser).

This re-scopes the ticket: the action is a close (terminal soft state), not a delete, and the superuser gate is dropped. This unblocks the ticket (the prior block awaited an operator decision on delete semantics).

# Operator follow-up — 2026-07-13

The assistant should receive the actual selected image content so its multimodal model can read a visible
registration, identify the case and then ask for confirmation before attaching the files. PII or client
data is not a reason to substitute filenames for raw image content. Four supported files failed to add in
the supplied live reproduction.

# Operator note — delete image

Add a delete button to each image. Deleting an image should remove its active stored copy and its archive copy as well as remove it from the case.

The original drop-note is preserved beside this file after distillation.

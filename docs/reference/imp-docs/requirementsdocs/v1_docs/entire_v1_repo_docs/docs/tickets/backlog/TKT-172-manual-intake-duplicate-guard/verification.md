# Verification — TKT-172: Check matching registrations before Manual Intake creates a case

## Verdict
PENDING

## Acceptance evidence matrix

| Acceptance | Offline evidence required | Signed-in/live evidence required | Verdict |
|---|---|---|---|
| A1 — canonical check in both modes | Domain/API/SPA tests prove spaced, punctuated and compact variants return the same active/held/pre-case candidates in image and instruction modes. | Signed-in browser proof on operator-designated real work enters equivalent registration shapes without creating a case and shows the same existing records. | PENDING |
| A2 — opposite-type merge proposal | Interaction and merge-contract tests prove the named survivor, carried data and explicit confirmation are shown before any mutation. | A naturally occurring operator-approved images-only↔instruction pair displays the merge/use-existing proposal and performs it only as a genuine signed-in decision. | PENDING |
| A3 — dismissible same-type warning | Tests assert creation remains available, requires a reason and writes a pair-specific audit decision. | A genuine same-type new incident shows the warning and retained audit reason when the handler proceeds operationally; no disposable case is created for proof. | PENDING |
| A4 — incident-date safety | Parameterized tests cover equal, different, corrected and missing dates and prove no date state auto-merges. | Operator-designated confirmed different-date cases are shown as distinct; a same-date pair still waits for signed-in confirmation, and an audited date correction is the only reopen path. | PENDING |
| A5 — sufficient candidate context | API projection and UI tests assert every required field and the “Awaiting instructions” fallback without technical labels. | Browser capture shows two real operator-designated candidates can be distinguished from the displayed context. | PENDING |
| A6 — transactional server recheck | API race tests insert a candidate after preflight and prove commit returns the updated decision instead of creating blindly; direct-request bypass is refused. | Live proof uses only a naturally occurring approved concurrency event; otherwise this class remains PENDING after isolated non-live race proof. | PENDING |
| A7 — no registration-only destructive merge | Integration tests cover cross-provider and conflicting-field candidates and assert no retire/overwrite until the canonical merge choices are confirmed. | Read-only signed-in inspection shows real cross-provider/conflict examples remain separate; any resolution is captured only when operationally required and approved. | PENDING |
| A8 — idempotent create or merge | Retry/response-loss/double-click tests assert one operation, case, Case/PO, evidence set and merge audit. | Observe an operator-approved real submission/retry and reconcile one case and one side-effect set; do not replay a live request solely to manufacture proof. | PENDING |
| A9 — deterministic multiple-candidate handling | Ranking, pair-suppression and reversal tests cover several matching records without hiding unrelated pairs. | Signed-in view shows all naturally occurring candidates and, when genuinely chosen, proves one separate-incident decision suppresses only that pair. | PENDING |
| A10 — complete regression and deployed flows | All named domain, API, merge, intake and SPA suites pass in both directions. | Recorded signed-in run covers image-only and instruction Manual Intake without mutating production cases. | PENDING |

## Pending / gaps
Implementation and all offline and signed-in/live proof are pending.

## How to re-verify
Run the offline matrix and gather signed-in evidence only from operator-designated real work, attach one concrete artifact to every available row, and retain PENDING for any live class that has not occurred naturally.

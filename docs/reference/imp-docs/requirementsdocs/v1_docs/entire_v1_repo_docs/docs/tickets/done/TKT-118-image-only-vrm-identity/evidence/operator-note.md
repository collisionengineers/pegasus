# Operator note — 2026-07-08 workstream

> New cases -> Image based: needs renaming from "Image Based" - could cause staff confusion with "Image Based Assessment" - Image based in this context just means images received without instructions. This type of case needs to be identified internally with VRM rather than Case/PO number (Case/PO cannot be generated until instructions received as we do not technically know which provider it is for. In some cases potentially inferrable, but overall the best process is to identify on VRM.

(Verbatim item from the 2026-07-08 operator workstream dump; distilled into TKT-118.)

Evidence preview is very slow to load.

Need to investigate whether this is just a box problem, or can be resolved.

If not, a simple spinning icon with loading text until the image is showing would improve the looks of the app, so this would be the task if speed can't be remedied.

Also need to be able to click and open larger view of the image. 


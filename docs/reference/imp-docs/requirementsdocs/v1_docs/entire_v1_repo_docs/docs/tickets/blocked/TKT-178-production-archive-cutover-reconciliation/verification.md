# Verification — TKT-178: Reconcile active cases and the Archive at production cutover

## Verdict
PENDING

## Acceptance evidence matrix

| Acceptance | Offline/rehearsal evidence required | Signed-in/live evidence required | Verdict |
|---|---|---|---|
| A1 — all execution gates fail closed | Input-contract tests reject any missing job sheet/hash/signature, EVA probe, exact production root/write authority, backup/restore proof, frozen dry-run hash, implementation verdict/version, template, exact production webhook+buffer, source-bearing durable renewal after the latest manual renewal or named window approval; executors reject missing/mismatched run ID, ledger hash, artifact hashes and fence token. | Signed-in preflight records every approved artifact, successful EVA probe, exact root/identity permissions, deployed versions, backups, independent template/webhook readback, Box hold proof, source-bearing durable renewal and approval references; command presence alone confers no authority. | PENDING |
| A2 — restorable backups and scoped compensation | Snapshot/inverse rehearsal reconstructs every scoped relationship and Archive object with matching hashes/IDs while proving unrelated newer work is preserved. | Authenticated immutable backup manifests and per-row/object inverse plans are captured; no unqualified whole-store restore may rewind unrelated post-snapshot work. | PENDING |
| A3 — complete deterministic ledger | Closed-world union tests cover every sheet row, terminal/retired/merged/un-numbered DB row, source/destination Archive object, EVA result, Outlook evidence item and prior-number observation, with explicit source-only/out-of-scope dispositions. | Read-only production counts independently reconcile roster state, source presence, completion evidence and every exact object/parent ID to the frozen ledger. | PENDING |
| A4 — precedence and ambiguity refusal | Matching tests cover exact, normalized, weak, one-to-many and conflicting cases and assert only unambiguous high-strength matches proceed. | Independent reviewer samples each live match class and every ambiguous row remains held before approval. | PENDING |
| A5 — one qualifying completion signal | EVA/Archive/Outlook fixtures prove each permitted case-specific report signal and reject generic/draft/estimate/inbound/sent-only controls. | Every proposed production completion links to inspectable authenticated evidence; Outlook requires delivery receipt/vendor acknowledgement, not merely Sent Items. | PENDING |
| A6 — EVA is a mandatory whole-run gate | Gate/auth/contract tests prove every unavailable, disabled, unauthenticated, misrouted or incomplete state aborts before mutation; no `not queried` path may proceed. | A successful authenticated non-mutating production EVA contract probe is captured before the execution window; every scoped ledger row records its EVA query result. | PENDING |
| A7 — immutable zero-write dry-run and approval | Mutation spies prove zero writes; output includes deterministic action/inverse bytes plus mapping graph, target occupants, exact-old/version predicates, temporary stages and transaction boundaries. | Production audit/network logs show zero writes and retain named approval of all exact hashes before a window is requested. | PENDING |
| A8 — lossless canonical rename/merge | Snapshot executor tests preserve every listed record/byte and deduplicate only stable-ID/hash twins under normal and fault paths. | Approved production before/after samples reconcile cases, folders, unique hashes, notes, links, decisions and audits. | PENDING |
| A9 — conflicts stop one row without overwrite | Collision/missing-byte/human-conflict fixtures prove row-local hold, continued run and resumable decision path. | Each live held conflict shows its reason and unchanged before-state; unrelated approved rows can complete. | PENDING |
| A10 — Outlook read-only and separately authorized writes | Graph/provider call tests reject Outlook mutations, Viewer-only Archive authority and any target/action absent from the frozen ledger/root/permission scope. | Authenticated Graph/audit evidence shows Outlook reads only; the acting Archive identity's least-privilege write scope and exact approval are proven before every production write beneath the approved root. | PENDING |
| A11 — durable idempotent checkpointed execution | Replay, response-loss, worker-recycle, interruption and partial-failure tests assert one case/Case-PO/folder/byte/link/status transition and one EVA submission using persisted operation state plus the vendor idempotency/correlation contract. | Reconcile checkpoint/resume on a genuine approved ledger operation if naturally required; prove the EVA operation cannot replay after recycle and do not interrupt production solely for proof. | PENDING |
| A12 — evidence-based completed placement | Status tests map qualifying evidence to existing completed categories and retain active/held state for negative and conflicting rows. | Signed-in UI plus database/source checks prove each sampled completed row and each retained active/held control. | PENDING |
| A13 — guarded pre-root webhook gate, final root commit and ingress canary | Tests keep app mint roots unchanged through DB/Archive actions, enforce an exact source/destination allowlist, reject broad scope, stage+independently read back the exact template/production webhook through the signed-run target operation while Box events buffer without writes, set the three roots only as the final reversible config commit, and claim the immutable ingress instruction before issuing its one-shot lease. | Record exact template/webhook ID/target/callback, buffer checkpoint, root/actor/scope/window/prior values/health; canonical metadata proves the ingress canary's DB folder ID, provider object ID and parent root match. | PENDING |
| A14 — 100% final accounting and invariants | Reconciliation tests fail on omitted rows, duplicate Case/PO/folder identity, orphan links, missing notes/hashes or unsupported completion. | Independent production queries and signed-in samples account for every ledger row and prove all aggregate invariants. | PENDING |
| A15 — runnable runbook and independent verification | A second operator follows the rehearsal runbook through interruption and rollback without undocumented steps. | Independent verifier samples every production outcome class against job sheet and retained source evidence before sign-off. | PENDING |
| A16 — scoped write fence and durable resume | Concurrency tests fence every scoped writer, manual/API/admin create and both allocators while Graph+Archive ACK/durable enqueue and Graph renewal stay live; both event streams queue durably without scoped writes; manual renewal invalidates the source-bearing gate; leases issue only after claim/revalidation and revoke on abort/expiry; resume occurs once without loss/duplication. | Window evidence records fence token, writer inventory, Graph/Archive queue checkpoints, source-bearing unattended renewal, zero scoped drift, both named canary-only releases and clean ordered resume only after Archive/EVA hard-green. | PENDING |
| A17 — missing cutover capabilities implemented | Production-shaped tests prove importer/hash, EVA reads/durable state, complete merge/inverse, exact-object Archive executor+metadata, signed-run exact-target webhook staging, Box-event buffering, source-bearing renewal telemetry, deterministic compiler, batch Case/PO mapper and permanent/per-prefix fail-closed floor health while prior floors remain authoritative; stubs/raw helpers fail. | Exact reviewed commit/package/schema/config/vendor versions match the independently verified manifest and read-only preflight. | PENDING |
| A18 — pre-window approval then fenced delta | Tests freeze/reproduce all source/action/inverse/artifact hashes before window request and force a close/reapproval on any window-start delta that changes output. | Named approval predates the window; high-water/delta evidence byte-matches it before the first mutation. | PENDING |
| A19 — atomic mappings and EVA boundary | Real-index fixtures prove swaps/cycles/occupied targets/many-to-one refusal/stale versions/crashes, no committed temporary/NULL value, prior-max floors and fail-closed read errors; every typed pre-EVA inverse pops in strict LIFO order with a verified postcondition, while EVA accepted/unknown paths never blind-resubmit. | Per-component transaction/audit evidence matches the graph; strict-LIFO pre-dispatch rollback or post-dispatch persisted-correlation forward recovery follows the recorded boundary. | PENDING |
| A20 — bounded two-canary proof | Timebox and lease tests cover one read-only-nominated pending ingress instruction plus one pre-existing genuine EVA-ready case, claim/reservation before distinct lease issuance, changed/consumed eligibility, token revocation, replay and expiry; pre-EVA compensation preserves every genuine byte/edit/identity and never deletes real work. | The signed run records both exact ledger rows, fenced eligibility/claim, maximum duration and distinct one-shot leases; only the ingress path and one production EVA dispatch execute, with revocation/preservation or forward recovery evidenced at the irreversible boundary. | PENDING |

## Pending / gaps
TKT-178 is **BLOCKED**. The signed job sheet has not been supplied; the production EVA API is not
enabled, authenticated and verified; and the production Archive target plus write/rename/merge/retarget
authorization have not been approved or proven. No live cutover, deployment, production mutation, EVA
call or Archive retarget has run.

## How to re-verify
Do not execute from this ticket. First complete the offline runbook and rehearsal evidence. Only after
all three operator inputs and the remaining gates exist in one named window may future implementation
attach authenticated live evidence to every matrix row. Preserve the dry-run approval hash and backups,
and keep `PENDING` until an independent verifier certifies all twenty acceptance lines.

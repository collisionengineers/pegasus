Email flagged as query but this is receiving images.

Need:

Seperate categories for:

Enquiries
Case Queries

When images received by e-mail, need to match to existing case where possible. Need to sync to appropriate box folder or create one if registration is viewable.

In this case, no registration viewable.

Process would be:

1. Match to existing case if possible (claimant name, prior e-mails, client ref [not case/po] 
2. Create specific box folder for images if not possible to match (by registration)
3. If 1 and 2 are not possible, create flag on the e-mail. This would happen in this case, because the case doesn't exist on this system (it should, but since the system is new it hasnt been parsed), and the registration isnt viewable in the images so OCR would fail. 


# VRM junk data fix — live Postgres, 2026-07-09 (PLAN-003, TKT-071/085/100)

Delta: `database/migrations/2026-07-09-vrm-junk-cleanup.sql` (backup-first,
audited, idempotent). Applied 2026-07-09 as the Entra admin (`SET ROLE csadmin`) with a
transient firewall rule (created + removed the same session).

## Pre-check — every affected row (the acceptance's "each cleared value recorded")

case_ (2 rows):

| case_po | old vrm | family |
|---|---|---|
| QDOS26056 | AND2 | function-word head (TKT-100) |
| A.PCH26003 | OCTOBER | month word (TKT-085) |

inbound_email.body_vrm (9 rows):

| subject (40) | old body_vrm | family |
|---|---|---|
| RE: Your Ref: A.pch25537, Our ref: 56656 | THE3 | function-word head |
| FW: RE: Enclosing Inspection Request to… | OCTOBER | month word |
| ***URGENT*** FW: HD4110 - LETTER OF INST | HD4110 | postcode-area head (TKT-071) |
| RE: Our Ref: 572238 - Your Ref: a.pch264 | THE1 | function-word head |
| 46670/1 - … | AND2 | function-word head (QDOS) |
| 46640/1 - … | AND2 | function-word head (QDOS) |
| 46533/1 - … | AND2 | function-word head (QDOS) |
| 46671/1 - … | AND2 | function-word head (QDOS) |
| New inspection request - AX Ref:1075568 | B519 | postcode-area head |

## Result

- Backup table `backup_20260709_vrm_junk`: case_=2, inbound_email=9 (all old values retained; rollback = restore from backup).
- Post-check: **0** rows remaining in any junk family (case_ + inbound_email).
- Audit: 2 `audit_event` rows (actor `delta:2026-07-09-vrm-junk-cleanup`, action `status_changed` — the nearest-fit generic case-mutation code, per the 2026-07-06 delta precedent) carrying before/after per cleared case.
- `A.PCH26003.vrm` and `QDOS26056.vrm` are now NULL (correct plate unknown at source — see the root-cause note below; staff can set it from the documents).

## TKT-085 root cause (established, not assumed)

- "OCTOBER" appears NOWHERE in the source email (raw .eml grep = zero hits) and the
  current engine's sniff of the same message returns the real plate `BE57JDS`.
- The live value entered through the **/parse VRM path**: `caseResolve` stamps the
  parser-derived VRM onto BOTH `case_.vrm` and `inbound_email.body_vrm`
  (`services/data-api/src/features/` "upgrade its body_vrm to the best VRM"). The live
  FC1 parse of the attached Audatex PDF surfaced the month word; a local re-parse of the
  same PDF with engine-v2.10 yields `BE57JDS` (correct).
- The accepting hole (now closed, engine-v2.10 + TS filter): NO layer rejected an
  all-alphabetic "mark" — the validation regex (`[A-Z0-9]{2,8}`) passes OCTOBER, provider
  label rules carry no digit requirement, and `_is_suspicious_value` had no all-alpha
  guard. The wave adds: all-alpha rejection + month/day denylist + function-word-head
  rejection in `vrm_candidate_is_bad`/`_is_suspicious_value` (Python) and the mirrored
  denylists + proximity/tight anchoring in `packages/domain/src/domain/vrm-filter.ts` (TS).

## Devnote item 5 — networkhduk → YML (checked, NOT applied)

`work_provider` row `YM Law/ NETWORK HD UK` (principal `YML`) exists and is active but its
`known_email_domains` is **EMPTY** — a networkhduk sender cannot match it. Operator-reviewable
corpus add (exact sender domain needed from a real message); not applied in this wave.

## Adjacent finding (new-ticket candidate)

`A.PCH26003.case_ref` = `RIGERANT R1234YF` — the /parse REFERENCE fallback captured a
fragment of "REFRIGERANT R1234YF" (air-con spec in the Audatex PDF) as the case reference;
reproduced locally with engine-v2.10. The TKT-103 money guard covers the CLASSIFIER's
`_job_reference`, not the /parse `_fallback_reference` — that path needs its own guard.

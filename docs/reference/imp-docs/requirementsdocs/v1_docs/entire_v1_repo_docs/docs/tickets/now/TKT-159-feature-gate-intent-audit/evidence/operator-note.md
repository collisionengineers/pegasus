# Operator note — feature gates

Make sure no feature that should be active is switched off. Compare the real deployed settings with the current code and intended behavior, enable what is genuinely ready and required, and leave an honest reason for anything intentionally off.


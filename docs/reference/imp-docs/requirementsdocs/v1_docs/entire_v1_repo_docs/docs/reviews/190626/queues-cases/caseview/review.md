# Overview

Far too loud / text heavy 

"Case facts (read-only)
Imported context — does NOT drive readiness." - need to define/confirm the source or remove this container

Upload evidence button is a mock still.

"Export Json (gated fallback)" - Gated fallback is a dev term, not for front facing / users to see


# Fields

Required fields all show "Claimant Name is required for EVA" or similar, can just have * or "Required" - clears up text noise

# Address

Remove the suggestion/reason in text box - its not applicable. 

# Chasers

Will adapt chasers from Job Sheet which are more minimalistic

"Log as drafted" button - poorly defined. Seemingly no function. Could be changed to "Log as chased" and an auto-note added. 

"Mark Held" unnecessary - a cases queue state depends on the information / Images / Instructions and whether we have them.


"This tool never auto-sends (ADR-0003) — it drafts only. Copy the text or log it as drafted; sending is a separate staff action." - clear dev docs reference leaking into app. ADR shouldn't be getting mentioned in the actual app text. Also contradicts the greyed out "Send Via Outlook" that is greyed out.

# Evidence

As per overview, too loud / cluttered - there are 4 seperate warnings on this page about no images.






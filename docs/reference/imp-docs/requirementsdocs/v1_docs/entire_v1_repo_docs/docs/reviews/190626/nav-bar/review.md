Nav Bar tabs need restructure:

1. Corpus - Rename to Provider Settings in app and on nav bar.

2. Audit -> change to Action Logs.

3. Done (today) unnecessary - can go in audit logs and in general display container on dashboard. Does not require a page. Alternatively can keep. See 4.

4. In Progress + Needs action + Ready for EVA should be merged into one. Should be different tabs in this page. Instructions (Awaiting Images), Images Only, Ready for Review.

5. Intake requires a second upload: Add Evidence. Must link to existing case to add evidence.

6. Make queues into a first class button - also expands on nav bar into sub-options for each specific queue 
1. All dropdowns currently applying to all queues.

- Provider should be for all queues
- Status depends on the state, Ready for EVA shouldn't need any, nor should done today. 
- Provider list needs filtering to only providers with an active case in that queue
- Need definitions on "Needs Action" - chasing requirement is once per week typically, need other definitions for other "needs action" requirements so that one can move to "In Progress".

2. Export Json should only show for "Ready For EVA" cases 

3. Need a new queue for e-mails that can't directly pass through. Based on requirements, shouldn't be able to create a case without basic information such as Claimant Name, Vehicle Reg, Similar 
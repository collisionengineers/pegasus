These files are to give an indicator for what needs to be contained under "Phase 3" (not authoritative versioning) - the Engineer Workspace. It is not for the system to generate the Audatex style assessments, but to be able to extract the data from them, and populate it, so that the engineer can generate a report immediately if approved.


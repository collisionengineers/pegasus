--- a/scripts/Test-MarkdownPlacement.ps1
+++ b/scripts/Test-MarkdownPlacement.ps1
@@ -28,7 +28,7 @@
     param([Parameter(Mandatory)][string] $Path)
 
     $normalized = $Path.Replace('\', '/') -replace '^\./', ''
-    return $normalized -match '^((docs/(prd|frd|adr|design))|workspaces/document-extraction|\.design-sync|\.grok|\.stitch|design/planning-and-old-designs)/.+\.md$'
+    return $normalized -match '^((docs/(prd|frd|adr|design|engineering|external-component-documents|docs-review-temp))|workspaces/document-extraction|\.agents/skills|\.design-sync|\.stitch|design/planning-and-old-designs)/.+\.md$'
 }
 
 $resolvedRoot = (Resolve-Path -LiteralPath $RepositoryRoot).Path

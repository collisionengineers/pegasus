---
name: research-repository-change
description: Gather bounded, source-classified repository and primary-source evidence for a planning task. Use only when explicitly routed by plan-repository-change to establish current behavior, callers, ownership, feasibility, external guidance, or test evidence without choosing a design.
---

# Research Repository Change

Keep this skill explicit-only and read-only. Resolve the shared task before work; return a concise evidence handoff for the planning lead to persist under `.repoplugin/tasks/<task-id>/planning/research/`.

Answer one bounded question. Trace the production entry point to its policy owner, adapters, persistence, and tests. Classify evidence as authoritative, observed, inferred, planned, unavailable, registered-only, or test-only. Search primary documentation for changing APIs, libraries, services, standards, or commands; record source and retrieval date.

Do not choose a solution, interview the user, draft a plan, implement, mutate cloud state, or call the Codex harness `update_plan` tool. Surface conflicts, unsupported assumptions, irreversibility, and missing evidence for `open-questions.md`.

interface:
  display_name: "Validate Repository Task"
  short_description: "Complete and validate an explicit task folder."
  default_prompt: "Use $validate-repository-task to complete or validate one explicit repository task."
policy:
  allow_implicit_invocation: false

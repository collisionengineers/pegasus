# Sign-in dialogs

The initial login has no dialog. Required fields, incorrect credentials
and signed-out confirmation appear in the form's own layout. The mockup
controls disclosure belongs to the review tools, not the product UI.

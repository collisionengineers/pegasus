# Inbox: panels

| Pane | Live class | Holds |
| --- | --- | --- |
| Scopes | `pane` · `scope-list` | Seven `scope-button`s with counts |
| Messages | `pane` · `inbox-filter`, `pane-head inbox-messages-head`, `pane-body pane-scroll`, `pagination` | The filter form, the sort toggle, the rows, paging |
| Preview | `pane` · `mail-preview` | Subject, route line, chip, excerpt, attachments, facts, actions |

// Stage 1 only. Reproducible shell/assets pinned to the researched source commit.
import {execFileSync} from 'node:child_process';
import {readFile,writeFile} from 'node:fs/promises';
import path from 'node:path';
import {current,repo,shell,esc} from './lib/shared.mjs';
import {designs,states,cases,files} from './lib/upload-designs.mjs';
const source='32dabfc59';
const gitFile=relative=>execFileSync('git',['show',`${source}:${relative}`],{cwd:repo,maxBuffer:32*1024*1024});
const web='src/Pegasus.Web/';
const regular=gitFile(web+'wwwroot/fonts/inter/InterVariable.woff2').toString('base64');
const italic=gitFile(web+'wwwroot/fonts/inter/InterVariable-Italic.woff2').toString('base64');
const css=gitFile(web+'wwwroot/css/site.css').toString().replace('url(../fonts/inter/InterVariable.woff2)',`url(data:font/woff2;base64,${regular})`).replace('url(../fonts/inter/InterVariable-Italic.woff2)',`url(data:font/woff2;base64,${italic})`);
const assets={css,sprite:gitFile(web+'Pages/Shared/_LucideSprite.cshtml').toString().replace(/@\*[\s\S]*?\*@/g,'').trim(),mark128:'data:image/png;base64,'+gitFile(web+'wwwroot/images/pegasus-mark-refined-128.png').toString('base64')};
const photo=(await readFile(path.join(current,'assets/upload-designs/synthetic-inspection-sheet.png'))).toString('base64');
const localCss=await readFile(path.join(current,'lib/upload-designs.css'),'utf8');
const runtime=await readFile(path.join(current,'lib/upload-designs-runtime.js'),'utf8');
const filename=id=>`pegasus_upload_${id}_new_v30.html`;
for(const design of designs){
 const controls=`<details class="ud-mock"><summary>Mockup controls · ${design.id.toUpperCase()} · ${design.name}</summary><div class="ud-mock-body"><nav aria-label="Upload design alternatives">${designs.map(d=>`<a href="${filename(d.id)}" ${design.id===d.id?'aria-current="page"':''}>${d.id.toUpperCase()}</a>`).join('')}</nav><label>Upload state<select data-state-picker>${states.map(([s,label])=>`<option value="${s}">${label}</option>`).join('')}</select></label><button data-action="sample">Load sample files · try the full journey</button><label class="ud-mock-check"><input type="checkbox" data-conflict>Simulate a conflict on confirmation</label><p>Offline proposal. Sample uploads and Case changes are simulated. Local file selection and previews stay on this device; local files are not uploaded or processed. Photographs are synthetic.</p><a href="pegasus_upload_designs_v30.html">Compare the five new designs</a><a href="pegasus_upload_${design.id}_v30.html?state=decision">Earlier design ${design.id.toUpperCase()}</a></div></details>`;
 const content=shell({route:'upload',assets,clock:'09:41',counts:{inbox:6,cases:17,operations:1},content:`<div class="ud" data-design="${design.id}" id="upload-design-root"></div>`});
 const html=`<!doctype html><html lang="en-GB"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data: blob:; font-src data:; style-src 'unsafe-inline'; script-src 'unsafe-inline'; form-action 'none'; base-uri 'none'"><title>${design.name} · Pegasus Upload</title><style>${css}\n${localCss}\n.ud-photo::before{background-image:url(data:image/png;base64,${photo})}</style></head><body>${assets.sprite}${content}<dialog class="ud-modal" id="ud-dialog" aria-labelledby="ud-dialog-title"></dialog><div id="ud-live-status" class="sr-only" role="status" aria-live="polite"></div><div id="ud-toast" class="ud-live" role="status" hidden></div>${controls}<script>window.uploadDesign=${JSON.stringify({...design,states,cases,files,source})};window.mockupErrors=[];window.addEventListener('error',event=>window.mockupErrors.push(event.message));</script><script>${runtime}</script></body></html>`;
 await writeFile(path.join(current,filename(design.id)),html);
}
const cards=[];
for(const [i,d] of designs.entries()){
 let preview='';try{const png=await readFile(path.join(current,`v30-upload-new-shots/${String(i+1).padStart(2,'0')}-${d.id}-ready-1440.png`));preview=`<img src="data:image/png;base64,${png.toString('base64')}" alt="${esc(d.name)} Upload design">`;}catch(error){if(error.code!=='ENOENT')throw error;}
 cards.push(`<article class="review-option"><a class="review-preview" href="${filename(d.id)}">${preview||'Open '+d.name}</a><div class="review-copy"><div class="review-option-head"><span class="review-letter">${d.id.toUpperCase()}</span><h2>${d.name}</h2>${d.recommended?'<span class="review-recommended">Recommended</span>':''}</div><p class="review-lead">${d.lead}</p><p>${d.benefit}</p><p class="review-tradeoff"><strong>Tradeoff</strong> · ${d.tradeoff}</p><div class="review-links"><a class="review-open" href="${filename(d.id)}">Explore ${d.name} →</a><a href="${filename(d.id)}?state=select">Start with file selection</a><a href="pegasus_upload_${d.id}_v30.html?state=decision">Earlier ${d.id.toUpperCase()}</a></div></div></article>`);
}
await writeFile(path.join(current,'pegasus_upload_designs_v30.html'),`<!doctype html><html lang="en-GB"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Five new Upload designs · Pegasus</title><style>${css}
.review{max-width:1480px;margin:auto;padding:40px 34px 60px}.review-header{display:flex;justify-content:space-between;gap:40px;border-bottom:1px solid var(--line);padding-bottom:27px}.review-kicker{font-size:10px;letter-spacing:.13em;text-transform:uppercase;color:var(--muted);margin:0 0 12px}.review h1{font-size:30px;letter-spacing:-.045em;font-weight:650}.review-intro{font-size:13px;line-height:1.8;color:var(--muted);max-width:790px;margin:12px 0 0}.review-header>a{font-size:11px;white-space:nowrap;text-underline-offset:4px}.review-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:34px;padding:24px 0;border-bottom:1px solid var(--line);margin-bottom:28px}.review-summary h2{font-size:12px;margin:0 0 6px;font-weight:650}.review-summary p{font-size:11.5px;line-height:1.7;color:var(--muted);margin:0}.review-options{display:grid;grid-template-columns:1fr 1fr;gap:28px}.review-option{background:var(--surface);border:1px solid var(--line);border-radius:4px;overflow:hidden;display:flex;flex-direction:column}.review-option:first-child{grid-column:1/-1;display:grid;grid-template-columns:1.55fr 1fr}.review-preview{display:block;aspect-ratio:1.6;background:var(--surface-2);border-bottom:1px solid var(--line);overflow:hidden}.review-preview img{width:100%;height:100%;object-fit:cover;object-position:top}.review-option:first-child .review-preview{height:100%;border-bottom:0;border-right:1px solid var(--line);aspect-ratio:auto;min-height:390px}.review-option:first-child .review-preview img{object-fit:contain;background:var(--bg)}.review-copy{padding:25px;display:flex;flex-direction:column;flex:1}.review-option-head{display:flex;align-items:center;gap:10px}.review-letter{display:grid;place-items:center;width:25px;height:25px;border:1px solid var(--line-strong);border-radius:3px;font-size:11px;font-weight:600}.review-option-head h2{font-size:19px;letter-spacing:-.03em;font-weight:650;margin:0}.review-recommended{margin-left:auto;font-size:10px;color:var(--red-dark);font-weight:650}.review-copy p{font-size:12px;line-height:1.75;color:var(--muted);margin:9px 0 0}.review-copy .review-lead{font-size:13px;color:var(--ink);font-weight:550;margin:18px 0 0}.review-copy .review-tradeoff{padding-top:13px;margin-top:14px;border-top:1px solid var(--line);font-size:11px}.review-links{display:flex;align-items:center;flex-wrap:wrap;gap:12px;margin-top:auto;padding-top:22px;font-size:11px}.review-open{background:var(--nav);color:white;padding:10px 13px;border-radius:3px;font-weight:600}.review-open:hover{background:var(--nav-2);color:white;text-decoration:none}.review-footer{margin-top:30px;padding-top:22px;border-top:1px solid var(--line);font-size:12px;color:var(--muted);line-height:1.8}.review-footer a{text-decoration:underline}.review-footer strong{color:var(--ink)}@media(max-width:950px){.review-option:first-child{display:flex}.review-option:first-child .review-preview{min-height:0;aspect-ratio:1.6;border-right:0;border-bottom:1px solid var(--line)}.review-options{gap:20px}.review-header{flex-direction:column;gap:20px}.review-copy{padding:20px}.review-recommended{font-size:9px}}@media(max-width:650px){.review{padding:26px 18px}.review-options,.review-summary{grid-template-columns:1fr}.review-option:first-child{grid-column:auto}.review-summary{gap:18px}.review h1{font-size:25px}}</style></head><body><main class="review"><header class="review-header"><div><p class="review-kicker">Pegasus · Upload design review · 25 September 2026</p><h1>From files to the right Case.</h1><p class="review-intro">Five new, interactive proposals. A quieter hierarchy, useful photographic previews and one explicit Case decision throughout. Each includes the full journey from selection to outcome, with the earlier five versions kept alongside for comparison.</p></div><a href="pegasus_v30_walkthrough.html">Back to the v30 walkthrough</a></header><section class="review-summary" aria-label="What changed"><div><h2>See the decision clearly</h2><p>Case, registration, claimant, Principal and Case/PO appear together. A possible match is never automatically selected.</p></div><div><h2>Make the files useful</h2><p>Real preview interactions, readable names, compact outcomes and a marked unreadable file. Eleven long filenames exercise every layout.</p></div><div><h2>Finish with certainty</h2><p>Review the exact Case before adding. Failures preserve the decision; success names the confirmed destination.</p></div></section><div class="review-options">${cards.join('')}</div><footer class="review-footer"><strong>My recommendation: A — Review desk.</strong> It gives the Case decision a clear home and keeps the complete file roster in view. C is the strongest visual option for image-heavy work; D is the most efficient for repeated batch review.<br><strong>Try it:</strong> select a Case, review, cancel, choose another Case, open a file or search for “Morgan”, “QDOS25802” or “26018”. Open Mockup controls for all 20 states, sample file selection and a confirmation conflict.<br><a href="upload-design-proposals.md">Read the proposals, tradeoffs and behaviour decisions</a> · <a href="assets/upload-designs/README.md">Synthetic image provenance</a>. Offline design artifacts; this review does not establish application behaviour.</footer></main></body></html>`);
console.log('Built five new Upload mockups and comparison page from '+source+'.');

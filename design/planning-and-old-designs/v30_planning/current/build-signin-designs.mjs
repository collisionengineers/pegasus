// Temporary Stage 1 login proposals. Run from any directory: node <this file>.
// Assets and tokens come from the recorded origin/dev snapshot, not a CDN.
import { execFileSync } from 'node:child_process';
import { readFile, writeFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const current = path.dirname(fileURLToPath(import.meta.url));
const repo = path.resolve(current, '../../../..');
const sourceRef = '32dabfc59';
const source = p => execFileSync('git', ['show', `${sourceRef}:src/Pegasus.Web/${p}`], { cwd: repo, maxBuffer: 8 * 1024 * 1024 });
const font = source('wwwroot/fonts/inter/InterVariable.woff2').toString('base64');
const mark = `data:image/png;base64,${source('wwwroot/images/pegasus-mark-refined-256.png').toString('base64')}`;
const liveCss = source('wwwroot/css/site.css').toString();
const tokens = liveCss.match(/:root\{[\s\S]*?\}/)[0];
const spriteSource = source('Pages/Shared/_LucideSprite.cshtml').toString();
const symbols = ['check', 'alert-circle'].map(id => spriteSource.match(new RegExp(`<symbol id="icon-${id}"[\\s\\S]*?</symbol>`))[0]).join('');
const sprite = `<svg class="sprite-sheet" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">${symbols}</svg>`;
const icon = id => `<svg class="icon" aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><use href="#icon-${id}"/></svg>`;
const options = [
  { id: 'a', name: 'Quiet focus', short: 'Light. Clear. Unhurried.', description: 'An open, light canvas with a compact brand row and a carefully aligned form. The simplest, quietest entrance to Pegasus.', benefit: 'The least visual weight; the form gets your attention immediately.', tradeoff: 'A larger visual departure from the dark application shell.', geometry: '380px form · 64px mark · no card or shadow' },
  { id: 'b', name: 'Brand split', short: 'A stronger first impression.', description: 'A charcoal brand panel gives Pegasus a distinct presence. A white form panel keeps the task clear, with the two halves balanced around the same centre line.', benefit: 'The clearest brand presence, while keeping the sign-in form calm.', tradeoff: 'Uses more of the screen; the brand panel stacks above the form on narrower windows.', geometry: '42 / 58 split · 360px form · 96px mark', recommended: true },
  { id: 'c', name: 'Charcoal frame', short: 'Familiar, with better balance.', description: 'The existing dark surround and white card, refined with a centred brand, lighter dividers and more deliberate spacing.', benefit: 'The closest fit to the current login, with a more composed hierarchy.', tradeoff: 'A more conservative change; the card remains the dominant shape.', geometry: '440px card · 360px form · 96px mark' },
];
const states = [['default', 'Initial page'], ['validation', 'Required fields'], ['error', 'Incorrect credentials'], ['signed-out', 'Signed out']];
const file = id => `pegasus_signin_${id}_v30.html`;
const css = `
@font-face{font-family:'Inter Variable';font-style:normal;font-weight:100 900;font-display:swap;src:url(data:font/woff2;base64,${font}) format('woff2')}
${tokens}
*{box-sizing:border-box}html,body{margin:0;min-height:100%;font-family:var(--font);font-size:13.5px;color:var(--ink)}body{background:var(--surface)}button,input,select{font:inherit}button,a,input,select,summary{-webkit-tap-highlight-color:transparent}button,a,summary{touch-action:manipulation}button{cursor:pointer}a{color:inherit}button,input,select{border-radius:var(--radius)}[hidden]{display:none!important}.sprite-sheet{position:absolute;width:0;height:0;overflow:hidden}.icon{width:18px;height:18px;flex:none}a:focus-visible,button:focus-visible,summary:focus-visible,select:focus-visible{outline:2px solid var(--focus);outline-offset:4px}input:focus{outline:2px solid var(--focus);outline-offset:2px}h1,h2,p{margin:0}.screen{min-height:100svh}.brand{display:flex;align-items:center;gap:16px}.brand img{display:block;width:64px;height:64px;object-fit:contain}.brand-copy{display:grid;gap:6px}.brand strong{font-size:20px;font-weight:760;letter-spacing:.09em}.brand span{font-size:12px;color:var(--muted)}.company{font-size:11px;letter-spacing:.025em;color:var(--muted)}.form-content{width:100%}.form-content h1{font-size:23px;font-weight:650;line-height:1.3;letter-spacing:-.035em;margin:0 0 28px;display:flex;align-items:center;gap:9px}.form-content h1.signed-out{color:var(--green);font-size:21px}.form-content h1 .icon{width:21px;height:21px}.login-form{display:grid;gap:20px}.field{display:grid;gap:8px;min-width:0}.field label{font-size:12px;font-weight:600}.field input{width:100%;height:36px;padding:0 10px;border:1px solid var(--line-strong);background:var(--surface);color:var(--ink);min-width:0}.password-control{position:relative}.password-control input{padding-right:65px}.reveal{position:absolute;right:3px;top:3px;height:30px;min-width:54px;padding:0 8px;border:0;background:transparent;color:var(--muted);font-size:11px;font-weight:600}.reveal:hover{background:var(--surface-3);color:var(--ink)}.reveal:focus-visible{outline-offset:0}.primary{display:flex;align-items:center;justify-content:center;gap:8px;width:100%;height:36px;border:1px solid var(--red);background:var(--red);color:#fff;font-weight:650;margin-top:4px;transition:background 140ms}.primary:hover{background:var(--red-dark);border-color:var(--red-dark)}.primary[aria-disabled=true]{cursor:wait;background:var(--red-dark)}.field-error{color:var(--danger);font-size:11px;line-height:1.45}.field input[aria-invalid=true]{border-color:var(--danger)}.notice{display:flex;gap:10px;align-items:flex-start;padding:12px;border:1px solid var(--danger);border-radius:var(--radius);background:var(--danger-bg);color:var(--danger);font-size:12px;line-height:1.55;margin-bottom:24px}.notice .icon{margin-top:1px}.notice:focus{outline:2px solid var(--focus);outline-offset:3px}
/* A: one open light surface; branding and form share a left edge. */
.design-a .screen{display:grid;grid-template-rows:1fr auto;padding:64px 24px 28px}.design-a .login-block{width:min(100%,380px);align-self:center;justify-self:center;padding:20px 0 56px}.design-a .brand{padding-bottom:28px;margin-bottom:32px;border-bottom:1px solid var(--line)}.design-a .company{text-align:center}
/* B: two simple planes, with the identity and form on a common centre line. */
.design-b .screen{display:grid;grid-template-columns:42% 58%;max-width:1580px;margin:auto}.design-b{background:var(--bg)}.design-b .identity{display:grid;grid-template-rows:1fr auto;min-width:0;background:var(--nav);border-top:3px solid var(--red);padding:64px;color:var(--nav-text)}.design-b .brand{align-self:center;justify-self:center;align-items:flex-start;flex-direction:column;gap:28px;padding-bottom:20px;width:min(100%,280px)}.design-b .brand img{width:96px;height:96px}.design-b .brand strong{font-size:26px;letter-spacing:.1em}.design-b .brand span{font-size:13.5px;color:var(--nav-muted);margin-top:4px}.design-b .company{color:var(--nav-muted);border-top:1px solid var(--nav-2);padding-top:20px;width:min(100%,280px);justify-self:center}.design-b .form-panel{display:grid;place-items:center;background:var(--surface);padding:64px 40px}.design-b .form-content{max-width:360px;padding-bottom:24px}.design-b h1{margin-bottom:32px}
/* C: refine the existing navless card, retaining its dark surround. */
.design-c .screen{display:grid;place-items:center;padding:48px 24px 72px;background:var(--nav)}.design-c .login-block{width:min(100%,440px)}.design-c .card{padding:32px 40px 40px;background:var(--surface);border-top:3px solid var(--red);border-radius:var(--radius-lg);box-shadow:0 8px 24px rgba(0,0,0,.09)}.design-c .brand{flex-direction:column;text-align:center;gap:16px;padding-bottom:28px;margin-bottom:28px;border-bottom:1px solid var(--line)}.design-c .brand img{width:96px;height:96px}.design-c .company{text-align:center;color:var(--nav-muted);margin-top:24px}
/* Review controls are outside the proposed product surface. */
.mockup-controls{position:fixed;z-index:5;bottom:14px;left:14px;max-width:calc(100vw - 28px);width:300px;background:var(--nav);color:var(--nav-text);border:1px solid #596167;border-radius:4px;box-shadow:0 4px 12px #0002;font-size:12px}.mockup-controls summary{list-style:none;cursor:pointer;padding:11px 14px;font-weight:600}.mockup-controls summary::-webkit-details-marker{display:none}.mockup-controls summary::after{content:'+';float:right;color:var(--nav-muted)}.mockup-controls[open] summary::after{content:'−'}.mockup-body{padding:0 14px 14px;display:grid;gap:12px}.mockup-body p{line-height:1.5;color:var(--nav-muted);font-size:11px}.mockup-body nav{display:flex;gap:8px}.mockup-body nav a{padding:6px 12px;border:1px solid #596167;border-radius:3px;text-decoration:none}.mockup-body nav a[aria-current=page]{background:var(--surface);color:var(--ink)}.mockup-body label{display:grid;gap:6px}.mockup-body select{height:32px;background:var(--surface);color:var(--ink);border:1px solid var(--line);padding:0 8px}.mockup-body .check-label{display:flex;align-items:center}.mockup-body input{accent-color:var(--red)}.mockup-body>a{font-size:11px;text-underline-offset:3px}.demo-feedback{color:var(--nav-text)!important}
@media(max-width:979px){.design-b .screen{grid-template-columns:1fr;grid-template-rows:auto 1fr}.design-b .identity{padding:28px 40px;grid-template-columns:1fr auto;grid-template-rows:auto;align-items:center;gap:24px}.design-b .brand{flex-direction:row;align-items:center;justify-self:start;width:auto;padding:0;gap:16px}.design-b .brand img{width:64px;height:64px}.design-b .brand strong{font-size:20px}.design-b .brand span{font-size:12px;margin:0}.design-b .company{border:0;padding:0;width:auto}.design-b .form-panel{padding:48px 24px 100px}.design-b .form-content{padding-bottom:0}}
@media(max-width:479px){.design-b .identity{padding:24px;grid-template-columns:1fr;gap:16px}.design-b .company{justify-self:start}.design-a .screen{padding:36px 24px 84px}.design-a .login-block{padding:12px 0 40px}.design-c .screen{padding:28px 18px 84px}.design-c .card{padding:28px 24px 32px}.design-c .brand{gap:12px;padding-bottom:24px;margin-bottom:24px}.design-c .brand img{width:80px;height:80px}}
@media(prefers-reduced-motion:reduce){*{transition:none!important;scroll-behavior:auto!important}}
@media(forced-colors:active){.primary{border:1px solid ButtonText}.identity,.card,.notice,.mockup-controls{border:1px solid CanvasText}.reveal{border:1px solid ButtonText}.brand img{forced-color-adjust:none}.field input[aria-invalid=true]{outline:2px dashed CanvasText}.brand span,.company{color:CanvasText}}
`;
const brand = `<div class="brand"><img src="${mark}" alt="" width="96" height="96"><div class="brand-copy"><strong>PEGASUS</strong><span>Case management</span></div></div>`;
const company = '<p class="company">Collision Engineers</p>';
const form = `<section class="form-content" aria-labelledby="heading">
<h1 id="heading">Sign in</h1>
<div id="credential-error" class="notice" role="alert" tabindex="-1" hidden>${icon('alert-circle')}<p>The username or password is incorrect. If your access has changed, contact an administrator.</p></div>
<form class="login-form" novalidate autocomplete="on">
<input type="hidden" name="ReturnUrl" value="/">
<div class="field"><label for="UserName">Username</label><input id="UserName" name="UserName" autocomplete="username" autocapitalize="none" spellcheck="false" maxlength="256" required autofocus aria-describedby="username-error"><span class="field-error" id="username-error" hidden>Enter your username.</span></div>
<div class="field"><label for="Password">Password</label><div class="password-control"><input id="Password" name="Password" type="password" autocomplete="current-password" maxlength="256" required aria-describedby="password-error"><button class="reveal" type="button" aria-label="Show password" aria-controls="Password" aria-pressed="false">Show</button></div><span class="field-error" id="password-error" hidden>Enter your password.</span></div>
<button class="primary" type="submit">Sign in</button>
</form></section>`;
const script = `
const params=new URLSearchParams(location.search);
const stateNames=['default','validation','error','signed-out'];
const state=stateNames.includes(params.get('state'))?params.get('state'):'default';
const form=document.querySelector('form'),user=document.querySelector('#UserName'),password=document.querySelector('#Password'),reveal=document.querySelector('.reveal'),submit=document.querySelector('.primary'),error=document.querySelector('#credential-error'),heading=document.querySelector('h1');
window.mockupErrors=[];window.addEventListener('error',event=>window.mockupErrors.push(event.message));
const setInvalid=(input,id,invalid)=>{document.getElementById(id).hidden=!invalid;if(invalid)input.setAttribute('aria-invalid','true');else input.removeAttribute('aria-invalid');};
const checkFields=()=>{const a=!user.value.trim(),b=!password.value;setInvalid(user,'username-error',a);setInvalid(password,'password-error',b);return a?user:b?password:null;};
const mask=()=>{password.type='password';reveal.textContent='Show';reveal.setAttribute('aria-label','Show password');reveal.setAttribute('aria-pressed','false');};
reveal.addEventListener('click',()=>{const show=password.type==='password';password.type=show?'text':'password';reveal.textContent=show?'Hide':'Show';reveal.setAttribute('aria-label',show?'Hide password':'Show password');reveal.setAttribute('aria-pressed',String(show));});
if(state==='validation')checkFields();
if(state==='error'){user.value='alex';error.hidden=false;}
if(state==='signed-out'){heading.innerHTML='${icon('check')}<span>You are signed out</span>';heading.classList.add('signed-out');}
for(const [input,id] of [[user,'username-error'],[password,'password-error']])input.addEventListener('input',()=>{if(input.value)setInvalid(input,id,false);});
form.addEventListener('submit',event=>{event.preventDefault();if(form.getAttribute('aria-busy')==='true')return;error.hidden=true;const invalid=checkFields();if(invalid){invalid.focus();return;}form.setAttribute('aria-busy','true');submit.setAttribute('aria-disabled','true');submit.textContent='Signing in…';setTimeout(()=>{form.removeAttribute('aria-busy');submit.removeAttribute('aria-disabled');submit.textContent='Sign in';password.value='';mask();heading.textContent='Sign in';heading.classList.remove('signed-out');error.hidden=false;error.focus();document.querySelector('.demo-feedback').textContent='Demo finished: incorrect-credentials state. No account was contacted.';},650);});
const controls=document.querySelector('.mockup-controls');controls.hidden=params.get('embed')==='1';
document.querySelector('#state-control').value=state;
document.querySelector('#state-control').addEventListener('change',event=>{params.set('state',event.target.value);location.search=params.toString();});
const revealToggle=document.querySelector('#reveal-control');revealToggle.checked=params.get('reveal')!=='off';
const applyReveal=()=>{reveal.hidden=!revealToggle.checked;if(reveal.hidden)mask();};applyReveal();revealToggle.addEventListener('change',applyReveal);
document.querySelector('#reset-preview').addEventListener('click',event=>{event.preventDefault();location.search='';});
window.mockupReady=true;
`;
for (const option of options) {
  const controls = `<details class="mockup-controls"><summary>Mockup controls · ${option.id.toUpperCase()}</summary><div class="mockup-body"><nav aria-label="Design alternatives">${options.map(o => `<a href="${file(o.id)}"${o.id === option.id ? ' aria-current="page"' : ''}>${o.id.toUpperCase()}</a>`).join('')}</nav><label for="state-control">Page state<select id="state-control">${states.map(([id,label]) => `<option value="${id}">${label}</option>`).join('')}</select></label><label class="check-label"><input id="reveal-control" type="checkbox" checked>Show-password control (proposal J)</label><p>Offline preview. Use sample details; a completed form demonstrates incorrect credentials.</p><p class="demo-feedback" role="status"></p><a href="pegasus_signin_designs_v30.html">Compare all three designs</a><a href="${file(option.id)}" id="reset-preview">Reset preview</a></div></details>`;
  const layout = option.id === 'b'
    ? `<main class="screen"><div class="identity">${brand}${company}</div><div class="form-panel">${form}</div></main>`
    : option.id === 'c'
      ? `<main class="screen"><div class="login-block"><div class="card">${brand}${form}</div>${company}</div></main>`
      : `<main class="screen"><div class="login-block">${brand}${form}</div>${company}</main>`;
  await writeFile(path.join(current,file(option.id)), `<!doctype html><html lang="en-GB"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; font-src data:; style-src 'unsafe-inline'; script-src 'unsafe-inline'; form-action 'none'; base-uri 'none'"><title>${option.id.toUpperCase()} · ${option.name} · Pegasus sign in</title><style>${css}</style></head><body class="design-${option.id}">${sprite}${layout}${controls}<script>${script}</script></body></html>`);
}

const reviewCss = `${css}
body{background:var(--bg)}.review{max-width:1420px;margin:auto;padding:48px 36px}.review-header{display:flex;justify-content:space-between;align-items:flex-start;gap:32px;padding-bottom:32px;border-bottom:1px solid var(--line)}.review-kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;color:var(--muted);margin-bottom:14px}.review h1{font-size:30px;letter-spacing:-.04em;font-weight:650}.review-intro{max-width:700px;font-size:14px;color:var(--muted);line-height:1.7;margin-top:12px}.review-back{font-size:12px;text-underline-offset:4px;white-space:nowrap}.alternatives{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:24px;margin-top:32px}.alternative{min-width:0}.thumbnail{display:block;aspect-ratio:1.6;border:1px solid var(--line);background:var(--surface);overflow:hidden;border-radius:4px}.thumbnail img{width:100%;height:100%;object-fit:cover;display:block}.thumbnail iframe{width:1440px;height:900px;border:0;transform:scale(.27);transform-origin:top left;pointer-events:none}.option-heading{display:flex;align-items:center;gap:10px;margin:24px 0 12px}.letter{display:grid;place-items:center;width:28px;height:28px;border:1px solid var(--line-strong);font-size:12px;font-weight:650;border-radius:3px}.option-heading h2{font-size:19px;letter-spacing:-.03em}.recommendation{display:inline-block;color:var(--red-dark);font-size:10px;font-weight:650;margin-left:auto}.alternative p{color:var(--muted);font-size:13px;line-height:1.7}.alternative .lead{font-size:14px;font-weight:600;color:var(--ink);margin-bottom:8px}.alternative dl{margin:20px 0;display:grid;gap:6px;font-size:12px;line-height:1.6}.alternative dt{font-weight:650;margin-top:6px}.alternative dd{margin:0;color:var(--muted)}.open-design{display:inline-flex;align-items:center;justify-content:center;min-height:36px;padding:0 16px;background:var(--nav);color:var(--nav-text);text-decoration:none;border-radius:3px;font-size:12px;font-weight:600}.open-design:hover{background:var(--nav-2)}.geometry{font-size:11px!important;margin:14px 0 0}.review-note{margin-top:36px;border-top:1px solid var(--line);padding-top:24px;display:grid;grid-template-columns:1fr 1fr;gap:40px}.review-note p{font-size:12px;line-height:1.7;color:var(--muted)}.review-note strong{color:var(--ink)}.review-note a{text-underline-offset:3px}@media(max-width:979px){.review{padding:28px 24px}.alternatives{grid-template-columns:1fr;gap:40px}.review-header{flex-direction:column;gap:20px}.review-note{grid-template-columns:1fr;gap:16px}.thumbnail{aspect-ratio:1.6}.option-heading{margin-top:16px}}
`;
const cards=[];
for(const o of options){
  let thumbnail;
  try { const png=await readFile(path.join(current,`v30-signin-shots/0${options.indexOf(o)+1}-${o.id}-default-1440.png`));thumbnail=`<img src="data:image/png;base64,${png.toString('base64')}" alt="${o.name}: ${o.short}">`; }
  catch(error) { if(error.code!=='ENOENT')throw error;thumbnail=`<iframe src="${file(o.id)}?embed=1" tabindex="-1" title="${o.name} preview"></iframe>`; }
  cards.push(`<article class="alternative"><a class="thumbnail" href="${file(o.id)}" aria-label="Open ${o.name}">${thumbnail}</a><div class="option-heading"><span class="letter">${o.id.toUpperCase()}</span><h2>${o.name}</h2>${o.recommended?'<span class="recommendation">Recommended</span>':''}</div><p class="lead">${o.short}</p><p>${o.description}</p><dl><dt>Why choose it</dt><dd>${o.benefit}</dd><dt>Tradeoff</dt><dd>${o.tradeoff}</dd></dl><a class="open-design" href="${file(o.id)}">Explore design ${o.id.toUpperCase()} →</a><p class="geometry">${o.geometry}</p></article>`);
}
await writeFile(path.join(current,'pegasus_signin_designs_v30.html'),`<!doctype html><html lang="en-GB"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Three sign-in designs · Pegasus v30</title><style>${reviewCss}</style></head><body><main class="review"><header class="review-header"><div><p class="review-kicker">Pegasus · Design review · 25 September 2026</p><h1>A considered first impression.</h1><p class="review-intro">Three directions for the initial sign-in page. Each uses the existing Pegasus mark, Inter typeface and Collision red, with a different balance of brand, space and focus.</p></div><a class="review-back" href="pegasus_v30_walkthrough.html">Back to the v30 walkthrough</a></header><div class="alternatives">${cards.join('')}</div><footer class="review-note"><p><strong>My recommendation: B — Brand split.</strong> It gives Pegasus a clearer identity before entering the Work Centre. Choose A for the quietest treatment, or C for the smallest departure from today’s design.</p><p><strong>Try the states.</strong> Each preview includes required-field feedback, incorrect credentials, signed-out confirmation and a working Show / Hide control. Use “Mockup controls” at the bottom left. These are offline design proposals; no sign-in takes place. <a href="signin-design-proposals.md">Read the proposals and review decisions</a> · <a href="pegasus_signin_v30.html?layer=baseline">View the current baseline</a>.</p></footer></main></body></html>`);
console.log('Built three offline sign-in mockups and the comparison page.');

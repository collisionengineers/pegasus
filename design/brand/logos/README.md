# Logos

## Approved source

| Relative source path | Variant/use | Current runtime destination/consumer |
| --- | --- | --- |
| `design/brand/logos/logo_no_margin.png` | master red gear-C Collision Engineers lockup | none yet; approved source for selected `0.1.0-alpha.1` implementation |

The file is an exact copy of `assets/logo_no_margin.png` from the provided
`collision-engineers-design-dev` source bundle, which is not retained in this
repository.
SHA-256: `E7247BE45911C46905343473E4C57B9F6ED7A450563D19C508C2D9652C2C63E2`.
The current `.brand-mark` renders the letters `CE` in CSS/HTML and is recorded
runtime divergence, not an approved logo variant.

## Rules

Never redraw the gear, extract it from a screenshot, recolour the master, or
invent another mark. A future selected `0.1.0-alpha.1` implementation copies/optimizes only
through a reviewed source-to-runtime mapping with checksum proof.

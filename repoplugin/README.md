# Retained Repoplugin workflow-design material

Status: **Historical/reference input — not active plugin source**

This directory retains the earlier CollisionSpike delivery-planning and domain-guidance design material so its useful claims and evolution remain reviewable. It is not an installed plugin, active runtime package, product authority, or permission to copy predecessor concepts into the application.

Active repository plugin source lives under `plugins/repoplugin-*` and is governed by the repository's current agent-routing and plugin contracts. Use those active packages through their exposed skills. Do not copy, reinstall, or synchronize this retained directory into the active packages by assumption.

When this material disagrees with current operator notes, the questionnaire, accepted ADRs, executable accepted contracts, or current plugin source, apply the repository source-of-truth order and record the difference. The live open-decision register is `docs/plans/open-decisions.md`; the active maturity owner is `docs/plans/feature-maturity-map.md`.

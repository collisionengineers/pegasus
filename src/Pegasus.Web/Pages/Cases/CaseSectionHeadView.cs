namespace Pegasus.Web.Pages.Cases;

/// <summary>The Case section head tools partial's model: the page and the section it decorates.</summary>
public sealed record CaseSectionHeadView(DetailsModel Page, string Key);

@page
@model Pegasus.Web.Pages.NotificationsModel
@*
    No markup: this route is the bell's two handlers, not a screen. The
    Notifications dialog (_ShellDialogs) posts here from every page — one form
    per row opens that notification, one form marks them all read — and each
    handler redirects back. A GET opens the dialog on the Work Centre.
*@

@{
    // The public upload link is the product's only external-facing screen. It
    // must not render the staff shell: the internal navigation, the sign-in
    // link, the product name in the tab title and the footer all told a third
    // party outside Collision Engineers how the office works inside.
    Layout = "Shared/_LayoutExternal";
}

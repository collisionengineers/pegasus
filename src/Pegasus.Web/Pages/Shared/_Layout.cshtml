<!DOCTYPE html>
@inject Microsoft.Extensions.Configuration.IConfiguration Configuration
@inject Microsoft.AspNetCore.Hosting.IWebHostEnvironment Environment
@{
    var localIntakeEnabled = Environment.IsDevelopment()
        && Configuration.GetValue<bool>("Features:LocalIntake");
}
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] · Pegasus</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <header class="app-nav">
        <div class="nav-inner">
            <a class="navbar-brand" asp-area="" asp-page="/Index"><span class="brand-mark">CE</span><span>Pegasus</span></a>
            <nav class="nav-links" aria-label="Primary">
                <a class="nav-link" asp-area="" asp-page="/Index">Case intake</a>
                @if (localIntakeEnabled)
                {
                    <a class="nav-link" asp-area="" asp-page="/Intake/Upload">Upload instruction</a>
                }
                <a class="nav-link" asp-area="" asp-page="/Triage/Index">Triage</a>
                @if (User.IsInRole(Pegasus.Core.Identity.StaffRoleNames.Administrator))
                {
                    <a class="nav-link" asp-area="" asp-page="/Administration/Index">Administration</a>
                }
                <a class="nav-link" asp-area="" asp-page="/Privacy">Privacy</a>
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <a class="nav-link" asp-area="" asp-page="/Account/ChangePassword">Change password</a>
                    <form method="post" asp-page="/Account/SignOut">
                        <button class="nav-link" type="submit">Sign out</button>
                    </form>
                }
                else
                {
                    <a class="nav-link" asp-area="" asp-page="/Account/SignIn">Sign in</a>
                }
            </nav>
        </div>
    </header>
    <div class="app-shell">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <footer class="footer text-muted">
        <div class="footer-inner">
            Pegasus · Collision Engineers case management
        </div>
    </footer>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

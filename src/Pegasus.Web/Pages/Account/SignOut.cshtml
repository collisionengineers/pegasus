@page
@model Pegasus.Web.Pages.Account.SignOutModel
@*
    No markup: this route is a handler, not a screen.

    The navigation posts sign-out directly, so the unstyled interstitial that
    used to live here was never reached in normal use. What was actually
    missing is a confirmation that the session ended, which the POST handler
    now redirects to. A GET still redirects without ending the session.
*@

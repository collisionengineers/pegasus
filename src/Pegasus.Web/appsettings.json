{
  "Runtime": {
    "Profile": "Production"
  },
  "Features": {
    "LocalDocumentCustody": false
  },
  "Bootstrap": {
    "//": "TEMPORARY. A disposable Administrator used to drive the deployed interface through a browser during UI verification, so that verification does not run as the owner's own account. It is deliberately recorded here rather than held out of band, because the operator asked for the details to be stored and has stated this account is removed before go-live. Retire it by replacing UserName/Password with { \"Removed\": \"claudeuiverification\" }, which deletes the account on the next start. Never leave it configured in a real production deployment.",
    "VerificationAccount": {
      "UserName": "claudeuiverification",
      "Password": "Pegasus-UI-Verify-2026!"
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

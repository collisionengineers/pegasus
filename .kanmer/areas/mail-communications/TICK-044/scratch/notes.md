2026-08-19 21:50Z ([[DELIV-012]]) — Checklist reconciled against the merged TICK-045 diff. The operator's recorded ruling on this ticket ("the retained mailbox viewer is meant to show this information… A policy referenced only by tests is incomplete and must not pass review as delivered") is now satisfied: `MailOperationalDestinationPolicy` has a production caller at `/Inbox/{id}`, delivered by PR #422 and verified by falsifiable Web tests. The five caller items are ticked with explicit provenance notes because the work landed on TICK-045's branch, not this one — recorded rather than silently claimed. The single remaining unticked item is the authenticated production mailbox-viewer check, which release 12's verification owns; TICK-044 stays in `verifying` until then.

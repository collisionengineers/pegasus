2026-08-19 — Taken over by claude-code under DELIV-012 by operator decision. Prior agent's PR #422 delivered no production code (only a capabilities.md line and a test that fabricated `MailClassificationResult.Ambiguous(..., "shared-mail-policy", 9)` and inserted it directly, so it could not fail for the reason it claimed) and used a fabricated mailbox address `claims@collisionengineers.co.uk`. Two Codex P1 review comments were unaddressed.

What changed in this session, in order:
1. Merged origin/dev (10 commits behind; clean automatic merge, no conflicts).
2. Investigated whether `MailOperationalDestinationPolicy` (shipped dark by TICK-044) had any production caller to wire up — found none; MAIL-02/UI-14 were explicitly logged as deferred "Next" capabilities with no workspace caller. Reported this finding rather than inventing a caller.
3. Rewrote `OneCorrectionPolicyAppliesIdenticallyAndIndependentlyAcrossMailboxes` (tests/Pegasus.IntegrationTests/RetainedMailPersistenceTests.cs) to resolve the registered `IMailClassificationPolicy` (`QdosMailClassificationPolicy`) from DI and classify real content that produces a genuine Ambiguous outcome, instead of storing a fabricated literal. Verified the test can fail: temporarily removed the DI registration, watched it fail, reverted.
4. Replaced the fabricated `claims@collisionengineers.co.uk` mailbox with the documented `engineers@collisionengineers.co.uk`, via the test file's own existing `SeedPollStateAsync` helper (no dedicated mailbox-approval helper exists in this test support).
5. Mid-task, the coordinator (operator, DELIV-012) supplied TICK-044's `open-questions` resolution directing that the retained mailbox viewer (`/Inbox/{id}`, `Message.cshtml(.cs)`) is the real caller for `MailOperationalDestinationPolicy` — not UI-14. Wired `MailOperationalDestinationPolicy.Map` into that page (new `MessageModel.Destination` helper, two new `<dl>` rows, `OperatorLabels.MailOperationalDestinationLabel` reusing the page's existing "Needs sorting" wording for the abstention case rather than inventing new copy). Added/extended two `MailWorkspaceWebTests` to prove it renders and can fail; verified by temporarily sabotaging the helper and watching the exact assertions fail, then reverting.
6. Rewrote `docs/capabilities.md` MAIL-02 (real caller, Web-tested, UI-14 not upgraded) and MAIL-03 (classification-policy evidence only) rows to state exactly what was proven.
7. Corrected TICK-045's own checklist items 4/5/11: item 4 is now genuinely true (both the pre-existing correction caller and the new destination-policy caller); item 5 was restated to drop the never-true "unsupported message failures" claim (only stale-correction and unknown-message failures are covered) rather than leaving a falsifiable tick; item 11 is now genuinely true (real registered policy, real documented mailboxes). Left TICK-044's own checklist/open-questions untouched — not this ticket; the coordinator owns reconciling TICK-044 after this PR merges.

Build (Release) 0 warnings/0 errors. Core.Tests 640/640. Targeted integration filter (RetainedMailPersistenceTests|MailWorkspaceWebTests) 31/31, run twice more after each sabotage-and-revert proof, all green.

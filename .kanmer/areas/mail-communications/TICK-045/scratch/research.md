Research check 2026-08-18: FRD-08 makes the approved mailbox estate the target and the settled MailTaxonomy the sole behaviour owner. Current Core has one shared taxonomy/result contract but automatic evaluation is intentionally route-owned: ProcessIntake selects IMailClassificationPolicy by accepted WorkProviderCode, and only QdosMailClassificationPolicy is registered. ApprovedMailbox identity/route scopes do not select classification policy. MAIL-21/22 shipped the QDOS automatic foundation and persisted full taxonomy. MAIL-03 therefore must not replace ADR-0008 route-specific predicates with a universal rule engine; its remaining gap is applying the canonical taxonomy consistently from the retained-mail workspace across exact messages in every approved mailbox, through the same Core command/current-decision owner later consumed by correction/history.

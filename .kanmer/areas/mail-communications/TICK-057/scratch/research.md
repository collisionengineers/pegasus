2026-08-20 refresh: inspected origin/dev b36c6666 and origin/main 2325ed4a rather than the stale local dev checkout (local dev is 103 commits behind origin/dev). Verified INTK-007 commit abd8a923 is on both remote branches and renamed the operator-facing abstention to Unidentified while preserving Triage. On origin/dev, MailOperationalDestinationPolicy is the sole Core map; RetainedMailSummary/MailWorkspaceScope and EfRetainedMailboxMessageStore.ListAsync still have no classification/queue list filter, while /Inbox/{id} already computes the destination live. Queue filtering must happen before SQL count/paging and must not duplicate the policy. Exact current file conflicts are with TICK-053/TICK-056 on RetainedMail.cs, EfRetainedMailboxMessageStore.cs, Mail/Index.cshtml.cs and MailWorkspaceWebTests.cs; TICK-064 also names the store and Index model, and TICK-050 names RetainedMail/store. Hard prerequisites TICK-009, TICK-010, TICK-044, INTK-007 are done. TICK-064 is not a semantic prerequisite for application queue filtering because MAIL-23 owns approved Outlook-folder identity binding, but its overlapping branch should land first per programme sequencing.

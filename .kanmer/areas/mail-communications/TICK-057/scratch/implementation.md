## 2026-08-20 implementation checkpoint

Implemented the accepted narrow read slice from exact origin/dev `4baae5f0`: Core owns aggregate query criteria beside `MailOperationalDestinationPolicy.Map`; `MailWorkspaceScope` accepts exactly zero-or-one aggregate destination or named detailed category; EF composes the existing current classification decision into SQL before `CountAsync`/`Skip`/`Take`; list rows reuse the landed decision mapper and derive destination on read. `/Inbox` now has one native labelled queue/detail selector and carries the canonical key through mailbox/folder/search/page/refresh and exact-message navigation/actions. Deleted Items plus a retained-classification view and unknown/legacy keys fail closed. No schema, migration, store, write path, external call, taxonomy, or framework was added.

Focused evidence: 38 Core policy/scope tests green; populated SQL current-projection test green; populated SQL correction/count/page test green; authenticated Web test green across Receiving work, Queries, reasoned Other, Unidentified, distinct Triage and General/autoreply detail, including list/detail context. Locked restore and Release solution build are green with zero warnings. Canonical non-corpus solution test is running; Core 862/862 and Architecture 98/98 have completed green.

Simplification findings so far: removed a fabricated classification result that existed only to obtain a label; reused a direct `MailCategory` label overload instead. Removed a second enum scan from queue parsing by carrying the destination in the existing view option. Moved contradictory scope validation ahead of destination lookup. No behaviour change.

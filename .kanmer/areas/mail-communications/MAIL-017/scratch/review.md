---
kind: review-attestation
pr: "571"
head_sha: "bd34b1a0d3ead3c91dc358c794b9a9f05263ed80"
verdict: pass
reviewer: "claude-fable-5 (independent reviewer, session 2026-08-27)"
independent: true
plan_hash: "eb0fd6e4a873fc59"
ticket_updated: "2026-08-27T11:21:36.955Z"
findings:
  - id: R1
    severity: note
    summary: "CI shard sql-integration (1) failed once on VehicleLookupGapFillTests.ASecondLookupDoesNotDuplicateOrOverwriteTheFirst with a SqlClient post-login connection timeout (runner infrastructure), unrelated to this diff; re-run once by the reviewer (gh run rerun 33065020285 --failed) and passed in 10m53s."
    disposition: accepted-risk
    reason: "Failure is a SQL connection timeout in a test that does not touch ApprovedMailboxes or migrations; the author's local full suite was 987/987 and the re-run is green. Flaky-runner risk is outside this ticket's scope."
  - id: R2
    severity: note
    summary: "The Designer file is 6983 lines because EF emits a full model snapshot per migration; the model snapshot itself is untouched."
    disposition: rejected-with-reason
    reason: "Generated by the repo-pinned dotnet ef tool, consistent with every existing migration; hand-trimming a Designer is not a convention here."
---

# Review — MAIL-017 / PR #571

## What the change is

- New migration `src/Pegasus.Infrastructure/Persistence/Migrations/20260827100901_ReactivateBoundApprovedMailboxes.cs` (+ Designer): `Up` is one `migrationBuilder.Sql` `UPDATE [dbo].[ApprovedMailboxes] SET [ActivatedAtUtc] = SYSDATETIMEOFFSET() WHERE [State] = N'Approved' AND [ActivatedAtUtc] IS NULL AND [MailboxIdentity] IS NOT NULL AND [InboxFolderIdentity] IS NOT NULL`; `Down` is empty.
- `tests/Pegasus.IntegrationTests/IntakePersistenceIntegrationTests.cs`: migration-head assertion extended with the new id.
- `docs/operations.md`: one paragraph appended inside the existing Release 33 bullet.
- Files changed: exactly the four above; `PegasusDbContextModelSnapshot.cs` is not in the diff (no model change).

## Checks performed

- Diff matches the plan steps 1-3 and the post-implementation report; no scope beyond the brief (EPIC-010: remediation, no features).
- Predicate safety: `State = N'Approved'` excludes Disabled/other states; both identity columns must be non-null, so an identity-less mailbox cannot be re-activated; `ActivatedAtUtc IS NULL` makes it a no-op where the operator re-save (10:20:33Z, per `scratch/live-evidence`) already set it. Matches the SQL literal in the ticket body.
- Empty `Down`: justified — nulling the column again would reinstate the defect being repaired and the original activation time is unrecoverable; consistent with the plan's simplification disposition.
- Grants: `scripts/Invoke-AzureDatabaseBootstrap.ps1` builds its permission matrix only from grant-carrying migrations; this migration issues no GRANT/DENY and touches only an existing table already covered by runtime grants, so no bootstrap change is required.
- Designer `[Migration("20260827100901_ReactivateBoundApprovedMailboxes")]` matches the test assertion and file name.
- `docs/operations.md` note sits in the Release 33 entry, indented to the list, hard-wrapped within ~78 columns, compact table convention unaffected.
- FRD-08 has no migration/activation text that this change contradicts; behaviour (activation required for polling/subscription, pre-activation mail skipped) is unchanged.
- Simplification pass in the plan (dated 2026-08-27) applies four lenses with honest dispositions and states "No unapplied findings"; matches the diff.
- Author `collisionengineers` / claude-code executed; this reviewer did not implement or commit to the branch.

## CI

Run 33065020285 at head `bd34b1a0`. Initial: all green except `sql-integration (1)` (FAILURE, see R1). Reviewer re-ran the failed job once; final state: `changes`, `documentation`, `local-development-scripts`, `reference-data`, `unit`, `browser`, `sql-integration (1)`, `(2)`, `(3)`, `sql-integration-coverage` all pass; `infrastructure` skipped (path filter). GitHub reports no branch-protection "required" list on the branch; every check that ran is green. No review threads exist (one Codex bot comment about its own usage limits; no content to disposition).

## Residual risk

The migration runs at the next release only; prod is already repaired by the operator re-save, so the `UPDATE` is expected to match zero rows there. Any database walking the release-33 chain from scratch is repaired by it.

## Verdict

pass — merge authorised by the launching instruction (`gh pr merge 571 --merge`), then one move Review → Verifying.

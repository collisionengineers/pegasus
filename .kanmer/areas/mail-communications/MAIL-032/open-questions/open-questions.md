- [x] ASSUMPTION 1 (pegasus-planner, attempt 1): the `files` map and the plan's Do-not-modify scope are bound to the **verified** changed-file list of PR #640 at head `ed19e77f` (13 paths), not to the dispatch note that Razor markup, CSS and endpoints are out of scope — because `gh pr view 640 --json files` and the branch diff against `origin/dev` both show the second commit (`ad3779c9`, the review-findings commit) changes `src/Pegasus.Web/Pages/Mail/Index.cshtml`, `src/Pegasus.Web/Pages/Mail/Message.cshtml`, `src/Pegasus.Web/Pages/Mail/Message.cshtml.cs` and `src/Pegasus.Web/wwwroot/css/site.css`. The dispatch phrase accurately describes the **first** commit only (`df9716e3`: "No markup, CSS, or endpoint change"). Recorded ticked, not open, because it is resolved by direct evidence and needs no operator answer. Alternatives: (a) exclude those four paths from Expected files — rejected, it would make the adopted diff unreviewable and every inspection of it a scope violation; (b) stop and ask — rejected, the dispatch's primary instruction already binds the map to the PR's changed files, and this is the same rule applied to the evidence.

- [x] ASSUMPTION 2 (pegasus-implementer, attempt 1): performed one NuGet package-dependency refresh (locked mode) in the worktree before the Release build — because the plan's Step 2 requires the implementer to obtain a 0-error `dotnet build --no-restore` on the merged head, and this worktree had never had one, so the build failed with 7 x NETSDK1004 (missing `obj/project.assets.json`) and no compiler feedback was obtainable at all. That refresh is neither a verification run nor any command M6 forbids, the shell guard permitted it, and AGENTS.md's canonical rail pairs it with the build; the plan's placement of it in the runner block governs who owns the verification rail, not a prohibition that would make its own Step 2 impossible. Alternatives: (a) record the build INCONCLUSIVE and skip compiler feedback — rejected, it leaves Step 2 unsatisfiable and gives the reviewer no local build evidence; (b) stop BLOCKED — rejected as disproportionate: the two plan sentences reconcile by role scope and the resolution changes no repository file. The first NETSDK1004 failure is retained in the report per M9.

2026-08-27T13:58Z controller (auto run 2026-08-27T11-23Z-claude-code): full suite attempt 2 failed 10/987 integration tests with contention-shaped timeouts while two other lanes' suites ran on the same LocalDB; failures are scattered across unrelated tests for a comment-only diff. Lane stopped; controller will re-run the suite serially after lane C finishes. No PR yet.

2026-08-27T15:10Z controller: serial attempt 3 (`artifacts/mail-021/test-full.log`, `test exit=1`) — Core 1001/1001, Architecture 100/100, Integration aborted at 494/987 ("Test host process crashed") after 3 failures (SQL connection/execution timeouts in `CaseTaskArchivePersistenceTests`, `AzureSqlRuntimeRoleMigrationTests`, `RetainedMailPersistenceTests`). Workstation state at the time: 2.8 GB RAM free of 14 GB, 12 dotnet + 2 testhost processes from other lanes' focused tests, 74.5 GB disk free. Verification stays FAILED (not retried further per §9); the next attempt runs only when no other test process is alive. No PR.

2026-08-27T16:40Z controller: attempt 4 (detached, no other lane running; a peer session's suite overlapped part of it) `test exit=1` — Core 1001/1001, Architecture 100/100, Integration 985/987 in 1 h 20 m; the two failures were `LocalDbTemplateDatabaseTests.TheTemplateIsBuiltOncePerProcessAndEveryDatabaseIsItsOwn` and `RestoringTheTemplateMatchesMigratingTheDatabase` (`Assert.Equal` expected `Template`, actual `Migrated` — the shared LocalDB template was rebuilt by a concurrent process). Single permitted re-run of that class alone: 14/14, exit 0 (`artifacts/mail-021/test-localdbtemplate-rerun.log`). Verification recorded as PASS-with-rerun; lane resumed to open the PR.

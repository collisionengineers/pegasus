2026-08-25 research orientation: moved Backlog -> Preparing after confirming both FRD refs satisfy the governing-doc gate. Current caller is InboxPollFunction (15-second timer) -> PollApprovedInbox -> GraphApprovedInboxSource delta read -> ReceiveIntake; Web has no mailbox poller. The production Web Container App is already one always-warm 1 CPU/2 GiB replica (minReplicas=maxReplicas=1), so this ticket does not need a warmth/scale change. Graph wake-up must preserve Worker cursor/processing ownership and reuse the current per-mailbox lease/delta recovery. MAIL-009 already makes unresolved sender neutral/provisional and must be regression-covered, not reimplemented. INTK-040 currently modifies DurableIntake/ProcessIntake/mailbox integration tests but not the Graph trigger surface; avoid those overlap files until its merge. MAIL-013 is blocked by INTK-041 and INTK-042 and blocks DELIV-021.

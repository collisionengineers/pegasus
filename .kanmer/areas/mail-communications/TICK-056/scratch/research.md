2026-08-20 UI-10 refresh on origin/dev a3c88a7b: current /Inbox list/detail already owns paging, mailbox/folder state, freshness, retained body/attachments/thread, classification correction, destination and association display. External CSP-safe wwwroot/js/site.js is loaded by _Layout, so the capability row's claim that preview client script is discarded is stale. Direct assembly blockers are TICK-053 (final search/Deleted/detail-return query shape) and TICK-057 (queue/category refinements); TICK-064 is only an upstream/coordination predecessor and action tickets are progressively surfaced controls, not UI-10 policy or hard blockers. Planned minimal owned slice: Index preview endpoint/markup using GetRetainedMail, site.js/site.css interaction, final list/detail journey and Web/browser accessibility evidence. No Core policy/store/action framework.

2026-08-21 post-merge refresh on origin/dev ee88c70c: TICK-053/UI-14 are landed. RetainedMailSummary already supplies every preview fact except attachment names; use one thin authenticated Index handler delegating to GetRetainedMail, then existing site.js/site.css. Imagegen preview-only target reinforced table-primary adjacent desktop preview and stacked constrained fallback; generated image stays outside repo. No Core/EF/schema/framework/action/external write.

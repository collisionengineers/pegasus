2026-08-27 worker: code complete at 77989d47 on task/mail-018-mailbox-subscription-health, pushed. Awaiting controller's serial full-suite run. Outstanding before PR: regenerate `docs/design/test-ui/pages/administration-mailboxes--default.html` via `pwsh ./scripts/Update-TestUiSnapshots.ps1` (then `-Verify`) in the worktree and commit it — the default page now carries Activated ("Not activated") and Subscription ("None.") columns, so the committed snapshot is stale. Logs under worktree `artifacts/mail-018/`.

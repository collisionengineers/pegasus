# MAIL-018 plan

Scope: per mailbox on `/Administration/Mailboxes`, show activation time, subscription lifecycle state, expiry, and last maintenance failure code. Labels and values only. No new features, no new port, no schema or grant change (Web already has SELECT).

## Steps

1. **Port** — add `Task<IReadOnlyList<ApprovedMailboxSubscription>> ListAsync(CancellationToken)` to `IApprovedMailboxSubscriptionStore` (reuses the existing owner of the table instead of a parallel queries port). Implement in `EfApprovedMailboxSubscriptionStore` with `AsNoTracking().OrderBy(ApprovedMailboxId)` and the existing `Map`. Update the one test fake in `GraphMailWebhookTests`.
2. **PageModel** — inject `IApprovedMailboxSubscriptionStore`; `LoadAsync` also fills `Subscriptions`. Add `SubscriptionStatusFor(ApprovedMailbox)` modelled on `PollStatusFor`: no row → `"None."`; else `"{Humanise(state)}. Expires {OfficeTime(expiry)}."` plus `" Last failure: {Humanise(code)}."` when a failure code is set.
3. **Razor** — add columns `Activated` (`OperatorLabels.OfficeTime(mailbox.ActivatedAtUtc, "Not activated")`, the existing absent-value convention) and `Subscription` (`Model.SubscriptionStatusFor(mailbox)`). No hint copy.
4. **Tests** — one integration test in `ApprovedMailboxAdministrationWebTests`: activate the seeded mailbox, save a subscription row through the real store with `Missed` state and a failure code, GET the page, assert the activation time, "Missed", the expiry, and "Last failure: ..." render; the existing no-identifiers test additionally asserts "Not activated" and "None." for the untouched seeded mailbox.
5. **Snapshot** — run `pwsh ./scripts/Update-TestUiSnapshots.ps1` then `-Verify` (repo procedure). Deferred to the controller's serial run: the capture suite is a large integration run beyond the one-focused-filter allowance on this workstation.
6. **Gate** — `dotnet restore --locked-mode`, `dotnet build -c Release --no-restore` (0 warnings), Core + Architecture tests, focused `--filter "FullyQualifiedName~ApprovedMailboxAdministrationWebTests"`.

## Acceptance

- Page renders four values per mailbox with no explanatory text.
- Build 0 warnings; Core, Architecture, focused integration filter PASS.
- Snapshot regenerated by script, not hand-edited.

## Simplification pass (2026-08-27, code-simplifier agent over the branch diff)

1. Altitude — `Subscriptions` property sat between helper methods; **applied**: moved beside `PollStatuses` so both `LoadAsync` targets are together.
2. Reuse — `OperatorLabels.OfficeTime(DateTimeOffset?, string)` / `Humanise` already used; **no change**.
3. Reuse — test could use `TestMailboxId.EnsureApprovedAsync`; **not applied**: that helper also overwrites identities and takes an address the test does not need; the direct two-line set states exactly what the assertion depends on.
4. Efficiency — `SingleOrDefault` per row is O(mailboxes × subscriptions); **not applied**: identical to adjacent `PollStatusFor`, estate is a handful of rows; a dictionary would diverge from the established shape.
5. Simplification — `ListAsync` shape; **not applied (already matches `ListMaintenanceCandidatesAsync` and `GetActiveAsync` conventions)**.
6. Altitude — no new abstraction (one method on the existing port); **no change**.

# MAIL-018 post-implementation report

Branch `task/mail-018-mailbox-subscription-health` (worktree `../pegasus-worktrees/mail-018-mailbox-subscription-health`), base `origin/dev` a9184315. PR #577 → `dev`.

## Delivered

- `IApprovedMailboxSubscriptionStore.ListAsync` on the existing port (no parallel query port); EF implementation via existing `Map`; webhook test fake updated.
- `/Administration/Mailboxes`: new **Activated** and **Subscription** columns — values only (`Not activated` / `None.` for the absent cases; `"{State}. Expires {time}. Last failure: {code}."` otherwise). No hint copy. No schema/grant change (Web role already had SELECT).
- Integration test `ThePageShowsActivationAndSubscriptionHealthPerMailbox` plus absent-case assertions in the existing no-identifiers test.
- Test UI snapshot `administration-mailboxes--default.html` regenerated by script; commit scoped to that page (+6 lines).

Commits: 77989d47 (feature + tests), 47ebad54 (snapshot).

## Plan vs. implementation

All six plan steps done. Deviation: the snapshot script also regenerated 49 unrelated pages (brand-mark inlining against stale committed snapshots, a different `case-create` capture candidate, `OperationId` nonces). Per controller decision those were discarded and deferred to [[MAIL-023]]; details in `scratch/snapshots`. `-Verify` was not run on this branch for the same reason.

## Evidence

Worker (`artifacts/mail-018/` in the worktree): restore --locked-mode exit 0; build Release exit 0, 0 warnings; Core 1001/1001; Architecture — first run host crash while concurrent with the integration filter (`arch-tests.log`), rerun alone 100/100 (`arch-tests-2.log`); focused `ApprovedMailboxAdministrationWebTests` 6/6; `Update-TestUiSnapshots.ps1` exit 0 (capture 265 passed / 11 skipped).

Controller serial run at 47ebad54: build exit 0 (`build-controller.log`); full suite `Category!=Corpus` (`test-full.log`, `test-exit.txt` = "test exit=1"): Core 1001/1001, Architecture 100/100, Integration 987/988. The failure was `QdosMappingExtractionTests.MappedInstructionEmailExtractsItsDocumentedFieldSet("(EREF9) RTA on 15_08_2026")` with `RegexMatchTimeoutException`, unrelated to this diff; that class alone re-ran 7/7, exit 0 (`test-qdosmapping-rerun.log`). The initial failure stays on record.

## Simplification pass

Recorded in the plan: 1 applied (property placement), 5 not applied with reasons.

## Open items for review

- The stale/nondeterministic Test UI snapshots on dev (MAIL-023) mean `-Verify` fails there independently of this PR.

# Proof — MAIL-005 (test-output + command-log)

Merged to dev in PR #485 (green CI) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`). On merged code: the retained-mail summary resolves a message's case link-first, else from the allocation attempt carrying the created case — the state the live SQL check proved unreachable before the fix ("Ready for case allocation" on an allocated receipt) now cannot occur; the outcome cell aligns chip + reference on one row. Evidence: new `ASucceededAllocationAttemptResolvesTheCaseWithoutALinkRow` plus RetainedMailPersistenceTests 27/27 — green in CI on #485 and the release build. Live: the pre-release-14 stranded rows the check found were removed by the PLAT-017 wipe; the Inbox rendered its honest empty state post-deploy (signed-in browser check), and the next allocated mailbox message shows its case link.

2026-08-20 refresh against origin/dev b36c6666: existing EvaluateIntakeCaseMatch is QDOS/provider-scoped and uses claim/VRM/name eliminators; it is not the accepted MAIL-09 system-wide unique-VRM/exact-thread matcher. Reusable owners are Case match normalization/query seams, IAutomaticCaseAssociationStore/EfIntakeMutationStore's serializable idempotent write/history, DurableIntake system-worker caller, and retained-mail identity joins. Current CaseMatchIndex contains QDOS-policy cases only; current retained mail case projection reads CaseIntakeLinks only and omits active IntakeManualAssociations, so an automatic association written by the reusable store would not yet appear in /Inbox. Exact live production DB write remains approval-gated per the ticket decision.

- [x] A production-path case retains and displays the correct due-by date — `CaseDueWork.DueBy` set on both live production cases (QDOS26001 `2026-07-15`, QDOS26002 `2026-08-16`).
- [x] The seven-day condition becomes visible at the correct time without duplicate effects — `CaseChaseSchedule.FirstChaseAt`/`NextChaseAt` (`CaseWorkScheduling.cs:84-92`) compute a single next-chase timestamp exactly 7 London-calendar days out; `DueWorkSweepFunction` runs every 5 minutes and `RunDueChasers` reports `ReplayCount`/`SupersededCount` alongside `GeneratedCount`, so re-runs before the due point are idempotent by design.
- [x] Staff can generate the approved copyable chaser and the system does not claim it was sent — `OnPostRecordManualChaseAsync` (`Tasks.cshtml.cs:134`) plus the Worker's own log message ("No outbound communication was attempted and no sending, receipt, or delivery was claimed") confirm the staff-only, non-sending design matches the ticket's explicit scope limit.
- [ ] Live evidence records environment, caller, timing, and limitations — recorded in `research.md`, with the honest limitation named: neither production case has yet crossed its first 7-day chase point, so no chaser has fired and no manual chase has been recorded live yet (`CaseDueChasers`/`CaseManualChases` both empty). Left unchecked deliberately: readiness is proved, an actual chase firing is not yet.

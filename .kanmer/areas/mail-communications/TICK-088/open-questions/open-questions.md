# Open questions — MAIL-12

- [x] **Is MAIL-12 activated despite its Later / 0.5.0 allocation?** — Yes. Activate it for full implementation in EPIC-006.
- [x] **What level of implementation is required?** — Full authenticated approved-mailbox drafts, compose, reply, forward and send, including To/CC/BCC, subject/body, attachments, provider-native thread semantics, configured per-mailbox signatures, explicit confirmation, idempotent submission, visible failure/retry, permanent attribution and exact Sent reconciliation. Web and Automation callers reuse one Core implementation.
- [x] **Where do outbound enablement and signatures live?** — The existing administrator-owned approved-mailbox setting is the narrow owner: add a distinct outbound-send scope and versioned configured correspondence signature. `InboundIntake` and `SentEvidence` never imply write authority. A missing/disabled outbound scope or signature fails closed. Do not reuse report-renderer engineer signatures or fabricate content.
- [x] **What proves a send?** — Persist the Graph immutable draft id and reconcile/probe the same id in the approved Sent folder. `202 Accepted` and a staff assertion are pending evidence, not delivery proof. Exact Sent evidence proves only that the item exists in the approved Sent scope; it does not prove delivery, reading, content correctness, Triage completion, report dispatch or Case closure.
- [x] **How are uncertain results retried?** — Same-key/same-fingerprint completed replay returns the recorded outcome and different input conflicts. A known failure may be deliberately retried with a new operation key. An unknown create/send response is recorded and never blindly repeated; first probe/reconcile the immutable provider item and require evidence that no send occurred.
- [x] **Does MAIL-12 include report sending or automatic outbound?** — No. MAIL-17 retains report/fee-note artifact, destination, Box, completion and post-report effects. MAIL-19 retains Worker scheduling/template/eligibility and autonomous retry. MAIL-12 is deliberate authenticated human-authored correspondence; an Automation caller follows the same confirmation/version/idempotency rules and is not an autosend worker.
- [x] **What is the implementation sequence?** — Execute after TICK-054/MAIL-13 lands (and its TICK-049/MAIL-07 predecessor), then rebase and reuse the actual exact-message, operation/history, Graph and recovery seams. TICK-053's message/thread/source read shapes should also stabilize before reply/forward.

## Parked (explicitly deferred)

- [x] **Is full live Outlook/Graph verification required, and which recipient is approved?** — Yes. Resolved by the operator on 2026-08-19: production acceptance must exercise draft, compose, reply, forward, attachment, configured signature, explicit confirmation, send, retry/idempotency and Sent-evidence reconciliation. The only approved test recipient is `digital@collisionengineers.co.uk`; never send operational correspondence.
- [x] **Does repository implementation authorize production permissions or writes?** — No. Local work uses LocalDB/local files and fake Graph HTTP only. Production enablement separately requires exact approval for the Entra application `Mail.ReadWrite` and `Mail.Send` permission/admin-consent changes, exact Exchange Application RBAC confinement to the approved sender mailbox, and a negative outside-scope test. Permission still does not authorize a message operation.
- [x] **What approval is required immediately before the live journey?** — Record exact approval naming the sender mailbox, exact reply/forward source messages, every attachment, final subject/body/signature, complete To/CC/BCC, each permitted draft/update/attachment/send write and maximum send count. Draft creation itself is an Outlook write. Abort on any mailbox/source/content/recipient/attachment/signature/version mismatch. Capture draft/version/fingerprint, operation key, send result/replay, immutable Sent/thread evidence and permanent attribution.

2026-09-01 (Claude controller, Phase 0 groom): disposition in the Claude programme is execute_after_exact_target_approval. Code and composition land in build wave B (after MAIL-027 and TICK-054, before MAIL-031, which this ticket blocks); the production activation (Graph application permission grant, the app setting that composes the real mover, the first live mutation against an approved test mailbox) is a Phase 4 approval packet with exact targets; the Done proof needs post-release live evidence. PR #640 wrongly named this ticket; it belongs to MAIL-032 and is being retitled.

## Files changed (implementation, 2026-08-20)

- `scripts/email-eval-desktop/EmailEvaluationWorkflow.cs` — added `IMailClassificationPolicy classificationPolicy` dependency; `LoadCurrentAsync` now builds an `IntakeSource` from the already-read message bytes, runs it through the existing (previously-unused) `sourceReader` to get an `IntakeSourceReadResult`, calls `classificationPolicy.Classify(...)`, and formats the result (category/subtype, matched-predicate evidence lines, policy key+version) into the pre-existing `suggestion` slot. Reused the file bytes for both display parsing and classification (single read). Classification faults are isolated behind their own `IntakeExceptionPolicy.IsRecoverable` guard so a rule-evaluation problem never blocks displaying or filing the message.
- `scripts/email-eval-desktop/Program.cs` — wires `new QdosMailClassificationPolicy()` as that dependency (the same Core `IMailClassificationPolicy` owner MAIL-21/22 shipped, `src/Pegasus.Core/Intake/DirectProviders/Qdos/QdosMailClassificationPolicy.cs`). Also dropped `FindRepositoryRoot`/the repo-root parameter to `CategoryCatalog.Load` — see TICK-004's files note for why (same commit).
- `scripts/email-eval-desktop/CategoryCatalog.cs` — TICK-004's fix (see that ticket's files doc); `Load()` is now parameterless and Core-sourced. Delivered in the same PR/branch per the orchestrator's instruction to land both tickets together.
- `scripts/email-eval-desktop/MainForm.cs` — `suggestionLabel` switched from `AutoSize` to a fixed, wrapping label (`AutoSize = false`, `TextAlign = TopLeft`) and its row grew from 38px to 110px so the multi-line category/evidence/policy-version block is legible instead of clipped.
- `scripts/email-eval-desktop/tests/DesktopEvaluatorTests.cs` — added `RuleClassifiedEmailPopulatesCategorySubtypeEvidenceAndPolicyVersion` (deterministic fixture: an in-memory `.eml` with an `Automatic reply:` subject, which the QDOS policy's `subject.automatic-reply` predicate matches; asserts the exact rendered `Suggested: Received / General/autoreply (policy qdos_mail_classification v3) ... Evidence: ...` string) and extended `FilingCopiesSourceAndEscapesReasonInOneJsonLine` to assert the JSONL log's `suggestedCategory` field for the unclassified case. All fixture emails are now built in-memory via MimeKit rather than copied from a repository file (see TICK-004 — that file path no longer exists).
- `docs/adr/0016-standalone-desktop-email-evaluator.md` — one-line Context correction: the taxonomy source is now named as Core's `MailTaxonomy` rather than the deleted `docs/reference/CollisionSPikeCurrenttree.txt`; the decision itself (standalone tool, calls the existing shared Core/infrastructure reader and policy) is unchanged.

No changes to `Pegasus.Web`, `Pegasus.Worker`, or any deployment/composition-root file — the desktop project remains outside `Pegasus.slnx` per ADR-0016.

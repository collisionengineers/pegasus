# Proof — TICK-047 / MAIL-05 (deployed at release 16, 2026-08-21)

Type: visual + test-output. Deployment bundle: [[DELIV-015]] proof. Deployed to production at `4111ad29` (branch an ancestor of the merged mail workspace; lost-work audit clean). The exact-message read derives the current folder recommendation from `MailLogicalFolderPolicy` and the approved-mailbox binding on every read; the message page's Decision card shows the derived destination ("Receiving work" live on the classified messages) and the honest unavailable state when classification is absent (PR-032's rule; the live Unclassified message shows no invented destination). No persistence, no Graph call, no mailbox mutation — as scoped. Live folder bindings configuration is the operator's administration act.

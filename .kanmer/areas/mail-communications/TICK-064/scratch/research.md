2026-08-20 refresh against origin/dev b36c6666 and origin/main 2325ed4a: MAIL-02's `MailOperationalDestinationPolicy` is already the sole operational-queue owner. Current callers are `/Inbox/{id}` on both main/dev and `MailMcpTools` on dev. FRD-08 already owns the 13 logical folder types and the exhaustive category-to-type catalogue, but repository code has no logical folder contract/policy and approved-mailbox administration can bind only Inbox/Sent identities. TICK-064 should not edit retained-message persistence to create a second mapping; it should provide the canonical folder-type mapping and mailbox-approved identity binding/resolution consumed later by MAIL-05. Unidentified remains the canonical abstention vocabulary; the one stale `Needs sorting` phrase in FRD-08's Triage row must be reconciled without collapsing Triage.

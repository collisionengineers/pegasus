## Research refresh — 2026-08-20

Inspected `origin/dev` at `b36c66662288adb0727299276f675337442a1e22`, TICK-051, both epic contexts, FRD-02/08, the design contract, runtime approval/grant rules, the retained-mail read path, the existing Intake association commands/store, the shared upload Case-search/attach presenter, and focused persistence/Web tests.

Provisional conclusion: MAIL-10 must land after MAIL-09 and reuse the existing `ILinkIntake` / `IReverseIntakeLink` + `EfIntakeMutationStore` serializable transaction. Exact manual behavior is staged: link an unassociated exact receipt; reasonedly unlink the current association; then reasonedly link the now-unassociated receipt to the replacement Case. There is no accepted direct active-to-active swap command. Each transition gets its own Case lease/version, idempotency key, attribution, before/after history and confirmation. TICK-051 must first make retained mail project the same current-association precedence and may adjust the transaction shape; TICK-052 consumes that landed shape rather than racing it.

The real gap is the retained-message detail caller: resolve the message's server-side `IntakeReceiptId`, reuse the established `ISearchCases`/target-summary convention and shared Web association orchestration, preserve message/list return context, and never duplicate association policy. Local implementation performs no Graph/Outlook write. The accepted production journey is a Pegasus database write and remains unauthorized until immediately preceding exact-target approval names the message, initial Case, replacement Case and reasons; it does not authorize Outlook mutation.

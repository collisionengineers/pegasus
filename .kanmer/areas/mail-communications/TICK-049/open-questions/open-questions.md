# Open questions — TICK-049 / MAIL-07

- [x] **Where does MAIL-06 confirmation live now that TICK-048 is archived?** — In MAIL-07. The opened message detail displays the current MAIL-05 recommendation, requires a separate reasoned Confirm/Cancel action, and invokes the move only from the confirmed POST. Do not revive TICK-048.
- [x] **Who chooses the destination?** — MAIL-23 supplies the typed approved binding and MAIL-05 supplies the current exact recommendation. The server revalidates both; the browser submits no mailbox, source-message or destination transport identity and cannot select an alternative folder.
- [x] **Which concurrency evidence makes a confirmation current?** — The MAIL-05 result must carry the current classification/recommendation and approved-binding versions. The POST submits those expected versions plus the internal retained-message id, reason and operation key; any changed classification/binding/location refuses and reloads.
- [x] **How are provider replay and uncertain responses handled?** — Reserve a unique operation key/request fingerprint before Graph. A matching completed replay returns its result; different inputs conflict. After an unknown response, probe the immutable item and accept recovered success only when it is in the already-approved destination. A deliberate retry after a recorded failure uses a new operation key.
- [x] **Does movement rewrite the retained arrival record or classification?** — No. The source row and classification/correction history remain immutable. An append-only move operation/current-location projection makes the item leave Inbox after success and preserves failed/superseded attempts.
- [x] **May the implementation add a generic mail-action framework or background retry?** — No. Add the smallest concrete folder-move operation/adapter. Later read/category/delete/send capabilities may reuse proven conventions, but this ticket creates no generic command vocabulary, Worker function or automatic retry.

## Parked (explicitly deferred)

- [x] **May MAIL-07 change Graph permissions, deploy/activate the writer, or perform a real Outlook move?** — No. The operator declined a live move on 2026-08-19. Current production documentation records Mail.Read, while Graph move requires Mail.ReadWrite and exact Exchange Application RBAC scope proof. Use local SQL/integration tests and fake HTTP Graph responses only. Do not alter tenant/application permissions, mailbox/folder state, cloud configuration or deployment. Report MAIL-07 as not live-mutation-verified. Any future live attempt requires fresh approval for the exact tenant, application, mailbox, source folder, disposable message, destination folder and operation, plus a negative scope test.

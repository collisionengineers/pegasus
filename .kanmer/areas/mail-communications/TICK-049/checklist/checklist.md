# Checklist — MAIL-07

- [x] After PR #469 merges/releases, fetch its `origin/dev` merge, re-read every overlapping file, create `task/tick-049-mail-07-confirmed-folder-move` in `../pegasus-worktrees/tick-049`, and take TICK-049.
- [ ] Add the focused Core move request/result/use case and ports, reusing staff authorization, classification/recommendation policy and approved-mailbox versions/bindings with no browser transport identity.
- [ ] Add the dedicated durable operation/current-location entity, EF configuration/store and migration while preserving retained arrival evidence.
- [ ] Extend the existing Graph client with only the exact folder-scoped move and immutable-id parent-folder probe; prove it solely with a fake HTTP handler.
- [ ] Overlay successful current location into retained-mail list/detail without regressing PR #469 search/paging behavior.
- [ ] Add the authenticated reasoned Confirm/Cancel Web POST, stale/failure/result/retry rendering, and no destination selector/input.
- [ ] Add focused Core, persistence, fake-Graph and authenticated Web evidence for validation, replay/conflict, concurrency, recovery, success, visible failure and explicit new-key retry with classification preserved.
- [ ] Run `dotnet restore`, Release build, focused tests, and the proportional full Core and Integration suites.
- [ ] Run the reuse, simplification, efficiency and altitude lenses; apply safe simplifications and record dated dispositions in the plan.
- [ ] Update capabilities/current architecture only to local evidence; explicitly record no permission, deployment, activation, live Graph call or Outlook mutation.
- [ ] Write the post-implementation report, record commits/PR, push, open the PR to `dev`, move TICK-049 exactly Implementing → Review, and hand off to `kanmer-review`.

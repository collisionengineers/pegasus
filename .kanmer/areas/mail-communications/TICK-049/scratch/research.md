2026-08-20 origin/dev refresh: archived TICK-048 has no documents and its sole confirmed behavior is absorbed here: the opened message detail shows the current MAIL-05 recommendation, asks for an explicit reasoned confirmation, and only that POST may invoke the move. Current Graph code is GET-only (`GraphMailClient`, `GraphApprovedInboxSource`) and Web composition has no mail mutation adapter. Retained rows preserve exact MailboxId, ImmutableMessageId and source FolderIdentity, but are documented write-once; MAIL-07 therefore needs a dedicated durable operation/history/current-location projection rather than overwriting source evidence. Graph v1 move is POST `/users/{mailbox}/mailFolders/{source}/messages/{immutableId}/move` with `{destinationId}`; immutable IDs require `Prefer: IdType="ImmutableId"` on each request and remain stable inside one mailbox. Production currently documents Mail.Read only; move needs Mail.ReadWrite, no permission/write approval exists, and operator explicitly declined a live move. TICK-064 then TICK-047 are hard predecessors; 049 must consume their versioned exact recommendation, not accept destination input.

## 2026-08-20 post-merge refresh

Verified `origin/dev` is `a1775841297108db8de2d612a2ba82452b02242e`, including MAIL-23 and MAIL-05. The landed read-only recommendation is `RetainedMailFolderRecommendation(FolderType, PolicyKey, PolicyVersion, Reason)`; its contract explicitly says a later move re-reads the exact current approved binding rather than carrying an opaque destination identity from the view. `ApprovedMailbox.Version` is the available non-secret binding freshness token and `MailClassificationDossier.Version` is the classification freshness token.

Execution is presently blocked by the active taken TICK-053 / PR #469 claim. Its diff overlaps `RetainedMail.cs`, `EfRetainedMailboxMessageStore.cs`, `GraphApprovedSources.cs`, `DependencyInjection.cs`, `Message.cshtml(.cs)`, and all three intended integration-test files. Per AGENTS.md, do not create/take TICK-049's branch/worktree until PR #469 merges and TICK-053 releases. Exact handoff: after #469 lands, fetch `origin/dev`, confirm it contains `a1775841` plus the #469 merge, re-read those landed files, create `../pegasus-worktrees/tick-049` on `task/tick-049-mail-07-confirmed-folder-move` from that `origin/dev`, then take TICK-049.

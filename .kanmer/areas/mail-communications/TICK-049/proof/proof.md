# Proof — TICK-049 / MAIL-07 (deployed and gated at release 16, 2026-08-21)

Type: test-output + visual. Deployment bundle: [[DELIV-015]] proof. Deployed to production at `4111ad29`. The confirmed-move implementation is live code behind its **unavailable-by-default provider composition**: Core revalidates classification/recommendation/mailbox versions, Infrastructure owns the durable `RetainedMailFolderMoves` operation and current-location record, the Graph client carries the exact folder-scoped move + immutable-id probe, and the message page's move dialog (expected-versions contract) renders live. **No production writer is activated and no Outlook mutation is claimed** — exactly the ticket's scoped boundary; activation (Graph write permission + provider composition) remains a separately approved act. Local SQL/Web/fake-HTTP evidence (replay, arrival-evidence preservation, Inbox removal, exact request shape, cancelled-move recovery) is the merged CI-green suite.

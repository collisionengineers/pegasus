# Files — PLAT-069 (2026-09-02, gpt-5.6-terra medium, wrapper-checked; revised 2026-09-03 after plan review)

## Planned files

| Path | Action (create/change) | Why | Reuses |
| --- | --- | --- | --- |
| `src/Pegasus.Core/Operations/ServiceHealth.cs` | change | Add the approved Core-owned health-notice predicate; no existing snapshot helper expresses it. | `ServiceHealthSnapshot`, `ServiceHealthState`, `ServiceHealthPolicy` |
| `src/Pegasus.Web/Pages/Operations/Index.cshtml` | change | Remove the Service health panel; render the administrator-only D37 notice (anchorless while `Pages/Administration/ServiceHealth` is absent) and strip the limit notice's explanatory sentence. | `.notice`, `.notice--warning`, `#icon-alert-triangle`, `User.IsInRole`, `_FreshnessBanner` |
| `src/Pegasus.Web/Pages/Operations/Index.cshtml.cs` | unchanged | The notice condition is the Core predicate called from the view over the snapshot `IndexModel` already loads; no code-behind change is made (plan review finding 4). | — |
| `src/Pegasus.Web/Presentation/OperatorLabels.cs` | change, shared-lock lease required | Centralize the `Partial data` and `Service health` notice labels rather than adding another literal label source. Retains the four existing `ServiceHealth*Name` helpers for PLAT-051's Administration table. | Existing `ServiceHealth*Name` helpers, nested label classes (`AiJobs`, `EvaHandoffs`) |
| `tests/Pegasus.Core.Tests/Operations/ServiceHealthTests.cs` | change | Prove the Core predicate: `Partial`/`Failed` true, `Running`/`Configured`/`ReviewRequired` false, `ExternalWorkLimitReached` ignored. | Existing `ServiceHealthPolicy` and `GetServiceHealth` fixtures (`Build(Sources)`) |
| `tests/Pegasus.IntegrationTests/OperationsWebTests.cs` | change | Table absence, Administrator notice, Engineer/User negative theory, no `href=""`, and the combined limit-plus-health state over a configurable `RecordingOperationsStore.LimitReached`. | `Configure(..., withServiceHealth: true)`, `RecordingOperationsStore`, `NoServiceHealthFacts`, `GetHtmlAsync`, `X-Test-Roles` |
| `docs/design/test-ui/catalogue.json` | change, shared-lock lease required | Add the Operations administrator partial-data notice state (ticket Verification: "Snapshot states updated"). | Existing `/Operations` entry (lines 531–549) |
| `docs/design/test-ui/pages/operations--*.html` | change (regenerated) | Regenerated by `./scripts/Update-TestUiSnapshots.ps1` after the page change, per CLAUDE.md. | Snapshot tooling |
| `tests/Pegasus.IntegrationTests/TestUiSnapshotTests.cs` | change | `StateMatches` entry for the new Operations notice state so the capture can be matched. | Existing `["operations--empty"]` pattern |

## Files PLAT-069 must not touch

- `src/Pegasus.Web/Pages/Administration/ServiceHealth/**` — owned by
  PLAT-051; it does not exist in this revision. PLAT-069 must not invent an
  Administration page to make its link work, and ships the notice anchorless
  until PLAT-051 adds the anchor with its destination.
- `src/Pegasus.Web/Pages/Administration/ActionLogs/**`,
  `src/Pegasus.Web/Pages/Administration/Reports/**`, and
  `src/Pegasus.Web/Pages/Administration/Shared/_AdminNav.cshtml` — owned by
  PLAT-051.
- `docs/design/test-ui/**` entries and pages for the Case record
  (`case-details--*`, Assessment, Engineer sections) — owned by UIIMP-014;
  PLAT-069 edits only its Operations states under the shared lock.
- `src/Pegasus.Infrastructure/Persistence/Migrations/**` — no schema change
  is required; migrations are serialized shared-lock work.
- `docs/frd/frd-12-operator-experience.md`, `docs/design/README.md`,
  `docs/capabilities.md` — DELIV-041 already records D37 (UI-19) at the
  relevant Operations and Administration sections; PLAT-069 does not alter
  governing documents. `docs/current-architecture.md` / `docs/operations.md`
  refresh belongs to DELIV-030 after release.
- `src/Pegasus.Web/Pages/Shared/*`, `wwwroot/css/site.css`,
  `wwwroot/js/site.js` — shell shared lock; the notice reuses existing
  classes and needs no shell change.
- `src/Pegasus.Web/Mcp/AutomationMcpExtensions.cs` — the `GetServiceHealth`
  composition stays as it is; PLAT-069 only consumes it.

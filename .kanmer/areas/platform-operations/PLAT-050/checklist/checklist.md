# Checklist — PLAT-050

- [ ] Root approves the whole plan/map and records exact shared-file
  clearances; fresh packet validates accepted base, migration reservation
  and the isolated claim/worktree before any source change.
- [ ] Extend only the existing creation/read contracts and Core contact
  normalization; update FRD-04 and the Principal design paragraph.
- [ ] Persist contact/hash/history atomically on the existing customer row;
  return it through existing projections; preserve code-replacement identity.
- [ ] Add the nullable migration/Designer/snapshot and pending-list entry;
  no backfill or redundant grants/bootstrap changes.
- [ ] Add the labelled Create input and populated Settings display only;
  retain all existing settings, credential and authorization behavior.
- [ ] Add focused Core, actual EF/Web, replay/conflict/replacement and
  migration tests; use the actual restricted Web role through existing
  helper and prove Worker create refusal.
- [ ] Freeze source; root records exact focused compiler/runtime exits,
  all attempts and retained TRX evidence, without duplicate broad suites.
- [ ] Root regenerates/verifies only the two Principal route snapshots and
  conditional index; catalogue/diff/grant checks recorded, no visual claim.
- [ ] Publish only after root evidence/authorization and fresh gates;
  independent exact-head review, no author merge.
- [ ] Post-merge exact source/caller proof accepted before Done/closeout;
  original settings/credential acceptance and separate API reconciliation
  history remain visible.

## Outcome

Prepared only. No source, worktree, test, live customer, e-mail, cloud or
deployment operation. Historical original scope is retained in ticket body
and research; new contact capability is not yet implemented.

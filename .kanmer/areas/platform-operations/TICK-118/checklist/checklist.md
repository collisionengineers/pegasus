- [x] Staff can record both completeness judgements through the live caller — `OnPostConfirmCompletenessAsync` (`Details.cshtml.cs:266`) wired through `IConfirmCompleteness` to the Core policy and `Cases` table; confirmed present at production release 13 (2325ed4a).
- [x] Review, Not ready, and Held contain the correct cases and agree with case detail/dashboard counts — confirmed live in production (prod-diagnostics §2: 2 `CaseWorkflows` rows, both `NotReady`, correctly surfaced on `Triage/Index`).
- [x] Authorization, validation, and visible failure behavior are exercised — `CaseDataOperations.cs:99,105` rejects a staff confirmation that contradicts Core's own completeness determination; the handler runs through the same authenticated, edit-leased, versioned command envelope as every other case command on the page.
- [ ] Live evidence states exact environment, caller, and limitations — stated in `research.md`, with the honest limitation named: zero production cases have yet had a completeness judgement recorded (both real cases are still incomplete on both dimensions). Left unchecked deliberately: this is evidence of readiness, not of an actual exercised use.

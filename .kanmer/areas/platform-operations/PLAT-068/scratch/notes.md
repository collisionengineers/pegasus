2026-09-02 kanmer-research run (Claude wrapper around `codex exec -m gpt-5.6-terra`, effort high) against `.worktrees/research` at `cad00be9` (= `origin/dev`); checkout clean before and after. Prompt and raw output: scratchpad `prep/PLAT-068/research-prompt.md`, `research-out.md`. Wrapper spot-checked eight VERIFIED claims in the main checkout; none dropped. Added to Files: the four Core test files constructing `StaffAccountSummary`, the conditional Edit-route rows, and `catalogue.json`. Codex's third question (behaviour on Engineer-role removal) was folded into research as a recommended plan default (retain data, derive eligibility from enabled + Engineer + flag) rather than an operator question. Two operator questions remain in `open-questions/`.

2026-09-02 kanmer-plan run started (Claude wrapper around `codex exec -m gpt-5.6-terra`, effort high) against `.worktrees/research` at `897db953` (= `origin/dev`, clean). Prompt: scratchpad `prep/PLAT-068/plan-prompt.md`; output `plan-out.md`. Kanmer MCP reads again returned only the project fingerprint; board state read from the kanmer worktree on disk and gates from `.kanmer/data/board.yml` (feature leave-preparing: research, files, plan, checklist).

2026-09-03 kanmer-plan wrapper completed. Codex was not re-run: the 2026-09-02 run's `plan-out.md` (scratchpad `prep/PLAT-068/`) was complete (both markers present) at `897db953` (= `origin/dev`, clean before and after) and its prompt was the instruction verbatim; only the board writes had not happened. Wrapper spot-checked the plan's line references and reuse claims in `.worktrees/research` (see plan "Wrapper checks"); none dropped. Wrapper added the FRD-04 application-data premise, the Settings label, the Edit-route note, a finer-grained checklist, and open question 3 (printed signatory name vs username — no account Name exists; `ActorDisplayNames` returns `UserName`). Kanmer MCP calls again returned only the project fingerprint; writes were verified on disk in the kanmer worktree and gates read from `.kanmer/data/board.yml` (feature leave-preparing: research, files, plan, checklist — all present). Ticket stays in preparing: three unticked open questions block the move.

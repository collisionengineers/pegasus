## Plan

1. **Trace both named leaks to their query owners.** `Activity.cshtml`'s `record.SubjectId` comes from `IAutomationActivityQueries`/`ListAutomationActivity` (Core) backed by `EfAutomationActivityStore` (Infra); the automation client's friendly name lives behind OpenIddict (`AutomationClientRegistry`, Web-only) so the resolution has to happen at the Web layer, not Core — reuses the existing `AutomationClientRegistry`/`AutomationMcpOptions` composition already used by the sibling `Index.cshtml.cs`. `_CaseSummary.cshtml`'s `approval.ApprovedBy.SubjectId` comes from `CaseDetails.Workflow.ReportApproval`, produced by `GetCase` (Core) — the existing composer that already injects several query ports.
2. **Build one shared resolver, not per-surface logic.** `ActorDisplayNames` (Core/Actors) owns the per-`ActorKind` fallback labels and the batched staff-name lookup through the existing `IStaffAccountQueries` port — reused by every Core query owner touched below, so the label vocabulary lives in exactly one place.
3. **Fix the two named sites in their existing query owners** — `GetCase` (Cases/CaseQueries.cs) for the case surfaces; the Web-layer `ActivityModel`/`OperatorLabels` for the Automation surface (no Core change needed there since the client's name is a Web-only, single-client concern per ADR-0011).
4. **Sweep** `src/Pegasus.Web/Pages` for `SubjectId` and `.Actor` renders. Found and fixed in the same read models' owners: `_CaseHistory.cshtml` (same `CaseDetails`/`GetCase`), `Triage/Details.cshtml` (`GetTriage`), `Mail/Message.cshtml` (`GetRetainedMail` — required parsing the existing `"{kind}:{subjectId}"` persisted convention, so `MailClassificationActor` was added as the one format/parse point, matching prefixes already established elsewhere in the repo rather than deriving them from `ActorKind.ToString()`, which doesn't hyphenate `SystemWorker`).
5. **Tests**: a Core unit test for the shared resolver, Core unit tests for `GetTriage`/`GetRetainedMail`'s resolution wiring, a Web-layer unit test for the Automation label decision, and HTTP-level "no raw GUID renders" assertions on the Case summary/history and Mail message pages (the two pages that already had test coverage to extend).

### Simplification pass — 2026-08-20

Ran `/simplify`-equivalent review over this branch's diff (reuse, simplification, efficiency, altitude):

- **Reuse**: confirmed `ActorDisplayNames` is the single resolver used by all three Core query owners (`GetCase`, `GetTriage`, `GetRetainedMail`) rather than three separate copies of the per-kind fallback logic. Confirmed `AutomationClientRegistry`'s own `DisplayName` const was folded into the new `AutomationMcp.ClientDisplayName` rather than left as a duplicate.
- **Simplification applied**: `GetRetainedMail`'s staff-id extraction originally filtered then re-parsed each packed actor string twice; changed to `packedActors.Select(TryParseStaffId).OfType<Guid>()` (single parse, no null-forgiving operator).
- **Efficiency**: `ActorDisplayNames.ResolveStaffNamesAsync` issues one `IStaffAccountQueries.GetAsync` per *distinct* staff id (already deduplicated before the loop) — no N+1 beyond that, and there is no existing batch-lookup method on `IStaffAccountQueries` to call instead (adding one would be a new capability with a single caller shape, out of scope for a fix).
- **Altitude**: left `MailClassificationActor` as its own small format/parse type rather than folding it into `ActorDisplayNames`, since it encodes a Mail-specific persisted-string convention that the other two read models don't have — a second concept, not a duplicate of the first.
- **Findings not applied**: none — no unapplied findings to name.

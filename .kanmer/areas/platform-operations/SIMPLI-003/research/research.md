# Research — SIMPLI-003: the alpha journey and the non-cutover scope

## Question

Where is the accepted alpha journey stated today, which `Now / 0.1.0-alpha.1` work sits outside it, and what does "classify as paused" mean against the repository's schedule vocabulary?

## Findings (2026-08-17 survey of `dev` @ d677a39d)

- **The journey is already decided — in the wrong file.** `docs/open-decisions.md:15-32` (decided 2026-08-02): "the first live journey is the full QDOS cutover — a genuine QDOS instruction email through intake, review, Case/PO allocation, Box custody, and the EVA handoff bundle", followed by an eight-step ordered critical path (green `main`; mailbox intake → custody → extraction draft → principal → Case/PO minted → Box folder [INT-02/08/09/19/22/25, CASE-07, DOC-01/02]; extraction thresholds INT-21 with zero false case creation; DOC-02 live then completeness gates + Review/Not ready/Held [CASE-13/14/15/16, UI-02/08]; EVA bundle EXT-03 + `First sent to Engineer` proxy CASE-21 + drag-and-drop; chasing CASE-17/18, MAIL-18; telemetry OPS-07 + three alerts, then cutover date; OPS-23/OPS-25 close `0.1.0-alpha.1`). `:34` already names the **non-blocking set**: MCP-01–04, INT-17, INT-31, the EVAL cluster, live DVLA/DVSA adapters (replay/`Unavailable` acceptable), MAIL-14/16, OPS-09. `:13-20` says that section "owns" the path — which contradicts `AGENTS.md:69` / `docs/index.md:14`: product intent, scope and boundaries are a **PRD**'s; open-decisions holds *unresolved* questions.
- **The alpha stops at the EVA handoff.** EVA keeps engineering and reports (`open-decisions.md:22-23`); report sending MAIL-17 is `Later / 1.2.0` (`capabilities.md:242`); RPT-01–05 are `Later / 1.1.0`. There is no "email → report sent" journey in alpha.
- **Only one PRD exists**, `docs/prd/pegasus-product.md` — it already carries the alpha scope sentence (`:8` "an Operations-first staff service focused on a QDOS intake route"), required outcomes (`:10-17`), permanent boundaries (`:50-61`), and the acceptance model (`:63-86`), but no journey section. The PRD template (`docs/prd/README.md:25-26`) offers "Scope and permanent boundaries". It is the natural owner.
- **Register facts.** 131 rows carry `Now / 0.1.0-alpha.1` (`docs/capabilities.md`; families CASE 26, INT 25, OPS 18, UI 11, ACC 11, TRI 9, MAIL 7, DOC 7, MCP 5, EXT 5, EVAL 5, DATA 1, AI 1). Timing vocabulary is exactly `Now` / `Next` / `Later` / `Not planned` (`:23`); `Now` targets only `0.1.0-alpha.1` (`:309`); changing a horizon or target needs product authority (`:317`). "Paused", "freeze", "non-cutover" appear nowhere under `docs/`. Several non-blocking rows already say so in their activation column (MCP-01–04/06 "non-blocking for `0.1.0-alpha.1` acceptance"; INT-17 "non-blocking"; INT-31 "allocated but non-blocking"; MAIL-14/16 "non-blocking; post-report tracking starts manual"; OPS-09 "deferred and gates no release"; the evaluator cluster OPS-22 / EVAL-01–05 / MAIL-20 "separately owned prerequisite; not QDOS delivery" at `:56-67`).
- **Twelve rows contradict the critical path.** TRI-01–09 (`:120-128`), EXT-14 WhatsApp material (`:176`), INT-13 image-led intake (`:102`), INT-27 (`:113`) are all marked "Required and accepted before 0.1.0-alpha.1" yet none is in the decided critical path. Either the path is incomplete or the register wording is.
- **Runbook coupling (new since SIMPLI-007).** `scripts/Invoke-QdosAlphaAcceptance.ps1 -Profile OfflineCandidate` reads the acceptance roster from the register's `0.1.0-alpha.1` rows at run time (`docs/runbook.md:718-724`). Re-targeting a row away from `0.1.0-alpha.1` shrinks the roster; annotating its activation column does not.
- **Deployed reality** (`docs/operations.md`): release 8 (2026-08-07) live plus an un-numbered post-release-8 deployment (2026-08-12); Worker enabled 2026-08-13 with config-only proof; "no browser journey has exercised the upload-to-case path" (`:357-360`); temporary Administrator to remove before go-live (`:361-368`); OPS-09 deferred; no operator acceptance of the workflow exists (`current-architecture.md:43`).
- **Board.** 64 tickets carry HZN-003: 9 done, 1 verifying, 1 review, 4 implementing (incl. this), 49 backlog — the cutover-proof set TICK-001, TICK-116–124, TICK-108–115, TICK-125, TICK-028/029/031/032, TICK-218/219; candidate-pause tickets TICK-011/013/014/018/020–027, TICK-102, TICK-065, TICK-042, TICK-003–010, INTK-001. [[SIMPLI-012]] (post-alpha AI/MCP disposition) is adjacent: this ticket classifies MCP/AI as *non-blocking for cutover*; SIMPLI-012 decides their *post-alpha* fate.
- **`docs/boundaries.md`** has one table keyed by deferred *area* (`:14-24`), no alpha/post-alpha axis and no capability IDs.

## Verified vs assumed

- Verified: every `file:line` above.
- Assumed (operator's call, see open questions): whether the non-blocking rows should also change horizon, and whether the twelve contradicted rows are journey or not.

## Implications

The ticket is a **routing and classification** task, not a new decision: move the decided journey into the PRD (its owner), retire the open-decisions section to a back-link plus the genuinely open items, and make the classification of every `Now` row explicit — **journey** (on the critical path) or **non-blocking for cutover** (may ship in `0.1.0-alpha.1` but does not gate it) — in the register's activation column, using the vocabulary the register and open-decisions already use. Whether "non-blocking" rows should also move to `Next` (which shrinks the acceptance roster) is a schedule change the operator owns.

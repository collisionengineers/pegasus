# Proof — TICK-029 (OPS-14)

Type: command-log. Completed by **release 14** (`39bb118a` on `main`, PR #475). Supersedes the earlier proof, which overstated: before release 14 the previous-artifact rollback half was only a retention statement plus one ad hoc incident record, not a documented procedure (release-14 verification finding).

- **Cutover half:** the release route is documented (runbook § Release artifacts and bootstrap; operations § Production environment) and has now been executed fourteen times, most recently for release 14 itself (full gate-by-gate transcript on DELIV-013).
- **Previous-artifact rollback half:** `docs/runbook.md` § "Previous-artifact rollback (Web and Worker)" (added by PR #475, on `main` and `dev`): approval-gated web re-pin (previous digest + revision, preview-gated provision), worker config-zip from the retained `artifacts/releases/release-<n>-<sha>/worker.zip`, roll-forward-only database stance with recovery cross-reference, smoke against the previous release's exact SHA/version, and the recording obligation. Inputs verified real: release-13's artifacts (incl. `worker.zip` and stored preview) are retained, and its image remains in the production ACR by digest.

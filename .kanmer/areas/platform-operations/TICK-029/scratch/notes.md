VERIFY2 dissent note (2026-08-20), recorded without reverting the stage: the proof's rollback evidence is real — operations.md does record the 2026-08-18 replacement-image attempt being rolled back to revision `pegasus-prod-web-252ow37gij--rollbacka593b` using the previously deployed image. Two caveats for the operator/next docs pass: (1) `docs/runbook.md:1064-1077` is the Worker **activation-flag disable** rollback, not a previous-artifact rollback — no reusable, commanded previous-digest rollback procedure exists in the runbook (no ACR digest lookup, no revision-repoint steps; "Production recovery" §1112 is prose obligations only), and `artifacts/releases/` is gitignored with only truncated digests recorded in operations.md; (2) the OPS-14 capability row still carries "implementation/recovery detail remains open", and its cited canonical owner (frd-12 #operator-experience) contains no cutover/rollback content at all. Either the row's remark + owner pointer should be corrected to match this ticket's done disposition, or the rollback-procedure write-up remains open work. Flagged to the orchestrator for adjudication rather than edit-warring the stage.

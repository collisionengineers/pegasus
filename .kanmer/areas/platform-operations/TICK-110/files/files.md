## Files — TICK-110 — retrospective backfill

| Path | Why |
|---|---|
| `docs/operations.md:405-413` | Records the local-azd-vs-production vault drift found and fixed (release 8), all six Key Vault references `Resolved`. |
| `docs/operations.md:442-450` | Records the standing rule ("read the deployed resource, not the local environment") this ticket exists to enforce. |

No source change proposed; this ticket reconciles the board record with already-documented operational evidence.

A coordination at 2026-09-06T06:28Z: validated F is dc3cfd908348b38d69b5d5357c6d5899bfe5f812 (see plan/foundation-handoff on all three owners). B/C may adopt that exact F now; do not wait for another Foundation label. Both remote desktops were checked and agents are active.

I have read B/C review notes. Common G1 is being authored at F in isolated task/pegasus-v1-foundation-g1, no helper PR:
- C source/third-party contracts get nullable document/version OR IntakeAsset identity and int occurrence; source queries support pre-case; ClaimSource reason; directory entity matches contact/source/audit contract and prefix indexes; inspection suggestions fixed internal20, callerlimit removed.
- B monetary Materials decimal18,2; versioned LabourRateCards table; WorkUnits/PaintWorkUnits widen18,6 (retain raw provider Time/TimeUnit provenance and don't round source inputs to0.1; B owns policy/import normalization). AwaitingImport enum added. Exact five approved valuation presets seeded once in sharedmigration.
- CaseDataFields/AssessmentFields closed SQL names replaced by nonempty/length constraints; Core owns vocabulary.
- A exact DocumentVersion Box file/version metadata + explicit CustodyDocumentVersion.BoxVersionId; mailbox subscription generation.
Same v1 migration is corrected because these are disposable prerelease branches; test schemas created from earlier F require fresh creation when G arrives. No old-data converter.

B confirmations: field rows for v3 facts and one factory/context/transaction are accepted; credentialgeneration mismatch expires Bsession with no cross-store callback; rawestimate import follows A custody then Bimport (MCP uses same logical retainedshape); existing PaintMaterials is Additional materials. B-only generation/preparation inputs may extend as stated, consumed shared records stay frozen. EvaAutomaticSubmission removal waits for exact B/C caller-retirement patch in later sharedG; keep it absent from new UI/contracts meanwhile.

G1 SHA will be published with required same-object no-ff merge instructions. Continue independent domain work against F; do not independently alter these shared signatures/entities. A04 paused for G; A01 continues.

G1 final independent review: READY, no concrete contract/schema blocker; narrowed SQL indexes, candidate XOR identity, decimal precision, explicit Box version and passive engineer choices verified. Final root solution rebuild initially failed (exit 1, MSB3027/MSB3021) because retained MSBuild node 33836 locked G Core.dll. Process command verified as reusable MSBuild node; shutting build servers down and rerunning with node reuse disabled. Earlier schema-role tests remain separately recorded; this failure is not a test PASS.

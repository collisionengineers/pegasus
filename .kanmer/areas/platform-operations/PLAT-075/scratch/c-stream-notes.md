Stream C (claude-fable-c, INTK-060) 2026-09-06T06:10Z: C worktree `../pegasus-worktrees/v1-intake` on `task/pegasus-v1-intake` is at D `3284f93f`, restore/build green, no domain commits; waiting for the published F SHA to `git merge --ff-only`. Wave 0 outputs are on INTK-060 scratch: `pr-639-preservation`, `pr-646-disposition`, `pr-671-disposition` (all three heads unchanged versus the pinned tips), `source-manifest` (81/81, 29/29, 14/14; E01–E28 unavailable) and `handoffs-to-a` — the exact C-F01 (`UnidentifiedItemEntity.ReconciledAssociationVersion bigint NULL`, no index, keep the worker-role UPDATE assertion, census entry), PR 646 shared-fixture (`DrainStagedToTerminalAsync` at `32a5a62ce`) and C-F06 (`ImageIntakeEntity.PrincipalId uniqueidentifier NULL`, FK Restrict, index, interceptor hook in `IntakeWebTestSupport`) requests, plus an index-coverage question on the Unidentified recheck query. Reviews of the S02 C-owned contract files will be posted here once the F branch is visible.

## Stream C review of the F candidate contracts (5713d9b58, 06:25Z) — please read before publishing F

Reviewed the seven C-owned S02 files plus CustodyContracts, DocumentContracts, StaffMailSend, V1FoundationEntities/ModelConfiguration and the DbContext diff. Accepted as-is: `TriageRecord.Reference/PrincipalId` + `TriageSequenceEntity` + unique Sequence/Reference indexes; `ImageIntakeRecord.PrincipalId` + FK/index; `UnidentifiedItemEntity.ReconciledAssociationVersion`; `RetainedInstructionAnalysisEntity` + `IntakeSourceCandidateEntity`; `IntakeOcrOperationEntity`; `DocumentContentCacheEntryEntity`; `PublicUploadSession/Occurrence`; `IntakeAssetEntity` custody columns; `ICaseArtifactCustody.RetainAsync`; `IReadLogicalDocumentVersion.OpenAsync`; `IStaffMailSend`/`StaffMailState` (S12); `ThirdPartyReportContracts.cs` (typed superset). Four findings that are cheaper to fix in F than by a later common G:

1. **`SourceFieldCandidate` (IntakeContracts.cs) cannot describe pre-case material.** It carries non-nullable `Guid DocumentId, Guid DocumentVersionId`, but the persisted `IntakeSourceCandidateEntity` keys on `IntakeAssetId`, and `AnalyzeRetainedInstruction` runs on retained receipts that have no Case document (that is the whole point of C01). Request: `Guid? DocumentId, Guid? DocumentVersionId, Guid? IntakeAssetId` with the same either/or rule as `ReadLogicalDocumentVersionRequest` and the `CK_IntakeOcrOperations_Source` constraint; also `int Occurrence` (entity) vs `string OccurrenceIdentity` (record) — pick one (the entity's `int` is fine, record should match). `ISourceCandidateQueries.GetAsync(actor, receiptId, documentVersionId)` then needs `Guid? documentVersionId`/asset overload or a receipt-only read.
2. **`SaveClaimSourceRequest` has no `Reason`.** The C plan and C-F05 acceptance require every Administrator write to carry actor, expected version, reason and operation key (matching `UpdateOrganizationRolesRequest`). Request: add `string Reason`. `ClaimSourceEntity.Contact` vs record `ContactName` is naming only and fine.
3. **`OrganizationDirectoryEntryEntity` is narrower than `OrganizationDirectoryRecord`.** Record: `ContactName, Telephone, Email, Address (non-null), Postcode, SourceVersion, UpdatedAtUtc`; entity: `Contact`, `Address?`, no `Telephone`, `Email`, `SourceVersion`, `UpdatedAtUtc`, `UpdatedBy`, and `SourceRecordId` is `string?` while the record has `Guid SourceRecordId`. Since A owns the entity and C the store, request the entity gain `Telephone`, `Email`, `SourceVersion (long)`, `UpdatedBy`, `UpdatedAtUtc`, `Guid? SourceRecordId`, and indexes on `(Role, NormalizedName)` and `(Role, NormalizedPostcode)` for the prefix search (or confirm you prefer the record trimmed instead — either way they should agree at F).
4. **`InspectionLocationChoicesQuery.Limit = 20` is caller-configurable.** The C plan freezes the 20-row cap as internal ("not caller-configurable"). Not a compile blocker — C will clamp to `min(Limit, 20)` — but if you would rather drop the parameter, now is the time. Port name `IInspectionLocationChoices` accepted over the handoff's `IInspectionAddressSuggestionQueries` (accepted contract wins).

Two questions, no change requested unless you agree: (a) `PrincipalEntity.EvaAutomaticSubmission` is still present at the F candidate; C-F05 says A removes the automatic-EVA column — is that deferred to a later G, in which case C keeps the field out of its contracts/forms now and the column drops later? (b) `ThirdPartyReportCandidate` keys on `DocumentId/DocumentVersionId` only; reports retained pre-case (mail attachment before allocation) would need the same nullable asset alternative as (1), or C05 will only emit candidates for Case documents — acceptable if that is the intended boundary.

C will not fast-forward until you record the adopted F SHA here or on PLAT-075 foundation.md.

Stream C 06:40Z: adopted validated F `dc3cfd908` by `git merge --ff-only` (same objects; ancestry verified) and pushed `task/pegasus-v1-intake` at F. Domain work has started in disjoint slices against F (C01 retained analysis + PR 639/646 ports; C07 pre-case: Image Intake principal, T references, public upload session, incoming custody). Acknowledged: G1 will carry the contract corrections and must be merged `--no-ff` as the same object; C will not alter shared signatures meanwhile and keeps `EvaAutomaticSubmission` out of new UI/contracts. Two further A-owned items C will need in the composition window, already on INTK-060 `scratch/handoffs-to-a`: the `DependencyDirectionTests` extraction-policy invariant rewrite and the principal-identification corpus rebuild; plus (new) the `{Corrected}` log placeholder in `IntakeFunctions.cs`, the `DrainStagedToTerminalAsync` shared helper, the `IntakeWebTestSupport` command-interceptor hook, and an Identity-owned eligible-engineer list query for Triage assignment if none exists.

Stream C 10:45Z: merged G1–G7 (`fec546170254f7ca8bbca427e5404ef12f58b434`, same objects, `--no-ff`) into `task/pegasus-v1-intake` → `efbf3c8f6`; pushed. Draft owner PR: https://github.com/collisionengineers/pegasus/pull/673 (base dev, stays open/unmerged). No A domain commits merged.

Concrete branch-local DI/host requests for the A patch against the C head (will be refined with exact type names when each slice lands; all types are C-owned and compile standalone):
1. `AddScoped<AnalyzeRetainedInstruction>()`; `AddScoped<EfRetainedInstructionAnalysisStore>()` and its ports (`IRetainedInstructionAnalysisStore`, `IRetainedInstructionAnalysisQueries`, `ISourceCandidateQueries` → same instance); `AddSingleton<InstructionExtractionPolicySelector>()` over `IEnumerable<IInstructionExtractionPolicy>`; `ProcessIntake` keeps its single QDOS `IInstructionExtractionPolicy` until C03, then the selector replaces it and the fourteen profiles register as `AddSingleton<IInstructionExtractionPolicy, XxxInstructionExtractionPolicy>()` (list to follow at the C03 head) with the `DependencyDirectionTests` invariant rewritten in the same patch.
2. `AddScoped<RetainIncomingArtifact>()` + `IIncomingArtifactCustodyStore` → `EfDocumentRequestStore`; `IPublicUploadSessionStore` → `EfDocumentRequestStore`.
3. `IProcessIntakeOcr` → `AzureDocumentIntelligenceOcr` (Infrastructure, `HttpClient` + `Azure.Identity`, no package) with an offline `Unavailable` implementation for the DevelopmentOffline profile; new external-work kind `intake_page_ocr` routed in `ProcessQueuedExternalWork` + `EfExternalWorkStore.MarkPoisonedAsync` + `ServiceHealth.ExternalWorkDependency` (C02 head).
4. `IInspectionLocationChoices` → `InspectionAddressChoicesQueries` (extended); `IClaimSourceAdministration`/`IClaimSourceQueries` → `EfClaimSourceAdministration`; `IOrganizationDirectoryQueries` → `EfOrganizationDirectory` (C06 head).
5. `IRetainedMaterialSearch` → `EfRetainedMaterialSearch` (C08 head).
6. Worker: `IntakeFunctions.cs` `{Corrected}` placeholder for the 4-arity `ReconcileUnidentifiedDestinationsResult`.

Requests/questions for A:
- A05 cursors: B's `CursorPage<T>`, `CursorPaging`, `CursorRejectedException` and the `CursorToken` codec live in B-owned `Cases/CaseQueries.cs` (a64e51d19). C must not reference B commits and will not copy the codec. C implements store-level keyset continuations for intake, Triage and Unidentified lists now (explicit decoded position parameters); please promote the cursor primitive into a common G8 (or name the shared file) so C binds the opaque token to the same codec — one list per concept.
- Admin nav: C adds static links for `/Administration/ActionLogs`, `/Administration/AiJobs`, `/Administration/Reports`, `/Administration/ServiceHealth` and B's `/Administration/ValuationPresets` (B route confirmed at `Pages/Administration/ValuationPresets/Index.cshtml`). Please confirm the four A route strings (none of those pages is on the platform branch yet); C uses static hrefs so the standalone branch renders, and the combined checkout proves them.
- A07 OperationsSnapshot: C removes `CaseActivity` and `MailActivity` from the `OperationsSnapshot` record and the Work Centre page (the Unidentified metric will read `Intake.NeedsSorting`), and stops calling `IDashboardQueries.GetCaseActivityCountsAsync`/`GetMailActivityCountsAsync`. The `IDashboardQueries` members and the `CaseActivityCounts`/`MailActivityCounts` records stay declared in C-owned `DashboardCounts.cs` until A deletes `EfDashboardQueries`' implementations; then C deletes the members in the next shared window. Say if you prefer a single G instead.
- Triage allocator: the C07 slice implements the global `T-00001` allocation inside `EfTriageStore.CreateAsync` (sequence from `TriageSequences` in the same serializable transaction) — this fixes the `CK_Triage_Sequence` failure you saw in the genuine triage-only parity test.
- Typed actor: Triage and Unidentified mutation commands take the typed `ActionActor` through Core authorization (PLAT-043) in the C07/C01 slices; exact source metadata + authorized streaming for intake source/asset reads land in C01 through `IReadLogicalDocumentVersion`/`IDownloadIntakeSource`.

Stream C 10:55Z — acknowledgements and the A07 inventory you asked for.

Accepted: A05 authors only the grant-id consent hunk in `src/Pegasus.Web/Pages/Connect/Authorize.cshtml.cs` (host composition exception); A02's mailbox-generation fake hunks in `PollApprovedInboxTests.cs`, `RetainedMailTests.cs`, `LocalIntakeAccessTests.cs`, `QdosAllocationRecoveryTests.cs` and A01's account-action tests in `AdministrationSearchAccountWebTests.cs` are preserved — C implementers are instructed not to edit those five files, so the combined checkout takes A's hunks verbatim with no C business-policy rewrite. The `EfDocumentRequestStore` `BoxFileId`/`BoxVersionId` assignment travels in C07 with `RetainIncomingArtifact`'s custody-state store method. G8 calendar: once published, C replaces direct time-zone lookups with `LondonCalendar.DateAt/LocalAt` and removes the silent UTC fallback in `OperatorLabels` (C08 follow-up on the same slice branch).

A07 — exact C-side call sites of the unused Case/Mail activity projections at the C head `efbf3c8f6` (C08 slice 1 removes the C callers and the two `OperationsSnapshot` members now; A's shared correction then deletes the interface members, the two records and `EfDashboardQueries` implementations without leaving anything standalone-broken):
- `src/Pegasus.Core/Operations/DashboardCounts.cs:47` `record CaseActivityCounts(...)`; `:67` `record MailActivityCounts(int ReceivedToday, int NeedsSorting)`; `:81` `IDashboardQueries.GetCaseActivityCountsAsync`; `:86` `IDashboardQueries.GetMailActivityCountsAsync` — declarations for A's shared correction to delete after C08 lands (C leaves them until then).
- `src/Pegasus.Core/Operations/OperationsSnapshot.cs:55–56` record members `CaseActivity`, `MailActivity`; `:131` and `:135` the two sequential query calls — C08 removes all four.
- `src/Pegasus.Web/Pages/Index.cshtml:51` `@Model.MailActivity.Unidentified` → C08 reads `Model.Counts.NeedsSorting` (`Intake` counts) for the Unidentified metric; `Pages/Index.cshtml.cs:22` property and `:45` assignment — C08 removes.
- `tests/Pegasus.Core.Tests/Operations/DashboardBoundaryTests.cs:359–374` fake implementations of both methods — C08 removes once the interface members go (until then they stay compiling); `tests/Pegasus.IntegrationTests/DashboardCountersWebTests.cs:123` direct `GetMailActivityCountsAsync` call — C08 rewrites the assertion against `Intake.NeedsSorting`.
- Outside C08's slice, two A-preserved test files call the methods and are yours to change in the same correction: `tests/Pegasus.IntegrationTests/IntakePersistenceIntegrationTests.cs:321, :722–726` (`GetMailActivityCountsAsync`) and `tests/Pegasus.IntegrationTests/QdosAllocationRecoveryTests.cs:1268` (`GetCaseActivityCountsAsync`) — C will not touch them.
- `src/Pegasus.Infrastructure/Persistence/EfDashboardQueries.cs:77, :113` implementations — A's.
Proposed order: C08 slice merges into `task/pegasus-v1-intake` (removing the Core/Web/C-test callers, leaving declarations) → A publishes the shared correction deleting the declarations + Ef implementations + the two A-owned test call sites → C merges that G. If you would rather delete the declarations in the same G before C08 lands, C08 can be re-pointed; say which.

Stream C 11:05Z: merged G8 `b260098a708213083f0d6691638a1fa1bf3e2365` (same object, `--no-ff`, after G7) → C head `4e8be0690`, pushed to `task/pegasus-v1-intake` / PR #673. C08 is paused (read-only) until C01 or C07 finishes, so at most two implementation editors are active; when C08 resumes it adopts `LondonCalendar.LocalAt` for office display in `OperatorLabels` and removes the silent UTC fallback there.

Stream C 11:20Z: merged G9 `2ec79df3f507b69ea4863b01661ffcb521183796` (same object, `--no-ff`, after G8) → C head `0840c33a5`, pushed to PR #673. C01/C07 implementers instructed to inject Core `ICursorProtector`, build scopes with `CursorPaging.CreateScope` from the typed actor plus filters/order, use the shared UTC timestamp codec and `CursorPage<T>`, and never copy B's `CursorToken`. Admin routes noted: `/Administration/Health`, `/Administration/ActionLogs`, `/Administration/AiJobs`, `/Administration/Reports` (C08 uses these static hrefs). C cursor port signatures (intake list, Unidentified queue, Triage list) will be published here when the slices reach READY_FOR_TESTS.

Stream C 11:45Z — C07 slice READY_FOR_TESTS at `447e1c271` on `c07-precase` (test wave running; independent review next; then merged into `task/pegasus-v1-intake`). Delivered: global T allocator (`EfTriageStore.CreateAsync` takes the next `TriageSequences` number under `UPDLOCK, HOLDLOCK` last in its serializable transaction; `Sequence`/`Reference` written; replay returns the same reference — fixes the `CK_Triage_Sequence` failure), Triage keyset continuation over G9, PR 671 re-application, public upload session policy + typed `LimitsVersionMismatch`/`MayReissue`, `RetainIncomingArtifact` + `EfPublicUploadRetentionStore` (writes `BoxFileId`/`BoxVersionId` on Confirmed), Triage notes, explicit engineer assignment, Provider API 30 MiB assertion.

C cursor port signatures (Triage; intake/Unidentified follow from C01):
```csharp
sealed record TriageListPosition(DateTimeOffset CreatedAtUtc, Guid Id);
sealed record TriageListSlice(IReadOnlyList<TriageSummary> Items, TriageListPosition? NextPosition);
Task<TriageListSlice> ITriageQueries.ListPageAsync(TriageState? state, TriageListPosition? after, int limit, CancellationToken ct);
sealed record ListTriagePageQuery(ActionActor Actor, TriageState? State, string? Cursor = null, int? Limit = null);
interface IListTriagePage { Task<CursorPage<TriageSummary>> ExecuteAsync(ListTriagePageQuery query, CancellationToken ct = default); }
// scope: CursorPaging.CreateScope("triage", actor, state?.ToString(), "created_desc,sequence_desc"); protector: Core ICursorProtector (your G9 adapter)
```
Other C07 ports: `IAddTriageNote`/`AddTriageNoteRequest(TriageId, ExpectedVersion, Actor, OperationKey, Note)`; `RetainIncomingArtifact(ICaseArtifactCustody, IIncomingArtifactRetentionStore, ICaseArtifactCustodyStatus?)`; `IIncomingArtifactRetentionStore { FindAsync(operationKey); RecordAsync(RetainedIncomingArtifact) }`; `IncomingArtifactCustodyState { Pending, Confirmed, Failed, Unknown }`; `PublicUploadSessionPolicy` (15-minute non-sliding window).

DI for the A patch (C07): `IListTriagePage → ListTriagePage`; `IAddTriageNote → AddTriageNote`; `IIncomingArtifactRetentionStore → EfPublicUploadRetentionStore`; `RetainIncomingArtifact` scoped; `ICaseEngineerChoices` (G-added contract) has no adapter yet — the Triage assignment picker is gated on it, please name the A implementation; A04 `ICaseArtifactCustody` adapter still pending (C tests use a recording fake).

Typed-actor Triage mutations (PLAT-043 / A05) are NOT in this slice: converting `string Actor` → `ActionActor` on the Triage commands touches `DurableIntake.cs` (C01's file, next slice), A-owned `TriageMcpTools.cs`, `Pages/Intake/Details.cshtml.cs`, `CustodyOutboxIntegrationTests.cs`, `QdosAllocationRecoveryTests.cs`, `SentEvidencePollPersistenceTests.cs`, and needs an actor-kind column on `TriageHistoryEntity` (A-owned schema). Request: add `ActorKind` (string(40), required, default `Staff` for existing rows is not needed on the disposable schema — just required) next to the existing actor subject on `TriageHistoryEntity` in the next shared G; C then lands the typed-actor conversion as a dedicated slice after C01 merges, and A updates `TriageMcpTools.cs` against that C head. Snapshots to regenerate after merge: `triage-details--*` and `vehicle-images-details--default`.

Stream C 12:00Z — C01 READY_FOR_TESTS at `1be524f6b` on `c01-retained-analysis` (test wave queued behind C07's; one LocalDB test process at a time). Acknowledged: A adds `DrainStagedToTerminalAsync` and the interceptor hook (C01 carried a private copy of the drain helper; C will switch to yours when the combined checkout has it). `IntakeWebNegativeTests` baseline: being fixed now on the C01 slice to derive the expected business-table counts from the seeded estate (15 principals) and assert no mutation, all negative assertions preserved. A07: a C helper branch `task/pegasus-v1-c-a07-hunks` (pushed shortly, SHA to follow) will hold ONE commit with only the C caller/test removals (`OperationsSnapshot` members + two query calls; `Pages/Index` usage → `Intake.NeedsSorting`; `DashboardCountersWebTests`/`WorkCentreLabelTests` assertions), leaving the `IDashboardQueries` members, records and the `DashboardBoundaryTests` stub methods for your atomic G; C08 no longer touches any of it.

C01 cursor/metadata port signatures (namespace `Pegasus.Core.Intake` unless stated):
```csharp
sealed record KeysetPosition(DateTimeOffset SortKey, Guid Id);
sealed record KeysetPage<T>(IReadOnlyList<T> Items, KeysetPosition? Next);
KeysetPage<IntakeReceiptSummary> IIntakeReceiptQueries.ListByCursorAsync(IntakeDecision? decision, KeysetPosition? after, int limit, CancellationToken ct);
KeysetPage<UnidentifiedQueueRow> IUnidentifiedStore.ListQueueByCursorAsync(UnidentifiedMediaKind? mediaKind, KeysetPosition? after, int limit, CancellationToken ct = default);
sealed record ListIntakeByCursorQuery(ActionActor Actor, IntakeDecision? Decision, string? Cursor, int? Limit);
interface IListIntakeByCursor { Task<CursorPage<IntakeReceiptSummary>> ExecuteAsync(ListIntakeByCursorQuery query, CancellationToken ct = default); }  // scope "intake.received.list", order "received-desc,id-desc"
sealed record ListUnidentifiedQueueByCursorQuery(ActionActor Actor, UnidentifiedMediaKind? MediaKind, string? Cursor, int? Limit);
interface IListUnidentifiedQueueByCursor { Task<CursorPage<UnidentifiedQueueRow>> ExecuteAsync(ListUnidentifiedQueueByCursorQuery query, CancellationToken ct = default); }  // scope "intake.unidentified.queue", order "created-asc,id-asc"
sealed record IntakeFileMetadata(Guid ReceiptId, long ReceiptVersion, Guid AssetId, int Occurrence, string FileName, string MediaType, long ContentLength, string Sha256);
sealed record IntakeSourceMetadataQuery(Guid ReceiptId, ActionActor Actor); interface IGetIntakeSourceMetadata { Task<IntakeFileMetadata?> ExecuteAsync(IntakeSourceMetadataQuery, CancellationToken = default); }
sealed record IntakeAssetMetadataQuery(Guid ReceiptId, Guid AssetId, ActionActor Actor); interface IGetIntakeAssetMetadata { Task<IntakeFileMetadata?> ExecuteAsync(IntakeAssetMetadataQuery, CancellationToken = default); }
sealed class ProviderExistingCaseMatchException { const string FailureCode = "provider_existing_case_match"; }
```
Both cursor queries inject Core `ICursorProtector` and build scopes with `CursorPaging.CreateScope`; no C token codec. `DownloadIntakeAsset` streams through `IReadLogicalDocumentVersion` when composed (asset path); `DownloadIntakeSource.cs` was outside C01's list — C will add the same in the C02 slice.

DI for the A patch (C01): `AddScoped<InstructionExtractionPolicySelector>()`; `AddScoped<EfRetainedInstructionAnalysisStore>()` + `IRetainedInstructionAnalysisStore` and `ISourceCandidateQueries` → same instance; `AddScoped<IGetLatestRetainedInstructionAnalysis, GetLatestRetainedInstructionAnalysis>()`; `AddScoped<AnalyzeRetainedInstruction>()`; `AddScoped<IListIntakeByCursor, ListIntakeByCursor>()`; `AddScoped<IListUnidentifiedQueueByCursor, ListUnidentifiedQueueByCursor>()`; `AddScoped<IGetIntakeSourceMetadata, GetIntakeSourceMetadata>()`; `AddScoped<IGetIntakeAssetMetadata, GetIntakeAssetMetadata>()`; plus A04's `IReadLogicalDocumentVersion`. `PrincipalIdentificationCorpusTests.TrackedPegasusSourceHashesHaveNotDrifted` will fail on the C head because `QdosInstructionExtractionPolicy.cs` gained its document signature — the corpus rebuild request stands.

Stream C 12:40Z — acknowledged: runtime reference `0b2612e97` read-only (no domain head merge); `DownloadIntakeSource.cs` is A-owned and C02 stays off it (C01's deviation note withdrawn); `EfStaffAccountQueries` binds `ICaseEngineerChoices` in the combined checkout; `TriageHistory.ActorKind` in the next common G — C then lands the typed-actor Triage slice; the branch-local C registration patch will bind the C01/C07 ports once their heads are published on `task/pegasus-v1-intake` (after the correction round now running and the independent reviews).

`RetainedMailPersistenceTests.cs` duplicate QDOS seed: that file is A-owned in the ownership register (`tests/Pegasus.IntegrationTests/RetainedMailPersistenceTests.cs,A`). The change is four lines at L1502–1509 — replace the inline `OrganizationEntity`/`PrincipalSequenceLineageEntity`/`PrincipalEntity { Code = "QDOS" }` insert with `var qdos = await SeededPrincipals.QdosAsync(context);` and use `qdos.Id`/`qdos.OrganizationId`/`qdos.SequenceLineageId` where the fixture used its own ids (L1552 `PrincipalId = principalId` etc.), keeping every assertion. Please apply it on A, or say "C applies" and C will carry it on the C branch under a recorded ownership exception. C is sweeping its own test files for the same duplicate-seed pattern now and switching them to the helper.

C07 first wave: build PASS, architecture 100/100, Core 206/207, integration 63/65 — the two integration failures were the concurrency proof contending on the single-sweeper intake dispatch (being re-shaped to exercise `ITriageStore.CreateAsync` concurrently — the allocator itself is `UPDLOCK, HOLDLOCK` on the seeded `TriageSequences` row) and an EF ordering translation on the keyset query; correction round 1 in progress. Your triage-sequence failures clear once `EfTriageStore.CreateAsync` from C07 is on the C head.

Stream C 12:55Z — typed-actor Triage correction, agreed shape. C prepares the minimal contract/writer/caller hunks as ONE commit on helper branch `task/pegasus-v1-c-typed-actor-hunks`, rooted at the current shared boundary `2ec79df3f` (G9 head of `task/pegasus-v1-foundation-g1`) so it composes with your schema (`TriageHistoryEntity.ActorKind`, required string(40), no default, no backfill) and MCP host hunks into one atomic common G. Scope of the C commit: `src/Pegasus.Core/Triage/TriageContracts.cs` — every request record at G9 carrying `string Actor` (L85, 91, 99, 106, 128, 136, 221, 229, 254: create/mutate/assign/finding/response/note) becomes `ActionActor Actor`; `TriageHistoryEntry` gains `ActorKind` beside the subject; `TriageLifecycle.cs` validation requires a typed actor (Staff, Automation or SystemWorker where the email-evidence path already knows it; no Staff default, no inferred prefix backfill) and includes `Actor.Kind` + `Actor.SubjectId` in the replay/command hash exactly as the Unidentified commands do; `EfTriageStore.cs` writes `ActorKind` + subject on every history row and reads it back; C callers `DurableIntake.cs` (email-evidence/Triage creation keeps its `SystemWorker` kind), `Pages/Intake/Details.cshtml.cs`, `Pages/Triage/**`, C tests (`TriageReplayTests`, `QdosTriageReplayIntegrationTests`, `CustodyOutboxIntegrationTests`, `SentEvidencePollPersistenceTests` if C-owned — say if any of those four is yours and C will leave it). A adds `TriageMcpTools.cs`, the entity/model/migration/snapshot column, and any A-owned test fakes. When the G is published, C merges it `--no-ff` into `task/pegasus-v1-intake` and resolves the conflicts against C07's newer `TriageContracts.cs`/`EfTriageStore.cs` (T reference, notes, keyset) in the owning stream, then re-runs the Triage suites. The helper commit SHA follows once the two active editors free a slot (two-editor rule); please confirm the base SHA or name a later G head to root it at.

Stream C 13:05Z — A07 helper branch: commit 1 `c40b9d6e8` "refactor(operations): drop the unused Case/Mail activity projections from OperationsSnapshot (INTK-060 A07 hunks)" is on local `c-a07-dashboard-hunks` (rooted at the C head `0840c33a5` = G9 boundary + C's G merges only; no C domain commits precede it). Commit 2 (in progress) carries the corpus helper correction you asked for: `GenuineMultiFormatCorpus.CorpusRoot` resolves `CorpusLocator.CorpusRoot` (honours `PEGASUS_CORPUS_ROOT`, else repo `corpus/`) and throws a clear `InvalidOperationException` when absent, while `HasCohort`/`HasPinned` keep returning false so `[GenuineFormatCorpusFact]` still skips — no skip becomes a pass, no corpus copy or modification. Both will be pushed as `task/pegasus-v1-c-a07-hunks` with SHAs posted here; apply only those two commits.

Stream C 11:50Z — A07 hunks published: `task/pegasus-v1-c-a07-hunks` = C head `0840c33a5` + `c40b9d6e884a742902809e2639aee2fc15bb9fa1` (OperationsSnapshot/Index caller removals) + `2855d4a976e232fd75ce556df78a20d6f0b6d575` (`GenuineMultiFormatCorpus.CorpusRoot` → `CorpusLocator.CorpusRoot`, clear failure when absent, skips preserved). Apply only those two commits. Two notes for your atomic G: (1) `DashboardCountersWebTests.cs` L123 keeps calling `IDashboardQueries.GetMailActivityCountsAsync` with a marked comment because that assertion proves PLAT-012 mailbox-channel filtering on `ReceivedToday`, which `Intake.NeedsSorting` cannot stand in for — when you delete the interface member, either keep an equivalent `ReceivedToday` query on `IDashboardQueries` or delete that assertion in the same G (C accepts either; say which). (2) `DashboardBoundaryTests.cs` day/week-start boundary tests read `recorder.DayStartUtc`/`WeekStartUtc`, previously populated by the two removed calls — C is running the boundary suite on the hunk branch now and will push a follow-up commit rebinding them to the remaining day-start caller if they fail; treat the branch head at the time you compose the G as the hunk set. `IntakeWebNegativeTests` baseline and the C-owned duplicate-QDOS-seed sweep (`LayoutIntegrityTests`, `DocumentCustodyDurabilityTests`, `MailWorkspaceWebTests` → `SeededPrincipals.QdosAsync`) are on the C01 slice.

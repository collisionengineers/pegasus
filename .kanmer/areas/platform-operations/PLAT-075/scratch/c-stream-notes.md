Stream C (claude-fable-c, INTK-060) 2026-09-06T06:10Z: C worktree `../pegasus-worktrees/v1-intake` on `task/pegasus-v1-intake` is at D `3284f93f`, restore/build green, no domain commits; waiting for the published F SHA to `git merge --ff-only`. Wave 0 outputs are on INTK-060 scratch: `pr-639-preservation`, `pr-646-disposition`, `pr-671-disposition` (all three heads unchanged versus the pinned tips), `source-manifest` (81/81, 29/29, 14/14; E01–E28 unavailable) and `handoffs-to-a` — the exact C-F01 (`UnidentifiedItemEntity.ReconciledAssociationVersion bigint NULL`, no index, keep the worker-role UPDATE assertion, census entry), PR 646 shared-fixture (`DrainStagedToTerminalAsync` at `32a5a62ce`) and C-F06 (`ImageIntakeEntity.PrincipalId uniqueidentifier NULL`, FK Restrict, index, interceptor hook in `IntakeWebTestSupport`) requests, plus an index-coverage question on the Unidentified recheck query. Reviews of the S02 C-owned contract files will be posted here once the F branch is visible.

## Stream C review of the F candidate contracts (5713d9b58, 06:25Z) — please read before publishing F

Reviewed the seven C-owned S02 files plus CustodyContracts, DocumentContracts, StaffMailSend, V1FoundationEntities/ModelConfiguration and the DbContext diff. Accepted as-is: `TriageRecord.Reference/PrincipalId` + `TriageSequenceEntity` + unique Sequence/Reference indexes; `ImageIntakeRecord.PrincipalId` + FK/index; `UnidentifiedItemEntity.ReconciledAssociationVersion`; `RetainedInstructionAnalysisEntity` + `IntakeSourceCandidateEntity`; `IntakeOcrOperationEntity`; `DocumentContentCacheEntryEntity`; `PublicUploadSession/Occurrence`; `IntakeAssetEntity` custody columns; `ICaseArtifactCustody.RetainAsync`; `IReadLogicalDocumentVersion.OpenAsync`; `IStaffMailSend`/`StaffMailState` (S12); `ThirdPartyReportContracts.cs` (typed superset). Four findings that are cheaper to fix in F than by a later common G:

1. **`SourceFieldCandidate` (IntakeContracts.cs) cannot describe pre-case material.** It carries non-nullable `Guid DocumentId, Guid DocumentVersionId`, but the persisted `IntakeSourceCandidateEntity` keys on `IntakeAssetId`, and `AnalyzeRetainedInstruction` runs on retained receipts that have no Case document (that is the whole point of C01). Request: `Guid? DocumentId, Guid? DocumentVersionId, Guid? IntakeAssetId` with the same either/or rule as `ReadLogicalDocumentVersionRequest` and the `CK_IntakeOcrOperations_Source` constraint; also `int Occurrence` (entity) vs `string OccurrenceIdentity` (record) — pick one (the entity's `int` is fine, record should match). `ISourceCandidateQueries.GetAsync(actor, receiptId, documentVersionId)` then needs `Guid? documentVersionId`/asset overload or a receipt-only read.
2. **`SaveClaimSourceRequest` has no `Reason`.** The C plan and C-F05 acceptance require every Administrator write to carry actor, expected version, reason and operation key (matching `UpdateOrganizationRolesRequest`). Request: add `string Reason`. `ClaimSourceEntity.Contact` vs record `ContactName` is naming only and fine.
3. **`OrganizationDirectoryEntryEntity` is narrower than `OrganizationDirectoryRecord`.** Record: `ContactName, Telephone, Email, Address (non-null), Postcode, SourceVersion, UpdatedAtUtc`; entity: `Contact`, `Address?`, no `Telephone`, `Email`, `SourceVersion`, `UpdatedAtUtc`, `UpdatedBy`, and `SourceRecordId` is `string?` while the record has `Guid SourceRecordId`. Since A owns the entity and C the store, request the entity gain `Telephone`, `Email`, `SourceVersion (long)`, `UpdatedBy`, `UpdatedAtUtc`, `Guid? SourceRecordId`, and indexes on `(Role, NormalizedName)` and `(Role, NormalizedPostcode)` for the prefix search (or confirm you prefer the record trimmed instead — either way they should agree at F).
4. **`InspectionLocationChoicesQuery.Limit = 20` is caller-configurable.** The C plan freezes the 20-row cap as internal ("not caller-configurable"). Not a compile blocker — C will clamp to `min(Limit, 20)` — but if you would rather drop the parameter, now is the time. Port name `IInspectionLocationChoices` accepted over the handoff's `IInspectionAddressSuggestionQueries` (accepted contract wins).

Two questions, no change requested unless you agree: (a) `PrincipalEntity.EvaAutomaticSubmission` is still present at the F candidate; C-F05 says A removes the automatic-EVA column — is that deferred to a later G, in which case C keeps the field out of its contracts/forms now and the column drops later? (b) `ThirdPartyReportCandidate` keys on `DocumentId/DocumentVersionId` only; reports retained pre-case (mail attachment before allocation) would need the same nullable asset alternative as (1), or C05 will only emit candidates for Case documents — acceptable if that is the intended boundary.

C will not fast-forward until you record the adopted F SHA here or on PLAT-075 foundation.md.

Stream C 06:40Z: adopted validated F `dc3cfd908` by `git merge --ff-only` (same objects; ancestry verified) and pushed `task/pegasus-v1-intake` at F. Domain work has started in disjoint slices against F (C01 retained analysis + PR 639/646 ports; C07 pre-case: Image Intake principal, T references, public upload session, incoming custody). Acknowledged: G1 will carry the contract corrections and must be merged `--no-ff` as the same object; C will not alter shared signatures meanwhile and keeps `EvaAutomaticSubmission` out of new UI/contracts. Two further A-owned items C will need in the composition window, already on INTK-060 `scratch/handoffs-to-a`: the `DependencyDirectionTests` extraction-policy invariant rewrite and the principal-identification corpus rebuild; plus (new) the `{Corrected}` log placeholder in `IntakeFunctions.cs`, the `DrainStagedToTerminalAsync` shared helper, the `IntakeWebTestSupport` command-interceptor hook, and an Identity-owned eligible-engineer list query for Triage assignment if none exists.

Stream C 10:45Z: merged G1–G7 (`fec546170254f7ca8bbca427e5404ef12f58b434`, same objects, `--no-ff`) into `task/pegasus-v1-intake` → `efbf3c8f6`; pushed. Draft owner PR: https://github.com/collisionengineers/pegasus/pull/673 (base dev, stays open/unmerged). No A domain commits merged.

Concrete branch-local DI/host requests for the A patch against the C head (will be refined with exact type names when each slice lands; all types are C-owned and compile standalone):
1. `AddScoped<AnalyzeRetainedInstruction>()`; `AddScoped<EfRetainedInstructionAnalysisStore>()` and its ports (`IRetainedInstructionAnalysisStore`, `IRetainedInstructionAnalysisQueries`, `ISourceCandidateQueries` → same instance); `AddSingleton<InstructionExtractionPolicySelector>()` over `IEnumerable<IInstructionExtractionPolicy>`; `ProcessIntake` keeps its single QDOS `IInstructionExtractionPolicy` until C03, then the selector replaces it and the fourteen profiles register as `AddSingleton<IInstructionExtractionPolicy, XxxInstructionExtractionPolicy>()` (list to follow at the C03 head) with the `DependencyDirectionTests` invariant rewritten in the same patch.
2. `AddScoped<RetainIncomingArtifact>()` + `IIncomingArtifactCustodyStore` → `EfDocumentRequestStore`; `IPublicUploadSessionStore` → `EfDocumentRequestStore`.
3. `IProcessIntakeOcr` → `AzureDocumentIntelligenceOcr` (Infrastructure, `HttpClient` + `Azure.Identity`, no package) with an offline `Unavailable` implementation for the DevelopmentOffline profile; new external-work kind `intake_page_ocr` routed in `ProcessQueuedExternalWork` + `EfExternalWorkStore.MarkPoisonedAsync` + `ServiceHealth.ExternalWorkDependency` (C02 head).
4. `IInspectionLocationChoices` → `InspectionAddressChoicesQueries` (extended); `IClaimSourceAdministration`/`IClaimSourceQueries` → `EfClaimSourceAdministration`; `IOrganizationDirectoryQueries` → `EfOrganizationDirectory` (C06 head).
5. `IRetainedMaterialSearch` → `EfRetainedMaterialSearch` (C08 head).
6. Worker: `IntakeFunctions.cs` `{Corrected}` placeholder for the 4-arity `ReconcileUnidentifiedDestinationsResult`.

Requests/questions for A:
- A05 cursors: B's `CursorPage<T>`, `CursorPaging`, `CursorRejectedException` and the `CursorToken` codec live in B-owned `Cases/CaseQueries.cs` (a64e51d19). C must not reference B commits and will not copy the codec. C implements store-level keyset continuations for intake, Triage and Unidentified lists now (explicit decoded position parameters); please promote the cursor primitive into a common G8 (or name the shared file) so C binds the opaque token to the same codec — one list per concept.
- Admin nav: C adds static links for `/Administration/ActionLogs`, `/Administration/AiJobs`, `/Administration/Reports`, `/Administration/ServiceHealth` and B's `/Administration/ValuationPresets` (B route confirmed at `Pages/Administration/ValuationPresets/Index.cshtml`). Please confirm the four A route strings (none of those pages is on the platform branch yet); C uses static hrefs so the standalone branch renders, and the combined checkout proves them.
- A07 OperationsSnapshot: C removes `CaseActivity` and `MailActivity` from the `OperationsSnapshot` record and the Work Centre page (the Unidentified metric will read `Intake.NeedsSorting`), and stops calling `IDashboardQueries.GetCaseActivityCountsAsync`/`GetMailActivityCountsAsync`. The `IDashboardQueries` members and the `CaseActivityCounts`/`MailActivityCounts` records stay declared in C-owned `DashboardCounts.cs` until A deletes `EfDashboardQueries`' implementations; then C deletes the members in the next shared window. Say if you prefer a single G instead.
- Triage allocator: the C07 slice implements the global `T-00001` allocation inside `EfTriageStore.CreateAsync` (sequence from `TriageSequences` in the same serializable transaction) — this fixes the `CK_Triage_Sequence` failure you saw in the genuine triage-only parity test.
- Typed actor: Triage and Unidentified mutation commands take the typed `ActionActor` through Core authorization (PLAT-043) in the C07/C01 slices; exact source metadata + authorized streaming for intake source/asset reads land in C01 through `IReadLogicalDocumentVersion`/`IDownloadIntakeSource`.

Stream C 10:55Z — acknowledgements and the A07 inventory you asked for.

Accepted: A05 authors only the grant-id consent hunk in `src/Pegasus.Web/Pages/Connect/Authorize.cshtml.cs` (host composition exception); A02's mailbox-generation fake hunks in `PollApprovedInboxTests.cs`, `RetainedMailTests.cs`, `LocalIntakeAccessTests.cs`, `QdosAllocationRecoveryTests.cs` and A01's account-action tests in `AdministrationSearchAccountWebTests.cs` are preserved — C implementers are instructed not to edit those five files, so the combined checkout takes A's hunks verbatim with no C business-policy rewrite. The `EfDocumentRequestStore` `BoxFileId`/`BoxVersionId` assignment travels in C07 with `RetainIncomingArtifact`'s custody-state store method. G8 calendar: once published, C replaces direct time-zone lookups with `LondonCalendar.DateAt/LocalAt` and removes the silent UTC fallback in `OperatorLabels` (C08 follow-up on the same slice branch).

A07 — exact C-side call sites of the unused Case/Mail activity projections at the C head `efbf3c8f6` (C08 slice 1 removes the C callers and the two `OperationsSnapshot` members now; A's shared correction then deletes the interface members, the two records and `EfDashboardQueries` implementations without leaving anything standalone-broken):
- `src/Pegasus.Core/Operations/DashboardCounts.cs:47` `record CaseActivityCounts(...)`; `:67` `record MailActivityCounts(int ReceivedToday, int NeedsSorting)`; `:81` `IDashboardQueries.GetCaseActivityCountsAsync`; `:86` `IDashboardQueries.GetMailActivityCountsAsync` — declarations for A's shared correction to delete after C08 lands (C leaves them until then).
- `src/Pegasus.Core/Operations/OperationsSnapshot.cs:55–56` record members `CaseActivity`, `MailActivity`; `:131` and `:135` the two sequential query calls — C08 removes all four.
- `src/Pegasus.Web/Pages/Index.cshtml:51` `@Model.MailActivity.Unidentified` → C08 reads `Model.Counts.NeedsSorting` (`Intake` counts) for the Unidentified metric; `Pages/Index.cshtml.cs:22` property and `:45` assignment — C08 removes.
- `tests/Pegasus.Core.Tests/Operations/DashboardBoundaryTests.cs:359–374` fake implementations of both methods — C08 removes once the interface members go (until then they stay compiling); `tests/Pegasus.IntegrationTests/DashboardCountersWebTests.cs:123` direct `GetMailActivityCountsAsync` call — C08 rewrites the assertion against `Intake.NeedsSorting`.
- Outside C08's slice, two A-preserved test files call the methods and are yours to change in the same correction: `tests/Pegasus.IntegrationTests/IntakePersistenceIntegrationTests.cs:321, :722–726` (`GetMailActivityCountsAsync`) and `tests/Pegasus.IntegrationTests/QdosAllocationRecoveryTests.cs:1268` (`GetCaseActivityCountsAsync`) — C will not touch them.
- `src/Pegasus.Infrastructure/Persistence/EfDashboardQueries.cs:77, :113` implementations — A's.
Proposed order: C08 slice merges into `task/pegasus-v1-intake` (removing the Core/Web/C-test callers, leaving declarations) → A publishes the shared correction deleting the declarations + Ef implementations + the two A-owned test call sites → C merges that G. If you would rather delete the declarations in the same G before C08 lands, C08 can be re-pointed; say which.

Stream C 11:05Z: merged G8 `b260098a708213083f0d6691638a1fa1bf3e2365` (same object, `--no-ff`, after G7) → C head `4e8be0690`, pushed to `task/pegasus-v1-intake` / PR #673. C08 is paused (read-only) until C01 or C07 finishes, so at most two implementation editors are active; when C08 resumes it adopts `LondonCalendar.LocalAt` for office display in `OperatorLabels` and removes the silent UTC fallback there.

Stream C 11:20Z: merged G9 `2ec79df3f507b69ea4863b01661ffcb521183796` (same object, `--no-ff`, after G8) → C head `0840c33a5`, pushed to PR #673. C01/C07 implementers instructed to inject Core `ICursorProtector`, build scopes with `CursorPaging.CreateScope` from the typed actor plus filters/order, use the shared UTC timestamp codec and `CursorPage<T>`, and never copy B's `CursorToken`. Admin routes noted: `/Administration/Health`, `/Administration/ActionLogs`, `/Administration/AiJobs`, `/Administration/Reports` (C08 uses these static hrefs). C cursor port signatures (intake list, Unidentified queue, Triage list) will be published here when the slices reach READY_FOR_TESTS.

Stream C 11:45Z — C07 slice READY_FOR_TESTS at `447e1c271` on `c07-precase` (test wave running; independent review next; then merged into `task/pegasus-v1-intake`). Delivered: global T allocator (`EfTriageStore.CreateAsync` takes the next `TriageSequences` number under `UPDLOCK, HOLDLOCK` last in its serializable transaction; `Sequence`/`Reference` written; replay returns the same reference — fixes the `CK_Triage_Sequence` failure), Triage keyset continuation over G9, PR 671 re-application, public upload session policy + typed `LimitsVersionMismatch`/`MayReissue`, `RetainIncomingArtifact` + `EfPublicUploadRetentionStore` (writes `BoxFileId`/`BoxVersionId` on Confirmed), Triage notes, explicit engineer assignment, Provider API 30 MiB assertion.

C cursor port signatures (Triage; intake/Unidentified follow from C01):
```csharp
sealed record TriageListPosition(DateTimeOffset CreatedAtUtc, Guid Id);
sealed record TriageListSlice(IReadOnlyList<TriageSummary> Items, TriageListPosition? NextPosition);
Task<TriageListSlice> ITriageQueries.ListPageAsync(TriageState? state, TriageListPosition? after, int limit, CancellationToken ct);
sealed record ListTriagePageQuery(ActionActor Actor, TriageState? State, string? Cursor = null, int? Limit = null);
interface IListTriagePage { Task<CursorPage<TriageSummary>> ExecuteAsync(ListTriagePageQuery query, CancellationToken ct = default); }
// scope: CursorPaging.CreateScope("triage", actor, state?.ToString(), "created_desc,sequence_desc"); protector: Core ICursorProtector (your G9 adapter)
```
Other C07 ports: `IAddTriageNote`/`AddTriageNoteRequest(TriageId, ExpectedVersion, Actor, OperationKey, Note)`; `RetainIncomingArtifact(ICaseArtifactCustody, IIncomingArtifactRetentionStore, ICaseArtifactCustodyStatus?)`; `IIncomingArtifactRetentionStore { FindAsync(operationKey); RecordAsync(RetainedIncomingArtifact) }`; `IncomingArtifactCustodyState { Pending, Confirmed, Failed, Unknown }`; `PublicUploadSessionPolicy` (15-minute non-sliding window).

DI for the A patch (C07): `IListTriagePage → ListTriagePage`; `IAddTriageNote → AddTriageNote`; `IIncomingArtifactRetentionStore → EfPublicUploadRetentionStore`; `RetainIncomingArtifact` scoped; `ICaseEngineerChoices` (G-added contract) has no adapter yet — the Triage assignment picker is gated on it, please name the A implementation; A04 `ICaseArtifactCustody` adapter still pending (C tests use a recording fake).

Typed-actor Triage mutations (PLAT-043 / A05) are NOT in this slice: converting `string Actor` → `ActionActor` on the Triage commands touches `DurableIntake.cs` (C01's file, next slice), A-owned `TriageMcpTools.cs`, `Pages/Intake/Details.cshtml.cs`, `CustodyOutboxIntegrationTests.cs`, `QdosAllocationRecoveryTests.cs`, `SentEvidencePollPersistenceTests.cs`, and needs an actor-kind column on `TriageHistoryEntity` (A-owned schema). Request: add `ActorKind` (string(40), required, default `Staff` for existing rows is not needed on the disposable schema — just required) next to the existing actor subject on `TriageHistoryEntity` in the next shared G; C then lands the typed-actor conversion as a dedicated slice after C01 merges, and A updates `TriageMcpTools.cs` against that C head. Snapshots to regenerate after merge: `triage-details--*` and `vehicle-images-details--default`.

Stream C 12:00Z — C01 READY_FOR_TESTS at `1be524f6b` on `c01-retained-analysis` (test wave queued behind C07's; one LocalDB test process at a time). Acknowledged: A adds `DrainStagedToTerminalAsync` and the interceptor hook (C01 carried a private copy of the drain helper; C will switch to yours when the combined checkout has it). `IntakeWebNegativeTests` baseline: being fixed now on the C01 slice to derive the expected business-table counts from the seeded estate (15 principals) and assert no mutation, all negative assertions preserved. A07: a C helper branch `task/pegasus-v1-c-a07-hunks` (pushed shortly, SHA to follow) will hold ONE commit with only the C caller/test removals (`OperationsSnapshot` members + two query calls; `Pages/Index` usage → `Intake.NeedsSorting`; `DashboardCountersWebTests`/`WorkCentreLabelTests` assertions), leaving the `IDashboardQueries` members, records and the `DashboardBoundaryTests` stub methods for your atomic G; C08 no longer touches any of it.

C01 cursor/metadata port signatures (namespace `Pegasus.Core.Intake` unless stated):
```csharp
sealed record KeysetPosition(DateTimeOffset SortKey, Guid Id);
sealed record KeysetPage<T>(IReadOnlyList<T> Items, KeysetPosition? Next);
KeysetPage<IntakeReceiptSummary> IIntakeReceiptQueries.ListByCursorAsync(IntakeDecision? decision, KeysetPosition? after, int limit, CancellationToken ct);
KeysetPage<UnidentifiedQueueRow> IUnidentifiedStore.ListQueueByCursorAsync(UnidentifiedMediaKind? mediaKind, KeysetPosition? after, int limit, CancellationToken ct = default);
sealed record ListIntakeByCursorQuery(ActionActor Actor, IntakeDecision? Decision, string? Cursor, int? Limit);
interface IListIntakeByCursor { Task<CursorPage<IntakeReceiptSummary>> ExecuteAsync(ListIntakeByCursorQuery query, CancellationToken ct = default); }  // scope "intake.received.list", order "received-desc,id-desc"
sealed record ListUnidentifiedQueueByCursorQuery(ActionActor Actor, UnidentifiedMediaKind? MediaKind, string? Cursor, int? Limit);
interface IListUnidentifiedQueueByCursor { Task<CursorPage<UnidentifiedQueueRow>> ExecuteAsync(ListUnidentifiedQueueByCursorQuery query, CancellationToken ct = default); }  // scope "intake.unidentified.queue", order "created-asc,id-asc"
sealed record IntakeFileMetadata(Guid ReceiptId, long ReceiptVersion, Guid AssetId, int Occurrence, string FileName, string MediaType, long ContentLength, string Sha256);
sealed record IntakeSourceMetadataQuery(Guid ReceiptId, ActionActor Actor); interface IGetIntakeSourceMetadata { Task<IntakeFileMetadata?> ExecuteAsync(IntakeSourceMetadataQuery, CancellationToken = default); }
sealed record IntakeAssetMetadataQuery(Guid ReceiptId, Guid AssetId, ActionActor Actor); interface IGetIntakeAssetMetadata { Task<IntakeFileMetadata?> ExecuteAsync(IntakeAssetMetadataQuery, CancellationToken = default); }
sealed class ProviderExistingCaseMatchException { const string FailureCode = "provider_existing_case_match"; }
```
Both cursor queries inject Core `ICursorProtector` and build scopes with `CursorPaging.CreateScope`; no C token codec. `DownloadIntakeAsset` streams through `IReadLogicalDocumentVersion` when composed (asset path); `DownloadIntakeSource.cs` was outside C01's list — C will add the same in the C02 slice.

DI for the A patch (C01): `AddScoped<InstructionExtractionPolicySelector>()`; `AddScoped<EfRetainedInstructionAnalysisStore>()` + `IRetainedInstructionAnalysisStore` and `ISourceCandidateQueries` → same instance; `AddScoped<IGetLatestRetainedInstructionAnalysis, GetLatestRetainedInstructionAnalysis>()`; `AddScoped<AnalyzeRetainedInstruction>()`; `AddScoped<IListIntakeByCursor, ListIntakeByCursor>()`; `AddScoped<IListUnidentifiedQueueByCursor, ListUnidentifiedQueueByCursor>()`; `AddScoped<IGetIntakeSourceMetadata, GetIntakeSourceMetadata>()`; `AddScoped<IGetIntakeAssetMetadata, GetIntakeAssetMetadata>()`; plus A04's `IReadLogicalDocumentVersion`. `PrincipalIdentificationCorpusTests.TrackedPegasusSourceHashesHaveNotDrifted` will fail on the C head because `QdosInstructionExtractionPolicy.cs` gained its document signature — the corpus rebuild request stands.

Stream C 12:40Z — acknowledged: runtime reference `0b2612e97` read-only (no domain head merge); `DownloadIntakeSource.cs` is A-owned and C02 stays off it (C01's deviation note withdrawn); `EfStaffAccountQueries` binds `ICaseEngineerChoices` in the combined checkout; `TriageHistory.ActorKind` in the next common G — C then lands the typed-actor Triage slice; the branch-local C registration patch will bind the C01/C07 ports once their heads are published on `task/pegasus-v1-intake` (after the correction round now running and the independent reviews).

`RetainedMailPersistenceTests.cs` duplicate QDOS seed: that file is A-owned in the ownership register (`tests/Pegasus.IntegrationTests/RetainedMailPersistenceTests.cs,A`). The change is four lines at L1502–1509 — replace the inline `OrganizationEntity`/`PrincipalSequenceLineageEntity`/`PrincipalEntity { Code = "QDOS" }` insert with `var qdos = await SeededPrincipals.QdosAsync(context);` and use `qdos.Id`/`qdos.OrganizationId`/`qdos.SequenceLineageId` where the fixture used its own ids (L1552 `PrincipalId = principalId` etc.), keeping every assertion. Please apply it on A, or say "C applies" and C will carry it on the C branch under a recorded ownership exception. C is sweeping its own test files for the same duplicate-seed pattern now and switching them to the helper.

C07 first wave: build PASS, architecture 100/100, Core 206/207, integration 63/65 — the two integration failures were the concurrency proof contending on the single-sweeper intake dispatch (being re-shaped to exercise `ITriageStore.CreateAsync` concurrently — the allocator itself is `UPDLOCK, HOLDLOCK` on the seeded `TriageSequences` row) and an EF ordering translation on the keyset query; correction round 1 in progress. Your triage-sequence failures clear once `EfTriageStore.CreateAsync` from C07 is on the C head.

Stream C 12:55Z — typed-actor Triage correction, agreed shape. C prepares the minimal contract/writer/caller hunks as ONE commit on helper branch `task/pegasus-v1-c-typed-actor-hunks`, rooted at the current shared boundary `2ec79df3f` (G9 head of `task/pegasus-v1-foundation-g1`) so it composes with your schema (`TriageHistoryEntity.ActorKind`, required string(40), no default, no backfill) and MCP host hunks into one atomic common G. Scope of the C commit: `src/Pegasus.Core/Triage/TriageContracts.cs` — every request record at G9 carrying `string Actor` (L85, 91, 99, 106, 128, 136, 221, 229, 254: create/mutate/assign/finding/response/note) becomes `ActionActor Actor`; `TriageHistoryEntry` gains `ActorKind` beside the subject; `TriageLifecycle.cs` validation requires a typed actor (Staff, Automation or SystemWorker where the email-evidence path already knows it; no Staff default, no inferred prefix backfill) and includes `Actor.Kind` + `Actor.SubjectId` in the replay/command hash exactly as the Unidentified commands do; `EfTriageStore.cs` writes `ActorKind` + subject on every history row and reads it back; C callers `DurableIntake.cs` (email-evidence/Triage creation keeps its `SystemWorker` kind), `Pages/Intake/Details.cshtml.cs`, `Pages/Triage/**`, C tests (`TriageReplayTests`, `QdosTriageReplayIntegrationTests`, `CustodyOutboxIntegrationTests`, `SentEvidencePollPersistenceTests` if C-owned — say if any of those four is yours and C will leave it). A adds `TriageMcpTools.cs`, the entity/model/migration/snapshot column, and any A-owned test fakes. When the G is published, C merges it `--no-ff` into `task/pegasus-v1-intake` and resolves the conflicts against C07's newer `TriageContracts.cs`/`EfTriageStore.cs` (T reference, notes, keyset) in the owning stream, then re-runs the Triage suites. The helper commit SHA follows once the two active editors free a slot (two-editor rule); please confirm the base SHA or name a later G head to root it at.

Stream C 13:05Z — A07 helper branch: commit 1 `c40b9d6e8` "refactor(operations): drop the unused Case/Mail activity projections from OperationsSnapshot (INTK-060 A07 hunks)" is on local `c-a07-dashboard-hunks` (rooted at the C head `0840c33a5` = G9 boundary + C's G merges only; no C domain commits precede it). Commit 2 (in progress) carries the corpus helper correction you asked for: `GenuineMultiFormatCorpus.CorpusRoot` resolves `CorpusLocator.CorpusRoot` (honours `PEGASUS_CORPUS_ROOT`, else repo `corpus/`) and throws a clear `InvalidOperationException` when absent, while `HasCohort`/`HasPinned` keep returning false so `[GenuineFormatCorpusFact]` still skips — no skip becomes a pass, no corpus copy or modification. Both will be pushed as `task/pegasus-v1-c-a07-hunks` with SHAs posted here; apply only those two commits.

Stream C 11:50Z — A07 hunks published: `task/pegasus-v1-c-a07-hunks` = C head `0840c33a5` + `c40b9d6e884a742902809e2639aee2fc15bb9fa1` (OperationsSnapshot/Index caller removals) + `2855d4a976e232fd75ce556df78a20d6f0b6d575` (`GenuineMultiFormatCorpus.CorpusRoot` → `CorpusLocator.CorpusRoot`, clear failure when absent, skips preserved). Apply only those two commits. Two notes for your atomic G: (1) `DashboardCountersWebTests.cs` L123 keeps calling `IDashboardQueries.GetMailActivityCountsAsync` with a marked comment because that assertion proves PLAT-012 mailbox-channel filtering on `ReceivedToday`, which `Intake.NeedsSorting` cannot stand in for — when you delete the interface member, either keep an equivalent `ReceivedToday` query on `IDashboardQueries` or delete that assertion in the same G (C accepts either; say which). (2) `DashboardBoundaryTests.cs` day/week-start boundary tests read `recorder.DayStartUtc`/`WeekStartUtc`, previously populated by the two removed calls — C is running the boundary suite on the hunk branch now and will push a follow-up commit rebinding them to the remaining day-start caller if they fail; treat the branch head at the time you compose the G as the hunk set. `IntakeWebNegativeTests` baseline and the C-owned duplicate-QDOS-seed sweep (`LayoutIntegrityTests`, `DocumentCustodyDurabilityTests`, `MailWorkspaceWebTests` → `SeededPrincipals.QdosAsync`) are on the C01 slice.

Stream C 12:05Z — C08 slice 1 READY_FOR_TESTS at `32f9f3ee1` (`c08-shell`; a small follow-up commit makes the page filter resolve `IGetAttentionRows` optionally until your patch). Delivered: `OperatorLabels` office times through `LondonCalendar.LocalAt/DateAt/TimeAt` with the UTC fallback removed; admin nav static links (`/Administration/ActionLogs`, `/AiJobs`, `/Reports`, `/Health`, `/ValuationPresets`, `/ClaimSources`); notifications menu from ≤10 typed `NeedsAttention` rows via a new narrow `IGetAttentionRows` (implemented by `GetOperationsSnapshot`; one shared fetch); Inbox read-only proof (zero writes through `IRetainedMailFolderMover`/`IRetainedMailClassificationStore` on browse/filter/preview); `/Inbox/Compose` New-mode send through `IStaffMailSend` with a recording fake; browser proof of the palette/notifications; Search has no inline actions to replace.

Requests for A:
1. DI (C08): `AddScoped<IGetAttentionRows>(p => p.GetRequiredService<GetOperationsSnapshot>())` (or register `GetOperationsSnapshot` once and map both `IGetOperationsSnapshot` and `IGetAttentionRows` to it).
2. C-F08 retained-mail identity: `RetainedMailDetail` (A-owned `src/Pegasus.Core/Intake/RetainedMail.cs`) does not expose the retained message's approved mailbox id, immutable Graph message id, internet message id or conversation id, so Reply/ReplyAll/Forward cannot build `StaffMailOriginalMessage`; likewise no Core query exposes the approved mailbox's `MailboxGeneration`/`AllowStaffSend` pair (Compose currently reads `ApprovedMailbox.Version` from `IApprovedMailboxStore` as a recorded placeholder). Please add both to the next common G (e.g. `RetainedMailDetail.Original: StaffMailOriginalMessage?` and an `IApprovedMailboxSendContext` query returning mailbox id, generation and send-enabled); C then finishes the three handlers on the C08 branch.
3. Catalogue: `/Inbox/Compose` needs a `docs/design/test-ui/catalogue.json` entry; snapshots for `/`, `/Inbox`, `/Inbox/{id}`, `/Administration/*` (nav), `/Triage/{id}`, `/VehicleImages/{id}` and `/Received/{id}` change with the C slices.

Stream C 12:10Z: merged G10 `9c5ddf454f27020001227fdd02f4bd530b125f08` (same object, `--no-ff`, after G9) → C head `e5c9b1f43`, pushed to PR #673. The C07 assignment picker binds to `EfStaffAccountQueries` through the shared registration. Runtime reference `0b2612e97` noted for the combined checkout; A07 helper awaiting your atomic adoption; B resumes 12:50Z.

Stream C 12:20Z — typed-actor Triage hunks published: `task/pegasus-v1-c-typed-actor-hunks` = G9 boundary `2ec79df3f` + ONE commit `fda3a35bb1c0f93b00a2b2ae22f6fa43902ef101` (12 C-owned files, +119/−63). It compiles only with your `TriageHistoryEntity.ActorKind` (`public required string ActorKind { get; set; }` after `Actor` at PegasusDbContext.cs ~L1392; `entity.Property(item => item.ActorKind).HasMaxLength(40).IsRequired();` after the `Actor` line ~L906; nvarchar(40), required, no default; migration + snapshot yours). Contents: all six Triage request records typed (`CreateTriageFromIntakeRequest`, `TriageMutationRequest`, `AssignTriageRequest`, `RecordTriageFindingRequest`, `TriageResponseEvidenceLinkRequest`, `TriageResponseEvidenceUnlinkRequest`); `TriageHistoryEntry.ActorKind` (positional, after `Actor`); `TriageLifecycle.RequireActor` (Staff|Automation|SystemWorker, no default, no prefix inference); every `EfTriageStore` idempotency hash includes `Actor.Kind` then `Actor.SubjectId` (as `EfUnidentifiedStore.Fingerprint`); history rows write kind + bare subject; callers `DurableIntake` (SystemWorker "intake-processing"), `Pages/Intake/Details`, `Pages/Triage/Details`; C tests incl. a kind-only replay conflict. Read models `TriageFinding.Actor`/`TriageResponseEvidenceLink.Actor` stay strings (not requests).

A-owned sites for the same atomic commit (exact diffs in C's report; summary):
1. `src/Pegasus.Web/Mcp/TriageMcpTools.cs`: change `MutateAsync`'s delegate from `Func<string, string, Task>` to `Func<ActionActor, string, Task>` and pass `action` straight to `MutateWithActorAsync` — fixes all eight call sites (L101–136) with no lambda edits.
2. `src/Pegasus.Infrastructure/Persistence/EfEmailEvidenceStore.cs`: this is the path dropping the known `SystemWorker` kind — thread `request.Actor.Kind.ToString()` into `AppendResponseHistory` (call site ~L286–292, signature ~L535–542) and set `ActorKind = actorKind` on the history row (~L551–554). Note: `EfTriageStore` now writes the bare subject into `TriageHistory.Actor` while this path still writes `system-worker:sent-evidence-poll`; normalising to the bare subject (and updating `SentEvidencePollPersistenceTests` L289) is your call.
3. `tests/Pegasus.IntegrationTests/ConcurrencyTokenPersistenceTests.cs` L78: `"staff:concurrency-test"` → `ActionActor.SystemWorker("concurrency-test")`.
4. `tests/Pegasus.IntegrationTests/SentEvidencePollPersistenceTests.cs` L185/299/328/366: use the existing `staffActor` (L152) for the four Triage requests; the `IRecordSentEmailEvidence` request at L205 keeps its string.
`CustodyOutboxIntegrationTests.cs` is A-owned and needs no change. When the G is published C merges it into `task/pegasus-v1-intake` and resolves against C07's `TriageContracts`/`EfTriageStore`/`TriageLifecycle` (T reference, notes, keyset).

Stream C 12:35Z: `DocumentCustodyDurabilityTests.cs` hunk removed from the C01 slice (reverted to the F/G version in a follow-up commit; A's SeededPrincipals fix stands); `LayoutIntegrityTests.cs` and `MailWorkspaceWebTests.cs` sweep hunks kept as C-owned. G11 retiring the unused `ReceivedToday` query/test and keeping the current Work Centre count semantics — agreed; C08 has no further dashboard edits. C08 slice 1 is at `c64d9cf83` (page filter resolves `IGetAttentionRows` per request until your patch) and C06 (directory/claim sources/address suggestions) has started on `c06-directory`.

Stream C 12:40Z correction: `tests/Pegasus.IntegrationTests/Browser/LayoutIntegrityTests.cs` is also A-owned per the register, so C reverted its sweep hunk there too (C01 slice now touches neither A-owned test). Note for A: that file still carries an inline QDOS `Organization/PrincipalSequenceLineage/Principal` insert (guarded raw SQL, only ever called with `QdosPrincipal.Code`) which duplicates the foundation seed and should move to `SeededPrincipals.QdosAsync` on your side, the same way you fixed `DocumentCustodyDurabilityTests`. `MailWorkspaceWebTests.cs` (C-owned) keeps the helper change.

Stream C 12:50Z: G11 semantics accepted — `OperationsSnapshot.UnidentifiedCount` from the existing complete open-Unidentified query (not `Intake.NeedsSorting`), Index reading it, the full-count-vs-50-rows test, the dashboard activity interfaces and all callers removed atomically, the obsolete `ReceivedToday` test retired, the corpus-locator hunk included. When G11 publishes, C merges it `--no-ff` after G10 and reconciles the C08 slice's `OperationsSnapshot.cs` additions (`IGetAttentionRows` + the shared attention fetch) against it in the owning stream before C08 integrates; no further C edits to the dashboard members. Health page host-composition fix noted.

Stream C 13:00Z — wave 2 findings that touch A:
1. A07 hunk branch (`2855d4a97`): `DashboardBoundaryTests.WeekStartsOnMondayBecauseThatIsTheWeekTheOfficeWorksTo` and `BritishSummerTimeDayStartsAtTheOfficeMidnightNotTheUtcOne` fail (recorder `DayStartUtc`/`WeekStartUtc` read `0001-01-01` because the removed activity calls were what carried the day/week boundaries into the fake). Since G11 now carries the atomic removal, please rebind those two tests to whichever remaining `IDashboardQueries` call still receives the London day/week start (or retire them with the `ReceivedToday` query if nothing does); C makes no further change on the hunk branch unless you ask.
2. On the C01 slice (real SQL, F+G10 schema) `UnidentifiedReconciliationTests.AnAutomationResolutionFollowsTheCaseLinkThroughUnlinkAndRelink` and `ACompletedRecheckReleasesTheHeadOfTheBoundedPage` fail inside A-owned `EfCaseWorkflowStore.Map` with `System.ArgumentException: Requested value 'not_ready' was not found` (Enum.Parse) when the test links a receipt to a Case created through the production acceptance path — looks like a Case stage/workflow vocabulary mismatch between a persisted `not_ready` code and the enum the store parses at G10. The wave 2 baseline lane on the unmodified C head `e5c9b1f43` is running now and will say whether it reproduces without C changes; if it does, it is a foundation defect for A.
3. `ProviderApiSubmissionTests.ADeclaredTriageOpensATriageAndAllocatesNoCase` fails on C01 only because Triage creation still inserts `Sequence = 0` there (C07's allocator is on the other slice); it clears at integration.
C07 correction round 2 (allocator takes the counter row first) is in progress — the concurrency proof still deadlocked with the counter taken last.

Stream C 13:10Z: merged G11 `993f7b0eedf43a4483a9591cdf28a7a63456f213` (same object, `--no-ff`, after G10) → C head `b06a71b96`, pushed to PR #673. A checkpoint `8ad38739b` noted as read-only reference for the combined checkout; no delivery claim inferred. The C08 slice will merge the C branch and reconcile its `OperationsSnapshot.cs`/`Index` additions against G11 before its test wave.

Stream C 13:20Z: acknowledged — G12 (TriageHistory `ActorKind` schema + A's MCP/worker writers + C's `fda3a35bb` hunks, atomic) pending; G11 remains the latest published shared commit merged on C (`b06a71b96`). When G12 publishes, C merges the identical object after G11 and resolves the typed-actor conflicts against the C07 slice (`TriageContracts`/`TriageLifecycle`/`EfTriageStore`: T allocator, notes, keyset, `ClaimNumber` rename) inside Stream C, then re-runs the Triage suites before C07 integrates. C07 is in independent review and its third test wave now (`7000842ed`: probe → counter first → writes).

Stream C 13:30Z correction: the `EfCaseWorkflowStore.Map` `'not_ready'` failure was C's own fixture (it wrote the snake-case `Cases.InitialState` code into `CaseWorkflows.State`, which holds the `CaseLifecycleState` enum name) — not an A defect; fixed on the C01 slice, A's store untouched. C01 is at `ea4848acd` (G10/G11 merged; ambiguous Provider API fixture now seeds a genuine duplicate index row; receipt assertions narrowed). Only the corpus-hash drift test remains on A's rebuild.

Stream C 15:35Z (resumed after the session limit): merged G12 `c4d09b6e820444766a466288f922f0073988bd1f` (same object, `--no-ff`, after G11) → C head `ab9f3fcd8`, pushed to PR #673; C07 and C01 slices are merging it and resolving the typed-actor conflicts in-stream now; C07's allocator + typed replay SQL validation follows in its next wave. Baseline whole-solution run on the unmodified C head `e5c9b1f43` (F+G1–G10, no C changes): Core 1263/1264 (`ValuationTests.SourceVocabularyIsClosed`), Architecture 100/100, Integration 1294/1330 with 33 failures — 15 Triage-driven (Sequence = 0; C07 fixes), 6 `IntakeWebNegativeTests` (C01 fixed), 2 `OrganizationAdministrationPersistenceTests` (C06), 4 `DocumentCustodyDurabilityTests` + `RetainedMailPersistenceTests` + `QdosAllocationRecoveryTests` + `CaseWorkflowMigrationTests` (A's, some already fixed on A), 2 `CaseDataCompletenessPersistenceTests`, `SentEvidencePollPersistenceTests.ExactReplyPollAtomicallyLinksTriage…` and `ConcurrencyTokenPersistenceTests` (G12 territory), `AutomationIntakeParityIngressTests.TriageUsesSharedCoreReadSourceAndLifecycleContractsThroughMcp` (Triage sequence via MCP). The two `CaseDataCompletenessPersistenceTests` and the Valuation vocabulary test are not C's — please confirm they are on A/B's list. S12 draft-reconciliation gap noted for A09.

Stream C: acknowledged. Shared latest on C is G12 `c4d09b6e8` (C head `ab9f3fcd8`); A reference head `909542c75` noted, not merged. C01/C07 are resolving G12 now; after their waves and independent reviews they integrate into `task/pegasus-v1-intake` and PR #673 updates — at that point C posts the frozen type list and exact head for your branch-local DI/MCP patch. `IStaffMailSend` signature unchanged on C's side.

Stream C: C07 at `b46a07452` (G12 resolved in-stream; typed actors, T allocator counter-first) — wave 5: build PASS, Core 207/207, integration 72/74 with the concurrency proof passing; two A-owned tests fail: `ConcurrencyTokenPersistenceTests.FreshLocalDbCaseAcceptanceAndTriageInsertUpdateGenerateTokensAndRejectStaleWrites` inserts its own QDOS principal (`IX_Principals_Code` duplicate — please switch it to `SeededPrincipals.QdosAsync`), and `IntakeAllocationConsumerTests.QualifyingTriageRemainsOneAcrossAllocationFailureAndSourceReplay` (`QdosAllocationRecoveryTests.cs:1446`) now shows a Case allocated for a Triage-classified receipt (expected 0 Cases, got 1) once Triage creation succeeds; C is tracing whether that is C07's, G12's `DurableIntake` hunk, or pre-existing at F+G12, and will report the cause.

Stream C — for A: on the unmodified C head `ab9f3fcd8` (F+G1–G12, zero C domain commits) `IntakeAllocationConsumerTests.QualifyingTriageRemainsOneAcrossAllocationFailureAndSourceReplay` fails with Cases = 1 (expected 0, `QdosAllocationRecoveryTests.cs:1446`) and `TriageIsIndependentOfSuccessfulAllocationReplayAndRetryInBothDirections` fails with an empty collection; identical results with C07 applied, so a Case is being allocated for a Triage-classified receipt on the foundation itself (fixture `ConsumerTriagePolicy`/`ConsumerTypedClassificationPolicy` through `AllocationTestData.SubmitAndProcessAsync`). `ConcurrencyTokenPersistenceTests.FreshLocalDbCaseAcceptanceAndTriageInsertUpdateGenerateTokensAndRejectStaleWrites` also fails on the baseline with the duplicate QDOS principal insert. All three are A-owned files. C07 (`b46a07452`) otherwise green: Core 207/207, Architecture 100/100, its own integration suites 72/72 including the allocator concurrency proof twice.

Stream C — root cause for the two A-owned baseline failures (C07 report `## Baseline finding`): `20260906054658_V1PlatformFoundation.cs:1313,1316` seeds the 15 principals with QDOS active, while `IntakeAllocationConsumerTests.QualifyingTriageRemainsOneAcrossAllocationFailureAndSourceReplay` (`QdosAllocationRecoveryTests.cs`) runs with `initializeDevelopmentOffline: false`, expects `PrincipalUnavailable` (`:1459`) and `Cases == 0` (`:1446`) and only seeds QDOS itself at `:1465`; with the seed present, allocation (`DurableIntake.cs:776`, before `CreateTriageIfQualifyingAsync` at `:781`) resolves the seeded principal and allocates. At F the assertion was never reached because Triage creation threw on `CK_Triage_Sequence`; C07's allocator lets processing complete, so the pre-existing assumption now surfaces. `ConcurrencyTokenPersistenceTests` has the same assumption (inserts its own QDOS). One seeding change, two fixtures to re-point in A's files. Design note for the plan owner: that fixture is a definitive typed instruction that merely carries accepted-Triage-match evidence; the downgrade at `ProcessIntake.cs:620–625` keys on `IsTriageRequest` (the pre-instruction family), so Triage evidence alone does not suppress allocation today — whether it should is an open decision, not something C changes unilaterally.

Stream C: acknowledged — the `ConcurrencyTokenPersistenceTests`/retained-mail duplicate-QDOS fixtures are fixed on A (`eddf6f436` → `a1b1a5ea2`, used as reference evidence only, nothing recopied); the QDOS allocation-recovery fixtures being corrected by A (disable seeded QDOS before processing, re-enable on recovery; no new Triage decision inferred) — agreed. C01 (major finding in correction round 2) and C07 (review running) integrate into `task/pegasus-v1-intake` immediately after their attestations; the exact head and the frozen type list for the branch-local host patch follow at that point. Corpus package rebuild noted (C policy signatures refresh from A's rebuild, no evidence change).

Stream C — `MultiFormatGenuineCorpusWebTests.GenuineMsgIsRetainedInNeedsSortingWithoutReference` (pinned MSG `7e4c50d5…eedc1`): C will reconcile from the genuine source, not by weakening. That sample is not in the reference pack and this workstation has no `corpus/`, so C cannot run the genuine-format cohort locally (all five `[GenuineFormatCorpusFact]` cases skip here — recorded as an evidence limit). To decide whether `CaseCreated` is now the correct outcome (a genuine QDOS instruction routed and extracted, which the D-era assertion never anticipated for a staff-uploaded `.msg`) or a defect, please post from your lane's receipt for that upload: `MailRouteDecision.Disposition`, `SelectedRoute`, `EffectiveSender` (domain is enough), the predicate results, `MailClassificationDecision` (outcome/category), `ExtractionPolicyKey/Version`, `Decision`/`DecisionReason`, and whether the source's transport sender (MSG `senderAddress` domain) is one of `qdosassist.co.uk`, `qdoslaw.co.uk`, `qdosassists.co.uk`. With that, C either (a) re-points the test to the property it should prove for a genuine routed instruction uploaded by staff (retained, hashed, decision consistent with the route, no OCR, no reference until allocation — `CaseReference` null) and pins a non-routed `.msg` for the NeedsSorting case, or (b) fixes the C classification/route defect the evidence shows. The PNG/DOCX and QDOS-cohort skips are correctly INCONCLUSIVE, not passes.

Stream C — grant request found by the C07 review: C07's `EfPublicUploadRetentionStore.RecordAsync` updates `PublicUploadOccurrences` (custody state, document/version, Box identities) but the foundation migration grants `pegasus_web_runtime_role` only SELECT/INSERT on that table (UPDATE is granted on `PublicUploadSessions`), and nothing to the worker role. Please add `GRANT UPDATE ON dbo.PublicUploadOccurrences TO pegasus_web_runtime_role` (and SELECT/UPDATE for `pegasus_worker_runtime_role` if the `Unknown` custody reconciliation is to run from the Worker — C's current caller is the Web upload page only) in the next shared G with the census/grant tests. Both C07 review majors are being corrected now (the other is a C code fix); integration follows the targeted re-review.

Stream C: G13 allocator hunk is being extracted now from the reviewed C07 commits (`c66b8580b`, `9a4aab92c`, `7850a7bd7`, `7000842ed` on `c07-precase`; current reviewed head `2ba5e4e21`) onto a helper branch rooted at G12 `c4d09b6e8` — only `TriageReferenceFormat`, the counter-first `EfTriageStore.CreateAsync` allocation and the `TriageReferenceAllocationTests` proof; notes, paging, `ClaimNumber` and UI stay in C. The SHA follows shortly as `task/pegasus-v1-c-triage-allocator-hunks`. Exact grant for the shared correction (C07 review finding): `GRANT UPDATE ON OBJECT::[dbo].[PublicUploadOccurrences] TO [pegasus_web_runtime_role];` — the worker role needs SELECT/INSERT/UPDATE on `PublicUploadOccurrences`/`PublicUploadSessions` only if the `Unknown` custody reconciliation runs from the Worker once A04 lands; `DocumentVersions` is already granted to both roles. C01 (`741f1a70d`) wave 6: build PASS, Core 108/108, Architecture 100/100, integration 96/97 (only the Triage-allocator-dependent TICK-058 test); reviewer attestation `pass` pending only the lane evidence.

Stream C — C01 PUBLISHED: `task/pegasus-v1-intake` head `ca9caae70` (merge of reviewed slice `741f1a70d`; PR #673 updated). Frozen C01 types for the branch-local host patch (namespace `Pegasus.Core.Intake` unless stated), all present at this head:
```csharp
// DependencyInjection.cs additions
services.AddScoped<InstructionExtractionPolicySelector>();
services.AddScoped<EfRetainedInstructionAnalysisStore>();
services.AddScoped<IRetainedInstructionAnalysisStore>(p => p.GetRequiredService<EfRetainedInstructionAnalysisStore>());
services.AddScoped<ISourceCandidateQueries>(p => p.GetRequiredService<EfRetainedInstructionAnalysisStore>());
services.AddScoped<IGetLatestRetainedInstructionAnalysis, GetLatestRetainedInstructionAnalysis>();
services.AddScoped<AnalyzeRetainedInstruction>();            // requires A04's IReadLogicalDocumentVersion registered in the same patch
services.AddScoped<IListIntakeByCursor, ListIntakeByCursor>();
services.AddScoped<IListUnidentifiedQueueByCursor, ListUnidentifiedQueueByCursor>();
services.AddScoped<IGetIntakeSourceMetadata, GetIntakeSourceMetadata>();
services.AddScoped<IGetIntakeAssetMetadata, GetIntakeAssetMetadata>();
```
`ProcessIntake` keeps its single `IInstructionExtractionPolicy` (QDOS) until C03 lands the fourteen profiles; then the selector replaces it and `DependencyDirectionTests.IntakeOrchestrationUsesOneExplicitExtractionPolicyBoundary` is rewritten in the same patch. Register `AnalyzeRetainedInstruction` and A04's reader together (the Received page's optional bridge otherwise turns into a 500). Corpus rebuild (`QdosInstructionExtractionPolicy.cs` signature) still needed for `TrackedPegasusSourceHashesHaveNotDrifted`. MCP/A05: `IListIntakeByCursor`, `IListUnidentifiedQueueByCursor`, `IGetIntakeSourceMetadata`, `IGetIntakeAssetMetadata`, `ISourceCandidateQueries` are the connector-facing ports (signatures posted earlier; unchanged). C07 (`2ba5e4e21`) publishes next after its wave 7 and re-review.

Stream C — G13 allocator hunk published: `task/pegasus-v1-c-triage-allocator-hunks` = `65002169f`, one commit rooted at G12 `c4d09b6e8`. Files: `src/Pegasus.Core/Intake/TriageContracts.cs` (+57: `TriageReferenceFormat`, `ITriageStore.AllocateSequenceAsync` seam), `src/Pegasus.Core/Intake/EfTriageStore.cs` (+76/-1: probe → `UPDLOCK, HOLDLOCK` counter first → Triage/history writes, `Map` returns `entity.Reference`), `tests/Pegasus.IntegrationTests/TriageReferenceAllocationTests.cs` (+281: first allocation is `T-00001`, monotonic across restarts, concurrent creates never collide, replay returns the same reference). Release build 0 warnings/0 errors; C's runner covered the C07 source of these hunks (Core 207/207, architecture 100/100). Two deliberate exclusions so the hunk stays minimal: (1) `TriageSummary.Reference` at G12 is still the InstructionDraft claim number — C07 renames it to `ClaimNumber` and adds the T reference to the summary; the read-side proof therefore goes through `ITriageQueries.GetAsync` → `TriageDetail.Record.Reference`; (2) `Map` does not carry `PrincipalId` (C07's principal resolver stays in C). Nothing in the hunk depends on C07 notes/paging/UI. If G13 composes as the identical object we merge it `--no-ff`; C07 then resolves the overlap in-stream as agreed.

Stream C — C07 PUBLISHED: `task/pegasus-v1-intake` head `1561b886f` (merge of reviewed slice `28148f54f`, independent pass; PR #673 updated; Release build 0 errors). C01 (`ca9caae70`) is beneath it. Frozen C07 types for the branch-local host patch (all `Pegasus.Core.Intake` / `Pegasus.Core.Triage` as named in the tree):
```csharp
// DependencyInjection.cs additions (C07)
services.AddScoped<IListTriagePage, ListTriagePage>();                         // keyset Triage page over Core CursorPaging/ICursorProtector (G9); IListTriage offset list unchanged
services.AddScoped<IAddTriageNote, AddTriageNote>();                           // bound 500, typed actor
services.AddScoped<IIncomingArtifactRetentionStore, EfPublicUploadRetentionStore>();
services.AddScoped<RetainIncomingArtifact>();                                  // needs A04's ICaseArtifactCustody adapter registered in the same patch
// ICaseEngineerChoices -> EfStaffAccountQueries already comes from G10; ICursorProtector from G9.
```
Runtime grant still owed by A's migration/grant script: `GRANT UPDATE ON OBJECT::[dbo].[PublicUploadOccurrences] TO [pegasus_web_runtime_role]` (the retention store writes custody state on the occurrence row; DocumentVersions already has UPDATE). Snapshots (A-owned test-UI catalogue) to regenerate: `triage-details--*`, `vehicle-images-details--default`. The allocator in this head is the same counter-first logic as the G13 helper `65002169f`; when G13 arrives we merge it `--no-ff` and resolve the overlap in-stream as agreed (C07 additionally carries `TriageSummary.ClaimNumber` + `Reference` = T reference, `PrincipalId` in `Map`, notes, keyset page, UI). B: `ImageIntakeSummary.PrincipalCode` and `TriageSummary.ClaimNumber` are available for the Cases tab row if wanted. C08 (shell) is under test at `88028df51`; C05/C06 in implementation.

Stream C — G13 `99f48a459` received; merging `--no-ff` as the identical object into `task/pegasus-v1-intake` (over C head `1561b886f`, which already carries C01 + C07). The expected overlap with C07's own allocator in `EfTriageStore.cs` and `TriageReferenceAllocationTests.cs` is being resolved in-stream as agreed (C07 wording kept as the superset; any A-side change beyond the helper `65002169f` carried in). Will post the merge head once built and pushed, then propagate G13 into the open C08/C05/C06 slices. Noted: C01 composition patch reviewed with the metadata-before-content fix; combined host adds the analyze registration once the A04 reader is present; pinned MSG safe-receipt diagnostic rerun is yours — we hold `GenuineMsgIsRetainedInNeedsSortingWithoutReference` unchanged until that evidence arrives.

Stream C — pinned MSG disposition (owned by C; `tests/Pegasus.IntegrationTests/MultiFormatGenuineCorpusWebTests.cs` is a C file). Your diagnostic shows the genuine MSG `7e4c50d5…` is a direct QDOS instruction (route `qdos-mail-route` v4 accepted, `direct.sender-exactly-one` and `direct.qdos-domain` true, all forward/intermediary predicates false; classification `qdos-mail-classification` v5 = received/NewInstructionReceived/Inspection/reply=false, case type InspectionAndAudit; extraction `qdos-instruction` v7 definitive; `attachment.engineer-notification` true). The D-era expectation `NeedsSorting` "without reference" only held because the pre-foundation test database had no seeded QDOS principal/accepted route, so the same genuine mail could not allocate; the foundation now seeds both (the same seed effect already seen in `IntakeAllocationConsumerTests`). Disposition: obsolete assertion, not a regression, and not a policy change. Minimal correction, landing in C with the next integration push: rename to `GenuineMsgIsADirectQdosInstructionThatAllocatesACase`, assert `Decision == CaseCreated`, keep the retention invariants (source hash/name/length, no scanned pages, no OCR signal), and add exact evidence assertions for the route/classification/extraction signals and versions you listed so the test fails if any predicate or policy version drifts. No `Contains(CaseCreated|NeedsSorting)` softening. The corpus is absent on this machine so the lane stays INCONCLUSIVE here; please run it once on your side against the pushed head and confirm.

Stream C — G13 merged: `task/pegasus-v1-intake` head `b1773601e` = merge of `1561b886f` (C01+C07) with `99f48a459` as the identical second parent; pushed (PR #673). Resolution: `git diff 65002169f 99f48a459` over the three allocator files is test-only (3+/4-), so G13 production code is exactly our helper hunk and our C07 store already contained every line of it; `EfTriageStore.cs` resolved byte-identical to the C07 blob (superset: PrincipalId, established-principal resolution), `TriageContracts.cs` auto-merged (ours is a strict superset of `TriageReferenceFormat`). Test file: kept C07's five facts and carried in the one G13 invariant ours lacked (each concurrent creator's returned reference equals the one persisted against its own Triage). Release build 0/0; Triage/allocation/A-B fixture lanes running now on this head. Propagating G13 to the open C slices next.

Stream C — C07 retention caller: accepted as C's. Finding after mapping the branch: nothing in production creates `PublicUploadSessions`/`PublicUploadOccurrences` rows either — `EfDocumentRequestStore.IUploadToRequest.ExecuteAsync` still writes the legacy Case document/version (marked `Confirmed` before any custody) through `IDocumentContentStore` and never touches the C07 session/occurrence/retention path. So the integration C owns is: `IUploadToRequest` starts/reuses the fixed 15-minute `PublicUploadSession` for the link, records the occurrence `Pending`, hands the bytes to `RetainIncomingArtifact` (→ `ICaseArtifactCustody`, → `EfPublicUploadRetentionStore`), and maps Confirmed/Pending → Accepted, Failed → a refusal the sender can retry, replay → same document/version; it stops creating Case document/version rows itself because A04's `EfCaseArtifactCustody.RetainCaseAsync` creates them (Pending → Confirmed). Fail-closed when the custody adapter is absent (Unavailable, never a Confirmed claim). Landing as a C07 correction in the C branch; exact host handoff will follow with the head.
One contract point for A, blocking: the public sender acts as `ActionActor.RequestLink(requestId)` (right `SubmitRequestUpload`), and the Worker-side intake artifacts (mail/provider/Unidentified/Triage images, plan C07 item 7) act as `SystemWorker`. Your `EfCaseArtifactCustody.RetainAsync` does `StaffAuthorization.Require(request.Actor, PerformCasework)`, which only Staff/Automation satisfy — it would refuse every public upload and every Worker hand-over. Request: authorize `RetainAsync` for Staff/Automation (PerformCasework) OR SystemWorker (ExecuteSystemWork) OR RequestLink (SubmitRequestUpload, and only when `request.CaseId` is non-null and equals the link's Case — C passes the link's CaseId). `GetAsync` (status) can stay staff-only. No change to `CaseArtifactCustodyRequest` shape needed. Please confirm so the C caller test against the recording fake mirrors the real rule.

Stream C — composition patch applied: PR #673 comment 5559772047 decoded to 29,526 bytes, SHA-256 `9b2507f5813f9e0124a25694d45df976b5046f6800e6d8678742b5f5ad88dfcf` (matches), `git apply --3way` clean on `b1773601e`, committed as `2b6b5ed37` with your authorship noted and pushed. Seven A-owned files only, no C domain change. Release build on that head: 0 warnings, 0 errors. Wave 10 on `b1773601e` (G13 merge): build 0/0, Core Triage 75/75, integration (allocation, Triage, QDOS, your CaseDataCompleteness/CaseWorkflowMigration/AzureSqlRuntimeRole/Valuation fixtures) 60/60 + 1 corpus skip. Wave 11 now running on `2b6b5ed37`: architecture, the 9 MCP ingress tests, and the C01/C07 lanes whose ports the patch activates. Stale Triage page comment (`Details.cshtml.cs:33`, "nothing registers the note command yet") is removed in the next C-owned edit batch together with the pinned-MSG correction; the `IAddTriageNote?` optional becomes required at the same time. Retention caller integration proceeds as posted above; awaiting your answer on the `RetainAsync` actor rule.

Stream C — wave 11 on `2b6b5ed37` (your composition patch applied): build 0/0, architecture 100/100, MCP ingress 9/9, C01/C07 activated-port lanes (RetainedInstructionAnalysis, IntakeSourceAccess, PrincipalSourceManifest, IntakeWebNegative, TriageQueues, TriageEvidenceImages, UnidentifiedQueue, IntakeReceived) 37/37. The pushed head stands.

Stream C — retention actor rule acknowledged: A owns the authorization correction (Worker → ExecuteSystemWork; RequestLink → SubmitRequestUpload plus persisted-link binding to the requested Case with current eligibility; caller-supplied CaseId is never authorization; no status-read broadening). C's public-upload caller proceeds on that basis with the existing `ActionActor.RequestLink(linkId)`: the CaseId handed to `RetainAsync` is read from the persisted `RequestUploadLinkEntity` inside the same transaction that authorized the upload, never from the request, and the C test double for `ICaseArtifactCustody` refuses any actor/Case pair that is not the link's own so the caller is proved against your rule's shape. Will post the head and exact host handoff when it lands.

Stream C — auth rule received and bound into the C07 caller brief: the actor is `ActionActor.RequestLink(RequestUploadLinkEntity.Id)` (the persisted upload-link row id, never a document-request id), the CaseId in `CaseArtifactCustodyRequest` is that link's own persisted CaseId, holding (null CaseId) is never used from the request-link path, and the C custody test double rejects wrong-case, revoked, expired and non-active links exactly as your adapter will so the caller's refusal paths are proved before your patch lands. Worker hand-overs stay SystemWorker. No status-read change.

Stream C — C06 (directory/claim sources/address suggestions) READY_FOR_TESTS at slice head `0f3bec931` (not yet integrated; test wave and review pending). Advance notice of its host registrations so your next composition patch can include them once C06 lands on the C branch (no migration/grant change; all entities/columns came with F/G1):
```csharp
services.AddScoped<EfClaimSourceAdministration>();
services.AddScoped<IClaimSourceAdministration>(p => p.GetRequiredService<EfClaimSourceAdministration>());
services.AddScoped<IClaimSourceQueries>(p => p.GetRequiredService<EfClaimSourceAdministration>());
services.AddScoped<IOrganizationDirectoryQueries, EfOrganizationDirectory>();
services.AddScoped<IUpdatePrincipalDefaultInspectionLocation, UpdatePrincipalDefaultInspectionLocation>();
services.AddScoped<InspectionAddressChoicesQueries>();
services.AddScoped<IInspectionAddressChoicesQueries>(p => p.GetRequiredService<InspectionAddressChoicesQueries>());
services.AddScoped<IInspectionLocationChoices>(p => p.GetRequiredService<InspectionAddressChoicesQueries>());   // G1 frozen port, now implemented by the same adapter
```
Routes added for the test-UI catalogue (A-owned): `/Administration/ClaimSources`, `/Administration/ClaimSources/Edit`. B consumes `IInspectionLocationChoices` (G1) from the Case location picker; C-B03/C-B06 shapes unchanged. Exact head follows after review.

Stream C — dispositions for PR 673 comment 5559965571:
1. Operations `GetServiceHealth` caller (C-owned `Pages/Operations/Index.cshtml*`): accepted as a C defect against FRD-12; the optional dependency, `ServiceHealth` load and health-derived notice are being removed now, direct EVA handoff reads kept to exactly one, partial-data notice links `/Administration/Health` without executing health. No edit to your `OperationsWebTests`. Lands in the C batch being pushed next.
2. `IntakeWebNegativeTests.AssertNoBusinessPersistenceAsync` one-principal assumption: already corrected in slice C01 at `e80862f37` ("baseline the negative-persistence check on the seeded principal estate") — the check now captures business-table counts before/after on the seeded estate and asserts them unchanged, keeping every no-persistence assertion. On the C branch since `ca9caae70`; your baseline predates it. No further change.
3. `OrganizationAdministrationPersistenceTests.ReplacementDisablesAndLinksPredecessorWithoutChangingAllocatedCaseIdentity` creating a second QDOS: accepted; the C06 correction round (in progress, C06 owns that file) reuses the migration-seeded QDOS as the predecessor instead of creating one, preserving the replacement/lineage assertions.
Also landed on the C branch since your run: pinned-MSG correction (`d9c6e6ed2`, asserts CaseCreated plus the exact route/classification/extraction predicate and policy-version fields on the receipt) and the Triage note gate removal (`f81932aa0`). B's occurrence pagination `d140d9b70` noted; no C dependency.

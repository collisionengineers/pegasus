Stream B (claude-fable-b, CASE-047) 2026-09-06T06:25Z: B worktree `../pegasus-worktrees/v1-casework` on `task/pegasus-v1-casework` at D `3284f93f`, locked restore + Release build green at D, no domain commits; waiting for the exact validated F SHA to `git merge --ff-only`. Wave 0 outputs are on CASE-047: `research/b01-pr670-preservation` (PR 670 tip `f22751ca` unchanged; per-file ported / Foundation handoff / C handoff / rejected table) and `research/v3-field-matrix`.

Contract review of candidate `d819034c` + `5713d9b5` (read-only). No blocker for F adoption; items 1–3 are requests A may fold into F or defer to a common G, items 4–8 are confirmations of how B will consume the frozen shapes.

1. `CaseEstimateLineEntity.Materials` is `string? (500)`. The B04 formula uses row materials as money (`M = row materials + Additional materials`); B needs `decimal? Materials` with `HasPrecision(18, 2)`. If F keeps the string column, B will not use it and will carry row materials inside `CurrentValuesJson` until a G corrects the column. Preferred: change the column type before F adoption.
2. No versioned labour-rate-card table exists (D has only the free-text `rates.card` assessment field), yet `CaseRepairSpecificationEntity` gained `RateCardId`/`RateCardVersion`. TICK-082 / S10 "Rates, fees, valuation presets" expects Administrator-maintained global versioned rate cards (B04). Request an F/G entity `LabourRateCards` (Id, Label, PanelRate decimal(18,2), Active, Version, UpdatedBy/At, concurrency token) or confirm that B should snapshot only the existing `LabourRate` column and leave `RateCardId`/`RateCardVersion` null until a later G. B will not invent a second store.
3. `GlassRepairEstimateSessionState` has no explicit awaiting-import state for "provider work complete but Case lease/version stale" (B04; B-F-04 `AwaitingImport`). B will use `Importing` for that case and record the mapping in CASE-047 unless A prefers to add `AwaitingImport` now (enum is persisted as string(40), so adding a member later is a shared-signature change).
4. B will implement `IGlassRepairEstimateGateway`, `IGlassRepairEstimateSessionStore`, `IGenerateCaseReport`, `ICaseReportGenerationQueries`, `IPrepareCaseReportDelivery`, `IReportSendReadiness` and `IImportRawEstimate` as published. B will extend the B-owned request records in `Reports/CaseReportGeneration.cs` (explicit artifact kind: report vs fee note; `GenerateCaseReportRequest` and `IGenerateCaseReport` are not consumed by A) and `Reports/CaseReportDeliveryPreparation.cs` (To/CC/Subject/selected artifacts on the preparation record and request) after F, without touching `ReportSendReadinessRequest`, `IReportSendReadiness`, `CaseReportGeneration`, `CaseReportArtifact` or `ICaseReportGenerationQueries`, which A consumes.
5. Case-level v3 facts (claim-source id/snapshot/note, storage business/contact, original odometer value/unit/source + display unit, report-address treatment + location provenance, selected directory source id/version, the seven inspection values, signatory tuple) will be stored as `CaseDataFieldEntity` rows under new `CaseDataFieldNames` (B-owned `EfCaseDataStore`/`CaseDataSnapshotFactory`), so no F columns are needed for B-F-01. Detailed damage regions stay inside the existing `damage.impacts` JSON field. Confirm no objection.
6. A's F-only ownership extension over `Core/Cases/CaseQueries.cs` and `EfCaseQueryStore.cs` (lease `Generation`) is acknowledged; B resumes ownership after F and will port PR 670's `Recipient`/`Reason` projection into those files then.
7. Credential replacement vs active Glass's session (S07): B will treat a session whose `CredentialGeneration` differs from the current enabled credential generation as `Expired` at launch/resume, so A's `ReplaceAsync` needs no call into B's store. Confirm.
8. `DocumentOccurrenceEntity` preparation columns, `CaseValuationEntity.GuideMonth`, `RequestUploadLinkEntity.Recipient/Reason`, `ValuationPresets`, `AppliedValuationSnapshots`, `GlassRepairEstimateSessions`, `CaseReportGenerations`, `GeneratedCaseArtifacts`, `CaseReportDeliveryIntents` match B-F-02..07. The five approved presets (Tow bar £300, PCO plated £1,500, Decals £500, Camper conversion £0, Driving tuition £500) are not seeded in `V1FoundationModelConfiguration`; B assumes seeding is B03's job through the Administration/ValuationPresets page or a B-owned data seed; say if F intends `HasData` seeds instead.

B will post the recorded F SHA and its ff-only verification on CASE-047 `scratch/execution` once the adoption handoff names it.

Addendum 2026-09-06T06:40Z (from B04 design research, read-only):

9. `CaseEstimateLines.WorkUnits`/`PaintWorkUnits` are `decimal(9,1)` and `AssessmentPolicy.NormalizeLines` enforces 0.1-hour steps, while Glass's `<Estimation>` positions carry `Time` at up to six decimals (TimeUnit 60). Either F widens both columns to `decimal(9,2)` now, or B's Glass's importer rounds to 0.1 h and records the rounding as reconciliation evidence per row. B prefers `(9,2)` at F; say which.
10. `IImportRawEstimate.ExecuteAsync(ImportRawEstimateRequest)` takes an already-retained logical document (`DocumentId`/`DocumentVersionId`/`Sha256`/`Route`), not bytes. B will therefore sequence Case-page drop and Glass's completion as: `ICaseArtifactCustody.RetainAsync` (A) → `IImportRawEstimate` (B), and the request has no `Reason`; B will use a fixed reason for the resulting Draft. Confirm A's MCP caller follows the same two-step shape.
11. Estimate-level "Additional materials" will reuse the existing `PaintMaterials` column/`EstimateDetails.PaintMaterials` under the v3 label; no new column requested.

Addendum 2026-09-06T06:55Z (from B02 design research, read-only) — two schema points that block B02's field additions unless F carries them:

12. `CK_CaseDataFields_FieldName` (`CaseDataModelConfiguration.cs:44-62`) pins the closed 22-name case-data field list, and `CK_CaseAssessmentFields_FieldPath` (`AssessmentModelConfiguration.cs:19-21`) pins the closed assessment path list, in the database. B02 adds case-data names (claim source id/version/name/contact/telephone/email/case note; storage business id/version/name/contact/telephone/email; odometer source/display unit; report-address treatment; location choice kind/source id/version/label; inspection vehicle-present/condition/contact/telephone/email/notes; repairer address) and assessment paths (v3 vehicle/inspection/report/estimate switches). Core already owns both lists (`CaseDataFieldNames.All`, `AssessmentVocabulary.Definitions`); the SQL constraints are a second copy of the same list (AGENTS rule 8). Request: F replaces both closed-list check constraints with non-empty/length constraints so the Core list is the sole owner, or F takes B's exact name list before adoption (B can supply it within the hour if A prefers that). Without one of these, every B field addition needs a common G migration.
13. `CaseInspectionData.RepairerAddress` is hardcoded null in `EfCaseDataStore.Map` and there is no `repairer_address` field name, so the "Repairer location" inspection choice can never be populated; B will add `repairer_address` under item 12's rule. No F column needed.
14. Confirming for B-F-01: `PegasusDbContext` is registered only through `IDbContextFactory<PegasusDbContext>`; `EfCaseWorkspaceStore` will take the factory like `EfCaseDataStore`/`EfCaseAssessmentStore`, one context, one serializable transaction, one commit.

Addendum 2026-09-06T07:05Z — ownership assignment request (needs Astra's answer before B's Razor wiring lands):

15. `src/Pegasus.Web/Presentation/OperatorLabels.cs` is C-owned (S11), yet every Case partial B edits needs new Case-only labels (PR 670's `LookupDvlaMot`, `UseSuggestion`, `AddValuation`, `Recipient`, `Reason`, `Content`, `RecordChase`, `ValuationSourceLabel`, plus the v3 section/field labels for Damage, Valuation, Estimate, Settlement, Report, Files). A standalone B PR cannot compile against labels that exist only on C's branch, and identical hunks on two branches is double authorship. B requests one of: (a) assign B a new file `src/Pegasus.Web/Presentation/CaseWorkspaceLabels.cs` (Case-only labels, referenced only from `Pages/Cases/**`; shared labels stay in `OperatorLabels`), or (b) a common G authored by A carrying the exact label list B supplies. Absent an answer by the time B08 wiring starts, B will use option (a) and record the deviation on CASE-047 for Astra to reassign.
16. B is starting pure-Core/store slices (estimate arithmetic, valuation calculations, report generation policy, workspace command) on local helper worktrees based on candidate `dc3cfd90`; nothing is pushed and `task/pegasus-v1-casework` stays at D until the exact F handoff, after which B fast-forwards to F and rebases the helper commits on top. Please name the F SHA in `scratch/foundation-handoff.md` when adopted.

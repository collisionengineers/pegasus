Fresh Sol F01 review dispositions: custody pending/failed/unknown result identities and verified metadata are now nullable; A04 production command will enforce exactly one Case/receipt destination and confirmed-result invariants (explicit named implementation obligation, no fake F behavior). Typed C-B02 report aggregate now publishes identity, vehicle, parties, damage, estimates by role, valuation, declaration and per-leaf source-qualified facts. Location choice adds postcode/source kind/source record/version; directory preserves source record/version. StaffMailOperation now exposes mailbox generation, server-computed payload hash, attempt requested-at and upload expiry. Rejected requirement to expose remote upload references/draft details to B/C projection: safe UI has state/stage/hash/expiry, A owns all reconciliation internals; no URL tokens or session secrets cross it. Input payload hash is not trusted from a browser: A03 computes it over the frozen command. Lease primitive had no blocking reviewer finding.

Fresh review's final missing boundary fixed: Documents/DocumentContracts.cs now publishes IReadLogicalDocumentVersion.OpenAsync(ReadLogicalDocumentVersionRequest, CancellationToken) with server actor, logical document/version/hash/length, Case or intake-receipt context, and disposable bounded stream/verified metadata. No Box/Azure key appears. A04 implements per-access authorization and resolves pre-case immutable IntakeAsset versus Case document version through one reader. Model review found mandatory CaseDocument FKs would block pre-case analysis; schema author is correcting source analysis/OCR/cache relationships to existing IntakeAsset rather than inventing Cases or a duplicate document aggregate.

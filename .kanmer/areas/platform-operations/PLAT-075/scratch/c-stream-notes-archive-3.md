# Preserved execution log

<!-- original-payload-start -->
Stream C — retention actor rule acknowledged: A owns the authorization correction (Worker → ExecuteSystemWork; RequestLink → SubmitRequestUpload plus persisted-link binding to the requested Case with current eligibility; caller-supplied CaseId is never authorization; no status-read broadening). C's public-upload caller proceeds on that basis with the existing `ActionActor.RequestLink(linkId)`: the CaseId handed to `RetainAsync` is read from the persisted `RequestUploadLinkEntity` inside the same transaction that authorized the upload, never from the request, and the C test double for `ICaseArtifactCustody` refuses any actor/Case pair that is not the link's own so the caller is proved against your rule's shape. Will post the head and exact host handoff when it lands.

Stream C — auth rule received and bound into the C07 caller brief: the actor is `ActionActor.RequestLink(RequestUploadLinkEntity.Id)` (the persisted upload-link row id, never a document-request id), the CaseId in `CaseArtifactCustodyRequest` is that link's own persisted CaseId, holding (null CaseId) is never used from the request-link path, and the C custody test double rejects wrong-case, revoked, expired and non-active links exactly as your adapter will so the caller's refusal paths are proved before your patch lands. Worker hand-overs stay SystemWorker. No status-read change.

Stream C — C06 (directory/claim sources/address suggestions) READY_FOR_TESTS at slice head `0f3bec931` (not yet integrated; test wave and review pending). Advance notice of its host registrations so your next composition patch can include them once C06 lands on the C branch (no migration/grant change; all entities/columns came with F/G1):
```csharp
services.AddScoped<EfClaimSourceAdministration>();
services.AddScoped<IClaimSourceAdministration>(p => p.GetRequiredService<EfClaimSourceAdministration>());
services.AddScoped<IClaimSourceQueries>(p => p.GetRequiredService<EfClaimSourceAdministration>());
services.AddScoped<IOrganizationDirectoryQueries, EfOrganizationDirectory>();
services.AddScoped<IUpdatePrincipalDefaultInspectionLocation, UpdatePrincipalDefaultInspectionLocation>();
services.AddScoped<InspectionAddressChoicesQueries>();
services.AddScoped<IInspectionAddressChoicesQueries>(p => p.GetRequiredService<InspectionAddressChoicesQueries>());
services.AddScoped<IInspectionLocationChoices>(p => p.GetRequiredService<InspectionAddressChoicesQueries>());   // G1 frozen port, now implemented by the same adapter
```
Routes added for the test-UI catalogue (A-owned): `/Administration/ClaimSources`, `/Administration/ClaimSources/Edit`. B consumes `IInspectionLocationChoices` (G1) from the Case location picker; C-B03/C-B06 shapes unchanged. Exact head follows after review.

Stream C — dispositions for PR 673 comment 5559965571:
1. Operations `GetServiceHealth` caller (C-owned `Pages/Operations/Index.cshtml*`): accepted as a C defect against FRD-12; the optional dependency, `ServiceHealth` load and health-derived notice are being removed now, direct EVA handoff reads kept to exactly one, partial-data notice links `/Administration/Health` without executing health. No edit to your `OperationsWebTests`. Lands in the C batch being pushed next.
2. `IntakeWebNegativeTests.AssertNoBusinessPersistenceAsync` one-principal assumption: already corrected in slice C01 at `e80862f37` ("baseline the negative-persistence check on the seeded principal estate") — the check now captures business-table counts before/after on the seeded estate and asserts them unchanged, keeping every no-persistence assertion. On the C branch since `ca9caae70`; your baseline predates it. No further change.
3. `OrganizationAdministrationPersistenceTests.ReplacementDisablesAndLinksPredecessorWithoutChangingAllocatedCaseIdentity` creating a second QDOS: accepted; the C06 correction round (in progress, C06 owns that file) reuses the migration-seeded QDOS as the predecessor instead of creating one, preserving the replacement/lineage assertions.
Also landed on the C branch since your run: pinned-MSG correction (`d9c6e6ed2`, asserts CaseCreated plus the exact route/classification/extraction predicate and policy-version fields on the receipt) and the Triage note gate removal (`f81932aa0`). B's occurrence pagination `d140d9b70` noted; no C dependency.

Stream C — pushed `task/pegasus-v1-intake` head `15518699c` (PR #673): `d9c6e6ed2` pinned-MSG correction for your combined genuine rerun (test renamed `GenuineMsgIsADirectQdosInstructionThatAllocatesACase`; exact `CaseCreated`; route `qdos_mail_route` v4 disposition/selected route/predicates, classification `qdos_mail_classification` v5 category/case type/predicates, extraction `qdos_instruction` v7 and the `established-principal` evidence item asserted on the receipt's typed decision fields, since `Evidence` does not carry predicates); `f81932aa0` Triage note gate removed, `IAddTriageNote` required; `15518699c` Operations page no longer injects or executes `GetServiceHealth`, health notice and `OperationsNotices.ServiceHealth` label removed, EVA handoff read once, partial-data notice links `/Administration/Health`. Wave 14 on that head: build 0/0, architecture 100/100, integration 62/70 with 5 corpus skips (pinned samples absent locally) and 3 failures, all A-owned and already addressed on your branch: `OperationsWebTests.LimitAndHealthNoticesRenderSeparately…` and `…ComposedServiceHealthRenders…` (the obsolete hand-composed health tests you are removing) and `QdosAllocationRecoveryTests.PrincipalCorrectionAndCompletedSourceRedeliveryCannotAllocateBeforeStaffRetry` (seeded-QDOS precondition, your `8e6f3b21d`). No C change touches those. Note: `/Administration/Health` has no page on the C branch yet (C08's admin nav and your health page are not integrated here), so the link resolves once both land.

Stream C — C07 retention caller implemented (slice `c07-retention-caller`, under test and review; head follows). Shape: `IUploadToRequest` now starts/reuses the fixed 15-minute session, records the `PublicUploadOccurrence` Pending, hands bytes to `RetainIncomingArtifact` as `ActionActor.RequestLink(RequestUploadLinkEntity.Id)` with the link's persisted CaseId, no longer writes Case document/version rows or content itself, maps Confirmed/Pending → Accepted, Failed → retryable refusal, replay → same version; without `RetainIncomingArtifact` registered every public submission returns 404 and writes nothing (fail-closed bridge). Two A items: (1) exact host registrations when C publishes: `services.AddScoped<RetainIncomingArtifact>(); services.AddScoped<IIncomingArtifactRetentionStore, EfPublicUploadRetentionStore>();` with `ICaseArtifactCustody` AND `ICaseArtifactCustodyStatus` resolving to A04's adapter (`RetainIncomingArtifact` takes the status port for `Unknown` reconciliation). (2) A-owned test hunk: `tests/Pegasus.IntegrationTests/DocumentCustodyDurabilityTests.cs` was the only direct `new EfDocumentRequestStore(...)` caller and asserts the legacy path (an orphaned content write on a failed request upload) which no longer exists once custody owns the bytes; C reverted its edit and holds the exact retargeting hunk ("a failed arrival leaves no occurrence and no hand-over before a safe retry", 186 diff lines) for you to adopt or replace — say where you want it (PR comment or helper branch). Correction to our earlier C07-R-1: `20260906054658_V1PlatformFoundation.cs:1319` already grants SELECT,INSERT,UPDATE on both `PublicUploadSessions` and `PublicUploadOccurrences` to the web role; no grant is outstanding for Web. Worker role only if the Unknown sweep runs Worker-side.

Stream C — on comments 5560061438 / 5560095062 / 5560149495:
1. A04 request-link custody fix noted (`7a6157d88`); C's caller matches the rule (RequestLink subject = `RequestUploadLinkEntity.Id`, link's persisted CaseId, no holding). Durability test hunk posted as PR 673 comment 5560181686 (plain diff, sha in the comment). C07 caller slice is in wave 16 + independent review; head follows.
2. Source-snapshot update applied exactly as authored: script saved to an ignored artifact, run against the C branch package, full-package hash `d0119fd2…` verified before write, committed on `task/pegasus-v1-intake` with your co-authorship; Core principal-corpus/hash lane running now; push follows with the result.
3. `OperatorLabels.StaffAccounts.ReviewDue` / `.Review`: on the C branch they are NOT dead — `src/Pegasus.Web/Pages/Administration/Accounts/Index.cshtml` lines 92, 135, 286 (A01-owned page at its F/G state) still render them, so removing them here breaks the C build until your A01 page change reaches this branch. Disposition: C removes the two constants and the `StaffAccessReviewProjection` comment in the same step that brings your A01 Accounts page here (a G, or your composition patch that carries the page) — say which and C commits the removal immediately after; or include the three-line constant removal in that A patch yourself since it is mechanical and C accepts it. Mailbox folder review labels untouched either way.
4. C06 domain head: slice `556a26b1a` is in independent review; a round-2 correction is landing for the `EvaSubmission` page's required `IUpdatePrincipalDefaultInspectionLocation` (500 without your DI) and a save-path 302 defect. Reviewed head publishes right after; the DI list posted earlier stands, and yes — once you supply the real directory registration the optional bridge on `InspectionAddressChoicesQueries` reverts to required in C.

Stream C — snapshot update pushed: `task/pegasus-v1-intake` head `d2b50f46e` (PR #673). Core lane on that head: `PrincipalIdentification|TrackedPegasusSourceHashes|SourceHash|Corpus` 7/7 PASS, including `TrackedPegasusSourceHashesHaveNotDrifted`. No assertion or policy change.

Stream C — one further note on `DocumentCustodyDurabilityTests.cs` (A-owned): reverting C's edit left its direct `new EfDocumentRequestStore(...)` call passing the `IDocumentContentStore` argument the store no longer takes (the caller change removes the legacy content-write path), so the C slice did not compile. C applied a one-line compile-only accommodation on that file (drop the `contentStore` argument, nothing else; slice commit `6c8b945bd`), leaving every assertion untouched. The tests in that file then fail at runtime for A-owned reasons — `SeedCaseAsync` inserts a second QDOS principal (same seeded-estate class you fixed elsewhere) and `FailedDatabaseSaveRollsBackCaseAndRemovesUnreferencedContent` asserts the legacy path — which the posted hunk (comment 5560181686) retargets. Your call on adopting it; C makes no further edit there.

Stream C — G14 `2a20adbed` merged `--no-ff` as the identical object: `task/pegasus-v1-intake` head `fa564c0a7` (parents `d2b50f46e`, `2a20adbed`), Release build 0/0, pushed (PR #673). C08's staff-mail caller will consume `StaffSend`/`Generation`/`VerifiedEncodedMessageSizeLimit` from this object; no mailbox store of C's own. Noted on comment 5560312193: A authors the durability retarget against C's reviewed C07 head; C sends that head after its correction round (four review majors on the caller: replay counting, Pending reconciliation, thrown hand-over → Unknown, Pending wording) clears re-review. C06 reviewed head also follows its round-3 (two settings-page POSTs still return 200).

Stream C — C07 caller: contract gap found while mirroring your status-read rule. Reconciliation of a public upload's Pending/Unknown occurrence needs `ICaseArtifactCustodyStatus.GetAsync`, and per comment 5560061438 ("Get authorization is unchanged") that read is staff/automation PerformCasework only — so the public sender (`ActionActor.RequestLink`) can never move its own Pending forward; C's caller now catches that refusal and keeps the state (nothing false rendered, nothing counted twice, bytes never re-offered; a staff or system-worker authority still converges it). Two shapes for A, your choice: (1) authorize `GetAsync` for `RequestLink` under exactly the link rule `RetainAsync` now enforces (persisted `RequestUploadLinkEntity.Id`, matching Case, active/unrevoked/unexpired at the injected clock), or (2) A04 sweeps its own Pending/Unknown Case-artifact rows Worker-side (SystemWorker/ExecuteSystemWork) so public Pendings converge without a public read. Second instruction for A04: C classifies any exception thrown out of `RetainAsync` after bytes were handed over as UNCERTAIN (records `unknown`, never re-offers) except `StaffAuthorizationException` and `ArgumentException`, which are refusals before a byte is read; so A04 must raise a refusal it wants surfaced as one of those two types (not `InvalidOperationException`), and transport faults may surface as anything else. Core names no transport type (architecture lane fixed). C07 caller head after re-review follows.
<!-- original-payload-end -->

Foundation verification owner: codex-astra on DESKTOP-S1M5C7P. Clean worktree ../pegasus-worktrees/v1-platform, task/pegasus-v1-platform at D. Locked restore exit 0. Sol lease author accidentally ran focused Core tests before the serialized window: initial compile exit 1 (admin method widened domain fake interface), corrected separate admin store port, rerun 19 passed exit 0. Full build/integration still pending. Ownership resolution extends A/F-only to Core/Cases/CaseQueries.cs and Infrastructure/Persistence/EfCaseQueryStore.cs solely to expose the independent lease generation to the administrative caller; B resumes ownership after F. No domain behavior change authorized by this exception.

Published contract definitions in the clean A checkout now compile: dotnet build src/Pegasus.Core/Pegasus.Core.csproj --configuration Release --no-restore, exit 0, zero warnings/errors. New schema owner paths assigned to A/F: src/Pegasus.Infrastructure/Persistence/V1FoundationEntities.cs and V1FoundationModelConfiguration.cs plus the single forthcoming V1PlatformFoundation migration/designer. These implement the supplied A/B/C inventories within the existing Infrastructure project, not a new persistence boundary. Fresh Sol contract review is running; no F SHA is published yet. Board sync at this checkpoint is ahead=0, behind=0; shared owner records are synchronized through the existing GUI.

Typed third-party candidate leaves accept nullable normalized values while retaining the raw SourceFieldCandidate and missing/ambiguous/conflicting disposition. An unreadable amount never requires a fabricated zero. A01 read-only preparation confirms periodic reviews derive from ActionHistory (no separate review column), existing per-request stamp validation/forced-password middleware can be reused, and deletion can clear current access while retaining stable historical actor identity. No A01 domain implementation before F.

Preserved validation failures during Foundation authoring: first Infrastructure compile failed on schema field placement/required initializers, corrected then passed; EF scaffold first failed because Web deps absent, Web build then passed and scaffold succeeded. Subsequent model review required regenerating the same unpublished migration. During nullable third-party leaf edit a PowerShell interpolation error temporarily produced invalid generic type arguments; schema build reported CS7003. Root restored the exact typed contract and applied nullable types correctly before further compilation. No failed run is being represented as PASS; final whole-tree validation remains pending.

F03 fixture alignment completed by Terra in the twelve explicitly scoped integration files plus SeededPrincipalTestData helper. Domain assertions preserved, pre-seed migration tests untouched. Exact ordered migration expected is 20260906054658_V1PlatformFoundation. All 17 new table DELETE denials and Worker IntakeAssets UPDATE reflected in permission expectations. Final model review accepts pre-case holding/OCR/cache, pending artifacts, generation version and fifteen supplied principal seeds; remaining redundant artifact DocumentId is being removed so version is sole persisted document identity. No full-solution/test PASS yet.

Root solution Release build --no-restore failed exit 1 with six fixture compilation errors: QDOS seed helper edits referenced nonexistent scope variables. Terra is correcting the single root cause using actual local providers. Product projects compiled. Independent final Sol contract/schema review passed after removing redundant GeneratedCaseArtifact.DocumentId; publication remains gated on successful root validation.

Corrected solution Release build passed exit 0, 0 warnings/errors. Focused ordered migration/runtime-role/lease integration run failed: foundation seed tried inserting Organizations.NormalizedName, an existing computed column. Stopped the repeatedly failing run (exit 1), retaining failure; schema author is correcting seed SQL before rerunning actual ordered migrations. No F publication.

Grant convention gate passed after canonical bracketed GRANT SQL: 95 migration files. Local Azure deployment plan passed; all Worker activation settings render disabled. Subsequent build failed exit1 MSB3027/3021 due reused MSBuild node34920 holding Infrastructure.dll, not source errors; verified process command and successfully shut down build servers before rebuild. Final prepublication logical-reader correction: nullable document/version OR intakeasset selector permits unallocated retained sources without fabricated Case documents; request/result and engineering signature registry updated, Sol re-review requested.

Candidate d819034ca6b4716e5f9a2e9a94f309edd14371c6 ordered migration test failed exit1 (one test): SQL Server rejected QUOTENAME inside EXEC concatenation. Corrected migration constructs @denySql before EXEC(@denySql); no new migration. Foundation adoption remains pending final correction commit and passing validation. Core CaseEditAuthorityTests passed 19/19 exit0 at candidate.

2026-09-01 (Claude controller, Phase 0 groom): rescoped to a feature. Operator decision D26: direct Case creation is settled as staff entering the required identity and attaching or recording the instruction; Pegasus persists an attributable intake receipt and then reuses the normal principal and Case/PO allocation policy; no parallel allocation implementation. So `/Cases/Create` gains a receipt-creating entry and the two shell call sites (`Pages/Shared/_ShellDialogs.cshtml`, `wwwroot/js/site.js` Ctrl N) target it; the Search header action follows. Refs FRD-02 (ways intake starts, INT-26) and FRD-12 (Add dialog). Placement: build wave B; global_shell lock for the two shell files. Leaves Backlog only after the governing-docs chore merges.

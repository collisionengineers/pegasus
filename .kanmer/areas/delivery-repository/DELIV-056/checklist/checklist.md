# Checklist — DELIV-056

- [x] Step 1 — Add the approved existing `IntakeWebTestSupport.cs` helper and replace obsolete automatic-allocation shorthand in the named ordinary intake callers, preserving existing route, asset, lifecycle and destination assertions.
- [x] Step 2 — Put documented formal evidence in the affected MultiFormat real transport scenarios; leave direct, malformed and resource-limit negative probes intentionally non-definitive.
- [x] Step 3 — Correct the SendToAI accepted-case seed and remove only the stale gated-control expectation, retaining absent-dialog and denied-POST coverage.
- [x] [pre-review] Static scope is the permitted fourteen-file map (eleven currently need edits); `git diff --check` passed. Host verifier owns the exact existing 13-class selection after its sequential build.
- [ ] [pre-review] Parent-coordinated review, commit, push and draft PR to `dev`.
- [x] [pre-review] Stop in Implementing: no merge, verification, release, build, test, browser, capture, package or deployment was run by this worker.

## Progress notes

- 2026-09-08: extraction and classification are separately satisfied by an attached/structured document, never by EmailBody.
- 2026-09-08: remaining legacy body markers are deliberate negative/limit probes, or body-field inputs mirrored into the attached formal document for the invalid-field scenario.

# Plan — DELIV-014 (release 15)

Sequenced; each step gates the next.

1. **Merge queue**: PRs #485 (MAIL-005), #487 (ENG-006), #488 (DOCS-005) — each merged only on green CI, serially, cleaning each lane (worktree + local/remote branch) after `state == MERGED`. Codex lanes (#470, #473, their branches/worktrees) untouched.
2. **Release branch**: `task/deliv-014-release-15` from the post-merge dev head, worktree `../pegasus-worktrees/deliv-014`. Docs refresh: `docs/operations.md` (release-15 row, deployed SHA, wipe note), `docs/current-architecture.md` (extraction writes facts; automatic vehicle-lookup sweep on the reconciliation timer; Box custody without binding files + attachment retention; retained-mail case resolution via allocation attempts; queues/assessment/case-page surface changes if the file describes them). PR → dev on green CI.
3. **Deploy** (runbook route): from the dev head — `azd provision --preview` byte-compare vs `artifacts/releases/release-14-d91fd7d7/azd-preview.txt` (expect no infra drift); `azd deploy` web + worker (worker config-zip route per release memory); EF migrations via the bootstrap script (two new migrations); store the new preview + artifacts under `artifacts/releases/release-15-<sha>/` and copy out before any worktree removal.
4. **Box cleanup** (DOCS-005 step, approved write, exact targets enumerated live): delete `pegasus-case-binding.json` / `pegasus-audit-binding.json` / `pegasus-accepted-source-binding.json` from each existing case folder under the approved custody root. Read-only listing first; deletions logged per file.
5. **Verify live** (browser + read-only SQL): the 15 round-2 issues one by one — upload one-unit card + thumbnails, extraction facts on a fresh instruction, auto lookup + assessment mileage prefill, short case page + toggle, queues one table/dropdowns/sort, assessment no-copy + damage save + preselect, inbox case links + tidy cell, Box folder clean + attachment file, wipe state (empty queues/inbox, login), allocation stability. Record each in proof.
6. **Promote**: dev → main exact-SHA non-force fast-forward (standing MERGE AUTH), after CI green on dev.
7. **Close out**: proofs on CASE-005/007/008, INTK-020/021/022, ENG-006, MAIL-005, DOCS-005, PLAT-016/017 → done (serialized moves); DELIV-014 proof + done; remove release-scope worktrees/branches; final board + git status check.

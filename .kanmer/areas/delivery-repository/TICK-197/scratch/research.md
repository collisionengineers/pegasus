Research found an existing credential-free validation route: scripts/Test-AzureDeploymentPlan.ps1 -Mode Local performs repository assertions and az bicep build only; cloud/azd operations are confined to later modes. Current .github/workflows/ci.yml never invokes it and its path detector excludes infra/**, azure.yaml, and the validator. Canonical docs already resolve the headline choice in favor of a lane: docs/engineering.md requires Bicep compilation at static/build/architecture tier. A live Local run failed before compilation because the validator still asserts minReplicas 0 while infra/modules/platform.bicep has minReplicas 1 since b3b4d88f; lane implementation must repair this existing drift. No UI or claimed-ticket file overlap found for the likely workflow/script-only implementation.

# Open questions — DELIV-002

- [x] GitHub-side branch protection and rulesets are intentionally out of scope on subscription grounds. The revised main-history guard is detective after a push; it cannot prevent an otherwise valid direct fast-forward.
- [ ] Which exact authorized local release procedure will promote the reviewed `dev` SHA to `main` after `MERGE AUTH GRANTED`? The plan needs the command, preflight checks, and post-push verification so the promotion is an exact fast-forward without a GitHub PR merge.

## Parked (explicitly deferred)

- None.

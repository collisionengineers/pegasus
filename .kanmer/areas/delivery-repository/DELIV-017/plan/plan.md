# Plan

- Update `docs/operations.md` with release 28's exact deployed SHA, digest, revision, migrations and observed smoke/permission evidence.
- Refresh `docs/current-architecture.md` so ENG-016's as-built export behavior is identified as deployed in release 28.
- Run Markdown placement/link validation and `git diff --check`.
- Deliver the two documentation edits through one PR to `dev`; no product rebuild or second Azure deployment.

## Simplification pass

n/a — docs-only. The two existing current-state owners are reused; no new document or abstraction.

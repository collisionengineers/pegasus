# Proof — DELIV-013 (Release 14)

Type: command-log + visual. The successful deployment is the proof; the full gate-by-gate transcript lives in this ticket's scratch notes.

- **Production serves release 14**: `/diagnostics/version` → `{"version":"0.1.0-alpha.1","sourceSha":"d91fd7d7835af116c0c769b75fd4ccae56ca377b"}`; web revision `pegasus-prod-web-252ow37gij--d91fd7d7835a` at digest `sha256:949797d4…` (== manifest digest), Running; worker config-zip "Deployment was successful."; `Invoke-ProductionSmoke` **passed** incl. `approved-live-worker` activation census.
- **Verification before the cut**: 10 independent lanes over all 36 then-verifying tickets + UI-copy audit + merge-integrity sweep (merge-tree recomputation of all 40 merges — nothing lost; migration census 58/58; config parity 37/37; verdict SAFE TO SHIP). Copy findings fixed pre-cut in PR #472 (new-in-release violations + safe word-level pre-existing ones); structural debt filed as [[PLAT-015]].
- **Database**: four migrations applied via efbundle (ImageIntakeSubmissionGroup, SendToAiConnectorSettings, ImageCaseCustody, ApprovedMailboxLogicalFolderBindings), head readback confirmed; bootstrap verified 501 catalogued + 337 effective grant rows.
- **Provision preview** byte-identical to the stored release-13 preview except the revision suffix.
- **Post-deploy live checks**: badge=rows=10, mailbox-only Received-today, U7 swept at 12:24:33Z, mailbox admin identifier-free with London clock, single readiness chip, image galleries + inline image route, .doc/.msg accepted, worker zero exceptions / zero exit-134, both polls completing clean.
- **Docs on both branches**: PR #475 (operations release-14 row + evidence bullet, current-architecture DOC/MSG reality, MCP-07 capability row, runbook previous-artifact rollback procedure) merged to dev, then **dev→main atomic exact-SHA promotion**: `main = dev = 39bb118ab5cfd72f6e970ce5b08e85b11c3d56d9` (MERGE AUTH GRANTED in the operator's 2026-08-20 directive; production serves the contained code SHA per the operations release rule for docs-only commits).
- **Closeout**: 34 release-scope tickets moved to done with proofs and released claims; TICK-102/TICK-104 held at verifying on the closed Features:SendToAi gate; release artifacts retained at `artifacts/releases/release-14-d91fd7d7`; git hygiene restored (release-scope branches/worktrees removed; in-flight codex lanes untouched).

# DELIV-012 — research/codebase-evidence.md

Read-only evidence gathered 2026-08-19 (~12:30Z) from `origin/*` refs after `git fetch origin --prune`, `gh` and file reads. No checkout, worktree, or branch was created or modified. Each fact is tagged **[verified: command]** or **[assumed: reason]**.

## 0. Anchors (re-verified — one anchor moved)

- Production serves release 10 = `origin/main` = `d8de29cb` (merged 2026-08-18T13:52:50Z) **[verified: `git log -1 origin/main`; `gh api repos/…/branches/main`]**.
- `origin/dev` was `4ba63888` at ticket creation; **PR #420 (TICK-093) merged at 2026-08-19T12:16:11Z, so `origin/dev` is now `560f741c`**, 42 commits ahead of main; main is an ancestor of dev **[verified: `git fetch`; `git rev-list --count origin/main..origin/dev`; `git merge-base --is-ancestor`]**. Everything below uses `560f741c`.
- Release 11 (DELIV-011) was prepared at `feda958f` (PR 409 merge) and held; the local branch `task/deliv-011-release-11` still sits there, 33 behind dev, no upstream **[verified: `git branch -vv`]**.
- Open PRs targeting dev: #416, #417, #422, #423, #424; #410 is the dev→main vehicle ("Release 11: dev → main…", now stale in title) **[verified: `gh pr view`]**.

## 1. What dev carries beyond main (first-parent merges, oldest → newest)

**[verified: `git log --first-parent origin/main..origin/dev`; `git diff --name-status <merge>^1 <merge>`; `gh pr view N --json …`]**

| PR | Ticket | Title | Merged (Z) | Files (grouped) | Migration | Infra | Workflow | Worker |
|---|---|---|---|---|---|---|---|---|
| 407 `f79c24d9` | DELIV-009 | Release 10: record promotion/deployment/connector evidence | 08-18 14:34 | docs: `docs/operations.md` | – | – | – | – |
| 408 `60fde326` | TICK-033 | docs: reconcile INT-31 upload-link boundary | 08-18 15:38 | docs: `docs/capabilities.md` | – | – | – | – |
| 409 `feda958f` | PLAT-006 | Centre the rail shell content region; redesign Upload | 08-19 08:08 | Web: `Pages/Upload.cshtml`, `Pages/UploadStatus.cshtml`, `Pages/Uploads/Request.cshtml`, `wwwroot/css/site.css`, `wwwroot/js/site.js`; docs: `docs/design/README.md` | – | – | – | – |
| 411 `dc77c29d` | TICK-044 (MAIL-02) | Canonical mail destination policy | 08-19 09:03 | Core: `Intake/Classification/MailClassificationContracts.cs`, **A** `Intake/Classification/MailOperationalDestinationPolicy.cs`, `Intake/DirectProviders/Qdos/QdosMailClassificationPolicy.cs`; tests: 3 Core.Tests files; docs: capabilities, current-architecture, frd-08 | – | – | – | – (Core only; runs in Worker classification path) |
| 412 `314a9b26` | TICK-204 | Missing assessment-report outcome variants | 08-19 09:17 | docs: frd-11 | – | – | – | – |
| 413 `4d1bff3d` | DOCS-002 | ADR-0028 renderer runs in Web Container App | 08-19 09:19 | docs: **A** `docs/adr/0028-…md`, `docs/adr/README.md` | – | – | – | – |
| 415 `b548b674` | SIMPLI-014 | Integrate CollisionRenderer into the monolith | 08-19 10:29 | Core: **A** `Reports/AssessmentReportRendering.cs`; Infra: `DependencyInjection.cs`, `Pegasus.Infrastructure.csproj`, **A** `Reports/PlaywrightAssessmentReportRenderer.cs`, `packages.lock.json`; Web: `Program.cs`, `packages.lock.json`; Worker: `packages.lock.json` only; tests: ArchitectureTests `DependencyDirectionTests.cs` + lock, **A** Core.Tests `Reports/AssessmentReportRenderingTests.cs`, **A** IntegrationTests `Reports/AssessmentReportRendererTests.cs` + lock; docs: current-architecture, design/README, **A** `docs/design/assets/report-renderer/templates/{assessment_fee_note,assessment_report}.scriban`, `report.css`, frd-11, open-decisions, operations, runbook; scripts: `Test-MarkdownPlacement.ps1`, `Test-TestMarkdownPlacement.ps1`; workflows: `.github/workflows/workspaces.yml`; workspaces: `workspaces/README.md` + **D** all 89 files under `workspaces/report-renderer/` | – | – | yes (removes report-renderer job) | lock only |
| 414 `33f00220` | TICK-043 (MAIL-01) | Identify inbound mailbox identity | 08-19 10:34 | Core: `Intake/MailboxIntake.cs`; Infra: `Persistence/EfRetainedMailboxMessageStore.cs`, `MailboxEntities.cs`, `MailboxModelConfiguration.cs`, **A** migration `20260819093019_RetainedMailboxInternetMessageIdentity` (+Designer), `PegasusDbContextModelSnapshot.cs`; tests: Core `PollApprovedInboxTests`, Integration `IntakePersistenceIntegrationTests`, `MailboxIntakeIntegrationTests`, `RetainedMailPersistenceTests`; docs: capabilities, frd-08 | **yes** | – | – | Core path executed by Worker `InboxPollFunction` |
| 419 `4f67a83e` | PR-009 | Preserve long assessment report tails | 08-19 11:21 | Infra: `Reports/PlaywrightAssessmentReportRenderer.cs`; tests: `Reports/AssessmentReportRendererTests.cs` | – | – | – | – |
| 418 `181fe331` | TICK-046 (MAIL-04) | Classification evidence + correction history | 08-19 11:23 | Core: `Intake/Classification/MailClassificationContracts.cs`, `Intake/RetainedMail.cs`; Infra: `DependencyInjection.cs`, `Persistence/EfIntakeReceiptStore.cs`, `EfRetainedMailboxMessageStore.cs`, `MailboxEntities.cs`, `MailboxModelConfiguration.cs`, `PegasusDbContext.cs`, **A** migration `20260819104953_MailClassificationCorrectionHistory` (+Designer), snapshot; Web: `Pages/Mail/Message.cshtml(.cs)`; tests: Core `RetainedMailTests`, Integration `IntakePersistenceIntegrationTests`, `MailWorkspaceWebTests`, `RetainedMailPersistenceTests` | **yes** | – | – | – |
| 421 `4ba63888` | TICK-213 | Normal-density report stress evidence | 08-19 11:37 | tests: `Reports/AssessmentReportRendererTests.cs` | – | – | – | – |
| 420 `560f741c` | TICK-093 | One canonical versioned repair specification | 08-19 12:16 | Core: `Assessment/AssessmentPolicy.cs`, **A** `Assessment/RepairSpecifications.cs`; Infra: `Persistence/AssessmentEntities.cs`, `AssessmentModelConfiguration.cs`, `EfCaseAssessmentStore.cs`, **A** `EfRepairSpecificationStore.cs`, **A** migration `20260819112640_VersionedRepairSpecifications` (+Designer), snapshot, `PegasusDbContext.cs`; tests: **A** Core `Assessment/RepairSpecificationPolicyTests.cs`, Integration `AssessmentPersistenceIntegrationTests`, `IntakePersistenceIntegrationTests`, **A** `RepairSpecificationMigrationTests.cs`; docs: frd-06 | **yes** | – | – | – |

PR sizes **[verified: `gh pr view --json additions,deletions,changedFiles`]**: 407 +23/−4 (1 file); 408 +1/−1; 409 +215/−13 (6); 411 +276/−14 (9); 412 +33/−0; 413 +85/−0 (2); 414 +6680/−29 (13); 415 +1153/−12399 (116); 418 +7514/−20 (17); 419 +50/−7 (2); 420 +7736/−7 (15); 421 +3/−3 (1). All merged PRs showed all-green checks at merge (`gh pr checks 409/411/414/415/418/419/420/421` → every lane pass, `infrastructure` skipped) **[verified]**.

### 1a. Totals

**Pending EF migrations since main (apply order by id)** **[verified: `git diff --name-only origin/main origin/dev -- '**/Migrations/**'`; `git ls-tree origin/main|origin/dev …/Migrations`]** — production head per `docs/operations.md` release-9/10 rows is `20260814094632_DropBoxFileRequests` (release 10 = "none"):

1. `20260819093019_RetainedMailboxInternetMessageIdentity` (TICK-043) — adds nullable `RetainedMailboxMessages.CanonicalInternetMessageIdentity nvarchar(500) BIN2`, backfills `UPPER(LTRIM(RTRIM(InternetMessageIdentity)))`, then creates **unique filtered index** `IX_RetainedMailboxMessages_MailboxId_CanonicalInternetMessageIdentity`. **Risk:** fails if production already holds two retained rows in one mailbox with the same trimmed/upper-cased Message-ID (e.g. the same message retained under two Graph immutable ids). No grants (column only) **[verified: file read]**.
2. `20260819104953_MailClassificationCorrectionHistory` (TICK-046) — adds `ConcurrencyToken`, `DecidedAtUtc`, `DecidedByActor`, `Version` to `IntakeMailClassificationDecisions` (SQL Server backfill from `IntakeReceiptEvents`/`IntakeReceipts`), creates `IntakeMailClassificationHistory`, and **does** grant `pegasus_web_runtime_role` SELECT/UPDATE on decisions and SELECT/INSERT on history with UPDATE/DELETE denied **[verified: file read]**.
3. `20260819112640_VersionedRepairSpecifications` (TICK-093) — drops `IX_CaseEstimateLines_CaseId_Position`, adds nullable `CaseEstimateLines.RepairSpecificationId`, creates `CaseRepairSpecifications` (check constraints + 3 unique indexes), backfills one `Draft/LegacyUnresolved` spec per case that has estimate lines and links the lines, adds FK. **No GRANT to either runtime role for `CaseRepairSpecifications`** although `EfCaseAssessmentStore.SaveAsync` (the existing Web assessment-save path) now reads/inserts that table (`EfCaseAssessmentStore.cs:117-135`) — see §4 and §7 **[verified: file read; `git grep GRANT` over migrations; runbook §"least privilege": neither role receives schema-wide access]**.

The `ModelSnapshot` differs main→dev accordingly **[verified]**. Open PRs add three more migrations (§3).

**Infra**: `git diff --stat origin/main origin/dev -- infra azure.yaml` → empty. No `infra/main.parameters.json`, `azure.yaml`, appsettings or `host.json` change **[verified]**.

**Workflows**: only `.github/workflows/workspaces.yml` −8 lines (the report-renderer validation step removed; `ci.yml` unchanged) **[verified: `git diff --stat … -- .github`]**.

**Solution / projects / packages** **[verified: `git diff … -- '*.csproj' '*packages.lock.json' …`]**: `Pegasus.slnx` unchanged. `src/Pegasus.Infrastructure/Pegasus.Infrastructure.csproj` adds `PDFsharp 6.2.4`, `Scriban 7.2.6`, `Microsoft.Playwright 1.61.0` and embeds `docs/design/assets/report-renderer/templates/*.scriban|report.css`, `docs/design/brand/logos/logo_no_margin.png`, `docs/design/brand/signatures/andy_patterson.png` as resources. Lock files updated for Infrastructure, Web, Worker, ArchitectureTests, IntegrationTests (Worker and Web pick the new packages transitively). `workspaces/report-renderer/**` (7 csproj/props) deleted. `src/Pegasus.Worker/**` has no source change — only `packages.lock.json` **[verified]**. Native deps already present on main (ONNX Runtime 1.20.1, SkiaSharp 3.116.1 + Linux NoDependencies) are unchanged; the new native-ish dependency is Playwright's bundled Node driver plus a Chromium browser that is **not** in the package.

**Renderer deploy-time needs (SIMPLI-014 / ADR-0028)**:
- `PlaywrightAssessmentReportRenderer.GetBrowserAsync()` calls `Playwright.CreateAsync()` then `Chromium.LaunchAsync(Headless=true)` — requires the pinned Chromium build plus Linux libs and fonts in the Web image **[verified: `src/Pegasus.Infrastructure/Reports/PlaywrightAssessmentReportRenderer.cs:90-98`]**.
- There is **no Dockerfile** in the repo; the release image is built by `dotnet publish … /t:PublishContainer` (`scripts/Build-ReleaseArtifacts.ps1:60`) on the default `mcr.microsoft.com/dotnet/aspnet` base, and `azure.yaml` uses `host: containerapp, remoteBuild: true` with no container customisation. `Pegasus.Web.csproj` has no `ContainerBaseImage`/`ContainerFamily` property. Therefore the release-12 image **will not contain Chromium or fonts**; the renderer is registered but would throw at first use **[verified: `git ls-tree -r origin/dev | grep -i dockerfile` empty; csproj read; `azure.yaml` read]**.
- `docs/operations.md` (dev) states this explicitly: renderer is "locally verified source, not deployed or live-verified … no Web container Chromium layer, Azure deployment … is claimed; DOCS-001 and PLAT-007 own those later gates" **[verified: diff main..dev docs/operations.md]**. No new app setting, Key Vault secret, or Azure resource is introduced **[verified: infra/appsettings diff empty]**.
- Container/worker package size will grow by Playwright's `.playwright/` driver and PDFsharp/Scriban assemblies **[assumed: standard Microsoft.Playwright package layout; not measured]**. `docs/runbook.md` now documents `playwright.ps1 install chromium` for the local Browser lane only **[verified]**.

## 2. CI state (2026-08-19 ~12:25Z)

**[verified: `gh run list --branch dev --limit 15`; `gh run list --branch main --limit 5`; `gh pr checks N`; `gh run view <id> --log-failed`]**

- `gh run list --branch dev` shows only `pull_request` runs of the dev→main PR #410 — latest completed success at head `feda958f` (2026-08-19T08:08Z). Later dev heads (`4ba63888`, `560f741c`) have no completed run; PR #410's current run `32251788504` (+ source-workspaces `32251788455`) for head `560f741c` is **pending** (changes/documentation/reference-data/source-workspaces passed; browser, unit, sql-integration 1-3 pending; infrastructure skipping). `ci.yml` triggers on `pull_request` and `push: [main]` only, so dev itself is never built outside a PR.
- `gh run list --branch main --limit 5`: `repository-check` success on `d8de29cb` (release 10 push) and `f1e116c6`; scheduled **`qdos-pressure` failed on `2b0df78c` (2026-08-18T03:49Z)**: `FailureInjectionTests.ConcurrentReplayPressureProducesOneDurableReceipt` — "A replayed upload landed on `/Upload/Status/<id>?duplicate=true`" (pre-existing on main, not introduced since release 10).
- Open PRs:
  - **#410** (dev→main): pending as above; `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`.
  - **#416** INTK-005 (`ed04f498`): **RED — real**. sql-integration 1/2/3 fail, browser/unit pass. Failing tests (run `32242883226`): `IntakePersistenceIntegrationTests.CommittedMigrationCreatesTheSqlServerSchema` (expected migration list lacks `20260819101344_GroupedIntakeSubmission`), `IntakeWebNegativeTests.ExternalReceiptTokenCaseVariantsReplayOneReceiptThroughUploadCaller` + `InstructionDraftWebTests.SameManualUploadTokenReplaysOneReceiptDraftAndAssetSet` + `IdenticalBytesWithDifferentTokensPersistDistinctSourceIdentitiesWithMatchingHashes` (receipt token now `<token>:0`), `IntakeWebNegativeTests.ArtifactFailureShowsRetryMessage…` (redirect now `/Upload/Group/…` not `/Upload/Status/`), `EmptyUploadReturnsValidationAndDoesNotPersist` ("That file is empty." gone), `QdosIntakeWebTests.ReadableManualUploadStagesPendingWork…`/`CompletedAllocatedUploadStatusLinksOnlyToItsCase` (Guid null). Root cause: the PR changed upload behaviour without updating existing expectations.
  - **#417** INTK-006 (`599bfe6d`): **no check runs on the current head** (`gh api …/commits/599bfe6d/check-runs` → 0). The earlier head `70d7c89c` (run `32244323472`) failed with exactly the inherited INTK-005 failures above. Stacked on #416 (contains `ed04f498`).
  - **#420** TICK-093 (`b0596c9b`): all green → merged 12:16Z.
  - **#422** TICK-045 (`139a4571`): sql-integration (1) fail — `MailWorkspaceWebTests.ScopingAndPagingCarryTheMailboxFolderAndPageForward` with `SqlException: Connection Timeout Expired … post-login phase` (infrastructure flake, not code); all other lanes pass. Re-run expected to go green.
  - **#423** INTK-008 (`855160b7`): sql-integration (2) fail — `ImageIntakeWebTests.StaffRegistersAnImageOnlyReceiptAndFindsItEverywhere` (**real**: `Pages/ImageIntake/Details.cshtml` lede changed from "Image intake registered — awaiting definitive instruction" to "Awaiting instruction" but `ImageIntakeWebTests.cs:75` still asserts the old text) and `QdosAllocationRecoveryTests.DistinctParallelRetriesResolveToOneCaseAggregate` (one of two parallel retries `Pending` instead of `Succeeded` — **[assumed: concurrency flake; not changed by the PR]**). Other lanes pass.
  - **#424** INTK-007 (`abd8a923`): **no check runs at all on its head** (`check-runs total_count 0`); `gh pr checks` → "no checks reported".
  - `mergeable`/`mergeStateStatus` from `gh pr view` were `UNKNOWN` for every dev-targeted PR at query time (GitHub had not recomputed after the #420 merge) **[verified]**; the merge-tree analysis in §3 is the authoritative conflict answer.

## 3. Open PR integration analysis

**[verified: `git merge-base origin/dev <head>`; `git rev-list --count`; `git diff --name-status <base> <head>`; `git merge-tree --write-tree --name-only origin/dev <head>`; pairwise `git merge-tree --write-tree --name-only --merge-base=<mb> <A> <B>`; `git log origin/dev..<head>`]**

| PR | Branch @ head | Forked at (merge-base with dev) | Ahead / behind dev | Files | Migration added | Conflict vs dev |
|---|---|---|---|---|---|---|
| 416 INTK-005 | `intk-005-grouped-upload@ed04f498` | `4d1bff3d` (after PR 413) | 1 / 25 | 16 | `20260819101344_GroupedIntakeSubmission` (tables `IntakeSubmissionGroups`, `IntakeSubmissionGroupMembers`; **no grants**) | **clean** |
| 417 INTK-006 | `intk-006-grouped-image-routing@599bfe6d` | `4d1bff3d` | 4 / 25 (includes 416's `ed04f498` — **stacked on #416**) | 22 | same migration as 416 | **CONFLICT**: `tests/Pegasus.IntegrationTests/IntakePersistenceIntegrationTests.cs` |
| 422 TICK-045 | `task/tick-045-shared-classification-policy@139a4571` | `181fe331` (after PR 418) | 1 / 10 | 2 (`docs/capabilities.md`, `tests/…/RetainedMailPersistenceTests.cs`) | – | **clean** |
| 423 INTK-008 | `intk-008-image-initiated-lifecycle@855160b7` | `4ba63888` (after PR 421) | 4 / 7 | 33 (incl. `CONTEXT.md`, `docs/operator-notes.md`, `docs/prd/pegasus-product.md`, ADR-0029 new, frd-01/02/05/06/12, Core ImageIntake, Infra custody + `EfImageIntakeStore`, Web `Pages/ImageIntake/*`) | `20260819112914_ImageInitiatedLifecycle` (new table `ImageIntakeLifecycleEvents` + 6 columns; **no grants**) | **CONFLICT**: `tests/Pegasus.IntegrationTests/IntakePersistenceIntegrationTests.cs` |
| 424 INTK-007 | `intk-007-unidentified-intake@abd8a923` | **`d8de29cb` = main** | 1 / 42 | 49 (docs incl. `docs/operator-notes.md`, `docs/prd/pegasus-product.md`, frd-01/02/03/06/08/09/10/12, runbook, design system docs + 2 TSX; Core `Intake/ProcessIntake.cs`, **A** `Intake/Unidentified/UnidentifiedContracts.cs`, `Operations/DashboardCounts.cs`; Infra DI, `EfDashboardQueries`, **A** `EfUnidentifiedStore`, **A** `UnidentifiedEntities`; Web **A** `Mcp/UnidentifiedMcpTools.cs`, `Pages/Index`, `Pages/Intake/Details`, `Pages/Mail/Message.cshtml.cs`, `Pages/Shared/_Layout.cshtml`, `_StatusChip`, `Pages/Triage/Index`, **A** `Pages/Unidentified/{Index,Details}`, `Presentation/OperatorLabels.cs`; tests 2) | `20260819115323_UnidentifiedWork` (tables `UnidentifiedItems`, `UnidentifiedSequences`, `UnidentifiedHistory`, backfill from `IntakeReceipts` decisions; **no grants**) | **CONFLICT**: `docs/capabilities.md` |

Pairwise (base = pair merge-base):

| | 417 | 422 | 423 | 424 |
|---|---|---|---|---|
| **416** | clean (417 ⊇ 416) | clean | clean | `docs/capabilities.md` |
| **417** | | `tests/…/IntakePersistenceIntegrationTests.cs` | `tests/…/IntakePersistenceIntegrationTests.cs` | `docs/capabilities.md` |
| **422** | | | `docs/capabilities.md` | `docs/capabilities.md` |
| **423** | | | | `docs/frd/frd-02-intake-and-source-identity.md`, `docs/frd/frd-12-operator-experience.md` |

All conflicts are the expected-migration-list in `IntakePersistenceIntegrationTests.cs`, the capability table, or adjacent FRD prose — none in C# product code. Shared-file overlap (non-conflicting auto-merges): `DependencyInjection.cs`, `PegasusDbContext.cs`, `PegasusDbContextModelSnapshot.cs` (416/417/423/424), `Program.cs` (416/417), `Pages/Mail/Message.cshtml.cs` (424 vs merged 418), `Pages/Upload.cshtml(.cs)` + `site.js` (416/417 vs merged PLAT-006), frd-06 (423/424 vs merged 420), frd-08 (424 vs merged 411/414). Stacking: only 417 contains 416's commit; no other PR contains another's commits **[verified: `git log --oneline A..B` per pair]**.

Protected/governance touches in open PRs **[verified: `git diff <base> <head> -- docs/operator-notes.md`]**: **#424 adds 27 lines** ("Unidentified received material" section; "Unidentified replaces the old broad `Needs sorting` destination") and **#423 rewrites an existing operator-notes sentence** plus adds an "Image-initiated Case clarification — 2026-08-19" section. `CLAUDE.md` product invariant says `Needs sorting` retains its settled meaning and operator-notes changes need user resolution — both need an explicit operator decision before merge.

**Model-snapshot caution**: each open-PR snapshot was scaffolded against an older model; after merge the textual auto-merge may not equal the real model. The plan should run `dotnet ef migrations has-pending-model-changes` (or the `CommittedMigrationCreatesTheSqlServerSchema` + `RepairSpecificationMigrationTests` lanes) after each merge **[assumed: standard EF behaviour]**. Migration ids interleave (`101344` < merged `104953`/`112640`); EF applies every unapplied id in order, so ordering is harmless for production (nothing since `20260814094632` is applied), but the bundle will apply six migrations in one release if all PRs land.

**Recommended merge order (for the plan to adopt):**
1. **#422 TICK-045** — 2 files, clean vs dev, only a SQL-timeout flake; re-run CI and merge. Removes a `capabilities.md` overlap early.
2. **#416 INTK-005** — clean vs dev but must first be fixed (7 real test failures + migration-list) and given runtime-role grants for its two tables; it is the base of #417.
3. **#417 INTK-006** — after #416 lands, rebase/merge dev (conflict only in the migration-list test), re-trigger CI (no runs on current head).
4. **#423 INTK-008** — merge dev (migration-list conflict), fix the `ImageIntakeWebTests.cs:75` wording assertion, add grants for `ImageIntakeLifecycleEvents`, and obtain operator resolution for the `operator-notes.md` rewrite.
5. **#424 INTK-007** last — stalest base (main), widest doc surface (capabilities + frd-02/frd-12 conflicts with #423), protected-doc and `Needs sorting` invariant change, no CI on head, MCP tool + nav additions; rebase onto dev with #423 in, add grants for three tables, CI, then merge.
Rationale: smallest/cleanest first, stacked pair together, the two PRs that both edit operator-notes/FRD-02/FRD-12 last and sequentially so one rebase absorbs the other's prose.

## 4. Feature entry-point / dark-feature audit

**[verified: `git grep` on `origin/dev`/PR heads; `git show` of files; `Program.cs` gate search]**

Composition gates on dev `src/Pegasus.Web/Program.cs`: `Features:LocalIntake` (l.109/586, DevelopmentOffline only), `Features:LocalDocumentCustody` (l.185), Automation MCP ingress "composition-gated off by default" (l.226), Send-to-AI hand-off gate (l.231). None of the post-release-10 changes adds a flag; nothing merged since main is behind a new gate.

| Capability | Entry point in production | Verdict |
|---|---|---|
| PLAT-006 shell/Upload (PR 409) | `/Upload`, `/UploadStatus`, `/Uploads/Request` pages; CSS `.app-rail-main` (`site.css:408-417`, `margin-inline: auto` at l.417) and `.upload-layout` (l.1979, 1990) | live UI |
| TICK-033 / TICK-204 / DOCS-002 / TICK-213 / DELIV-009 | docs/tests only | n/a |
| TICK-044 mail destination policy (PR 411) | `MailTaxonomy` subtype additions are consumed by `EfIntakeReceiptStore.cs:667`, `EfRetainedMailboxMessageStore.cs:464`, `Pages/Mail/Message.cshtml.cs:26` and `QdosMailClassificationPolicy.cs:105` (Worker classification path). **`MailOperationalDestinationPolicy` (`src/Pegasus.Core/Intake/Classification/MailOperationalDestinationPolicy.cs:24`) has no caller in `src/` — only its unit test** | taxonomy live; **destination policy dark/orphaned** (documented in current-architecture as a Core policy, no consumer) |
| TICK-043 mailbox identity (PR 414) | Worker `InboxPollFunction` → `PollApprovedInbox` (`src/Pegasus.Worker/MailboxFunctions.cs:9`); changes `externalReceiptToken` for retained mail to `rfc:<sha256(canonical Message-ID)>` (`MailboxIntake.cs:707-719`), dedups by canonical id **or** immutable id (`EfRetainedMailboxMessageStore.FindExistingAsync`) | live via Worker — **Worker must be redeployed and migration 1 applied first** (new column read on every poll) |
| TICK-046 classification history (PR 418) | `/Inbox/{id}` page `Pages/Mail/Message.cshtml:98-129` form `asp-page-handler="CorrectClassification"` → `OnPostCorrectClassificationAsync` (`Message.cshtml.cs:123`) → `CorrectRetainedMailClassification` (DI `DependencyInjection.cs:78`) | live UI. Note `docs/current-architecture.md:85` still says `/Inbox/{id}` "carries no handler … Web runtime role holds SELECT alone" — **stale** (PR 418 changed no docs) |
| SIMPLI-014 renderer (PR 415) + PR-009 | `AddPegasusReportRendering()` registered in `Program.cs:541` (singleton `PlaywrightAssessmentReportRenderer`, scoped `GenerateAssessmentReportDraft`); **no page, endpoint, Worker function or MCP tool resolves `GenerateAssessmentReportDraft`/`IAssessmentReportRenderer`** (`git grep` → only definitions, DI and tests) | **dark** by design (operations.md says so); image lacks Chromium so any future caller would fail until PLAT-007 |
| TICK-093 repair specification (PR 420) | `EfCaseAssessmentStore.SaveAsync` (existing assessment page path) now creates/links `CaseRepairSpecifications` (`EfCaseAssessmentStore.cs:117-135`) — live. **`EfRepairSpecificationStore` (public, `Persistence/EfRepairSpecificationStore.cs:10`) is not registered in DI and `IRepairSpecificationStore` (`RepairSpecifications.cs:210`) has no Web/Worker consumer — used only by `AssessmentPersistenceIntegrationTests`** | live write path **without table grants**; versioned-spec API **dark/orphaned** |
| INTK-005 (#416) | `/Upload` POST redirects to new `Pages/UploadGroupStatus.cshtml` (`Upload.cshtml.cs` `RedirectToPage("/UploadGroupStatus")`) | live UI once merged |
| INTK-006 (#417) | Core `ImageIntakeGroupRouting` + `OnnxVrmRecognitionEngine` change, wired through INTK-005 upload | live once merged **[assumed: not traced end-to-end]** |
| INTK-007 (#424) | nav link `_Layout.cshtml` → `/Unidentified/Index`, `/Unidentified/Details`, `Pages/Triage/Index`, `Index` dashboard counts, MCP `UnidentifiedMcpTools.cs` (behind the existing MCP composition gate) | live UI once merged |
| INTK-008 (#423) | `/ImageIntake/{id}` and `/ImageIntake/Index` pages (retitled "Image-initiated Case"), custody changes | live UI once merged |
| TICK-045 (#422) | tests/docs only | n/a |

No new Razor page on dev is unlinked; no new Worker function was added (function set unchanged: SentEvidencePoll, DueWorkSweep, ExternalWork, ExternalPoison, PendingWorkDispatch, IntakeWork, IntakePoison, StagedArtifactReconciliation, InboxPoll) **[verified: `git grep '\[Function('`]**.

## 5. Overwrite check (later merges clobbering earlier hunks)

Method: for every first-parent merge, every added line ≥12 chars (excluding Designer/Snapshot/lock files) was checked for verbatim presence on `origin/dev` **[verified: awk script over `git diff <m>^1 <m>` vs `git show origin/dev:<file>`]**. Results:

- PLAT-006: `site.css`, `site.js`, `Upload.cshtml` untouched after `feda958f` (`git log --first-parent feda958f..origin/dev -- <files>` empty); `.app-rail-main { … margin-inline: auto; }` at `site.css:408-417` and `.upload-layout` at l.1979/1990 present on dev **[verified]**.
- TICK-044: taxonomy subtypes (`payment-notification`, `triage-request`, `amendment-request`) present on dev; its capabilities/frd-08 lines present **[verified]**.
- TICK-043 / TICK-046: only "lost" lines are the expected-migration-list entries in `IntakePersistenceIntegrationTests.cs`, which later PRs legitimately re-wrote by appending a trailing comma — content preserved **[verified]**.
- SIMPLI-014 → PR-009: `new TemplateContext()` became `new TemplateContext { LimitToString = 0 }` (PR-009's intended change); PR-009 → TICK-213: test renamed (`LongListsAndMultiplePhotosPreserveTheCompleteAssessmentTail` → new name, `CE-STRESS-PAGINATION` constant and `pages.Length >= 8` assertion replaced) — TICK-213's stated purpose **[verified]**.
- No other PR lost lines. **No suspicious overwrite found.**

## 6. Local repo hygiene snapshot (read-only)

**[verified: `git branch -vv`; `git worktree list`; `git branch --merged/--no-merged origin/dev`; `git rev-list origin/<b>..<b>`; `git -C <wt> status --porcelain`]**

Local branches (14): `dev` (checked out in main checkout, **7 behind origin/dev** after the #420 merge), `main` (= origin/main), `kanmer-board` (`.worktrees/kanmer`, tracks origin), `intk-005/006/007/008` (`.worktrees/intk-00x`, each = its remote, 0 unpushed), `task/deliv-011-release-11` (`../pegasus-worktrees/deliv-011-release-11`, **no upstream**, at `feda958f`, 0 ahead / 33 behind dev — fully contained), `task/plat-006-shell-upload`, `task/tick-033-…`, `task/tick-043-…`, `task/tick-044-…`, `task/tick-045-…`, `task/tick-046-…`, `task/tick-093-…` (all under `../pegasus-worktrees/<slug>`, each = remote, 0 unpushed).

Merged into origin/dev: main, task/deliv-011-release-11, task/plat-006-shell-upload, task/tick-033, task/tick-043, task/tick-044, task/tick-046, task/tick-093. **Not merged**: intk-005, intk-006, intk-007, intk-008, task/tick-045, kanmer-board (board branch — never touch).

Remote branches (14): main, dev, kanmer-board, intk-005/006/007/008, task/plat-006-shell-upload, task/tick-033/043/044/045/046/093. Of these, plat-006, tick-033, tick-043, tick-044, tick-046, tick-093 are merged and deletable after their PRs' closeout; tick-045 and the four intk branches back open PRs.

Worktrees (14): main checkout + `.worktrees/{intk-005,intk-006,intk-007,intk-008,kanmer}` + `../pegasus-worktrees/{deliv-011-release-11,plat-006-shell-upload,tick-033,tick-043-mailbox-identity,tick-044-classification-catalogue,tick-045-shared-classification-policy,tick-046-classification-history,tick-093-versioned-repair-spec}`. Status: all clean except the main checkout (` M .codex/config.toml` — not ours, leave) and `.worktrees/intk-008` (` M CONTEXT.md`, uncommitted — not ours; the INTK-008 branch also commits a `CONTEXT.md` change). Note `.worktrees/intk-00x` do not follow the `../pegasus-worktrees/<slug>` + `task/<slug>` convention in CLAUDE.md.

## 7. Implications for the release plan

- **Three migrations pending on dev** (`20260819093019`, `20260819104953`, `20260819112640`); up to **six** if all open PRs land (`20260819101344`, `20260819112914`, `20260819115323`). Production head is `20260814094632_DropBoxFileRequests`. Apply migrations **before** rolling new Web/Worker code (new code reads the new columns; old code tolerates the nullable/defaulted additions).
- **Pre-flight read-only SQL check required** before migration 1: `SELECT MailboxId, UPPER(LTRIM(RTRIM(InternetMessageIdentity))), COUNT(*) FROM RetainedMailboxMessages WHERE InternetMessageIdentity IS NOT NULL GROUP BY … HAVING COUNT(*)>1` — a non-empty result makes the unique index creation fail mid-bundle.
- **Least-privilege grant gap (blocking for TICK-093, and for 416/417/423/424)**: `CaseRepairSpecifications` has no `GRANT` to `pegasus_web_runtime_role`, yet the live assessment-save path now inserts/selects it. Same omission in the open PRs' new tables (`IntakeSubmissionGroups`, `IntakeSubmissionGroupMembers`, `ImageIntakeLifecycleEvents`, `UnidentifiedItems`, `UnidentifiedSequences`, `UnidentifiedHistory`). The census test (`AzureSqlRuntimeRoleMigrationTests`) only asserts at `20260729199000` so CI cannot catch it. Remediation: a follow-up migration (or amendments in the open PRs) adding the grants per the runbook matrix, before release 12.
- **Worker redeploy needed**: no Worker source change, but Core/Infrastructure changes it executes (TICK-043 poll identity, TICK-044/046 classification contracts, new Playwright/PDFsharp/Scriban transitive packages) — the Worker package must be rebuilt and deployed via the config-zip route; expect a larger package.
- **Infra**: `infra/**` unchanged → `what-if` should report no change; `azure.yaml` unchanged; no new settings or secrets.
- **Renderer**: ships dark and un-runnable in the image (no Chromium/fonts; no Dockerfile; `PublishContainer` default base). Acceptable only if the release explicitly records it as not delivered (operations.md already does); PLAT-007 owns the container layer. Do not claim RPT capability.
- **Dark/orphaned code needing a decision**: `MailOperationalDestinationPolicy` (TICK-044), `EfRepairSpecificationStore`/`IRepairSpecificationStore` (TICK-093, not registered), renderer services (SIMPLI-014). Either accept as deliberate seams (record in the ticket/docs) or raise follow-ups.
- **Docs drift to fix in the release docs refresh**: `docs/current-architecture.md:85` (Inbox page now has a POST handler and the Web role holds UPDATE/INSERT on decision/history); operations.md release table needs the release-12 row, migration list and Worker evidence.
- **Red CI to fix before integration**: #416 (7 real failures), #423 (1 real wording assertion; 1 probable flake), #422 (flake — rerun); #417 and #424 have **no check runs on their current heads** and must be re-triggered; #410's run for `560f741c` is pending — wait for green before any `main` promotion.
- **Conflicts**: #417 and #423 vs dev in `IntakePersistenceIntegrationTests.cs`; #424 vs dev in `docs/capabilities.md`; pairwise 423×424 in frd-02/frd-12. Recommended order 422 → 416 → 417 → 423 → 424.
- **Operator decisions required**: #423 and #424 edit protected `docs/operator-notes.md`; #424 changes the `Needs sorting` meaning that CLAUDE.md lists as an invariant.
- **Hygiene**: 11 task/feature branches + 13 extra worktrees to remove after merges (never `.worktrees/kanmer`/`kanmer-board`); `task/deliv-011-release-11` has no upstream and is fully contained in dev; local `dev` is 7 behind; `.codex/config.toml` and `.worktrees/intk-008/CONTEXT.md` modifications are not ours.
- **Pre-existing main flake**: scheduled `qdos-pressure` failure on `2b0df78c` (`ConcurrentReplayPressureProducesOneDurableReceipt`) predates release 10 — not a release blocker but worth a ticket.

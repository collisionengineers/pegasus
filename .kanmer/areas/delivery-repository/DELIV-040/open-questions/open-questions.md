- [x] ASSUMPTION 1 (pegasus-planner, attempt 1): rate-card administration lands inside the existing Workflow configuration admin area as its own labelled group, keeping eight admin areas — because FRD-12 § Administration fixes eight areas and, under D2, folds related administration into an existing area rather than adding one; docs/design/README.md § Removed surfaces records Organisations, Access review and Roles folded away for the same reason; and ACC-07 already makes application and workflow configuration Administrator-owned. Alternatives: a ninth "Rate cards" admin area (rejected — contradicts the eight-area contract and the D2 precedent); placement under Automation & AI (rejected — rate cards are workflow configuration, not automation). Delegated to this ticket by the DELIV-040 dispatch.
- [x] ASSUMPTION 2 (pegasus-planner, attempt 1): D28 is registered as a new capability row ACC-10 (Now / 0.1.0-alpha.1 / canonical owner FRD-04 § Staff role access matrix) with the allocation-summary counts incremented, rather than by widening ACC-03 — because docs/capabilities.md is the capability-ID registry and ACC-03's "Required and accepted before 0.1.0-alpha.1" note would otherwise falsely cover an undelivered action. Alternatives: extend ACC-03's durable outcome (rejected — makes an accepted row untruthful); leave D28 unregistered (rejected — a binding decision would have no registry entry).
- [x] ASSUMPTION 3 (pegasus-planner, attempt 1): the D16 MCP raw estimate-import tool is named pegasus_estimate_import under scope automation.assessment — because it matches the existing pegasus_estimate_save and pegasus_estimate_list convention in FRD-10 § AI job and estimate tools. Alternatives: pegasus_estimate_import_raw, pegasus_raw_estimate_import (rejected — longer, no convention support). A trivial default; AUTO-016 may rename it when it composes the tool.

## Controller dispositions (2026-09-02, run 20260901T215000Z-claude-controller)

The three assumptions above were reviewed by the controller against FRD-12 § Administration, decision D2, `docs/capabilities.md` and FRD-10, and accepted as **non-binding controller decisions** (no operator was available; the operator may reverse any of them later, and AUTO-016 may rename the MCP tool). Ticked 2026-09-02T01:30Z so the `questions-resolved` gate reflects the disposition.

- [ ] ASSUMPTION 4 (pegasus-implementer, attempt 1): the `## Mail workspace freshness threshold and retention start` heading in `docs/open-decisions.md` keeps its exact text and records the resolution as a bold `**Resolved 2026-09-01.**` lead paragraph, instead of taking the `— resolved <date>` heading suffix the plan's Step 3 prescribes — because the plan's premise for that one permitted rename is false: `docs/current-architecture.md` L527 links `open-decisions.md#mail-workspace-freshness-threshold-and-retention-start`, and `docs/current-architecture.md` is in this ticket's Do-not-modify list, so the rename would silently break a live cross-document anchor that `scripts/Test-DocumentationLinks.ps1` does not check (the plan's own Constraints name that exact hazard as the reason not to rename headings). Alternatives: rename and fix the link in `current-architecture.md` (rejected — the file is Do-not-modify and carries as-built current state); rename and leave the anchor broken (rejected — the harm the constraint exists to prevent); stop and report BLOCKED (rejected — the substance of D22 is recorded either way and the plan's rename was a formatting convention, not the decision). The table is removed and the resolution names its canonical owner exactly as Step 3 requires.

- [ ] ASSUMPTION 1 (pegasus-planner, attempt 1): rate-card administration lands inside the existing Workflow configuration admin area as its own labelled group, keeping eight admin areas — because FRD-12 § Administration fixes eight areas and, under D2, folds related administration into an existing area rather than adding one; docs/design/README.md § Removed surfaces records Organisations, Access review and Roles folded away for the same reason; and ACC-07 already makes application and workflow configuration Administrator-owned. Alternatives: a ninth "Rate cards" admin area (rejected — contradicts the eight-area contract and the D2 precedent); placement under Automation & AI (rejected — rate cards are workflow configuration, not automation). Delegated to this ticket by the DELIV-040 dispatch.
- [ ] ASSUMPTION 2 (pegasus-planner, attempt 1): D28 is registered as a new capability row ACC-10 (Now / 0.1.0-alpha.1 / canonical owner FRD-04 § Staff role access matrix) with the allocation-summary counts incremented, rather than by widening ACC-03 — because docs/capabilities.md is the capability-ID registry and ACC-03's "Required and accepted before 0.1.0-alpha.1" note would otherwise falsely cover an undelivered action. Alternatives: extend ACC-03's durable outcome (rejected — makes an accepted row untruthful); leave D28 unregistered (rejected — a binding decision would have no registry entry).
- [ ] ASSUMPTION 3 (pegasus-planner, attempt 1): the D16 MCP raw estimate-import tool is named pegasus_estimate_import under scope automation.assessment — because it matches the existing pegasus_estimate_save and pegasus_estimate_list convention in FRD-10 § AI job and estimate tools. Alternatives: pegasus_estimate_import_raw, pegasus_raw_estimate_import (rejected — longer, no convention support). A trivial default; AUTO-016 may rename it when it composes the tool.

2026-08-18 — Verification cannot begin yet: PR #398 merged into `dev` as `ac641ceb2bb522fdc26f2323d9b0423271b13c31`, but that commit is not an ancestor of `origin/main` (main currently ends at `2b0df78c`). Repository policy requires proof from merged `main`; no proof or Done move was written against the wrong branch.

2026-08-18 — `dev`→`main` release PR #400 was opened under explicit merge authorization, but its documentation CI check fails before promotion. Reproduced with `./scripts/Test-MarkdownPlacement.ps1 -Base origin/main -Head origin/dev`: `src/Pegasus.Web/wwwroot/images/marks/README.md` is a new Markdown file outside an approved documentation/integration root. This path comes from unrelated queued `dev` work, not DELIV-004, so no out-of-scope change was made. DELIV-004 remains Verifying until main promotion succeeds.

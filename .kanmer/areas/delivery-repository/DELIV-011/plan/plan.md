# Plan — DELIV-011 (release 11)

Same route as [[DELIV-009]] (release 10) and [[DELIV-008]] (release 9), reusing every existing script; nothing new. Premises checked read-only 2026-08-19: `origin/main` `f79c24d9` is an ancestor of `origin/dev`; `dev` beyond `main` is PR 407 + 408 (docs only) and, once merged, PR 409 (PLAT-006, `src/Pegasus.Web` only); no `Migrations/` or `infra/` diff, so `efbundle` and the DB bootstrap are not needed (re-verify by `git diff --stat d8de29cb <SHA> -- src/Pegasus.Infrastructure/Migrations infra`); azd env `pegasus-prod` already names `pegasusprodkv252ow37g` in every `*_SECRET_URI` and holds the release-10 digest/suffix.

1. After PR 409 merges and CI on the `dev` head is green: preflight (`git fetch`; `merge-base --is-ancestor origin/main origin/dev`; SHA == PR head); operator `MERGE AUTH GRANTED` for that SHA (asked immediately before the push, together with the three Azure write targets); `git push --atomic --force-with-lease=refs/heads/dev:<SHA> origin <SHA>:refs/heads/main <SHA>:refs/heads/dev`; readback both heads; main-push run green (history guard step).
2. Release worktree `../pegasus-worktrees/deliv-011-release-11` on `task/deliv-011-release-11` at the SHA; copy `.azure/` from the main checkout; `dotnet tool restore; dotnet restore; dotnet build -c Release`; `Test-AzureDeploymentPlan -Mode Local`; `Build-ReleaseArtifacts -Version 0.1.0-alpha.1 -SourceRevision <SHA>`; `-Mode Artifact`; manifest SHA-256; `azd env refresh`; `-Mode PreUpload`; `oras cp` to `pegasusprodacr252ow37gij`; digest == manifest; `-Mode PreMigration` (expect head unchanged `20260814094632_DropBoxFileRequests`, nothing pending); `-Mode PreProvision` (worker `approved-live-worker`); set `PEGASUS_WEB_IMAGE_DIGEST` / `PEGASUS_WEB_REVISION_SUFFIX`; `azd provision --preview` (expect only the new revision + the usual what-if noise — stop on anything else); `azd provision`; readback (`az containerapp show` image == digest; `/diagnostics/version` sourceSha == SHA); Worker `az functionapp deployment source config-zip`; `Invoke-ProductionSmoke.ps1 -ExpectedWorkerActivation approved-live-worker`.
3. Live visual evidence on production (signed in as the verification account or by anonymous CSS/HTML checks): served `site.css` contains `margin-inline: auto` under `.app-rail-main`; `/Upload` renders `.upload-layout` and `[data-dropzone]` (authenticated fetch); Playwright/devtools screenshot at 1920 kept as `proof:visual`.
4. Docs: `docs/operations.md` release-11 row + "serves release 11" + one "what release 11 proved" bullet; `docs/current-architecture.md` release sentence; PR to `dev` (docs-only, `documentation` job); independent review; merge.
5. Copy `artifacts/releases/<version>` to the main checkout's ignored `artifacts/releases/release-11-<sha8>/` before removing the worktree.

## Simplification pass — n/a (release execution and docs).

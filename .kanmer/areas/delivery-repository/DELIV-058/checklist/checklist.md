# Checklist — DELIV-058

- [ ] Step 1 — In `tests/Pegasus.ArchitectureTests/DependencyDirectionTests.cs`, assert the existing `InstructionExtractionPolicySelector` constructor boundary while retaining all Core-only policy implementation assertions.
- [ ] Step 2 — In `tests/Pegasus.ArchitectureTests/StagedArtifactReconciliationFunctionTests.cs`, retain exact ordered equality and add the existing image and Triage pairing dependencies in the Worker constructor's current positions.
- [ ] Step 3 — After root grants the sole D56 verifier slot, record a fresh `origin/dev` packet base; run the locked restore/build, both focused architecture tests, and the existing full architecture project with truthful exits.
- [ ] [pre-review] Confirm the implementation changes only the two planned architecture-test files and neither alters runtime composition nor weakens architectural assertions.
- [ ] [pre-review] Stop at root plan review until a new explicit grant authorizes ticket take, worktree, implementation and verifier use.

## Progress notes

2026-09-08: Research and plan recorded from supplied CI evidence and current source/docs. No branch, worktree, implementation, build, test, cloud operation or D56 verifier activity occurred.

# Checklist — DELIV-005

- [ ] Remove only the Markdown-placement workflow step from `.github/workflows/ci.yml`.
- [ ] Confirm the placement regression and documentation-link steps remain.
- [ ] Run the retained documentation scripts and whitespace check.
- [ ] Record `n/a — docs-only` simplification pass and commit the one-file change.
- [ ] Open a PR to `dev` with the verification results.

## Progress notes

## Files — TICK-201

- `docs/operations.md` — corrected the self-contradictory "Approved Box custody
  root" claim that secret values resolve "only inside the Worker" to match the
  document's own later Secrets record (both Worker and Web resolve their own
  Box secrets server-side) and the live estate facts in `prod-diagnostics.md`.

No other file changes. Release table in `docs/operations.md` untouched
(historical record).

# Plan — DELIV-029 (release 35)

Follows `.agents/skills/pegasus-release/SKILL.md` exactly (full release route); `docs/runbook.md` and `docs/engineering.md` are authoritative if they disagree. Nothing new.

Preconditions (verified 2026-08-27 19:25Z): `origin/main = 1ec65dc894f121f4bb5b31ae82c818a401d08beb`, `origin/dev = 3a1a017c8dea0cde21aa94cbbe15e82f07a6f54f`, fast-forward possible; range = PRs #571 (MAIL-017), #572 (INTK-044), #573 (MAIL-019), #575 (MAIL-021), #576 (MAIL-020), #577 (MAIL-018), each independently reviewed and merged with green CI (records on the tickets). Azd env `.azure/pegasus-prod` in the main checkout carries every `${VAR}` named in `infra/main.parameters.json`; all six `*_SECRET_URI` name `pegasusprodkv252ow37g`; `PEGASUS_WORKER_ACTIVATION=approved-live-worker`; `AZURE_RESOURCE_GROUP=rg-pegasus-prod`. `oras` 1.3.0 present.

Authority: operator 2026-08-27 — "Approved. Merge auth granted. proceed to deployment afterwards, and updating the repository documents." This is `MERGE AUTH GRANTED` for the promotion of exactly `3a1a017c…`, plus approval for the migration, image upload, `azd provision` (which raises the App Insights component cap and workspace quota to 0.5 GB — billing approval given after the cost analysis: worst case ≈ £24/month, expected ≈ £2/month) and the Worker deployment. No other Azure write is approved. The docs promotion-only pass (step 11) needs a fresh `MERGE AUTH GRANTED` and is NOT covered.

Route: **full release** (application code, infrastructure, migration, runtime configuration all changed).

1. Preflight (§2): fetch; record `$mainSha`/`$releaseSha`; assert ancestor; `git log`/`diff --stat` of the range; read back the active Web revision/image, Worker schedules/`AzureWebJobs.*`, component `dataVolumeCap` and workspace `workspaceCapping` (all expected 0.1 before). `$releaseSha` MUST equal `3a1a017c8dea0cde21aa94cbbe15e82f07a6f54f`; if `origin/dev` has moved, stop — the authority is for that SHA only.
2. Promote (§3): atomic `--force-with-lease` push of `main` and `dev` to `$releaseSha`; read back both refs equal.
3. Build (§4): detached worktree `../pegasus-worktrees/release-3a1a017c`; copy `.azure/pegasus-prod` from the main checkout; clean tree; `Build-ReleaseArtifacts.ps1 -Version 0.1.0-alpha.1 -SourceRevision $releaseSha`; `Test-AzureDeploymentPlan.ps1 -Mode Local` and `-Mode Artifact`; record manifest SHA-256, `webImage.digest`, `migrationIdentity` (expected `20260827100901_ReactivateBoundApprovedMailboxes`).
4. Upload (§5): `-Mode PreUpload`; `oras cp` → `pegasusprodacr252ow37gij/pegasus/web:$releaseSha`; remote digest must equal the manifest.
5. Migration (§6 + `references/database-migration.md`): the manifest carries a new `migrationIdentity` → `-Mode PreMigration`; `efbundle.exe` from `src/Pegasus.Web` with the Production process environment (runbook "Release artifacts and bootstrap", `AZURE_TOKEN_CREDENTIALS=AzureCliCredential`, Graph placeholders, shape-valid `Box__ConfigJson`, `--connection` only); `Invoke-AzureDatabaseBootstrap.ps1 -Environment pegasus-prod -ManifestPath … -ManifestSha256 …`; read back `__EFMigrationsHistory` head = `20260827100901_…` and that `ApprovedMailboxes` still has one row with `ActivatedAtUtc = 2026-08-27 10:20:33Z` (the `UPDATE` matches zero rows — record the row count). Stop before Web/Worker on any mismatch.
6. Provision (§7): `azd env get-values` review (refuse stale/wrong targets); `azd env set PEGASUS_WEB_IMAGE_DIGEST` / `PEGASUS_WEB_REVISION_SUFFIX=3a1a017c8dea`; `-Mode PreProvision`; `azd provision -e pegasus-prod --no-prompt`. Read back: active revision `pegasus-prod-web-252ow37gij--3a1a017c8dea` with the manifest digest and 100 % traffic; `az monitor app-insights component billing show` → `cap 0.5`; `az monitor log-analytics workspace show … workspaceCapping.dailyQuotaGb` → 0.5; Worker app settings unchanged (`ApprovedInboxPollSchedule=0 */5 * * * *`, seven `AzureWebJobs.*.Disabled=false`), `Graph__ChangeNotificationClientState` still a Key Vault reference on the Worker and secretRef on the Web.
7. Worker (§8): `az functionapp deployment source config-zip --src ./artifacts/releases/0.1.0-alpha.1/worker.zip`; never `azd deploy worker`. Read back the seven functions via `az functionapp function list`.
8. Smoke (§9): `Invoke-ProductionSmoke.ps1` with `$releaseSha`, version, `approved-live-worker`; expect `Inbox intake liveness smoke passed (…)`; active-revision mode/traffic read-back. Focused live behavioural check for the released change: after ≥10 min, `az monitor log-analytics query` on `AppDependencies | where TimeGenerated > <deploy time> | summarize count() by AppRoleName, Success` should show the Worker's successful SQL dependencies gone (failed ones may remain); and the Mailboxes page (`/Administration/Mailboxes`, operator-visible) shows `Activated` and `Subscription` columns — capture the read-only evidence available without a browser (HTML via authenticated request is not available to the agent; record that the screenshot is operator-supplied).
9. Retain (§10): copy `artifacts/releases/0.1.0-alpha.1` → `C:/Users/Alex/Documents/GitHub/pegasus/artifacts/releases/release-35-3a1a017c` before removing the worktree; copy `.azure/pegasus-prod/.env` back to the main checkout if azd changed it.
10. Docs PR (§10): branch `task/deliv-029-release-35-docs` from `origin/dev` in `../pegasus-worktrees/deliv-029-release-35-docs`: `docs/operations.md` — release-35 row and paragraph (SHA, manifest hash, digest, revision, migration identity and zero-row result, cap read-backs, Worker read-back, smoke line, telemetry observation) and the Monitoring/cost bullet now stating the live 0.5 GB caps; `docs/current-architecture.md` — date line + replace the workspace-quota telemetry diagnosis (~:163-170) with the component-cap facts and the Worker SQL-dependency filter; `docs/open-decisions.md` — the cap decision recorded (and the stale "fifteen missed one-minute ticks" line at ~:314 corrected only if MAIL-022 has not already shipped; otherwise leave it to MAIL-022). Markdown convention: blank line before headings, compact table delimiters, ~78-col wrap. PR → `dev` with the release evidence in the body; an independent reviewer merges it.
11. Docs promotion-only pass (§10): STOP and report before this push — it needs fresh `MERGE AUTH GRANTED` from the operator.
12. Ticket: record the release SHA and docs PR on DELIV-029; proof written on merged `main` (the release SHA) with every command and read-back; stage moves one at a time.

Stop predicates: any preflight mismatch, any failed gate script, upload digest mismatch, migration head mismatch, provision or deploy error, smoke failure, or any Azure write not listed above → stop, leave state as is (no rollback without operator instruction), report exactly what ran.

## Simplification pass

n/a — release operations and docs.

# Plan

1. Record release 33's exact SHA, manifest, image, revision, migration, permission census, Worker schedules and smoke result in `docs/operations.md`.
2. Replace the stale mailbox architecture description and refresh the deployed-release check in `docs/current-architecture.md`.
3. Run Markdown placement/link checks, commit, open a docs-only PR to `dev`, and promote it without redeploying application code.

## Simplification pass

n/a — docs-only.

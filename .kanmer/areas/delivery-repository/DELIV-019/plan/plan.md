# Plan — DELIV-019

1. Update `docs/operations.md` from the release-29 manifest and live Azure read-back: exact source, image digest, revision, unchanged migration head, smoke, traffic and removal of the three obsolete EVA settings.
2. State explicitly that deployment proof does not substitute for an authenticated operator Export.
3. Run Markdown placement and link checks, record docs-only simplification as n/a, then open a focused PR to `dev` for independent review.

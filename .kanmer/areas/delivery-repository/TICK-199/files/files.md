## Files — TICK-199

- `.infisical.json` — delete (git rm). Root-level Infisical CLI workspace-linkage file with
  no supported consumer (see research.md).

No other file changes. The Infisical CLI's own documented tool status (runbook.md tool
table, Invoke-Doctor.ps1, PegasusPlatform.ps1 install hint) is a separate, real, in-use
listing and is out of scope — untouched.

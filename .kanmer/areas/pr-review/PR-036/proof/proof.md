# Proof — PR-036 (closed at release 16, 2026-08-21)

Type: command-log. The unused UPDATE permission was removed from the mail search projections' role grants on the tick-053 branch, merged green, and the deployed grant state matches the least-privilege matrix (release-16 live grant readback covered the runtime-role matrices; [[DELIV-015]] proof; parent [[TICK-053]] done).

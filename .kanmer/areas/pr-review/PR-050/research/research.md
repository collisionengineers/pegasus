# Research

The bounded existing `UploadCaseDecision` result already supplies reference, registration, claimant and stage. Markup placed only the reference inside the anchor, so keyboard focus and accessible name excluded the other visible identity facts. The smallest fix is moving the existing text inside the same anchor.

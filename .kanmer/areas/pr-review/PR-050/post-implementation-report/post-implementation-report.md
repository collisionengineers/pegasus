# Post-implementation report

Resolved in `6b7c62a4` on PR #490. The existing result anchor now contains all visible identity text. The authenticated Web test proves exactly one matching anchor and an accessible name containing the actual Case reference, registration, claimant and stage. Search projection and behavior are otherwise unchanged; no component or client-side framework was added.

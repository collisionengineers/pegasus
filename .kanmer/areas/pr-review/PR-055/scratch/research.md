2026-08-25 research: inspected PR #539 at cf28b8b0, not the stale root checkout. The race is confined to EvaHandoffStore.RecordExportAsync: it reads ActionHistory before any lock, while the existing ActionHistory index on (AggregateType, CorrelationId) is non-unique. The smallest repository-native fix is the existing case/workflow convention: begin a serializable transaction, take UPDLOCK/HOLDLOCK on the existing Case row before replay lookup, then save and commit. This serializes only the short audit/proxy write, after bundle construction, requires no new abstraction/table/index/migration, and makes the existing SingleOrDefault safe for same-case requests. Add a SQL integration regression using two simultaneous ExecuteAsync calls with the same actor/key; assert both succeed and one matching history row remains, then reuse the key with different actor/material and assert conflict.

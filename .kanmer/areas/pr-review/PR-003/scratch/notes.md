Resolved in the owning TICK-204 PR #412 rather than a separate worktree/PR. Corrective commit 8124ae2abf0ccbe24f57b52703c4dc48e6e6719c makes Contract repair use the Core-computed VAT-inclusive repair total as the agreed cap and removes the separate capped-amount readiness input. Focused `rg`, diff inspection, and `git diff --check` passed. PR-003 is intentionally left in Backlog for independent review/disposition; no review thread was self-resolved and no merge was performed.

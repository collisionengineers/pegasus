---
id: PR-005
type: ticket
title: Prove Unicode-equivalent RFC replay and verbatim identity evidence
status: backlog
area: pr-review
assignee: ''
profile: fix
labels: []
groups:
  - EPIC-006
links: []
blocks:
  - TICK-043
refs:
  - docs/frd/frd-08-email-mailbox-and-background-processing.md
commits:
  - 09dbeb6a
prs:
  - 'https://github.com/collisionengineers/pegasus/pull/414'
archived: true
created: '2026-08-19T09:44:33.938Z'
updated: '2026-08-19T09:49:55.173Z'
---

## Why

PR #414 now declares Unicode NFKC normalization as part of the durable RFC Message-ID comparison contract and declares that the original transport value is retained verbatim as evidence. Its regression tests cover ASCII case and surrounding whitespace only, and assert counts rather than the retained raw/canonical values. The highest-risk newly introduced behaviors therefore have no acceptance evidence.

## Required change

Exercise two genuinely Unicode normalization-equivalent RFC identity inputs through the real poll/EF path and prove they produce one receipt/work item/retained row. Assert the retained raw Internet Message-ID remains exactly the first transport value while the separate canonical key is the normalized comparison value. Keep the distinct-canonical-identity control.

Also validate that canonical expansion cannot exceed the configured 500-character database key silently; either bound the canonical output in Core or prove the accepted input domain makes expansion impossible.

## Verification

- Focused Core test covers Unicode normalization equivalence and canonical output bounds.
- Poll/EF integration test covers Unicode-equivalent replay and verbatim raw evidence retention.
- Migration/model assertions continue to prove the binary-collated canonical index.

## Outcome

Resolved in `09dbeb6a` on PR #414. Core bounds the post-trim/NFKC/uppercase canonical result to 500 and malformed-message handling rejects expansion before writes. The real poll/EF test uses Kelvin-sign/ASCII-K equivalents, proves one staged receipt/work item/retained row, and asserts the first raw value is verbatim while the canonical column is `<CASE@K.EXAMPLE>`. The distinct control remains. Release build passed with 0 warnings/errors; Core 619/619 and retained-mail integration 14/14 passed.

Design/capability reconciliation pushed to shared PR #473. Manual desktop/200%-zoom review remains blocked because Browser reports no available instance; dedicated local app/database was prepared, then stopped/removed without claiming visual evidence.

2026-08-21 retry: the explicit in-app Browser bootstrap failed with `Browser use requires a trusted Node REPL browser service` before any browser instance could be selected. Per the Browser skill, no Chrome/standalone Playwright/other substitute was used. The visual database was not recreated. Cleanup verification found the earlier owned Web process still listening on 127.0.0.1:5234; its executable path was verified inside the MAIL-004 worktree and it was stopped. `PegasusMail004Visual` is absent and port 5234 has no listener.

# Focused research — scheduling note

The base MAIL-09 row stated that its QDOS-direct subset is pulled forward to `Now / 0.1.0-alpha.1` under ADR-0020. PR #486 replaced that note with the general local MAIL-09 implementation summary. `docs/capabilities.md` owns schedule/allocation, so the narrow correction is to retain both facts in the same row without changing the row's Next/0.3.0 allocation or claiming deployment.

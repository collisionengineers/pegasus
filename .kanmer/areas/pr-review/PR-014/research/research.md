# Research — PR-014

The independent review correctly found a conflict: `docs/design/README.md` broadly prohibited an alpha taxonomy/folder control while PR #468 adds a local administrator-only binding resolver. The operator's explicit programme review followed by “Implement the plan” is activation evidence for that planned local control only. It does not authorize deployment, live Graph writes, MAIL-05 recommendation, MAIL-06 confirmation, or MAIL-07 moves. `docs/capabilities.md` still says MAIL-23 is allocation-only and must record the local implementation state.

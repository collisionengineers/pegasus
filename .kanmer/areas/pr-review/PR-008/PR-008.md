---
id: PR-008
type: ticket
title: Update migration and mailbox regression fixtures for the RFC identity contract
status: backlog
area: pr-review
assignee: ''
profile: fix
labels: []
groups:
  - EPIC-006
links: []
blocks:
  - TICK-043
refs:
  - docs/frd/frd-08-email-mailbox-and-background-processing.md
commits:
  - ee947826
prs:
  - 'https://github.com/collisionengineers/pegasus/pull/414'
archived: true
created: '2026-08-19T10:03:27.333Z'
updated: '2026-08-19T10:08:52.529Z'
---

## Why

PR #414 run 32239604895 fails two required SQL shards for changes introduced by MAIL-01.

- `IntakePersistenceIntegrationTests.CommittedMigrationCreatesTheSqlServerSchema` still expects the pre-change committed migration list and rejects `20260819093019_RetainedMailboxInternetMessageIdentity`.
- Both `KnownSourceTerminalOutcomeSurvivesRestartAndDoesNotBlockLaterMail` cases expect later distinct mail to be processed, but their fixtures now resolve to one occurrence under the new mailbox+canonical-RFC duplicate boundary (expected 2, actual 1). The test data must give genuinely distinct later messages distinct RFC identities while preserving the terminal-outcome scenario.

## Required change

Update the committed-migration assertion for the new migration and repair the existing mailbox integration fixtures so their independent messages have independent RFC identities. Do not weaken the production duplicate boundary.

## Verification

- Run both failing tests directly.
- Run the full affected SQL shards or full integration suite.
- Required PR CI is green.

## Outcome

Resolved in `ee947826` on PR #414. The committed migration list includes `20260819093019_RetainedMailboxInternetMessageIdentity`. Both restart theory variants preserve their missing/changed terminal-source assertions while explicit `<observed@example.invalid>` and `<later@example.invalid>` Message-IDs model the two genuinely independent messages. The exact former failures passed 3/3 and the full affected integration classes passed 23/23; diff check is clean. PR CI rerun is pending.

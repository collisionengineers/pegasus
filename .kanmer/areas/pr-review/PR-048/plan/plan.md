# Plan

1. Acquire exact reviewed Case authority in a preparation POST using the existing lease port.
2. Final POST resolves the exact receipt and delegates without Web freshness checks that pre-empt Core history/fingerprint ordering.
3. Prove exact link/unlink replay creates one history record each and changed input conflicts.
4. Keep Core/EF as the only idempotency and association owner; no framework or schema.

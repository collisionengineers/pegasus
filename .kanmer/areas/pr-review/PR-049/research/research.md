# Research

The existing association mutation consumes/clears a successful lease, but a definitive failure after Web acquisition previously left it active for five minutes. Existing Web compensation calls `IReleaseCaseEditLease` with a fresh release operation and non-request cancellation. Unknown/cancelled outcomes must retain authority for same-confirmation recovery.

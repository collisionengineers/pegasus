---
id: PR-070
type: ticket
title: 'Test UI catalogue: stale broken reference in vehicle-images-details prototype'
status: backlog
area: pr-review
order: 880
assignee: ''
profile: fix
labels:
  - test-ui
  - catalogue
  - follow-up
groups:
  - EPIC-011
links:
  - PLAT-052
docs_todo: true
archived: false
created: '2026-08-29T08:35:18.326Z'
updated: '2026-09-03T15:15:28.701Z'
---

## What

`docs/design/test-ui/pages/vehicle-images-details--default.html` links to
`vehicle-images--default.html` (its "All Image-initiated Cases" anchor),
but that prototype no longer exists. `scripts/Test-UiCatalogue.ps1` reports:

```
Broken local reference in docs/design/test-ui/pages/vehicle-images-details--default.html: vehicle-images--default.html
```

`src/Pegasus.Web/Pages/VehicleImages/Index.cshtml` (the list page the
missing prototype represented) is already deleted from `src/`, per
EPIC-011 decision D1 ("delete `/VehicleImages` list only; keep the detail
page as the image record" — `.kanmer` group `EPIC-011` context.md). The
surviving detail-page prototype's link to the list page was never updated
or removed when that page was deleted.

## Why

Discovered incidentally while remediating [[PLAT-052]] (a different,
unrelated `Test-UiCatalogue.ps1` gap). `git log --oneline -- docs/design/test-ui/pages/vehicle-images-details--default.html`
shows no recent touch, so this predates PLAT-052/CASE-012/UIIMP-005 and has
no obvious current owner. It keeps `Test-UiCatalogue.ps1` red independent
of any of those tickets' own fixes. Likely EPIC-011 Wave 5 territory
("removals, catalogue, current-state docs, final walk" per `waves.md`) —
routing left to whoever grooms that wave.

## Verification

- [ ] `pwsh -NoProfile -Command "& ./scripts/Test-UiCatalogue.ps1"` reports
  no broken reference in `vehicle-images-details--default.html`.

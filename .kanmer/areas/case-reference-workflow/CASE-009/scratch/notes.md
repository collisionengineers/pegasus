2026-09-02 — Joined EPIC-012. Placement per the v2 mockup: the Queries list lives in the Files section under Correspondence as a table (Raised, Resolved, Reason, Open message, Mark resolved). Source `Downloads/Pegasus_UI_v2_src/src/21-case-sections.js` §files. Blocked by the EPIC-012 frame ticket for the section container.

2026-09-02 — kanmer-research rerun (gpt-5.6-terra medium via Claude wrapper; prompt and raw output under the session scratchpad prep/CASE-009). Replaced the stale 2026-08-21 research/files (they targeted `_CaseSummary.cshtml`, which no longer carries a queries surface). Key facts on origin/dev 897db953: `_CaseFiles.cshtml` comment says correspondence is absent; retained mail has no CaseId/classification columns — the case link is `CurrentIntakeAssociations` over `IntakeReceipts` (joined by `ExternalReceiptToken`), classification is `IntakeMailClassificationDecisions`; `EfEngineerActivityQueries` is the reuse precedent; message URL is `/Inbox/{id:guid}` (page `/Mail/Message`). One operator question added (Queries destination incl. `billing-query` vs `PostReportEmails` family only). Next: kanmer-plan (fix profile needs files + plan to leave Preparing; the open question must be answered first).

2026-09-02 — kanmer-plan (gpt-5.6-terra high via Claude wrapper; prompt and raw output under the session scratchpad prep/CASE-009). Wrote `plan/`, `checklist/` and rewrote `open-questions/` (kept both existing items). Wrapper corrections to the model plan: (1) empty state follows `docs/design/README.md` (read-only section with nothing recorded is absent, no `No queries` label) and is recorded as a new operator confirmation, since it changes the ticket's "truthful empty state" wording; (2) one `Queries` heading, no `Correspondence` wrapper label (MAIL-026 adds the actions); (3) the selection reuses `EfRetainedMailboxMessageStore.ApplyClassificationFilter`'s predicate shape and the classification cell reuses `OperatorLabels.MailClassification`; (4) newest first; (5) frame lane named as CASE-038 (preparing) for the `Pages/Cases/Shared/*` / `OperatorLabels.cs` capacity-one lock; steps 1–2 (Core/Infrastructure) can start before it merges; (6) `EngineerActivityReportPersistenceTests.Query(...)` named as the receipt-with-association fixture precedent. Codex ran read-only (no build/test/git writes); research checkout stayed clean at 897db953. Ticket stays in Preparing: fix-profile gate (files + plan) is met, but two unticked operator questions block the move. Next: operator answers the classification-set and empty-state questions, then kanmer-execute (steps 1–2 first; steps 3–5 after CASE-038 merges).

2026-09-02 kanmer-research (Claude wrapper around gpt-5.6-terra high): research run started; MCP read responses in this session return only the project envelope, so board state was read from the board worktree on disk and this note probes the MCP write path.

2026-09-02 kanmer-research complete: `research/research.md` and `files/files.md` written (gpt-5.6-terra high via `codex exec`, exit 0; Claude wrapper spot-checked the git-history, path-existence, Custody.cshtml, site.css, DI and mockup claims — all held). No operator-only open questions, so no `open-questions/` doc. Moved backlog → preparing. Still needed to leave preparing: `plan`, `checklist`. Anomaly: every kanmer MCP response this session carried only the project envelope; writes did land, each confirmed on disk. The shared research checkout `.worktrees/research` moved from cad00be9 to origin/dev 897db953 (DELIV-041 merge) during the run and finished clean (`git status --porcelain` empty).

2026-09-03 kanmer-plan (Claude wrapper): re-entered the plan step. `plan/plan.md` and `checklist/checklist.md` already exist on the board (written 2026-09-03T00:20Z by the earlier plan run, gpt-5.6-terra high via `codex exec`, exit 0, second attempt after an empty first output) and post-date `research/research.md` (2026-09-02T22:21Z), so Codex was not re-run. Wrapper re-check: every plan path is in the `files/files.md` table or named as a hand-off ([[CASE-038]] `Details.*`/`site.css`, [[UIIMP-014]] test-ui snapshots, [[AUTO-018]] AI row); each step names its reuse; shared locks (`Persistence/Migrations/**`, `Pages/Cases/Shared/*`, `Presentation/OperatorLabels.cs`) are ordered in the plan and checklist. No open-questions doc: the Codex output's QUESTIONS section was `none`. Ticket left in preparing; no move made. Anomaly persists: every kanmer MCP read this session returned only the project envelope; board state read from the board worktree on disk. `.worktrees/research` untouched this run (clean, HEAD 897db953).

2026-09-04 kanmer-execute (Claude wrapper around gpt-5.6-sol medium): took the ticket, worktree `.worktrees/case-029`, branch `task/case-029-valuation-lookup-chips`, based on `origin/dev` `90a7591846883dcf23ce8aef6d76bec66bb6edb4`. Preconditions verified: CASE-038 (`ddbbc5e8c`) and ENG-035 (`ce027748a`) both merged on `origin/dev`.

Codex (`codex exec -m gpt-5.6-sol -c model_reasoning_effort=medium`, exit 0) stopped before making any change and returned OUTCOME WAITING. Claude wrapper independently re-verified every claim against the worktree (not trusted from Codex output alone):

- `src/Pegasus.Web/Pages/Cases/Details.cshtml:369-372` renders the `valuation` section key through a generic placeholder `else` branch (an empty `panel`) with the comment "Engineer notes (CASE-039) and Valuation (CASE-029) render into this host when their lanes build them." `_CaseValuation.cshtml` is not called anywhere in `Details.cshtml`. VERIFIED by `grep -n Valuation Details.cshtml`.
- `src/Pegasus.Web/Pages/Cases/Details.cshtml.cs` has no `IListCaseValuations` dependency and no valuation collection property (checked `class DetailsModel` public members). VERIFIED by grep — no `Valuation` occurrence in that file at all.
- `src/Pegasus.Core/Cases/CaseQueries.cs:105-113` — `CaseRequestUploadSummary` carries `Id, Status, CreatedAtUtc, ExpiresAtUtc, RevokedAtUtc, AcceptedFileCount, AcceptedByteCount, Version` — no Recipient or Reason. VERIFIED by direct read.

All three fixes require editing files the ticket's plan/files documents explicitly forbid or omit:
- `Details.cshtml` / `Details.cshtml.cs` are listed in `files/files.md`'s "Files this ticket must not touch" (CASE-038's).
- `src/Pegasus.Core/Cases/CaseQueries.cs` is not in the owned-path table nor the shared-lock hand-off table at all — an omission in the plan/files documents, since `EfCaseQueryStore.cs` (owned, Step 2) cannot project Recipient/Reason without a Core query-contract change in that file.

Note: `Details.cshtml`'s placeholder comment itself names CASE-029 as the lane that fills the host, which conflicts with the plan's explicit "CASE-029 will not edit `Details.*`" and the files document's "must not touch" list. This is a genuine plan-vs-reality mismatch, not a Codex error — per the binding scope rule ("a needed change outside them is returned as outcome waiting with the reason, never made"), no file was touched. `git status --porcelain` is empty; branch and worktree remain as taken. Returning outcome waiting to the controller for plan/files correction (does CASE-029 now own the `Details.*` wiring for the valuation host and `CaseQueries.cs`'s Recipient/Reason fields, or does another ticket/hand-off supply them?).

2026-09-02 — research wrapper (Claude around gpt-5.6-terra medium) started; Codex runs read-only in `.worktrees/research` (cad00be9, 3 docs-only commits behind origin/dev 897db953; no src/tests drift). Board reads done from the board worktree on disk because the Kanmer MCP returned empty envelopes this session.

2026-09-02 — research done: `research/`, `files/`, `open-questions/` written (Codex gpt-5.6-terra medium output, wrapper spot-checked 12 VERIFIED claims, all confirmed; wrapper added the `EfDashboardQueries.GetCaseStageCountsAsync` Not-ready-addend finding). Moved backlog → preparing. Next: kanmer-plan needs `plan` and `checklist`; two unticked operator questions (group label wording; Vehicle column source) must be ticked or parked before the ticket leaves preparing. Research checkout `.worktrees/research` left clean (porcelain empty).

2026-09-03 — plan wrapper (Claude around gpt-5.6-terra high): `plan/` and `checklist/` written. Reused the Codex output already in scratch (`prep/CASE-042/plan-out.md`, gpt-5.6-terra effort high, exit 0, prompt per the kanmer-plan brief) instead of re-running. Wrapper adjustments: required `AwaitingInstruction` field on `CaseStageCounts` (no optional/compat default; the one test fake `DashboardBoundaryTests.cs:355` updated in the same diff); CASE-032 dependency sharpened against its actual brief (image count = its `files` half; Received/Source need a CASE-032 amendment or ship absent); attach form script-free so `site.js` (CASE-038 lock) is untouched; lifecycle chip omitted on the Awaiting tab. Kanmer MCP still returns empty envelopes on reads; disk verification used. Two operator open questions remain unticked; the plan proceeds on defaults (label unchanged, no Vehicle column) so ticking both as-is needs no plan change. Ticket stays in preparing. Next: operator ticks/parks the two questions, then kanmer-execute (after CASE-032 and CASE-038's label handoff).

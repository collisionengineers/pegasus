# Proof — CASE-007 (test-output + visual)

Merged to dev in PR #482 (green CI after the applock fix landed under it) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`). On merged code: the read-only case page renders only populated sections (a fresh Not-ready case ends shortly after Chase history), the EVA panel is one compact card with operator-worded reasons behind the assessment disclosure chip, edit mode is an action-bar toggle over the existing lease with the Save/Discard/Keep-editing dialog on unsaved changes, the chase reason reads "Details are incomplete" (legacy rows display-mapped), the inspection mode renders words, and detail rows contain their provenance icons. Evidence: CaseDetailsWebTests + CaseEditModeWebTests 23/23, OperatorJourneyTests 4/4 (browser), Core 842/842, adjacent suites 11/11 — green in CI on #482 and the release build. Live: cases were wiped; the operator's next allocated case demonstrates the page in production.

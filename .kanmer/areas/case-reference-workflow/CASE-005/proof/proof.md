# Proof — CASE-005 (test-output)

Merged to dev in PR #481 (all shards green — including the sql-integration shards that used to flake) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`). Fix: `EfIntakeAllocationStore.BeginAsync` takes a per-receipt `sp_getapplock` (Exclusive, Transaction scope, resource `intake-allocation:{receiptId:N}`) before the Serializable check-then-insert, so parallel same-receipt retries serialize instead of deadlocking; the convergence window in `AwaitRecordedOutcomeAsync` widened 40×25ms → 100×100ms. `QdosAllocationRecoveryTests.DistinctParallelRetriesResolveToOneCaseAggregate` rewritten to five contention rounds — 20 consecutive rounds green locally, and every subsequent CI run in this release (PRs #482–#488) passed the shard that previously deadlocked. Verification checklist items on the ticket body: deadlock cause named from the lock semantics (Serializable range locks on the check-then-insert), repeated-run evidence, and convergence to one case aggregate asserted per round.

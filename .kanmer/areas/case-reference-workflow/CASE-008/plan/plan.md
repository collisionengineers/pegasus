# Plan — CASE-008

Branch `task/case-008-auto-vehicle-lookup` from origin/dev, worktree `../pegasus-worktrees/case-008`.

1. **Core use case** (reuse: `VehicleLookupAvailability`, `ActionActor.Automation`, existing external-work outbox): `ReconcileAutomaticVehicleLookups.ExecuteAsync(max, ct)` — returns 0 immediately when `RequestsEnabled` is false, else calls `IAutomaticVehicleLookupStore.EnqueueDueAsync(max, ct)`. Operation key `vehicle-lookup:auto:{registration}` (unique per case+registration by the existing (CaseId, OperationKey) uniqueness; ≤100 chars).
2. **Store** (reuse: `RequestOnceAsync`'s row shapes, `VehicleLookupRequest` validation, `CaseLifecycleRules.IsTerminal`): one query for up-to-`max` eligible cases — not archived, non-terminal lifecycle, has current registration (single Confirmed value, else single Fact value from `CaseDataFields`), and no `VehicleLookupRequestEntity` with (CaseId, that Registration). Per case: skip if `VehicleLookupRequest` ctor rejects the value; otherwise insert `ExternalWorkItems` (pending, due now) + request row attributed to the Automation actor with `ResultingCaseVersion` = current version — **no lease requirement, no version bump** (evidence gathering, not a staff mutation). Per-case try/catch so one bad case doesn't kill the sweep.
3. **Worker**: add the reconcile call beside `ReconcileStagedArtifacts`/`ReconcileGroupedImageIntake` in the existing timer function (batch 50). No new app settings.
4. **Assessment prefill** (reuse: `IVehicleEvidenceQueries`, `CaseAssessmentProjection.Field`): page model exposes `VehicleEvidence`; view renders "Mileage" input (`value=` saved `vehicle.odometer_miles`, else the estimate's miles) and "Source" dropdown (saved `vehicle.mileage_source`, else `online_data` when the value came from the estimate), plus make/model/year/fuel/engine `value=` prefill from saved field else lookup `VehicleDetails`; the two hint sentences on these fields go (T6/ENG ticket sweeps the rest of the page).
5. **Tests**: store-level sweep tests (enqueues on Fact registration; skips already-requested, archived, terminal, invalid; second run enqueues nothing; corrected registration enqueues one new); assessment web test asserting prefilled values; suites: CaseVehicleWebTests, VehicleWorkflowTerminalTests, new file, assessment web slice; Release build 0/0.

Acceptance: ticket's "How to verify". Deviation: subagents barred — self-review recorded.

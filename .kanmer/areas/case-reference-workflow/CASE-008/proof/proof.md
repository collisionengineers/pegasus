# Proof — CASE-008 (test-output)

Merged to dev in PR #483 (green CI) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`) with the Worker package carrying the reconciliation sweep. On merged code: `ReconcileAutomaticVehicleLookups` runs as the fourth reconcile on the existing staged-artifact timer, enqueuing one lookup per active case whose current registration (Confirmed else Fact) has no request row — idempotent per case+registration, leaseless, Automation-attributed; the existing dispatcher/queue/DVSA adapters/mileage policy complete the path; the assessment page prefills Mileage/Source (and make/model/year/engine/fuel) from the evidence. Evidence: AutomaticVehicleLookupTests 5/5 (enqueue-on-fact + idempotence, confirmed-over-fact + ambiguity skip, terminal/invalid skip, corrected-registration one-new-lookup, unavailable no-op), AssessmentVehiclePrefillWebTests 1/1, adjacent vehicle/assessment suites 25/25, architecture timer test 2/2 — green in CI on #483 and the release build. Live: the wiped estate is sterile; the next case with a registration exercises the sweep in production (worker activation `approved-live-worker` confirmed by smoke).

# Open questions — SIMPLI-011

- [x] **Endpoint shape: one page per family with named handlers, or one dispatching `OnPostActionAsync` per family (the `Triage/Details` pattern)?** Decided (planner, 2026-08-17): **named handlers on a page per family** — it is what "capability-specific Razor endpoints" says, it keeps `?handler=` names (so `handler=RetryCustody` etc. and the existing test idiom survive), and each handler keeps its own parameter list instead of a switch over an action string.
- [x] **Shared plumbing: duplicate per page (current convention, e.g. `Documents/Export.cshtml.cs`) or a base class?** Decided: **one abstract `CaseMutationPageModel`** (precedent `Administration/AdministrationPageModel.cs`). Five copies of ~200 lines of lease/actor/command plumbing is exactly the third-copy stop condition; the base is a move, not new behaviour. `Documents/Export.cshtml.cs` may adopt it in passing only if the change is trivial; otherwise it is left and noted.
- [x] **Where does `EvaDownload` go?** Decided: its own `Cases/Eva/Download` page mirroring `Cases/Documents/Download` — a file response does not belong on a mutation page; the download form/link changes target; the Browser assertion follows.
- [x] **Do `ClaimLease` / `RenewLease` / `ReleaseLease` stay on `DetailsModel`?** Decided: **yes** — they are workspace state (CASE-27's single recovery-interaction set, `docs/design/README.md:604`), not a business capability; the ticket says "keep DetailsModel focused on loading and displaying the workspace", and edit-mode is part of displaying it.
- [x] **`TempData["CaseDetailsStatus"]` written but never read?** Decided: not this ticket's cause — file as a small chore if not trivially folded into the base's status handling during the move.

G4 follow-up requiring B ownership confirmation: A04 author mechanically added version.BoxFileId/BoxVersionId to the existing DTO projections in B-owned EfAssessmentReportProjectionSource and EvaCaseImageReader, then passes row/selected IDs as the final two ManagedDocumentContentAddress arguments. No filters/order/authorization/report/EVA domain policy changed. These 18 added/6 removed projection-only lines remain UNCOMMITTED on A; root will not absorb B domain scope. Can B apply the same mechanical projections on its branch, or confirm these exact byte-address propagation hunks as a shared G correction? Exact edits per file: source EF new DTO(... priorLastField, version.BoxFileId, version.BoxVersionId); DTO appends string? BoxFileId,string? BoxVersionId; managed read address appends row.BoxFileId,row.BoxVersionId (selected in EVA). Production A04 managed reads now require the persisted exact IDs, so callers must carry them. Please record disposition; common G4 contract is da02b625b0ef569eff3b5c50673ef033cfd40bb2.

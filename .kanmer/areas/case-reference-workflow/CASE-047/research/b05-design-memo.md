# B05 / B06 / B07 design memo — report generation, asset preparation, delivery preparation

Read-only Opus research, 2026-09-06, over the B worktree at D `3284f93fc`. Nothing was modified. Reconciliation against Foundation candidate `dc3cfd90`: entities now exist for generations (`CaseReportGenerationEntity`: Id, CaseId, CaseVersion, SnapshotHash unique per Case, SnapshotJson, TemplateVersion, RendererVersion, State, GeneratedAtUtc, Version, SupersededById), artifacts (`GeneratedCaseArtifactEntity`: Id, GenerationId, VersionId?, Kind, Sha256?, State, OperationKey unique, FailureCode; check: Confirmed ⇒ VersionId and Sha256 non-null and no FailureCode), delivery intents (`CaseReportDeliveryIntentEntity`: Id, GenerationId, GenerationVersion, PayloadJson, PayloadHash, ActorSubjectId, PreparedAtUtc, OperationKey unique per generation, Version), and image preparation columns on `DocumentOccurrenceEntity` (PreparationRole(20), SupportingOrder, RotationDegrees ∈ {0,90,180,270}, CropLeft/Top/Width/Height (8,7) with bounds check, PreparationVersion, PreparedBy, PreparedAtUtc). A publishes `ICaseArtifactCustody.RetainAsync` (Custody/CustodyContracts.cs), `IReadLogicalDocumentVersion.OpenAsync` (Documents/DocumentContracts.cs), `IStaffReportSend`/`StaffReportSendCommand`/`StaffMailAttachment`/`StaffMailState` (Operations/StaffMailSend.cs), and the B-owned contract files `Reports/CaseReportGeneration.cs` (`CaseReportArtifact`, `CaseReportGeneration`, `GenerateCaseReportRequest`, `IGenerateCaseReport`, `ICaseReportGenerationQueries` — A consumes the first two and the queries interface; B may extend the request/use case) and `Reports/CaseReportDeliveryPreparation.cs` (`ReportSendReadinessRequest`, `IReportSendReadiness.RequireReadyAsync` — A consumes; `CaseReportDeliveryPreparation`, `PrepareCaseReportDeliveryRequest`, `IPrepareCaseReportDelivery` — B extends). Memo §A/§C proposals below are adapted to those published shapes; the memo's proposed type names are superseded where A's exist. No staff-account version column, no valuation version column and no signature content-type column exist (memo F-5/6/7): snapshot the signature digest and the applied-valuation snapshot id instead. Decisions taken by the B orchestrator: H3 — report generation and readiness do not depend on EVA (D02, B-F-05, B07 override the D47 encoding in `AssessmentAccessPolicy.CanOpen`; the Review → ReportPreparation transition stays reachable through Send to EVA and through explicit `StartCaseWork`/Engineer assignment); H5 — no new report sentence is invented: the "Disclose guide source" control and the guide sentence come from the approved v3 specification (D01) where it supplies them, and when no Glass's guide is selected the Glass's sentence is omitted, not replaced.

## 0. Headline findings

| # | Finding | Anchor |
| --- | --- | --- |
| H1 | Generate/Preview are dead on dev: `DetailsModel` passes `signatory: null` to `AssessmentReportProjection.Prepare`, so "Sign-off Engineer" is always outstanding and `_CaseReport.cshtml` always renders the disabled branch | `Details.cshtml.cs:487-490, 567-571`; `AssessmentReportProjection.cs:119-123`; `_CaseReport.cshtml:14, 30-38` |
| H2 | Retired D18 gates still live: `EvaluatePostReviewReadiness` requires `engineer.name/qualifications/signature` | `AssessmentPolicy.cs:283-288` (from `AssessmentReportProjection.cs:109`) |
| H3 | EVA is a hard gate: `AssessmentAccessPolicy.CanOpen` requires `LatestExportVersion >= LatestReviewVersion` from `EvaFirstHandoffProxies`; `GenerateCaseAssessmentReportDraft` calls `IGetAssessmentAccess` first | `AssessmentWorkspace.cs:45-54`; `EfAssessmentAccessSource.cs:27-30`; `AssessmentReportProjection.cs:431-437`; FRD-07 `:62-67, 131-141`; FRD-11 `:118-121` |
| H4 | No outbound mail exists; `ApprovedMailboxRouteScope` = {InboundIntake, SentEvidence}; `GraphMailClient` read-only; `IStaffReportSend` is A's new build | `ApprovedMailboxAdministration.cs:5-9`; `GraphApprovedSources.cs:247`; `Details.cshtml:615-618` |
| H5 | "Disclose guide source" exists nowhere in the repo; no approved alternative to `StatementOfTruth3` | `AssessmentReportRendering.cs:19` |
| H6 | (resolved at F) the six new ports now exist as contracts; `DocumentSource.Generated` still has no producer | `DocumentContracts.cs:22` |
| H7 | `EfMarketResearchAiJobCompletionStore` holds a Serializable transaction across the Box write — do not copy; `EvaSubmissionStore` (transport outside the transaction, outcome recorded even if the transition fails) is the right precedent | `EfMarketResearchAiJobCompletionStore.cs:47-91`; `EfDocumentCustodyStore.cs:835-844`; `EvaSubmissionStore.cs:157, 253, 305-311` |
| H8 | `assessment.statement_of_truth` is a persisted field never read by projection or renderer | `AssessmentContracts.cs:96, 209`; `_CaseReport.cshtml:57` |

## 1. Projection and rendering

Projection input `AssessmentReportProjectionInput(Assessment, ClaimantName, OurReference, YourReference, ReportFor, ReportDate, Photos, Sources, Costs, CurrentEstimate = null, Signatory = null)` `AssessmentReportProjection.cs:44-55`; result `:61-66`; `AssessmentReportSnapshot` (39 members) `AssessmentReportRendering.cs:209-244`, `Validate()` `:246-301`, `Presentation()` `:303-328`. Readiness: `AssessmentPolicy.Evaluate(includeReviewEntryRequirements:false)` `:211-374` items at `:266-289` (incl. D18 `:283-288`), conditional `:306-336`, per-unconfirmed-field `:355-362`, per-line `:364-371`; `Prepare` adds "Sign-off Engineer" `:119-123`, `RepairCostRequirement` `:79/:128-132`, `LabourRateRequirement` `:80/:133-137`. Signatory tuple: `CaseSignOffEngineerResolver.Resolve(persisted, assigned, eligible)` `CaseLifecycle.cs:150-168`; `SignOffEngineerProfile(StaffId, PrintedName, Qualifications, Signature, SignatureContentType, IsDefault)` `StaffAccountAdministration.cs:26-32`; `IStaffAccountQueries.ListSignOffEngineersAsync` `:159`; content type constant `SignOffSignaturePolicy.MediaType = "image/png"` `:477-480`; mapped to `ReportSignatory` at `EfAssessmentReportProjectionSource.cs:122-127`; `IsComplete` `AssessmentReportRendering.cs:163-166`. `CostsOf` `AssessmentReportProjection.cs:88-99` and `ReportRepairCosts` `AssessmentReportRendering.cs:148-154` re-derive money (third implementation): VAT label lie (`VatPercent > 0` drives "VAT (20% — parts & paint only)" at renderer `:223`), paint labour invisible (`:220-221`); equity from collapsed total `:304`. Glass's wording `StatementOfTruth3` `AssessmentReportRendering.cs:19`, emitted unconditionally `PlaywrightAssessmentReportRenderer.cs:301-307` into `{{ statement_of_truth }}`; guide facts available: `ValuationSource` `Valuations.cs:8-14`, `RepairSpecificationSourceRoute` `RepairSpecifications.cs:13-22`. Fee: `FeeNet`/`FeeVat` (20%, 2 dp away from zero)/`FeeTotal` `AssessmentReportRendering.cs:330-332`; `ParseMoney` invariant `:356-360`; `Validate` fee > 0 `:264`. Dates (ENG-037): culture-sensitive parse at `AssessmentReportProjection.ParseDate` `:362-365` and `AssessmentPolicy.NormalizeValue` Date case; report date computed at load `EfAssessmentReportProjectionSource.cs:116`. Escaping `Encode` `:337` applied in `Rows/AmountRows/List/Paragraph/ImpactRows/Tiles/Footer`; `{{`/`«` rejected `:122-125`.

Renderer: templates embedded from `docs/design/assets/report-renderer/templates/*.scriban` and `report.css` (`Pegasus.Infrastructure.csproj:43-52`), cached `:15-17`; global `SemaphoreSlim(1,1)` `:19,:27`; single headless Chromium `:94-103`; `RenderPdfAsync` `:105-137` (`SetContentAsync` `:127`, `PdfAsync` A4 `:128-136`); no timeouts, `ct` checked once `:112`; images as base64 data URIs `:294-299`, only bound `ContentLength <= int.MaxValue` `EfAssessmentReportProjectionSource.cs:79`; both PDFs rendered every call `:82-83` (`AssessmentReportDraft` `AssessmentReportRendering.cs:346-348`); `Artifact` `:139-149` → `RenderedReportArtifact` with `TemplateVersion = "rendererref1-v2"` (`:8`) and `$"Playwright/{version}; Chromium"`. Projection source `EfAssessmentReportProjectionSource.cs:24-128`: confirmed custody join `:46-67`, sources `:69-74`, photos `:76-80`, batch read `:98`, `Costs: null` `:119`.

Handlers: `OnPostGenerateReportDraftAsync` `Details.cshtml.cs:853-896` (nothing persisted; fee note discarded); `OnGetPreviewReportDraftAsync` `:898-914`. DOCS-014: `Pages/Cases/Documents/Download.cshtml.cs:16-21` records `document_downloaded` per view (`EfDocumentCustodyStore.cs:166-205`) — a view is not a download. Tests: `AssessmentReportProjectionTests.cs` (`:219-244` pins the collapsed mapping), `AssessmentReportRenderingTests.cs` (`FeeTotalsAreComputedInCore` `:65`), `Reports/AssessmentReportRendererTests.cs` (`[Trait("Category","Browser")]`, real Chromium, PdfPig, genuine asset `reference/eva_information/screenshots/engineer-screens/engineer1.png`), `Reports/AssessmentReportDraftWebTests.cs` (`FakeRenderer` `:317`; EVA-cycle gate test `:125`), `CaseReportApprovalWebTests.cs`. Chromium lane: `--filter "Category=Browser&Category!=Corpus" -- xUnit.MaxParallelThreads=2` (runbook `:326`).

## 4. Custody (A-owned, read-only)

`CaseFiles.Live` `DocumentContracts.cs:89-110`; identity triple document `CaseDocument.Id` (`(CaseId, SourceOccurrenceIdentity)` unique), version int + surrogate, `Sha256` char(64). `EvaCaseImageReader.LoadEligibleImagesAsync` `EvaCaseImageReader.cs:20-126` is the closest ordered-images-with-bytes read model. Occurrence `:67-88`; Box name `$"{ordinal:000} {name}"` (`BoxDocumentContentStore.cs:29-48`) so ordinal is not a display order. `DocumentCustodyStatus.Pending` never written in production (`EfDocumentCustodyStore.cs:815`, `EfQueuedCustodyProcessor.cs:411`). Download page `Download.cshtml.cs` (`inline` allowlist `:104-111`, headers `:53-56`); viewer URLs `_CaseFiles.cshtml:39-62`, `_CaseDocuments.cshtml:54-70`, `GalleryImage.cs:11`. Add path `IAddCaseDocument`/`AddCaseDocumentCommand` `DocumentContracts.cs:113-129, 216`; `EfDocumentCustodyStore.cs:23-106` (`PrepareAddAsync` `:768-849` writes bytes inside the transaction). No concurrency token on document tables (only `RequestUploadLinkEntity.Version`). Client-side rotate only `site.js:1942-1965`; third-party marking precedent `DocumentContracts.cs:57-58, 200-207, 258-263`, `EfDocumentCustodyStore.cs:452-543`, `Custody.cshtml.cs:95-117`. SkiaSharp already referenced (`OnnxVrmRecognitionEngine.cs:108-117`).

## 5. Sent evidence and EVA (A-owned, read-only)

`PollSentEvidence.cs:158,175` (SystemWorker + ExecuteSystemWork `:196-200`); match signals `ApprovedSentItemProvenance` `:35-46` — `InReplyToIdentities` (Triage) and `AuthoritativeCaseIdentities` (report) — branching `:426-530`; outcomes `:16-26`; only two evidence states (linked/unlinked by `CaseReportSentEvidence.CaseId`). Retention `ApprovedMailboxReportSentEvidence.cs`; `EfCaseReportSentEvidenceStore.RetainOnceAsync` `:94` (Serializable, replay by id/operation key, `EnsureExactReplay` `:197`). Link guard `EfCaseWorkflowStore.cs:988-1075` codes: `case_not_report_preparation`, `evidence_not_found`, `evidence_not_authoritatively_retained`, `case_already_has_report_evidence`, `evidence_already_linked`, `evidence_future_dated`, `evidence_predates_report_approval`, `evidence_predates_report_preparation` — reuse for `IReportSendReadiness` refusal codes. "Mark report sent" is `Details.cshtml:620-651` → `Tasks.cshtml.cs:201`; read model `CaseDetails.AvailableReportSentEvidence` `CaseQueries.cs:145`, `EfCaseQueryStore.cs:174-181`. EVA: `_EvaHandoff.cshtml` (AssignEngineer `:11-40`, SetSignOffEngineer `:49-74`, Download ZIP `:84-91`, Send via API `:92-114`); `Eva/Send.cshtml.cs:34` (`OnGetAsync` `:56`, `OnPostSubmitAsync` `:117`); `EvaSubmissionStore.ExecuteAsync` `:44` (images only `:135` via `EvaHandoffPolicy.SelectEligibleImages` `EvaBundleSchema.cs:146-156`; transport outside txn `:157`; `RecordSubmissionAsync` `:253` with UPDLOCK/HOLDLOCK `:266-273`, D47 only when `IsDelivered` `:283-287`, row recorded even if transition fails `:305-311`); the report PDF is never sent to EVA. ENG-024 residual: `EvaSubmissionPolicy.IsRetryable` `:164-165`, `EvaSubmissionRetryPolicy` `:284-305`, `EvaSubmissionWorkItem.cs:224-233` — make Unknown terminal, delete the ladder, keep per-attempt keys, keep `EvaApiTransportTests` four-outcome proofs; open gap: no way to record "claim arrived at EVA". D47 verbatim (EPIC-012 context.md:82): first send from Review performs `StartCaseWork` atomically with the handoff; re-send changes no state; gated on `IsDelivered`; implemented `EvaSubmissionPolicy.cs:120-129`, `EvaBundleSchema.cs:130-135`, `EvaSubmissionStore.cs:283-303`, `EvaHandoffStore.cs:232-244` (also clears the edit lease). Recipients: no recipient model; only `contact_email_address` (`CaseDataEntities.cs:55`); C06's contacts/claim sources (`IClaimSourceQueries`, `IOrganizationDirectoryQueries` at F) supply structured recipients; `CaseManualChaseEntity` is a log only. Tables: `CaseReportSentEvidence` (`CaseWorkflowEntities.cs:84-107`), `ApprovedSentPollStates/Outcomes`, `EvaSubmissions` (Outcome check Succeeded|Rejected|Partial|Unknown), `EvaFirstHandoffProxies` (H3's row), `CaseReportApprovals` (`UNIQUE(CaseId, ArtifactIdentity, ArtifactSha256)`).

## A. `Reports/CaseReportGeneration.cs` (extend A's file)

Add: `CaseReportArtifactKind { AssessmentReport, FeeNote }`; `CaseReportGenerationStatus { Pending, Confirmed, Failed, Unknown }` (artifact `State`; generation `State` ∈ Pending/Confirmed/Stale); `CaseReportGenerationSnapshot` (CaseId/version, assigned id, signatory staff id, signature sha256 + content type, current estimate id/version, `EstimateTotals` breakdown, accepted Engineer value + applied valuation snapshot id, `CaseReportContentSwitches(DiscloseGuideSource, IncludeValuationCommentary, IncludeUnrelatedDamage)`, report date + overridden flag, narrative, agreed fee + description lines, sources (name/version/sha256), prepared images (role/order/rotation/crop + occurrence/version/sha256), template/renderer version, guide sources for wording); `GenerateCaseReportRequest` gains `Kind` (B-only consumer) and a reason; `CaseReportGenerationResult { Outcome Generated|NotReady|NotFound|Pending|Failed, Generation?, Reasons }`; `ICaseReportGenerationStore { FreezeAsync (short serializable txn: generation row + one artifact row Pending, replay by operation key), ConfirmArtifactAsync (after custody Confirmed: VersionId/Sha256), RecordArtifactOutcomeAsync (Pending/Failed/Unknown + FailureCode), GetAsync, GetCurrentAsync, ListAsync, MarkStaleAsync(caseId, reasonCode) }`; `IGeneratedCaseArtifactStore.OpenAsync(caseId, generationId, artifactId, actor)` reading confirmed bytes through `IReadLogicalDocumentVersion` by exact hash; `CaseReportReadiness.Evaluate(input)` = post-review readiness minus D18, plus eligible complete signatory, current estimate + rate, accepted Engineer value, exactly one Close-up and one Overview with hash-matching supporting images, applicable content fields; EVA is never a readiness item. Stale rule: accepted assessment facts, method/location, current estimate identity/version/lines/header, accepted Engineer value/applied valuation, content switches/report date, image preparation or source version, signatory or signatory account fields, template/renderer version → stale; Case notes, Engineer notes, recipient edits, navigation/preview/download, the other artifact kind from the same snapshot → not stale. Handler sequence: guard → txn 1 freeze (no Chromium/Box/HTTP inside) → render requested kind outside → `ICaseArtifactCustody.RetainAsync` with `OccurrenceIdentity = $"case-report:{generationId:D}:{kind}"` → txn 2 confirm/record → PRG; retry reuses generation id/hash/operation key; a concurrent material change between freeze and confirm leaves the generation confirmed but stale. `OnGetGeneratedArtifactAsync` opens confirmed bytes only; a view is not a download. Preview is GET, labelled, creates nothing.

## B. `Documents/CaseAssetPreparation.cs` (new)

`CaseAssetReportRole { NotUsed, CloseUp, Overview, Supporting }` (not `DocumentSemanticRole` — EVA eligibility, intake role assignment and the third-party guard read that enum); `CaseAssetRotation { None=0, Clockwise90=90, Half=180, Clockwise270=270 }`; `CaseAssetCrop(Left, Top, Width, Height)` fractions of the rotated source, `Full`, `Validate()`; `CaseAssetPreparation(PreparationId=occurrence, CaseId, OccurrenceId, DocumentId, VersionId, SourceVersion, SourceSha256, SourceContentType, Role, Order, Rotation, Crop, PreparationVersion, PreparedBy, PreparedAtUtc)`; `CaseAssetPreparationEdit(OccurrenceId, ExpectedPreparationVersion, Role, Order, Rotation, Crop)`; `SaveCaseAssetPreparationRequest`/`ResetCaseAssetPreparationRequest : CaseMutationRequest`; `ICaseAssetPreparationQueries.ListForCaseAsync`; `ICaseAssetPreparationStore { SaveAsync, ResetAsync }`; `CaseAssetPreparationPolicy.ValidateSet` (≤1 Close-up, ≤1 Overview as save rule; exactly one each is readiness; Supporting orders contiguous from 1; accepted content type; crop bounds; cross-Case rejection; sha256 matches current confirmed version) and `ForReport`. Key on `OccurrenceId`. Concurrency: Case version via `CaseMutationGuard` and per-row `PreparationVersion`. Expose `internal static PrepareSaveAsync(context, workflow, request, now, ct)` so B02's workspace transaction can include it. One preparation model for Files and Report cards; client rotate promoted to persisted value; never touch bytes.

## C. `Reports/CaseReportDeliveryPreparation.cs` (extend A's file)

Keep A's `ReportSendReadinessRequest`/`IReportSendReadiness.RequireReadyAsync` untouched (A consumes). Extend `CaseReportDeliveryPreparation` and `PrepareCaseReportDeliveryRequest` with To/Cc (structured contact ids resolved server-side to addresses), Subject, selected artifact ids; add `SendPreparedCaseReportRequest` and `ICaseReportDeliveryPreparationStore { PrepareAsync, GetAsync, GetCurrentAsync }` over `CaseReportDeliveryIntentEntity` (PayloadJson/PayloadHash). `RequireReadyAsync` reloads persisted state and refuses on: actor not signed-in staff with PerformCasework, Case version moved, preparation version moved, generation missing/stale/artifact not Confirmed, attachment sha256 mismatch, recipient no longer authorized; refusal codes extend the link-guard list. Handlers: `OnPostPrepareReportDeliveryAsync` (guard; load generation by id; refuse missing/stale/unconfirmed; re-verify hashes; resolve/authorize recipients; write intent; PRG; recipient edits never stale) and `OnPostSendPreparedReportAsync` (staff only; re-run Prepare checks; call A's `IStaffReportSend.SendAsync(StaffReportSendCommand(mail, ReportSendReadinessRequest))`; return A's state; never a local Sent; Unknown retained, no auto-retry; duplicate operation key replays). "Mark report sent" stays the link act; `_EvaHandoff.cshtml` stays optional and separate.

## D. Template / projection changes

D1 delete `CostsOf` (`AssessmentReportProjection.cs:88-99`) and `ReportRepairCosts` (`AssessmentReportRendering.cs:139-155`), replace `Costs` with the canonical `EstimateTotals` breakdown plus descriptive hours/rates; renderer `CostRows` `:218-224` prints Labour hours, Hourly rate, Labour, Paint hours, Paint, Parts, Other, Sub total, `VAT ({VatPercent}%)`, Total from the breakdown; repoint `Presentation()` `:315,320,325`, `Tiles` `:276,283-284`, `Validate` `:263-267`, equity `AssessmentReportProjection.cs:304`; the figures-without-estimate path `:36-41` has no caller — delete. D2 split `StatementOfTruth()` `:301-307` into guide and repair clauses selected by `DiscloseGuideSource` and the snapshot's guide sources; omit the Glass's sentence otherwise (no invented wording). D3 delete `AssessmentPolicy.cs:283-288`; fix H1 by passing the resolved signatory in `DetailsModel`. D4 report date only at generation (delete `EfAssessmentReportProjectionSource.cs:116`); ENG-037 invariant parsing at `ParseDate` `:362-365` and `NormalizeValue` Date. D5 fee preview reuses `FeeNet/FeeVat/FeeTotal`; `assessment_fee_note.scriban:4-5`. D6 `IAssessmentReportRenderer.RenderAsync(snapshot, kind)` → one `RenderedReportArtifact`; delete `AssessmentReportDraft` `:346-348` and the two-artifact loop `:366-373`. D7 per-image byte cap and count via `CaseAssetPreparationPolicy.ForReport`; pass `ct` and explicit timeouts to `SetContentAsync`/`PdfAsync`; keep the semaphore until measured. D8 `Photos` `:294-296` emits a wrapper with rotation and fractional crop; `report.css:211-212` replaces `object-fit: cover` with an explicit crop container honouring Close-up first, Overview second, Supporting by order. D9 `{{ date }}` (`assessment_report.scriban:2`, `assessment_fee_note.scriban:3`) becomes the frozen report date; capture template/renderer/Chromium versions.

## E. Tests

New `tests/Pegasus.IntegrationTests/Reports/CaseReportGenerationWebTests.cs` (`Browser` + `SqlServer`; fakes only for `ICaseArtifactCustody` and `IStaffReportSend`): `SavedChoicesGenerateReportAndFeeAsSeparateParseableArtifacts`, `ReopeningAGeneratedArtifactReturnsByteIdenticalConfirmedBytes`, `GenerateReportInvokesTheRendererOnceForTheReportOnly`/`…FeeNoteOnly`, `ChangingAMaterialFactMarksTheGenerationStaleAndRefusesPreparation`, `RegeneratingAfterAStaleChangeProducesANewGenerationAndPreservesThePrior`, `PreparingAndSendingInvokesTheRecordingSendTransportOnce`, `SentAppearsOnlyAfterAMatchingFakeProviderObservation`, `PreviewCreatesNoGenerationNoArtifactAndNoSentEvidence`. New `tests/Pegasus.Core.Tests/Reports/CaseReportGenerationTests.cs`: each readiness blocker; incomplete/ineligible signatory; retired D18 absent; missing/duplicate Close-up/Overview; stale matrix; report date defaults only at generation; override frozen; switches independent; snapshot hash stable; template version mismatch. New `Reports/CaseReportGenerationPersistenceTests.cs`: two-transaction freeze/confirm; pending → confirmed; pending → failed → retry same snapshot/key; Unknown retained; stale never rewrites prior; concurrent change between freeze and confirm. New `tests/Pegasus.Core.Tests/Documents/CaseAssetPreparationTests.cs` and `tests/Pegasus.IntegrationTests/CaseAssetPreparationPersistenceTests.cs` per B06's list. New `Reports/CaseReportDeliveryPreparationTests.cs` and `CaseReportDeliveryPreparationWebTests.cs` per B07's list. Extended: `AssessmentReportProjectionTests.cs` (`:219-244` rewritten; D18 dropped; th-TH regression), `AssessmentReportRenderingTests.cs`, `AssessmentReportRendererTests.cs` (report-only/fee-only; bounded image; timeout; crop/rotation), `AssessmentReportDraftWebTests.cs` (`:125` retargeted per H3), `CaseReportApprovalWebTests.cs`, B07's evidence/EVA tests (`EvaApiTransportTests` four outcomes preserved).

## F. Depends on Foundation / A / decisions

Resolved at F: generation/artifact/intent/preparation entities, `ICaseArtifactCustody`, `IStaffReportSend`, `StaffMailState`, `IReadLogicalDocumentVersion`, C's contact/directory queries. Open for A: `ICaseArtifactCustody` implementation, transport, send-state projection, 24-hour cache reader, `ApprovedMailboxRouteScope.Send` + size-ceiling columns (A02/A03 scope), DI patches for B's concrete types. Decisions recorded above: H3 and H5. B02 must expose report switches/signatory/report date and call `MarkStaleAsync` from the workspace transaction; B03/B04 supply the applied-valuation identity and current estimate guide source.

# Proof — INTK-021 (test-output)

Merged to dev in PR #479 (green CI) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`). On merged code: acceptance writes extracted unambiguous typed-valid values as facts (`CaseDataSnapshotFactory.AddExtractedValue` → `CaseDataCodes.Fact`), so `Current = Confirmed ?? Fact ?? Suggestion` auto-adds them across the case surface; label synonyms, subject-line grammar, the vehicle-description field and prefix/suffix registration validation raised measured coverage on the real corpus (75 accepted-route instructions): claimant 4→48, claim number 4→60, registration 57→68, make 13→54, model 14→47, incident date 7→45 — floors enforced by the corpus-conditional `QdosExtractionCoverageTests` (CSV artifact under `artifacts/evaluation/`). Suites: extraction policy fixtures + updated persistence/web tests green in CI on #479 and the release build. Live: the wiped estate means the next inbound instruction demonstrates auto-added details in production.

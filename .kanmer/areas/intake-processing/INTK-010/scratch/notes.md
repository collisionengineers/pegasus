2026-08-20 ([[DELIV-012]] coordination) — Drag-and-drop root cause settled after the lane's two faithful reproductions (page-level DragEvent and CDP `Input.dispatchDragEvent`) both PASSED against the deployed code: in-zone drops work; the defect is that **no document-level drag handlers exist**, so any drop landing outside the small dashed zone triggers Chrome's default action and navigates the tab to the dropped file — the page vanishes, which the operator reasonably reported as "drag+drop doesn't work". Fix directed: document-level `dragover`/`drop` preventDefault (a stray drop can never navigate), the whole upload panel as an effective drop target with the existing `is-dragover` affordance while a `Files` drag is in the window (depth-counted enter/leave), no affordance logic reading `dataTransfer.files` before `drop`, and body-drop regression tests via the lane's CDP harness. The two in-zone tests are kept as permanent regression coverage.

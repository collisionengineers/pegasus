Simplification pass in flight (4 lenses). Efficiency lens findings received: (1) image-intake lookup now runs for every image receipt incl. common miss path — gate fallback on decision==ImageIntakeRegistered || submissionGroupId present; (2) UploadCaseDecision fetches heavy IGetCase before already-linked early-returns and uses it only for Reference+Version — reorder early returns above the read; (3) sequential getIntake/getCase (moot after 2); (4) autocomplete lacks AbortController cancellation (server token is threaded); (5) eager per-form combobox wiring (minor, defer); (6) setActive DOM re-query (trivial, defer).

Simplification lens findings received: base-class dedupe of handlers (CaseMutationPageModel precedent), dead `duplicate` param, layout TempData["Confirmation"] reuse, tuple-switch collapse, merged-branch/Attached-branch dedupe, double success-return, dead data attribute, Cancel placement question, redundant ARIA writes, data-defer-refresh generic attr, reference-resolution simplification, dead test local, dual case-seeding fixtures.

# Checklist — TICK-012 (INT-25)

- [x] Distil contract/caller/failure into proof.md "What was verified"
- [x] Run focused Core intake suites: AllocateDefinitiveIntakeTests, DefinitiveIntakeCaseTypeTests, Qdos/QdosMailClassificationPolicyTests, CaseMatching/EvaluateIntakeCaseMatchTests
- [x] Run focused integration recovery suites: QdosAllocationRecoveryTests, IntakeAllocationConsumerTests, CaseAcceptanceReplayTests
- [x] Write proof.md with real `dotnet test` output (local caller-proof tier) + honest "Not covered" (live/deployed journey, QDOS-only breadth, OCR-literal Audit bound)
- [~] Record activation-criteria decision + the two scope open-questions; move ticket to `review` (not `done`) — moved to `review`; operator activation decision + follow-up filing pending user input

## Progress notes

- 2026-08-13: Mechanism found already implemented, wired and test-covered (see research.md). No source change made. Release build 0 errors; Core intake 64 passed; integration recovery/replay 22 passed (LocalDB) → 86 local caller-proof tests green. Live/deployed tier deferred (requires-live-approval). Moved to `review` for operator activation decision; did not self-advance to `done`.

- 2026-08-14: Operator reports the live feature does NOT work — cases are not created in the running system. The green tests above are fake/LocalDB-backed and do NOT prove the live caller; framing this as review-ready was wrong. Reopened to `in-progress` for live-defect investigation. Primary suspect: the durable Worker dispatch (timer + `intake-work` queue → `ProcessQueuedIntake` → allocation) not running/wired against real storage, or classification never reaching a definitive `CaseCreated` decision. See research.md caller chain.

- 2026-08-14: LIVE ROOT CAUSE (read-only prod SQL, operator-approved). Prod DB `pegasus`: **Cases=0, IntakeAllocationAttempts=0, ExternalWorkItems=0**, but IntakeReceipts=3 — two at `Decision=case_created` ("eligible for case allocation"). Worker running, all 9 functions enabled, intake work items `completed` — yet **automatic allocation never executes** → no case → (downstream) no Box folder. Deployed worker (az_cli deploy 2026-08-12T07:02Z) **predates** the Audit-intake/allocation fixes now on `dev` (`0743ac32` "Fix Audit intake evidence and custody" 08-12; `73a3380d` "Fix forwarded QDOS Audit intake" 08-13): prod marks audit-without-report receipts `case_created`, which current dev gates to `needs_sorting` (ProcessIntake.cs:155-166). FIX = **redeploy worker (+web) from current dev** + re-drive the 3 `completed` receipts (completed work can't self-replay). Deploy is a WRITE needing approval; **dev code is already correct → no code PR for the core defect**. Box wiring + root ID `405543781910` verified correct in code, infra (bicep) and live worker/web config — NOT the cause.

- 2026-08-14 (CORRECTED live diagnosis — supersedes the earlier "principal missing / just redeploy" note; DOC-01/INT-25 resolution plan approved): Fresh read-only prod check (signed in as digital@collisionengineers.co.uk; SQL pegasus-prod-sql-252ow37gij; Box CLI).
  - **QDOS principal is ALREADY seeded and active** — Principals: Code=QDOS, IsActive=1, InspectionMode=image_based_assessment; Organizations: QDOS/NormalizedName=QDOS; OrganizationRoles: work_provider. The 2026-08-13 missing-principal blocker has since been resolved out-of-band. **No seeding needed.**
  - **Real blocker: the deployed worker+web (0.1.0-alpha.9 / source 75f39c70) are MISSING commit `73a3380d` "Fix forwarded QDOS Audit intake"** — they carry the earlier `0743ac32` but not fix #2. Worker deployed 2026-08-12T07:01Z (code ≈ dd61ac56); web image tagged 75f39c70. Both fixes ARE on origin/dev (HEAD ddc40790), never merged to main/deployed.
  - **Split state**: fix #2's migration 20260813025241_StandaloneAuditReportDecision is applied to prod out-of-band (codex-current-machine-20260812 fw rule), but the fix #2 CODE is not deployed → StandaloneAuditReportAssessment NULL on all rows.
  - Live counts: Cases 0, CaseSequences 0 (never minted), IntakeReceipts 3 (2× case_created QDOS audit Aug 13 — EREF9 476a384c, EREF18 0e1bab7d — stranded with IntakeWorkItems completed + 0 allocation attempts; 1× needs_sorting QDOS inspection_and_audit Aug 12), IntakeAllocationAttempts 0, ExternalWorkItems 0, StandaloneAuditEvidence 0. Mailbox poll healthy (last 2026-08-14T05:22Z). Box root 405543781910 has 0 children.
  - **FIX = merge origin/dev→main (already contains fix #2 + follow-up f08e2df6) + deploy worker/web, then re-forward the QDOS emails to re-drive the stranded receipts. No core code PR needed.** GATED on MERGE AUTH GRANTED + per-target deploy approval. Re-pointed to worktree task/doc-01-box-resolution.

- 2026-08-14 PHASE 1 DEPLOYED (operator-authorised MERGE AUTH + deploy). Merged origin/dev→main (aecad247, carries fix #2 73a3380d) and deployed both hosts to prod:
  - Web via `azd deploy web` → new Container App revision, /diagnostics/version sourceSha=aecad247, /health/ready 200, 100% traffic, single-revision mode.
  - Worker via `func azure functionapp publish` (azd's Flex zipdeploy hit an Oryx remote-build error; func deploys the pre-built package, remotebuild=false). Host Running; all 9 functions enabled; mailbox poll completed 06:10:45Z after deploy with no failure.
  - Migration 20260813025241 already applied out-of-band; efbundle not needed; __EFMigrationsHistory unchanged. No infra provision (bicep minReplicas 0→1 intentionally NOT applied), no data mutation.
  - Env drift handled: azd env lacked AZURE_CONTAINER_REGISTRY_ENDPOINT; set to pegasusprodacr252ow37gij.azurecr.io.
  - REMAINING: operator re-forwards the QDOS audit emails (new source identity) → verify case minted + Box folder under 405543781910; then refresh docs/operations.md + current-architecture.md. The 3 stranded receipts (completed work) are handled under TICK-113.

- 2026-08-14 TRUE ROOT CAUSE FOUND (deeper than the deploy gap; via App Insights + prod SQL). After deploying fix #2, operator re-forwarded a QDOS audit; it processed correctly (route accepted, EffectiveSender=nduncombe@qdosassist.co.uk resolved, CaseType=audit, StandaloneAuditReportAssessment=repairable persisted) but the worker THREW: `SqlException: The SELECT permission was denied on the object 'StandaloneAuditEvidence'` (AppExceptions 06:14:21, AppRoleName=pegasus-prod-worker) → work item stuck retry_scheduled → no evidence/allocation/case.
  - **The worker's least-privilege SQL role `pegasus_worker_runtime_role` was never granted the case-creation permissions.** WorkerGrants matrix (`20260729199000_RuntimeRoleReconciliation.cs:166-206`) is STALE — it predates automatic allocation moving to the Worker (INT-25). Prod confirmed: worker had only `DELETE=DENY` on StandaloneAuditEvidence, `Cases`=SELECT,UPDATE (no INSERT), and NO grant on CaseSequences/Principals/Organizations/PrincipalSequenceLineages/VehicleConfirmations. The WebGrants matrix has them (web ran staff-create). **Local tests never caught this — LocalDB runs full-privilege (the "prove the live caller not local tests" trap).** This — not fix #2 — is why no case ever minted in prod.
  - **HOTFIX applied (operator-approved) to prod DB**: granted `pegasus_worker_runtime_role` the case-creation set mirroring the web role: StandaloneAuditEvidence(S,I), Cases(S,I,U), CaseSequences(S,I,U), CaseIntakeLinks(S,I), CaseWorkflows(S,I,U), CaseHistory(S,I), ExternalWorkItems(S,I,U), Principals(S,U), PrincipalSequenceLineages(S,I), Organizations(S), OrganizationRoles(S), VehicleConfirmations(S,I). DELETE stays DENY. Verified in sys.database_permissions. Role membership confirmed (pegasus_worker_runtime user → role). Then nudged the stuck work item DueAtUtc=now to re-drive; verifying case mint + Box folder.
  - **PERMANENT FIX STILL NEEDED (code)**: a new migration adding these worker grants to WorkerGrants so future deploys/reconciliations carry them (the hotfix is otherwise drift). Owned here / TICK-113.
  - Separate non-blocking items observed: (1) SentEvidencePollFunction throws `UnauthorizedAccessException: claimed mailbox not approved for Sent-evidence polling` every minute — noisy, not blocking case creation; (2) the inbox "shows forwarder not original sender" is a mail-DISPLAY artifact (routing resolved the original QDOS sender correctly) — cosmetic, separate from minting.

- 2026-08-14 07:20-08:00 GRANT CASCADE fully resolved (via /azure:azure-diagnostics + App Insights AppExceptions DbUpdateException inner messages + code enumeration). The case-acceptance transaction (EfCaseAcceptanceStore.AcceptOnceAsync) INSERTs ~20 tables in ONE SaveChanges batch; SQL reports only the FIRST denied table per attempt, so each re-forward surfaced the next missing worker grant in sequence: StandaloneAuditEvidence → WorkflowConfigurations → CaseMatchIndex → CaseDataFields. CaseDataFields was invisible to direct-DbSet enumeration because it is an EF CASCADE child of CaseDataSnapshot (`CaseDataEntities.cs:20 List<CaseDataFieldEntity> Fields`). Enumerated ALL child collections on acceptance entities — CaseDataFields is the only cascade — so the set is now provably complete. FINAL verify: all 20 acceptance write-set tables GRANTED to pegasus_worker_runtime_role ( + reads Principals/PrincipalSequenceLineages/WorkflowConfigurations/IntakeCaseMatchDecisions).
- Re-drive learning: resetting a COMPLETED work item to re-process fails with `staged_artifact_integrity_failure` — the transient staged bytes are reconciled/removed after processing, so completed work cannot be re-run from source (by design). Valid re-drives for a stuck failed-allocation receipt: (a) staff "Retry allocation" (RetryAsync replays the frozen command; no staged bytes needed), or (b) a fresh forward (new staged artifact). SQL reset of the work item is NOT viable.
- The 08:52 BST (07:52 UTC) forward classified `needs_sorting` ("readable content does not provide enough evidence to suggest a principal") — NOT a permission issue; that specific email did not present as a definitive QDOS audit (content/format), so it correctly withholds. Distinct from the 08:17 case_created path.
- PERMANENT FIX (code) still owed: a migration adding all the above worker grants to WorkerGrants (the hotfixes are drift until codified). The worker matrix in `20260729199000_RuntimeRoleReconciliation.cs` predates auto-allocation-on-worker and must be brought up to the acceptance/custody write-set.

## Closeout — TICK-012 (2026-08-18)

- [x] Work merged via PR #376 (2026-08-17) and on `main` since #394; no open PR/branch for this ticket
- [x] proof.md finalised (release-9 addendum: grants migration codified and applied); moved to Done; Outcome + follow-ups recorded; deployment = production
- [x] No worktree/branch to remove (ticket was never taken with one)
- [x] Released

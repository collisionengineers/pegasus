# Checklist — INTK-065

- [x] Step 1 — Correct the five current inventory declarations and Core expectations; relocate README.md and qdos.md byte-identically to principal-profiles; repair only the matching docs index link. Do not execute commands or alter JSON.
- [ ] Step 2 — With root's explicit verifier slot, run the existing helper and focused recorded checks; materialize only the approved JSON metadata/reference changes and record all exits/diffs.
- [ ] Step 3 — Run git diff --check, write the report, commit/push the declared paths, and open the single draft PR for independent review.

## Re-forward result — 2026-08-27 10:51Z

Operator re-forwarded EREF10. Received 10:51:09Z → retained 10:51:12Z (Graph webhook wake, actor `system-worker:approved-inbox-notification`, work item `mailbox:491D6E8B…`) → evidence recorded 10:51:16Z → allocation `succeeded` → **`a.QDOS26025`** (audit, repairable), 7 s end to end. So the Graph notification path works and the Audit acceptance path works when run alone. The 10:25Z failure is not a subscription problem: both original messages came in through the 5-minute poller (actor `approved-inbox-poller`), and the only distinguishing condition of the failed attempt is that it ran one second behind another acceptance in the same first batch, both under `Serializable`. Leading theory is therefore a concurrent-acceptance interaction that surfaces as a non-retryable exception; needs a captured exception (MAIL-020) or a two-message replay to confirm. Original receipt `f2ac0509…` remains `case_created` with a terminal `blocked` attempt and a duplicate now exists — the dead-end route still needs fixing.

## Root cause reproduced locally — 2026-08-27 (INTK-044 worker)

Prod read (read-only SQL): attempt `614a21bd` (EREF8) began 10:25:26 and its case `QDOS26024` carries `CreatedAtUtc` 10:25:27 — the acceptance transaction was open when attempt `f598ac23` (EREF10, audit, evidence `3a45b161`, expected version 1) began at 10:25:27; it failed at 10:25:29. New integration test `ConcurrentAutomaticAuditAndInspectionAllocationsForOnePrincipalBothSucceed` (two automatic allocations for one principal via `Task.WhenAll`, one a standalone Audit) failed on round 0 with exactly `FailedRecoverable/Unexpected` and captured the exception the Worker lost:

`System.InvalidOperationException: An exception has been raised that is likely due to a transient failure. Consider enabling transient error resiliency by adding 'EnableRetryOnFailure' to the 'UseSqlServer' call.` → `DbUpdateException` → `SqlException 1205 (deadlock victim)` at `EfCaseAcceptanceStore.AcceptOnceAsync` `SaveChangesAsync`.

So it was the Serializable sequence-row deadlock after all; `IsRetryableConcurrencyFailure` unwrapped only `DbUpdateException`, not EF's outer `InvalidOperationException`, so the retry loop never ran and `Classify` fell to `Unexpected`. The ~2 s matches SQL Server deadlock detection. Fix: unwrap every inner layer (the `EfIntakeReceiptStore` convention).

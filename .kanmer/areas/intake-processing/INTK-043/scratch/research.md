Research correction: the supplied ~17.4 s “stage-to-post-reader” observation does not isolate IIntakeSourceReader. ProcessQueuedIntake performs staged Blob download and integrity hash, a second immutable Blob write, ProcessIntake (identity lookup, source reader, sequential candidate retention, assessment and receipt persistence), then work completion before later association/allocation. Existing telemetry exposes one process_intake duration only and no sub-stage timings. Static candidates (EML/PDF byte copies, sequential attachment decode/read/retention, staging-to-durable Blob copy) remain hypotheses until measured.

Clarification audit — 2026-08-19: the two Case-origin types are now recorded in scope/plan/checklist. Remaining contract decisions before implementation: (1) no usable or ambiguous VRM cannot produce a VRM-based Image-initiated reference; confirm this routes to INTK-007 Unidentified with U<n> until staff resolves identity; (2) conflicting accepted VRMs likewise need a single Unidentified/fail-closed destination; (3) decide whether Image-initiated Case is a new Case type/row or a recast of the existing ImageIntake aggregate (current Cases require PrincipalId; current ImageIntake is explicitly pre-Case); (4) define whether matching closes/converts the Image-initiated Case or retains it as a linked source Case; (5) define lifecycle, queues, search, Operations counts, and history treatment; (6) define normalized VRM, per-VRM atomic sequence starting at 01, replay/concurrency, and no-reuse rules. Accepted ADR-0013 currently says image-led work stays pre-Case, so it must be superseded or its conflicting functional wording replaced through the governing-doc process.

Open-question spot-check coordination — 2026-08-19: the operator states the remaining INTK-006 lifecycle, operational-treatment, and reference-allocation questions were likely already answered in the active implementing agent's conversation. Do not ask the operator to repeat them. Reconcile that conversation into open-questions and governing docs before proceeding. The spot-check recorded only the explicit shared-U-reference answer and the architecture-derived ImageIntake persistence disposition.

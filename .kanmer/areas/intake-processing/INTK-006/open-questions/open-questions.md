# Open questions

All INTK-006 questions are resolved for the narrowed grouped-routing scope. The Image-initiated lifecycle contract is tracked in [[INTK-008]].

- [x] No usable or ambiguous VRM enters [[INTK-007]] as one grouped Unidentified U<n> work item with the explicit conflicting_vrms reason where multiple valid VRMs were read. No VRM reference is fabricated.
- [x] A usable VRM with no unique Instruction-initiated match uses the existing ImageIntake aggregate and per-VRM reference owner; no principal-less formal Cases row is inserted.
- [x] Merge lifecycle, searchable state, staff closure, Box custody, and formal-Case history are specified for [[INTK-008]]: the Image-initiated Case is closed/converted as merged or subsumed when matched, remains searchable with history, and may be staff-closed with a reason if instructions never arrive.
- [x] Existing staff authorization and custody boundaries are reused; Image-initiated references remain separate from Case/PO, Audit, and U<n> references.
- [x] ADR-0013 is superseded through a new ADR rather than edited in place; the governing-doc reconciliation is tracked in [[INTK-008]].

## Parked (explicitly deferred)

- None.

2026-08-25 repository research: `MarkDispatchedAsync` stores `dispatched`, sets `DueAtUtc` to the publication time, and clears the dispatch lease. `RecoverExpiredLeasesAsync` selects only leased `dispatching|processing`, while `ClaimDispatchAsync` selects only due `pending|retry_scheduled`; therefore an unconsumed/lost message strands the row. Existing `ProcessQueuedIntake`/`ClaimProcessingAsync` semantics make duplicate delivery a no-op or safe replay. Host queue visibility is 5 minutes and max dequeue count is 5; `SendMessageAsync` does not set an explicit message TTL. The stale age should be Core/reconciliation policy settled by blocking [[INTK-041]], not hard-coded in EF. Existing `(State, DueAtUtc)` index can support selection. No code or cloud state changed.

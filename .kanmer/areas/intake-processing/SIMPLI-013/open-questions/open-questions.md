# SIMPLI-013 open questions

None blocking. Scope was fixed by the operator (2026-08-20): CollisionDocNet scoped to `.doc`/`.msg`; PdfPig remains the PDF path.

## Parked (explicitly deferred)

- [ ] `.doc` embedded-picture extraction (EXT-DOC-009: descriptors only, no BLIP decode exists) — deferred with reason: no decodable image bytes exist in the imported slice; source bytes are retained for operator review as today.

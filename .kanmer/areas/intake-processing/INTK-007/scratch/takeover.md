## Takeover — claude-code, 2026-08-19, DELIV-012, operator decision

Taken over from another agent by operator decision. Working only in `.worktrees/intk-007` on branch `intk-007-unidentified-intake`, PR #424. Branch was ~42 commits behind `dev` (forked from `main`), PR state CONFLICTING, no CI had ever run on it.

Operator ruling (already given, quoted verbatim): shown this PR's `docs/operator-notes.md` section, the operator answered **"Confirmed — Unidentified replaces Needs sorting."** This ticket therefore owns completing the replacement everywhere, including the `AGENTS.md`/`CLAUDE.md` product invariant. Because the replacement is confirmed, the mapping at `src/Pegasus.Web/Pages/Mail/Message.cshtml.cs` (`NeedsSorting => "Unidentified"`) that codex flagged as a defect is correct and was not reverted.

Work done this session:
- Merged `origin/dev` (42 commits), resolved the one conflict in `docs/capabilities.md` (kept dev's implemented MAIL-01/02 rows, reworded MAIL-02 from "Needs sorting" to "Unidentified").
- Fixed blocker 1 (missing GRANTs on UnidentifiedItems/Sequences/History), blocker 2 (retryable reader failures were burning a U-reference on the first attempt — added IntakeExceptionPolicy.IsTransientFailure and an isFinalAttempt flag threaded from DurableIntake's own retry accounting), blocker 3 (backfill read the wrong column and seeded an all-zero replay fingerprint).
- Fixed all 14 remaining reviewer comments (see plan.md "Review fixes" section for full dispositions) — reason-mapping, timestamp source, image-only fallback registration, stale-item reconciliation on reevaluation, replay-fingerprint comparison, history truncation, destination-port validation on resolve, raw enum/UTC rendering, unstable operation key, MCP default-to-Open and numeric-enum rejection.
- Corrected the Unidentified grants twice: first pass granted both runtime roles the same blanket matrix; the coordinator (main) pushed back for per-object least privilege, so I traced every actual caller of IRegisterUnidentified/IResolveUnidentified/IUnidentifiedStore and re-derived the matrix (Web never registers, so it lost INSERT on Items and all of Sequences; Worker gained UPDATE on Items because my own reconciliation feature resolves stale items from the Worker side). Migration SQL and scripts/Invoke-AzureDatabaseBootstrap.ps1's census were kept in sync.
- Added a `20260819115323_UnidentifiedWork` census block to `scripts/Invoke-AzureDatabaseBootstrap.ps1` per a mid-session coordinator addition (Test-AzureDeploymentPlan.ps1 -Mode Local, landing via PR #426) — verified locally against a copy of the not-yet-merged Test-MigrationGrants.ps1 and Test-AzureDeploymentPlan.ps1 -Mode Local.
- Updated `AGENTS.md`/`CLAUDE.md` product invariant, `docs/current-architecture.md` (two mirrors of the same invariant), and reworded (not deleted) the three stale "Needs sorting" lines in `docs/operator-notes.md`.
- TICK-045 (the sibling lane making `MailOperationalDestination.NeedsSorting` reachable from the retained mailbox viewer) has **not** merged into `origin/dev` as of this session (confirmed via `git merge-base --is-ancestor`); its current pushed state doesn't yet wire that enum into any UI caller. I renamed `MailOperationalDestination.NeedsSorting` -> `Unidentified` anyway since it was only reachable from its own policy file and unit test, updated FRD-08's catalogue for the operational-destination column only (left the FRD-03 cross-reference note alone), and updated docs/capabilities.md's MAIL-02 wording. If TICK-045 merges before this ticket, the merge will need to pick up the renamed enum member.

See plan.md for the full simplification pass and per-comment dispositions, checklist.md for honest progress, post-implementation-report.md for the stale-term inventory.

## Second dev merge — 2026-08-19

Between the first merge and finishing this session, `dev` advanced 12 more commits, including PR #422 (TICK-045, which wired `MailOperationalDestinationPolicy` into the retained mailbox viewer `/Inbox/{id}` for the first time) and PR #426 (DELIV-012 grant/docs fixes, landing the real `Test-MigrationGrants.ps1` and the census entries for `20260819104953_MailClassificationCorrectionHistory.cs` that were pending). Merged `origin/dev` again (commit 3356f0aa): three conflicts (`docs/capabilities.md`, `scripts/Invoke-AzureDatabaseBootstrap.ps1`, `IntakePersistenceIntegrationTests.cs`), all resolved additively/chronologically.

TICK-045's merge exposed the exact hand-off named in the takeover brief: it added `OperatorLabels.MailOperationalDestinationLabel` with a switch case `MailOperationalDestination.NeedsSorting => "Needs sorting"` — referencing the enum member I'd already renamed to `.Unidentified` two commits earlier on this branch. Git's line-based auto-merge didn't flag it as a conflict (no overlapping lines), but it would not have compiled. Fixed the case to `.Unidentified => "Unidentified"`, and fixed `MailWorkspaceWebTests.cs`'s matching assertion, which had itself hardcoded the literal old "Needs sorting" text even though its own comment said the intent was consistency with the page's pre-existing "Unidentified" wording.

Post-merge verification: build green; the now-real `Test-MigrationGrants.ps1` passes for all 50 migrations; `Test-AzureDeploymentPlan.ps1 -Mode Local` passes cleanly (no more pending-migration gap); full Core suite (655) and the task's IntegrationTests filter (27, up from 23) both green; docs link check clean. Pushed as 3356f0aa. `gh pr view 424` now reports `mergeable: MERGEABLE` (previously `CONFLICTING`) and CI has been triggered for the first time (checks pending as of this note).

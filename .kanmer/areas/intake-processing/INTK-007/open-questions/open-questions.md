# Open questions

No unresolved questions. The operator has fixed the reference format, grouping rule, required reason, permanent history, resolution behavior, and wide replacement scope.

Implementation is deliberately blocked until kanmer-docs reconciles the protected and governing documents. That is a prerequisite, not an open product question.

## Parked (explicitly deferred)

- None.

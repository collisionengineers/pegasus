2026-08-26 — Fresh CI for dfda320d passed Browser (49/49) but all SQL shards exposed incomplete test-only composition: production-profile test hosts lacked the two newly required queue URI settings, and isolated SQL service providers lacked the mandatory publisher ports. Commit eae300f9 supplies inert valid queue URIs to the shared production factory and the existing committed-work test double to the shared LocalDb harness; it also records that an image-registration replay deliberately has no transient newly-created outbox ID. Integration project Release build passed. Fresh full CI is running; do not merge until it is green.

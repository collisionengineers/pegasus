# Proof — INTK-020 (test-output + command-log)

Merged to dev in PR #478 (green CI) and promoted in main `f0b01f39`; deployed to production in release 15 (`6d04f89d`, smoke passed 2026-08-20). On merged code: UploadGroupStatus renders one "This submission" decision card while any member is undecided (group registration via the automation group key, group attach via per-member single-path replay keys, Cancel), per-file actions suppressed, and image members render thumbnails on UploadGroupStatus/UploadStatus/Unidentified Details/Intake Details via the staff-only inline image route. Evidence: `UploadConfirmationWebTests` (three new tests incl. group-register and group-attach round-trips) green in CI on #478 and in the release build; live upload surface verified rendering post-deploy (a real grouped upload is the operator's next natural action — the wiped estate was kept sterile).

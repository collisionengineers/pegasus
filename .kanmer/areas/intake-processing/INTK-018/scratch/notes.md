PR #453 CI: first run had every browser-lane test fail simultaneously with the identical "Connection Timeout Expired ... pre-login handshake" LocalDB error at the 17.5 s mark — runner-side SQL LocalDB flake, not the change. Verified by running the exact CI filter locally on this branch (`Category=Browser&Category!=Corpus`, MaxParallelThreads=2): 45/45 pass in 4 m 23 s. sql-integration (1-3) + unit + coverage all green on the same run. Failed job re-run (same run id 32332909130).

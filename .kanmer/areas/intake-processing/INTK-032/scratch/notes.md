2026-09-02 — Linked from EPIC-012. Until the Document Intelligence path lands, the Case page shows an Overview blocker "Original report verdict" with a staff action "Record verdict" (Repairable / Total loss + reason) that allocates the `a.`/`ap.` audit reference and completes the instruction requirement. Source `Downloads/Pegasus_UI_v2_src/src/21-case-sections.js` (`audit-verdict`). Owner of that action: this ticket, or a small linked fix if it lands before the DI port.

2026-09-02 — Joined EPIC-012. Plan input: the mockup crop tool in `Downloads/Pegasus_UI_v2_src/src/24-cropper.js` and the report-images grid in `22-case-engineer.js` §report: crop stored as fractions {x,y,w,h} of the rotated source plus rotation (0/90/180/270); roles Close-up / Overview / Supporting / Not used; supporting order by drag or move buttons; pointer drag/resize with clamping, keyboard nudge (arrows, Shift+arrows resize, R rotate), aspect lock (free / 4:3 / 3:2 / 1:1), live preview, Reset to original; validation exactly one Close-up and one Overview before report generation. Owns `Pages/Cases/Shared/_CaseReportImages.cshtml` and `wwwroot/js/cropper.js`; blocked by the EPIC-012 sections-move ticket.

2026-09-02 — kanmer-research wrapper started (Claude wrapper around gpt-5.6-terra xhigh, read-only checkout `.worktrees/research` at origin/dev 897db953). Research and files documents follow.

2026-09-02 — kanmer-plan wrapper (Claude around gpt-5.6-terra xhigh, read-only checkout `.worktrees/research` at origin/dev 897db953, clean after the run). Plan and checklist written; the two open questions stay unticked with the plan's ASSUMED defaults recorded beside them (Q1 approval as snapshot boundary; Q2 "Not used" suffices). Wrapper findings: the only approval caller is `Closure.cshtml.cs` `OnPostRecordReportApprovalAsync` with no Razor control posting to it yet; `CaseLifecycle.cs` is held by CASE-040 this wave; `OperatorLabels.CaseStage` is a method. Ticket stays in Preparing until the operator answers or parks the questions and ENG-034 (Report host) lands.

2026-09-01 (Claude controller, Phase 0 groom, operator-authorised recut): the PLAT-065 -> TICK-085 blocks edge is removed and TICK-041 -> TICK-085 is the only hard edge. This ticket owns the Glass's parser (`RepairSpecificationSourceRoute.Glasses` has no implementation on dev), explicit Glass's/Audatex format routing, the four embedded-text oracles, and a recorded `prebuilt-layout` fixture for YL69YFO through TICK-041's Core-neutral OCR result port so the parser proves the fifth oracle without Azure. PLAT-065 owns provisioning, RBAC and the live production canary that proves YL69YFO end to end after the release. Placement: build wave B; oracle lane reads PEGASUS_WORK_PACK_ROOT and hash-checks artifact-manifest.yml before parsing.

# Checklist — TICK-085

- [x] Extend the existing parser port and single PDF container; implement complete Glass coordinate parsing and explicit Glass/Audatex routing with narrow shared font qualification.
- [x] Wire existing document OCR pending/completed evidence through canonical ImportRawEstimate; use narrow shared import persistence without weakening Automation SaveEstimate, source authorization, replay or Engineer Current rules.
- [x] Replace Web bypass and provide durable-source Complete import; adapt real MCP and Glass callers and record actual scope/forged-source negative tests.
- [x] Prove four readable hash-bound full-line oracles and canonical callers, obtain root focused runtime and fresh scoped UI snapshot evidence, and retain every attempt in the report.
- [ ] During coordinated PLAT-065/TICK-041 release, prove genuine retained YL69YFO Azure OCR against the independently reviewed fifth full-row oracle, then complete exact-merge/final acceptance before Done.

## Source freeze

Implementation and focused tests are authored on the recorded isolated worktree.
Unchecked acceptance remains deliberate: root runtime/snapshot verification and
the genuine fifth retained OCR oracle are still outstanding. No builds/tests,
commit, PR or external service calls were performed by the author.


Pre-runtime review corrections: browser authority on source replay, persisted
assessment-editable-state gate before read and final save, and canonical Web
initial naming are implemented and frozen. Runtime/all-five acceptance boxes
remain unchecked; root owns the first combined verification. No test failure
has yet been observed on this branch because no runtime command has run.


Root first verification attempt 80403: restore PASS, Release build FAIL
58.02 seconds / 0 warnings / 5 test compile errors; no tests or TRX. Four mapped
fixture-only corrections are frozen for root rerun. All acceptance boxes stay
unchecked; the failure remains in the post-implementation report.

## Second root attempt and approved fixture correction

Session 24236: incremental Integration build PASS 17.71s / zero warnings;
Core 61 PASS and Integration 113 PASS / 4 FAIL, no skips. Both unique TRXs and
hashes are retained in the report, including all four full-row oracle passes
and only the fifth sample's six-page qualification. No fifth OCR PASS claim.

Root-approved three-file fixture correction is implemented and frozen:
existing processed-receipt setup reuse (real acceptance/version/completeness
preserved), omitted-null response assertion and existing Engineer refusal
wording. Plan 1432dbe470b90de2 / files 53c037ca57c47e28 were read back before
editing. Exact 17 consumers plus two assertion-only methods are the 19-method
root follow-up; no repeat of unchanged 113 passes. Bounded diff check PASS.
All acceptance boxes remain unchecked until root checks and remaining genuine
OCR/snapshot acceptance are disposed. No author build/test/PR or live call.

## Publication milestone approved — 2026-09-08

Root session 30380: incremental Integration build PASS 24.32s, zero warnings;
all 19 corrected/shared-consumer methods PASS, no skips (1m54s). Session 4872:
three canonical Case capture methods PASS 37s, scoped update 3 PASS 225ms,
scoped verify 3 PASS 6s, catalogue 60 routes / 67 prototypes / 0 broken PASS.
Both unique TRXs, all 24 fresh capture file hashes and exact generated-output
identities are in the report. Earlier five compile errors and four runtime
failures are retained, with no repeated unchanged 113-test cohort.

Root authorizes one [skip ci] dev PR and Review now. An independent review
precedes any merge. The fifth genuine OCR/full-row oracle remains unchecked
and keeps TICK-085 Verifying after integration; no Done or final v1 PASS.
No author test/build, independent review, merge, deployment or live call.

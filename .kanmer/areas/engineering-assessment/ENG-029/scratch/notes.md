2026-09-02 — kanmer-research started (Claude wrapper around gpt-5.6-terra high, read-only checkout `.worktrees/research` at 897db953 = origin/dev). Research and files documents to follow via set_ticket_doc.

2026-09-02 — kanmer-plan completed (Claude wrapper around gpt-5.6-terra high, read-only `.worktrees/research` at 897db953 = origin/dev; checkout clean afterwards). First Codex run exited 126 before starting (39 KB brief exceeded the command-line argument limit); the retry passed the brief as a file path and exited 0. Written: `plan/plan.md` and `checklist/checklist.md`. No operator-only open questions (Codex: none). One wrapper correction recorded in the plan: an existing `case-details--*` snapshot changed by this PR's own diff regenerates in this PR (capacity-one lease), new states stay UIIMP-014. Ticket left in `preparing`; not moved. Next: implementation waits for ENG-035, ENG-034, PLAT-068, CASE-040, DOCS-017 and the explicit `Details.cshtml.cs` whole-file hand-off from CASE-038/CASE-040.

2026-09-02 note from the DOCS-018 plan wrapper: DOCS-018 (fee-note preview, D42) is sequenced after ENG-029 merges and delivers the whole feature itself — `OnGetPreviewFeeNoteAsync` in `Details.cshtml.cs`, one `Preview fee note` anchor beside `Preview report draft` in `_CaseReport.cshtml`, one `PreviewFeeNote` key in `OperatorLabels.CaseWorkspace.EngineerSections`, and a test in `AssessmentReportDraftWebTests`. ENG-029's plan carries nothing for the fee note beyond the agreed-fee and description-line editors it already owns; it must not add a fee-note control or handler.

2026-09-03 — cross-model plan review. gpt-5.6-sol xhigh read the plan read-only in `.worktrees/research` at `897db953` (checkout clean afterwards; first invocation exited 126 on the 48 KB argv brief, retry passed the brief as a file path and exited 0). Verdict REQUEST CHANGES, 8 findings. Claude Opus re-verified each against the checkout and dispositioned all 8: 6 fixed as suggested, 2 fixed with the suggestion rejected (no operator reason control — the predecessor handler used a fixed system sentence; no extension of the shared `RetainableFormFields` allowlist — not an ENG-029 file, and it would duplicate the settlement field list). Plan and checklist rewritten via `set_ticket_doc` and confirmed on disk; the findings table is appended to the plan under "Plan review (2026-09-03, gpt-5.6-sol xhigh; dispositions Claude Opus)". No open question was raised. Ticket left in `preparing`; not moved.

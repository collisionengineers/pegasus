# Checklist — ENG-029 (2026-09-02)

- [ ] Dependencies merged on `origin/dev`: [[ENG-035]] (vocabulary, migration, derived figures, projection), [[ENG-034]] (section shells, Generate/Preview on the Case host), [[PLAT-070]] (D44 removal of the staff-review flags, which reshapes `CaseCompleteness` and the `CaseMutationPageModel` form-field lists), [[PLAT-068]], [[CASE-040]], [[DOCS-017]]; the explicit whole-file hand-off of `Pages/Cases/Details.cshtml.cs` from [[CASE-038]]/[[CASE-040]] is recorded on this ticket.
- [ ] Precondition: every D41 vocabulary constant exists (excess, settlement betterment, claimant VAT registered, reserve, repair duration, repair delays, report delay, storage per day, recovery, hire start, daily hire cost, diminution, salvage logistics) plus the Core-owned derived repair-cost/equity/ratio values — stop and report an [[ENG-035]] gap if any is absent.
- [ ] Precondition: `AssessmentPolicy.IsWritableState` includes `PostReport` so the D30 editable window matches `AssessmentAccessPolicy.IsReadOnly` — stop and hand the gap to [[ENG-035]] rather than widening the gate in Web.
- [ ] Precondition: `DetailsModel.Section` accepts `settlement` and `report` (added by [[CASE-038]]/[[ENG-034]]), so the section PRG resolves.
- [ ] `take_ticket` with worktree `.worktrees/ENG-029` and branch `ENG-029-settlement-report-editors`; lane refreshed with `git merge --no-edit origin/dev`.
- [ ] Verify at execution time which ENG-035 vocabulary constants exist for each D41 field, which ENG-034 `CaseWorkspace.EngineerSections` keys exist, and the post-[[PLAT-070]] shapes of `CaseCompleteness` and `AssessmentWorkspaceTestData.Create`; record the lists in scratch.
- [ ] Step 1: acquire the serialized `OperatorLabels.cs` lock and add only the ENG-029 editor labels the plan names (none already added by ENG-034).
- [ ] Step 2: inject `ISaveAssessment` into `DetailsModel` and add the single `OnPostSaveAssessmentAsync` handler in the predecessor's explicit shape (`36655f26^`) — `TryGetActor`, `IsOperationKeyValid`, `NewOperationKey`, own `try`/`catch`, `ClearLeaseState`, `HandleLeaseFailure` — **not** `ExecuteCaseCommandAsync` (it hard-codes `RedirectToDetails(id)` with no section), redirecting to `/Cases/{id}?section=settlement|report` on every path.
- [ ] Step 2: the request's reason is one fixed system sentence per save (Settlement, Report); no operator reason control is added, and the expected version comes from the loaded case.
- [ ] Step 2: a refused, stale or lease-lost save surfaces Core's message through the Case page's existing error `TempData`; `RetainableFormFields`/`BooleanFormFields` in `CaseMutationPageModel.cs` are not edited.
- [ ] Step 3a: replace the `_CaseSettlement.cshtml` shell body with the lease-aware Settlement form (four outcomes; total-loss category/salvage; every D41 field; repair cost from `EstimateTotals.Compute`; equity/ratios only from Core values).
- [ ] Step 3b: replace the `_CaseReport.cshtml` shell body with the lease-aware Report form (comments, history check, agreed fee, description lines; CASE-040 sign-off shown read-only; named readiness from `AssessmentReportProjection.Prepare`; ENG-034 Generate/Preview reused).
- [ ] [[ENG-031]]'s `_CaseReportImages.cshtml` mount stays where ENG-034 places it, outside this ticket's form, lease and read-only gates, so the D46 crop entry point survives without Edit Case; ENG-029 adds no crop or curation markup.
- [ ] Both partials omit every mutation control when `AssessmentIsReadOnly` is true; no explanatory copy; no disabled placeholder.
- [ ] Step 4: create `tests/Pegasus.IntegrationTests/CaseAssessmentEditorsWebTests.cs` proving the single `SaveAssessmentRequest` per post (including its exact fixed reason), version/operation-key/lease inputs, the success **and** failure `Location` headers carrying `?section=`, a refused/stale save surfacing Core's message, saved values on preview, every D41 field round-tripping, four outcomes and total-loss-only salvage controls, a save permitted in `PostReport`, named readiness without a percentage, read-only omission at `PostReportComplete`, and read-only sign-off display.
- [ ] `git diff --name-only origin/dev` shows only owned paths (plus a regenerated existing `case-details--*` snapshot if `-Verify` required it); no Core, Infrastructure, migration, CSS/JS, catalogue, or other lane's file.
- [ ] `dotnet restore ./Pegasus.slnx --locked-mode` passes.
- [ ] `dotnet build ./Pegasus.slnx --configuration Release --no-restore` passes.
- [ ] `dotnet test ./Pegasus.slnx --configuration Release --no-build --filter "Category!=Corpus"` passes (the canonical gate; Browser is not excluded).
- [ ] `./scripts/Update-TestUiSnapshots.ps1` run; any changed existing `case-details--*` state committed in this PR; no new state added.
- [ ] `./scripts/Update-TestUiSnapshots.ps1 -Verify -SkipCapture` passes.
- [ ] `./scripts/Test-UiCatalogue.ps1` passes.
- [ ] Simplification pass run over the branch diff; findings and dispositions recorded under a dated heading in the plan.
- [ ] post-implementation report written
- [ ] PR opened with Kanmer: ENG-029

2026-09-02 (wrapper, second pass): an earlier run had already executed Codex
(gpt-5.6-terra xhigh, `.worktrees/research` at cad00be9, checkout clean) and
written `research`, but stopped before `files`. Reused that run's Document 2
from the scratch output rather than re-running Codex; added the two
`docs/design/test-ui/**` rows the research wrapper-check called for, and
recorded the out-of-lane Assessment-route callers found by
`grep -rn 'Cases/Assessment|/Assessment' src/Pegasus.Web` (Details.cshtml:276
→ CASE-038; Operations/Index.cshtml.cs:427 → still works through the 301).
No operator questions.

2026-09-02 (wrapper, third pass — currency check only): `research` and `files` already existed and match the ticket body (D30, 301 to `/Cases/{id}?section=estimate`, five sections, ENG-028 reuse, read-only once Complete, catalogue update). Confirmed `origin/dev` is still `cad00be9d` (= `.worktrees/research` HEAD, checkout clean, no commits since) and re-spot-checked five VERIFIED claims in the main checkout: `AssessmentAccessPolicy.CanOpen`/`IsReadOnly` (`AssessmentWorkspace.cs:45-59`), catalogue `visual` entry for `Pages/Cases/Assessment/Index.cshtml` (`catalogue.json:286-292`), `Suggestions.cshtml:37` Back link, `Details.cshtml:274-281` Open Assessment, `RedirectPermanent` stubs (Triage/Unidentified/Cases Index), `_CaseValuation.cshtml` absent, `Test-UiCatalogue.ps1` lines 20/37. Codex not re-run. Feature profile leave-preparing still needs `plan` and `checklist`. No operator questions. Note: kanmer MCP read tools returned only the project header this session; board state was read from `.worktrees/kanmer/.kanmer` directly.

2026-09-02 (wrapper, kanmer-plan): Codex gpt-5.6-terra xhigh ran read-only in `.worktrees/research` at `origin/dev` = `897db953` (three DELIV-041 docs-only commits since the research's `cad00be9d`; checkout clean before and after). The 43 KB prompt exceeded the Windows argument limit on the first attempt (exit 126, "Argument list too long"); re-run with the prompt on stdin (`codex exec … -`), exit 0. Wrote `plan/plan.md` and `checklist/checklist.md` (20 items). Wrapper-checked every reuse claim (17 vocabulary paths, three `OperatorLabels` helpers, nine `Index.cshtml` line ranges, five test fakes, eleven governing-doc headings) and added two corrections: (1) CASE-038 must ship the four `_Case{Damage,Estimate,Settlement,Report}.cshtml` files as heading-only shells so its `<partial>` composition renders green before ENG-034 replaces them (Files rows become `change`); (2) the 301 `Location` is exactly `/Cases/{id}?section=estimate`. Handler-host decision: option A — CASE-038's `DetailsModel` hosts the moved Assessment handlers; seven-item contract recorded for CASE-038's plan. Codex asked no operator questions; no open-questions doc. Leave-preparing gate (research, files, plan, checklist) is now complete; ticket left in `preparing`. kanmer MCP reads still return only the project header this session; board state verified from `.worktrees/kanmer/.kanmer` directly.

2026-09-02 (wrapper, kanmer-plan re-entry — currency check only): `plan/plan.md` (19.8 KB, H1 `# Plan — ENG-034 (2026-09-02, gpt-5.6-terra xhigh)`) and `checklist/checklist.md` (20 items) already existed and post-date `research` (activity log: plan/checklist written 2026-09-03T00:15Z; research 2026-09-02T20:53Z, files 20:56Z). Codex not re-run per step 1. `origin/dev` = `897db953` = `.worktrees/research` HEAD, checkout clean, unchanged since the plan was written. `data/board.yml` feature profile leave-preparing = research, files, plan, checklist — all present; no `open-questions/` doc. Plan's out-of-owned-path files (`OperatorLabels.cs`, `docs/design/test-ui/**`, six Assessment test files, new `CaseEngineerSectionsWebTests.cs`) are all rows in `files.md`; Details.cshtml(.cs) named as CASE-038 dependency, not planned. Ticket left in `preparing`; no move made. kanmer MCP reads still return only the project header; board read directly from `.worktrees/kanmer/.kanmer`.

2026-09-02 (cross-reference from the CASE-038 plan wrapper): CASE-038's plan (`plan/plan.md`, "Contract reconciliation with ENG-034") reconciles this ticket's seven-item contract and settles the handler-host decision as **option B**, not A: CASE-038 ships the eleven hosts, the fragment convention, the four heading-only shells, the lease handlers and `AssessmentIsReadOnly` on `DetailsModel`; it does not copy the nine Assessment handlers (AGENTS.md rules 1, 2, 8, 14 — unreachable code and a duplicate handler set on `dev` until this ticket deletes the originals). ENG-034 adds the nine handlers, `OnGetPreviewReportDraftAsync`, the assessment projection and estimates/editor state to `DetailsModel` in the same PR that moves the forms and retires the route; `Details.cshtml.cs` is a merged, unleased file by then because CASE-038 blocks ENG-034. Orchestrator action before ENG-034 executes: amend this plan's Step 1 ("no new host, lease, or handler path is created"), Step 3 ("remove the source handler surface only after CASE-038 owns it") and the option table (row B adopted). Nothing else changes. The CASE-038 plan keeps an "Option A contingency" step in its scratch if the orchestrator overrides.

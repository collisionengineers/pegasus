2026-08-20 implementation notes:
- Real Audatex sample verified in the session scratchpad only (never committed): the -layout text extraction genuinely mis-associates values (the wrong-money hazard is real, not theoretical); PdfPig baselines showed the value column sits exactly ~1.0pt below its description row, which made deterministic pairing + a deferred "bare row" rule (a guideless description row is a new line only if a value row follows it) sufficient. All four section checksums (labour WU, paint WU, parts sub-total, extras total) match the document's printed totals.
- Retry-after-phase-B-failure nuance: if the draft fails after the document was retained, a retry with the fresh form key retains a second occurrence of the same file (custody keeps both; content is identical by hash). Accepted as safe; noted in the report.
- Test auth: IntakeWebApplicationFactory default does NOT use the IntegrationTest auth handler — the Engineer-role tests need `useIntegrationTestAuthentication: true` plus the new additive `X-Test-Roles` header.

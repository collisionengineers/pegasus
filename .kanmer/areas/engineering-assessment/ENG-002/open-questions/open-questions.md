# ENG-002 open questions

## Parked (explicitly deferred)

- [ ] Please supply a real Glass's estimate export sample (file format and a representative document). The corpus contains no Glass's material, so the `Glasses` import route ships parser-less; the route enum, custody path, and specification landing are already in place for it. — Parked: operator-blocked, does not block the Audatex + MCP + drag-and-drop delivery.
- [ ] Does the operator want re-import to replace an existing *draft* specification in one action (today a second import is refused while a draft exists; recovery is the existing line-replacing assessment save)? — Parked: usability follow-up, current behaviour is safe and honest.

# Files — ENG-030

## Where the change lands

| Path | Why |
|---|---|
| `src/Pegasus.Web/Pages/Cases/Assessment/Index.cshtml` | Delete the two `<span class="gated">…Glass's…</span>` / `…Audatex…` control blocks at lines 219–224 (no form, no `href`, no `data-dialog-open` — permanently inert). Reword the razor comment at line 155 ("Glass's and Audatex are the D7 disabled seams (EXT-09)") so it no longer describes controls that no longer exist. |
| `src/Pegasus.Web/Pages/Cases/Assessment/Index.cshtml.cs` | Delete the `EstimatingServiceCondition` property and its doc comment (lines 322–329) — the ticket's "hard-coded direct-service condition property." Reword the class `<summary>` (lines 34–40), which currently lists "the Glass's and Audatex disabled seams" among what the record bar draws. |

## Context files

| Path | What it tells the implementer |
|---|---|
| `src/Pegasus.Web/Pages/Cases/Assessment/Index.cshtml:196-218` | The live "New estimate" and "Import estimate" controls that sit immediately beside the two being removed — both post through a form/dialog and are gated by `ActorIsEngineer`/`ImportCondition`, not by `EstimatingServiceCondition`. Do not touch; this is the boundary of the deletion. |
| `docs/design/README.md:689-719` (Absent versus disabled) | The D7 seam-table convention this ticket is narrowing. On current `dev` the table still lists "Glass's, Audatex \| Assessment record bar sources \| EXT-09" — that row is corrected by PR #643 (DELIV-040), not by this ticket. Do not edit this file here. |
| EPIC-011 `context.md` §1.9, §1.14, D7, D21 | The governing text for exactly this removal: no Import-estimate-dialog/Glass's/Audatex launch control; Experian and Cazana remain approved D7 seams; manual Glass's/Cazana/Engineer valuation records stay active. Quote D21 in the commit/PR body if precedent is useful. |
| `docs/frd/frd-07-eva-and-external-engineering-handoff.md:130-145` (External boundary) | Direct estimating integrations (Audatex, Glass's) are already documented as deferred pending real contracts/auth/idempotency/operator acceptance — this ticket does not need a new FRD edit; the existing text already supports the removal. |
| ENG-025 `proof/proof.md` ("Reversed out of Done under the strict rule 14" section) | Records exactly why these two controls disqualified ENG-025 from Done and names ENG-030 as the ticket that discharges the finding — read it to understand the acceptance bar (a disabled control with no reachable enabled state is never a delivered capability, D21) before writing this ticket's own proof. |
| `src/Pegasus.Web/Presentation/OperatorLabels.cs:381-382` | `RepairSpecificationSourceRoute.Glasses`/`.AudatexPdf` labels for **imported** estimates — a different capability from the launch controls; confirms nothing here needs to change. |
| `tests/Pegasus.IntegrationTests/Browser/AssessmentReadinessSummaryBrowserTests.cs` | The one browser test on this page; its control-count assertions are scoped to `.record-bar-end .gated` and `.estimate-tabs`/`.estimate-empty`, neither of which the two deleted buttons belong to — confirms no test edit is needed, and confirms the locator patterns to reuse if the implementer wants a belt-and-braces assertion. |

## Ripple effects

- **Callers**: none — `EstimatingServiceCondition` has exactly one reader
  (the two view spans being deleted alongside it); no other file references
  it (`git grep -n EstimatingServiceCondition origin/dev -- 'src/**' 'tests/**'`
  returns only the two `Index.cshtml`/`Index.cshtml.cs` hits already listed
  above).
- **Tests**: no existing test asserts on these two buttons or on a total
  record-bar control count; none need editing per the research above. The
  test runner (not this ticket's implementer) will confirm build/test still
  green — M6 forbids running tests here.
- **ENG-025**: this removal is the literal discharge of the finding that
  reversed ENG-025 out of Done. No file edit in ENG-025's own scope is
  required; the Kanmer `blocked_by: [ENG-030]` edge clears once this ticket
  reaches the state the orchestrator's wave gate checks for.
- **Test UI catalogue/snapshot** (`docs/design/test-ui/catalogue.json`,
  `docs/design/test-ui/pages/case-assessment--default.html`): the snapshot
  currently still renders both buttons and will go stale in the removal
  direction once this ticket's code lands. Under the `test_ui_catalogue`
  lock and the programme's per-merge regeneration convention (UIIMP-011),
  the implementer does not regenerate it — M6 forbids running snapshot
  scripts. Disclose the staleness in the post-implementation report/proof
  rather than silently leaving it unmentioned.
- **DELIV-040 PR #643** (open, `docs/capabilities.md`, `docs/boundaries.md`,
  `docs/design/README.md`): already carries the capability/boundary-map
  correction this removal implies ("allocated to ENG-030, not delivered").
  No action needed from this ticket; note the PR number in the plan so a
  later reader does not duplicate the docs edit.

## Out of scope

- `docs/capabilities.md`, `docs/boundaries.md`, `docs/design/README.md` —
  owned by PR #643 (DELIV-040), already open and covering this exact
  removal; editing them here would duplicate/conflict with an open PR
  outside this ticket's file scope.
- `docs/design/test-ui/catalogue.json` and
  `docs/design/test-ui/pages/case-assessment--default.html` — the
  `test_ui_catalogue` lock; regeneration happens once per merge on the
  merging branch, not in this worktree (M6, and precedent from ENG-025's
  proof).
- The Import estimate dialog/handler, `ImportCondition`, and the whole-page
  drop import flow (ENG-028's delivered work; the eventual whole-page-drop
  replacement is ENG-033/D16) — a different capability, not named in this
  ticket's Approach.
- `OperatorLabels.cs`, manual `ValuationSource.Glasses` records, and the
  Experian/Cazana D7 disabled seams — explicitly preserved by D21 and D7;
  no file in that surface is touched.
- `src/Pegasus.Core/Assessment/Estimates.cs`, `EstimateImport.cs`,
  `RepairSpecifications.cs` — inside the `assessment_shared` lock group but
  not named by this ticket's Approach or Owns; no Core change is implied by
  deleting a Web-only condition property and its two view spans.
- TICK-085 (Glass's file import) and ENG-032 (Audatex full-report
  reconciliation) — separately ticketed, unaffected by this removal.

# Proof — ENG-006 (test-output)

Merged to dev in PR #487 (green CI) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`). On merged code: 39 hint sentences removed across the assessment sections with 24 "Required." hints becoming visual label markers (`aria-required` on controls); the three explainer cards, explaining captions, Working column, remove-and-refit note and section ledes are gone; the repair-lines grid dropped its two provenance micro-columns and its 78rem floor; the damage diagram is nine clickable regions where one click saves `assessment.impact_location` through `ISaveAssessment` under a handler-acquired lease (the report draft already requires and prints that field — saving is the export); the Impact location dropdown preselects the saved value; the method radios preselect the case's recorded inspection mode. Evidence: AssessmentDamageAndCopyWebTests 8/8 (six no-copy sections, damage save round-trip with the recorded `SaveAssessmentRequest` and highlight re-render, method preselect), AssessmentVehiclePrefill/EstimateImport/SendToAi 12/12, AssessmentPolicyTests 20/20 — green in CI on #487 and the release build. Live: an assessment page needs a case; the wiped estate defers the live view to the operator's next case.

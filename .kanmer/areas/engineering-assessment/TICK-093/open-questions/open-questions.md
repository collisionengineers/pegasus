# Open questions

No operator-only question remains. Ordinary assessment rendering uses one accepted, versioned canonical repair specification with route provenance; Audit separately retains two role-labelled accepted versions.

## Parked (explicitly deferred)

- Per-provider extraction/mapping work remains with its owning capability.
- Audit template remains deferred.

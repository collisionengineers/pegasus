## Backfill post-implementation report (VERIFY2, 2026-08-20)

No implementation occurred under this ticket. AI-09 (durable idempotent Send-to-AI work request, pointer-only hand-off, ADR-0021) was already implemented and covered by round-trip integration tests before this ticket was worked; the code matches its capability text clause-by-clause (see `research.md`): `SendCaseToAi`/`ChannelAiHandOffTransport` (`src/Pegasus.Web/AiWork/`), `AiHandOffPointer` (pointer-only hand-off), `CaseVersionAtSend` (immutable case/version stamp), idempotent replay, `Created→HandedOff→Completed/Failed/Cancelled/Expired` tracking-record status states, `EfAiWorkRequestStore` (durable store), and the unconfirmed-writes review path at manual engineer assignment (ADR-0021).

**This ticket stops at `verifying` and does not proceed to `done`.** The decisive fact is the production composition gate: `SendToAiOptions.TryCreate` throws `InvalidOperationException` if `Features:SendToAi` is ever set outside the `DevelopmentOffline` runtime profile (`SendToAi.cs`); the flag is absent from `infra/modules/platform.bicep` (production Web app settings), and production runs `ASPNETCORE_ENVIRONMENT=Production`. This is a closed composition gate, and per this run's operating instruction a closed composition gate means the capability is NOT delivered live, regardless of how complete the code is. `docs/capabilities.md`'s own AI-09 row already states production activation needs a separate non-preview transport decision that has not been made.

`deployment` is left unset (not `production`) for this reason. What activation needs: (1) the non-preview transport decision named in ADR-0021's own stated next step, (2) setting `Features:SendToAi` (or equivalent) in production app settings once that decision is made, (3) live evidence of a real Send-to-AI round trip in production after activation.

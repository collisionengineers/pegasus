## Plan — TICK-024 (MCP-02) — retrospective backfill

Reconcile the board with the already-shipped Case-action tools. Confirmed file presence at production SHA `2325ed4a`, confirmed shared Core use cases (no duplicate implementation), re-ran `AutomationMcpIngressTests` (6/6). No governing document modified; FRD-10 already owns this behaviour.

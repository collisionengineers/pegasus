# Research — TICK-061: provider credential lifecycle

## Question

How should Pegasus issue, reset, revoke, pause, and resume a Principal's provider credential through the redesigned Administration surface while preserving principal isolation and one-time secret handling?

## Findings

- ADR-0004 and FRD-09 require separately issued principal-scoped client IDs and opaque secrets, hash-only storage, rotation, and revocation.
- Existing Organization/Principal administration already has Administrator-only Razor Pages, Core authorization, expected-version checks, reason, operation key, and durable history (`src/Pegasus.Web/Pages/Administration/Organizations/Edit.cshtml.cs`, `src/Pegasus.Core/Cases/OrganizationAdministration.cs`, `src/Pegasus.Infrastructure/Persistence/EfOrganizationAdministration.cs`).
- The Automation administration surface provides a close UI precedent for enable/disable and rotate-once secrets, but provider credentials belong to the Principal surface, not Automation (`src/Pegasus.Web/Pages/Administration/Automation/Index.cshtml.cs`).
- PLAT-028 now owns the consolidated Organizations/Principals redesign and provider controls; PLAT-024 is archived.
- The operator chose principal-level pause: deny new submissions immediately while authenticated reads of prior receipts/results remain available. Revocation instead invalidates authentication.
- One credential per Principal is the smallest v1 contract; a second concurrent key has no current second caller.
- No credential entity or authentication handler exists today, so API-04 must add an external security boundary, persistence, migration, and Web composition.

## Implications

Add one Principal-owned credential record with immutable client ID, password-hasher output, lifecycle timestamps, enabled-for-submission flag, version, and no recoverable secret. Generation and reset create a cryptographically random secret shown once; reset immediately replaces the hash. Revocation clears/invalidates the hash. Pause changes only submission permission. Every mutation requires Administrator authorization, expected version, reason, replay-safe operation key, and permanent action history. PLAT-028 renders the controls on the redesigned Principal detail.

## Open questions

The operator decisions needed for a v1 plan are resolved. Multiple simultaneous credentials and live rollout remain explicitly deferred.

## Azure architecture refresh — 2026-08-21

### Verified live facts
- Production already has Azure SQL, an RBAC-enabled/purge-protected Key Vault, and a managed identity on the Web Container App.
- No provider credential configuration exists in the Web environment.
- Microsoft positions Key Vault secrets for application credentials/API keys and recommends rotation and monitoring: https://learn.microsoft.com/azure/key-vault/secrets/secure-secrets

### Design consequence
A provider's clear secret is generated by Pegasus, displayed once, and never needs to be recovered by Pegasus. Persist the verifier/hash plus lifecycle metadata in the existing Azure SQL database, not the clear secret in Key Vault. Key Vault remains appropriate for Pegasus-owned recoverable service credentials, but using it for one secret per Principal would add a second lifecycle owner and allow recovery contrary to this contract. Reuse the Web managed identity and existing SQL access. TICK-061 supplies the Core lifecycle, persistence, and verification port only; the first authentication handler is composed with TICK-058's real endpoint so no dormant authentication scheme ships.

## Implementation refresh — 2026-08-28 (read-only checks on origin/dev 658a7984)

- EPIC-011 D8 makes the Principal "Pegasus API key" the Provider API
  credential; FRD-04 § Principals administration already names issue, reset,
  revoke, pause, resume with a reason, secret shown once, hash retained.
- Hashing convention (checked): staff passwords go through ASP.NET Identity's
  `UserManager<PegasusIdentityUser>` — PBKDF2 via `PasswordHasher<T>`.
  `Pegasus.Infrastructure` already references
  `Microsoft.AspNetCore.Identity.EntityFrameworkCore`, so
  `PasswordHasher<PrincipalApiCredentialEntity>` is available with no new
  package. No `Rfc2898`/`PasswordHasher` use exists in Core or Infrastructure
  today; SHA-256 in the repo is content/request fingerprinting only.
- Freshest schema precedent: AUTO-011 — `AiJobEntity` + model config +
  `20260828084601_AiJobs` + `20260828084644_GrantAiJobs` (web role
  SELECT/INSERT/UPDATE, no worker) + `Get-ExpectedMatrix` block in
  `scripts/Invoke-AzureDatabaseBootstrap.ps1` + names appended to the applied
  migration census in `IntakePersistenceIntegrationTests.cs`.
- Administration precedent: `OrganizationAdministrationPolicy.Normalize`
  (Administrator right, identifier, expected version, reason/operation-key
  length) and `EfOrganizationAdministration` (Serializable transaction,
  operation receipt replay via `OrganizationAdministrationOperations`,
  `ActionHistory` with before/after JSON, concurrency retry).
- Migration tooling: `dotnet ef` 10.0.10 is installed;
  `Build-ReleaseArtifacts.ps1` fixes the invocation shape
  (`--project src/Pegasus.Infrastructure --startup-project src/Pegasus.Web`).
- Capabilities arithmetic (checked): Now 140 / Next 29 / Later 35 /
  `0.1.0-alpha.1` 140 / `0.4.0` 5. Moving API-01 and API-04 gives
  Now 142 / Next 27 / `0.1.0-alpha.1` 142 / `0.4.0` 3.

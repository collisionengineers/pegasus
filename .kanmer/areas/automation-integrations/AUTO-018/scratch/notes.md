2026-09-02 — kanmer-research started (Claude wrapper around gpt-5.6-terra high; Codex reads `.worktrees/research` at origin/dev, wrapper owns board writes).

2026-09-02 — research and files written (gpt-5.6-terra high, wrapper spot-checked 3 VERIFIED claims, dropped the "CLAUDE.md is empty" claim). One operator question recorded in open-questions (MarketResearch completion: terminal Completed on evidence write vs DraftReady until staff review). Moved backlog → preparing. Next: kanmer-plan (plan + checklist); the open question must be answered or parked before leaving preparing. Shared locks needed: Presentation/OperatorLabels.cs and Persistence/Migrations/**.

2026-09-02 — kanmer-plan (Claude wrapper around gpt-5.6-terra high; Codex read `.worktrees/research` at 897db953, clean before/after, exit 0, one run). plan/plan.md and checklist/checklist.md written; open-questions kept as one unticked item with the new FRD-11 evidence ("None on the job", frd-11:270) so the operator can answer. Plan default: MarketResearch completion → DraftReady (existing actor rules untouched); the alternative is one Core line. Guide month: AUTO-018 adds the nullable Core field + column (it owns Valuations.cs and the migration), CASE-029 owns the dialog entry (FRD-06:218-220). Gate: research, files, plan, checklist present; the unticked open question still blocks leaving preparing. Next: operator answers (tick or park the question), then kanmer-execute. Shared locks needed: Presentation/OperatorLabels.cs and Persistence/Migrations/**.

2026-09-03 — cross-model plan review (gpt-5.6-sol xhigh read in `.worktrees/research` at 897db953, clean before and after, exit 0 on the second attempt; the first attempt failed with E2BIG because the 33 KB prompt was passed as argv — rerun by pointing Codex at the prompt file). Verdict REQUEST CHANGES: 5 blockers, 2 should-fixes; Claude added 2 more (C1 valuation-source label with no caller, C2 overloaded generic complete tool). All 9 verified against the same checkout and dispositioned in the plan's new "## Plan review" table: 8 fixed, 1 partly rejected (migrations/OperatorLabels are capacity-one shared locks, not another lane's property — the plan now states the lock protocol instead). plan/ and checklist/ rewritten and confirmed on disk; open-questions unchanged (its one question was already resolved on 2026-09-03 and nothing new needed the operator).

Material plan changes: guide month removed from AUTO-018 entirely (D40 / frd-06:218-220 give it to CASE-029); new Step 4b adds `MarketResearch` to `CanCompleteByHand` in `Pages/Operations/Index.cshtml.cs` so the settled "existing staff confirmation" is actually reachable (a minimal, recorded extension beyond the approximate owned paths — no other wave-1 lane owns that file, and no review flag/dialog is added, D44); MCP completion becomes its own tool `pegasus_ai_job_complete_market_research`; the two-scope workflow (`automation.jobs` + `automation.cases` for the edit lease) is stated and tested; `MaximumDocumentBytes` moves to `AutomationMcpErrors` as one rule; only the job-kind label is added; a new "Evidence tier and activation" section records that no production creation caller exists until CASE-029 merges.

Note for the implementer: `files/files.md` was NOT rewritten in this pass and is now stale in three places — it still lists the guide-month column, the `AI market research` valuation-source label, and omits `Pages/Operations/Index.cshtml.cs`. The plan's per-step file lists are authoritative. Next: kanmer-execute (no stage move made here).

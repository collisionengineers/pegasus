## Plan — TICK-025 (MCP-03) — retrospective backfill

Reconcile the board with the already-shipped intake-queue tools. Judged scope against `docs/capabilities.md` MCP-03's own row (queue list + durable submission only — not the broader intake-action surface some research notes elsewhere describe as missing). Confirmed file presence at production SHA `2325ed4a`, re-ran `AutomationMcpIngressTests` (6/6). No governing document modified; FRD-10 already owns this behaviour.

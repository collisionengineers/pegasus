2026-08-20 first-pass findings: UnidentifiedMcpTools defines list/get/resolve but AutomationMcpExtensions does not register it; exact tools/list inventory test also omits all three. INTK-007 shipped the class inside a cross-cutting intake change with no HTTP integration tests, and TICK-025 later treated file presence at production SHA as exposure. Even when registered, get returns only aggregate metadata/history; staff detail additionally uses IGetIntake and staff source download uses IDownloadIntakeSource, both already Automation-authorized through PerformCasework. SubmissionGroup origins require enumerating the existing group members rather than pretending there is one receipt. No other [McpServerToolType] class is unregistered. Triage has no MCP class or approved inventory and is split to AUTO-005 because it is a distinct workflow/authority decision. AUTO-003 already owns missing classified-mail callers.

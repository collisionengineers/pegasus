# Checklist — TICK-194

- [ ] Create the isolated task worktree, claim the ticket, and add the root task plan.
- [ ] Implement the explicit before/head main-history validation script with fail-closed diagnostics.
- [ ] Invoke the guard for pushes to `main` from the existing full-history `changes` job.
- [ ] Add synthetic Git-history architecture tests for allowed merge-only and rejected direct, mixed, missing, zero, and rewritten histories.
- [ ] Run focused tests, Release build, applicable repository checks, and confirm the diff excludes UI/design paths.
- [ ] Write the implementation report, commit, push, open the `dev` PR, record traceability, and move the ticket to Review.

## Progress notes

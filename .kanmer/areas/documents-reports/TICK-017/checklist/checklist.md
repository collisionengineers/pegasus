# Checklist — TICK-017 (DOC-01)

- [x] Distil contract/caller/failure into proof.md "What was verified"
- [x] Run focused custody suites: ProductionBoxCustodyTests, CustodyOutboxIntegrationTests, ProductionCompositionTests
- [x] Overwrite placeholder proof.md ("Operator confirmed") with real `dotnet test` output (local caller-proof tier) + "Not covered" (live Box target, migration, deployment, operator acceptance)
- [x] Confirm INT-25 reference forms (`QDOSyyNNN`, `a.`/`ap.`) satisfy `CustodyNames.SafeName` — closes blocks-INT-25 handoff (dot is not an invalid char; forms ~9–12 chars, all valid)
- [~] Record live-tier deferral + the "wire disposable subtree 392761581105?" decision; move ticket to `review` (not `done`) — moved to `review`; live-approval decision pending user input

## Progress notes

- 2026-08-13: Four local boundary behaviours (immutable naming, response-loss-safe binding, fail-closed conflicts, human reasoned recovery) found already implemented and test-covered (see research.md). No source change made. Release build 0 errors; custody suites 34 passed (LocalDB). Replaced placeholder proof.md with real evidence. Reference-format handoff from INT-25 confirmed. Entire remaining tier (live Box target, migration, deployment, operator acceptance) is requires-live-approval and deferred. Moved to `review`; did not self-advance to `done`.

- 2026-08-14: Operator reports Box case folders are NOT created in the running system. Box custody is only wired in the Production composition and is enqueued only AFTER a case is accepted, so this is downstream of the INT-25 case-creation failure (blocks edge TICK-012 → TICK-017). Green tests are fake-Box-backed and do not prove a live Box call. Reopened to `in-progress` for live-defect investigation.

- 2026-08-14: DOC-01 confirmed strictly downstream of the INT-25 live defect. Prod `ExternalWorkItems=0` → no custody/Box work was ever enqueued, because no case was ever created (custody is enqueued at case acceptance). Box is correctly wired to root `405543781910` in code/infra and on the live worker+web (Production profile) — no Box wiring/ID fix needed. DOC-01 stays blocked by TICK-012 until automatic allocation runs in prod.

- 2026-08-14 (resolution plan approved; corrected diagnosis): Box custody adapter + root 405543781910 verified correct in code/infra/live worker+web config; the root has 0 children only because no case is minted — strictly downstream of the INT-25 deploy gap (deployed alpha.9 / 75f39c70 is missing fix #2 73a3380d; QDOS principal is already seeded). The literal "fake box link" is the replaced/dead **"Box file requests"** feature (EfBoxFileRequestStore → /Requests/Box/{id}?token=… → 404). Operator confirmed that feature was replaced (by Public upload requests) and is NOT to be made real.
  - Plan (approved): **Phase 1** deploy origin/dev→main [GATED: MERGE AUTH + deploy approval] → cases mint → real Box folders created under 405543781910. **Phase 2** (this branch task/doc-01-box-resolution): (2a) add a real clickable https://app.box.com/folder/{CustodyRootRemoteId} link on the case page (none exists today); (2b) remove the dead Box-file-request feature end-to-end + drop its table. **Phase 3** refresh docs/operations.md + docs/current-architecture.md after the first live case.

- 2026-08-14 RESOLVED (live) + PR #376 opened to dev (branch task/doc-01-box-resolution). Core outcome achieved: forwarded QDOS audit auto-created case QDOS26001 and a REAL Box folder a.QDOS26001 (id 409001353539) under root 405543781910, CustodyState=confirmed. Box custody works end-to-end.
  - PR #376 contains: (1) `AddWorkerCaseCreationGrants` migration codifying the live worker-grant hotfix (durability); (2) DOC-01 UI — real https://app.box.com/folder/{CustodyRootRemoteId} link on the case Document-custody panel (rendered only when CustodyState=Confirmed; plain state label otherwise, never a dead link); (3) removal of the dead/replaced "Box file requests" feature end-to-end + DropBoxFileRequests migration. Real BoxCaseCustody untouched. Full-solution Release build 0/0.
  - REMAINING GATES before this is fully closed: independent pre-merge review of PR #376; CI green; refresh docs/operations.md + docs/current-architecture.md for the 2026-08-14 deploy + first live case (Safety-rails, owed); MERGE AUTH + deploy the branch's migrations (grant + drop) to prod; optional: clear pre-fix stuck-receipt backlog via staff Retry; fix the Invoke-AzureDatabaseBootstrap expected matrix; quiet the SentEvidencePoll "mailbox not approved" noise.

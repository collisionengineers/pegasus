# Open questions

No operator-only question remains. Existing evidence supports per-template density only; the approved initial assessment/fee-note family has no auto-fit target and uses normal accepted styling with clean page flow.

## Parked (explicitly deferred)

- Any future template-specific page target requires its own accepted visual evidence.

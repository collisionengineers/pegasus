# Proof — DOCS-005 (test-output + command-log)

Merged to dev in PR #488 (green CI) and promoted in main `f0b01f39`; deployed in release 15 (`6d04f89d`). On merged code: no binding JSONs are written (case root, audit folder, accepted source), the DB-stored remote folder id remains the identity authority, a same-name folder is adopted as the case's, the staged owner-token create/promote is unchanged, and each instruction attachment lands beside the source at `002` onward (Box + Local, replay-verified). Evidence: ProductionBoxCustody/BoxDocumentContentStore 19/19 on the unbound behavior, the new end-to-end `AcceptedCaseRetainsInstructionAttachmentsBesideTheSource` through ProcessIntake → acceptance → the real processor → Local custody, and the custody batch 45/45 — green in CI on #488 and the release build.

**Deployment step executed post-deploy (2026-08-20, operator-approved Box write)**: all eight legacy binding files deleted (HTTP 204 each) — `pegasus-case-binding.json` and `Evidence/Original instruction/pegasus-accepted-source-binding.json` in each of `a.QDOS26001`–`a.QDOS26004` under custody root `405543781910`. Note for the operator: those four case folders now belong to wiped test cases (PLAT-017); they were left in place because the wipe scope was Azure-side only — say the word if they should go too.

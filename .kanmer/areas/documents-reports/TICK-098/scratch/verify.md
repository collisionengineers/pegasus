2026-08-20 — Verification blocked. PR #466 is merged to `dev` as `b36c66662288adb0727299276f675337442a1e22`, but that commit is not reachable from `origin/main` (main is `2325ed4a31d7dad65a00a7ae5ea0c41ca869bfa5`). The merged FRD-11 states Audit has no active caller and remains unavailable on the initial renderer surface; the registered caller is the assessment renderer only. Therefore proof cannot truthfully assert that Audit and Inspection currently use the same caller. Ticket remains Verifying; retain branch/worktree pending a main promotion and a caller-backed implementation ticket.

2026-08-20 — Operator clarification: every document/report type must call one shared function/service in the Pegasus .NET monolith. A report type may supply a different approved template to that caller, but it may not acquire a type-specific caller or service. [[TICK-081]] now owns the future shared-caller activation. This does not assert that Audit has an active caller today; FRD-11's closed activation boundary remains factual until EXT-08 is activated.

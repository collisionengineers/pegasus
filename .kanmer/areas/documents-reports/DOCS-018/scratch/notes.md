2026-09-02 kanmer-research wrapper (Claude Fable 5.1 around gpt-5.6-terra medium): Codex ran read-only in `.worktrees/research` at origin/dev 897db953, exit 0, checkout clean afterwards. Research and files written; no operator-only open questions. Planner finding: the fee-note PDF artifact already exists (`AssessmentReportDraft.FeeNote`, `assessment_fee_note.scriban`); the gap is a read-only `PreviewFeeNote` handler and a "Preview fee note" control on the Report section, whose host files (`_CaseReport.cshtml`, Case handler host) are owned by ENG-034 then ENG-029 — the plan must sequence DOCS-018 after ENG-029 or record the hand-off in ENG-029's plan so DOCS-018 ends with a named production caller. Kanmer MCP responses carried only project metadata this session; writes were confirmed on disk in the board worktree.

2026-09-02 kanmer-plan wrapper (Claude Fable 5.1 around gpt-5.6-terra high): Codex ran read-only in `.worktrees/research` at origin/dev 897db953, exit 0, checkout clean afterwards. Plan and checklist written; files.md revised to the plan's owned set. Hand-off settled as option (a): DOCS-018 follows CASE-038 → ENG-034 → ENG-029 and then owns the whole D42 feature (handler in `Details.cshtml.cs`, one anchor in `_CaseReport.cshtml`, one `OperatorLabels` key, Web test); `_CaseFeeNote.cshtml` is not created. Wrapper corrections: the Test UI snapshot commands do apply (verify byte-compares the `case-details--*` captures, which compose `_CaseReport.cshtml`); owned set refined. No operator-only open questions. Kanmer MCP responses carried only project metadata again; writes confirmed on disk. Ticket left in preparing.

2026-09-03 cross-model plan review wrapper (Claude Opus around gpt-5.6-sol xhigh): sol read the plan independently in `.worktrees/research` at origin/dev 897db953, exit 0, checkout clean afterwards (`git status --porcelain` empty). Verdict REQUEST CHANGES with 3 findings; I added 2 wrapper findings and dispositioned all 5. Accepted and fixed: ownership reallocation must be recorded on ENG-029/UIIMP-014 before any shared-file lease (new "Ownership reallocation" section + checklist step 1a); the fee-note Web test must first give `FakeRenderer` distinct per-family bytes (both artifacts share `pdfBytes` today, so the assertion could not fail) and must cover ready/not-ready/404 for the new GET; step 3 uses the existing `asp-page-handler` tag helpers instead of a hand-built `?handler=` URL. Rejected with evidence: the reviewer's ungated `Preview fee note` + separate fee-note projection path — FRD-11:130-133 defines the preview as the renderer's emitted artifact, `AssessmentPolicy.cs:256` already requires `fee.agreed_fee` for readiness so the "fee-ready but report-incomplete" Case cannot exist, and a fee-note-only projection is a second Core implementation for one caller; and the `Category!=Browser` test filter, which is correct (`AssessmentReportDraftWebTests` is `Category=SqlServer`, and the Browser complement runs inside `Update-TestUiSnapshots.ps1`, mirroring ci.yml:178/:236). No operator question raised; no open-questions document needed. Plan and checklist rewritten with `set_ticket_doc` and confirmed on disk; Kanmer MCP still returns only project metadata. Ticket left in `preparing` — not moved.

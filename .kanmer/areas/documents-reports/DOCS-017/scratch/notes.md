2026-09-02 kanmer-research (Claude wrapper over gpt-5.6-terra medium): Codex ran read-only in `.worktrees/research` (origin/dev); worktree clean afterwards. Prompt/output kept in the session scratchpad `prep/DOCS-017/`. Research and files docs written; three-plus VERIFIED claims spot-checked in the main checkout (one line-number correction, one missing call site added). No operator questions. Ticket moved backlog → preparing. Note for plan: DELIV-041 (blocker) has not yet landed D31 in FRD-11/FRD-04 on `dev`; the CASE-040 / PLAT-068 seams do not exist yet, so the plan must define the `ReportEngineer` tuple contract DOCS-017 owns and what it consumes.

2026-09-02 kanmer-plan (Claude wrapper over gpt-5.6-terra high): plan run started. Since research: DELIV-041 merged to dev (897db953, PR #647) — FRD-11 lines 75-95/338-340 and FRD-04 77-88 now carry D31; PLAT-068 and CASE-040 research treat DOCS-017 as owner of the Core signatory contract and the renderer (DOCS-017 blocks both). First Codex invocation failed (Windows "Argument list too long" for the 39 KB prompt argument); retried with the prompt on stdin.

2026-09-02 kanmer-plan (Claude wrapper over gpt-5.6-terra high): plan/plan.md and checklist/checklist.md written (activity 22:34:52Z / 22:34:58Z). Codex output kept in the session scratchpad `prep/DOCS-017/plan-out.md`; `.worktrees/research` clean afterwards. Chosen seam: `ReportSignatory(Name, Qualifications?, SignatureContent, SignatureContentType)` on `AssessmentReportProjectionInput`; Core removes the dictionary/key; interim production behaviour is the fail-closed `Sign-off Engineer` readiness item until CASE-040 + PLAT-068 populate the tuple. No operator questions. Ticket left in preparing; gates for leaving preparing (research, files, plan, checklist) all exist on disk. Kanmer MCP still returns an empty payload on every call although writes land.

2026-09-03 cross-model plan review (gpt-5.6-sol xhigh reads, Claude Opus dispositions): sol ran read-only in `.worktrees/research` at `897db953`; `git status --porcelain` clean afterwards. Verdict REQUEST CHANGES, nine findings (5 blocker, 4 should-fix). Six fixed, one partly fixed with an accepted and recorded risk, two rejected with evidence (fabricated-fixture claim — the values are the D43-permitted mockup fixtures; OperatorLabels indirection — every existing Core readiness Requirement is already rendered verbatim). No operator question raised, so `open-questions/` stays absent. plan/plan.md and checklist/checklist.md rewritten with `set_ticket_doc` and confirmed on disk (400 / 22 lines). Ticket not moved; still `preparing`. Prompt and output kept in the session scratchpad `prep2/DOCS-017/`. Kanmer MCP still returns only the project fingerprint on every call; writes land.

Substantive corrections worth carrying into implementation: the routed Assessment page calls `AssessmentReportProjection.Prepare` directly at `Index.cshtml.cs:459`, so the new readiness item changes `docs/design/test-ui/pages/case-assessment--default.html:183,188` and the Test UI snapshot must be regenerated (a new step 6, plus the shared-lock claim); the verification filter moved to the canonical `Category!=Corpus` because `AssessmentReportRendererTests` is `[Trait("Category","Browser")]`; `TemplateVersion` bumps once to `rendererref1-v2`; and a symbol-level co-ownership table now covers the ENG-035 overlap on `AssessmentReportProjection.cs`, `Infrastructure/Reports/**` and the projection tests, which `files/files.md` had not mentioned at all.

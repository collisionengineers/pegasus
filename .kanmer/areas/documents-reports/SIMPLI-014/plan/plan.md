# Plan — SIMPLI-014: Integrate CollisionRenderer behind a Core-owned render contract

## Approach

Fold only the caller-backed CollisionRenderer engine and approved assessment/fee-note surface into the existing four-project Pegasus monolith: a small typed port and application use case in `Pegasus.Core`, one Scriban/Playwright/PDFsharp adapter inside `Pegasus.Infrastructure`, and composition in the existing always-warm `Pegasus.Web` Container App. Reuse the workspace's single `DocumentRenderer` pipeline, validators, HTML composition, Chromium engine, PDF page/evidence helpers and fake-engine test seam, but rename/re-home them so none is mistaken for domain Core. Pin the existing governed design assets as embedded resources. Retire the standalone API/CLI/MCP/MCPB, arbitrary template/path rendering, unsupported catalogue entries and workspace build boundary. This beats retaining a fifth project/service/package (forbidden by ADR-0025 and the repository architecture) and beats rewriting the proven renderer (duplicate implementation and unnecessary parity risk).

This ticket establishes the Core caller → Infrastructure adapter → composed Web boundary and proves a representative accepted render through that boundary. [[DOCS-001]] remains the owner of automatic “assessment complete” triggering, durable idempotent job/reference/custody persistence and correction lineage; [[PLAT-007]] remains the owner of Azure deployment/runtime proof. SIMPLI-014 therefore exposes no staff/API/MCP render endpoint and does not claim approval, issue, sending or deployment.

## Governing docs

- **Modifies and meets — `docs/frd/frd-11-reports-correspondence-and-reviewed-proposals.md`.** The operator explicitly resolved the report behaviour on 2026-08-19. Record the initial closed activation surface (rendererref1 assessment + fee note only), four outcomes, Core-owned readiness/calculation/template selection, exact supplied wording/signature constraints, normal page flow, unavailable Audit/unsupported families, and the distinction between generation and approval/issue. Steps 1–2 turn approved evidence into normative behaviour; steps 3–8 implement and prove it.
- **Meets — `docs/adr/0025-integrate-renderer-and-extractor-into-the-application.md`.** Steps 2–8 integrate the renderer into existing projects behind a Core port, use a real Core application caller, preserve caller-backed tests and representative parity, retire the source workspace/standalone hosts, and do not introduce a repository/package/service/deployment unit.
- **Also conforms to ADR-0002 and ADR-0009.** The project graph remains Core ← Infrastructure with Web as composition root; the work provides the contract, caller, parity/security/licence/build/recovery evidence available locally. No new ADR is needed because the existing four-project boundary carries the change. Production runtime/capacity/recovery evidence remains explicitly assigned to PLAT-007 before deployment.

## Steps

1. **Make the approved behaviour authoritative.** Update FRD-11 with the operator-resolved initial activation contract: exactly `total_loss | repairable | cash_in_lieu | contract_repair`; assessment and fee-note families only; Core owns readiness, selected accepted source versions, computations and template choice; missing/mismatched wording/signature data fails closed; generic/Audit/other catalogue families are unavailable; generation is a draft artifact and never approval/issue/send. Reuse the existing FRD-11 finality/provenance requirements rather than create a second report rules document. Do not modify `reference/rendererref1/`.
2. **Add the smallest Core-owned rendering boundary and caller.** Under `src/Pegasus.Core/Reports/`, define a closed request/result contract for the approved assessment/fee-note operation, technical-neutral artifact bytes/metadata, template/payload/calculation identity, cancellation and typed failures, plus a Core application use case that invokes the port. Reuse existing assessment outcome/readiness vocabulary and accepted source-version concepts; do not duplicate assessment policy, custody, idempotency or durable report-reference logic owned by TICK-092/093/094 and DOCS-001. The use case accepts an already-approved immutable render snapshot so this ticket can prove the real application caller without inventing automatic triggering.
3. **Migrate and simplify the renderer engine into Infrastructure.** Move the reusable workspace pipeline into `src/Pegasus.Infrastructure/Reports/`: renderer orchestration, Scriban composition, validation, Chromium/PDF engine, formatting, placeholder/resource handling, page counting/evidence append and the fakeable PDF seam. Rename namespaces/types to the Pegasus adapter boundary and make arbitrary local paths/base64/caller-selected density/template IDs unavailable from the Core contract. Reuse the existing code where behaviour is proven; delete workspace-only authoring/preview/local-output mechanics unless directly required by the approved family.
4. **Implement the closed assessment/fee-note adapter and resources.** Map the Core request to the four approved rendererref1 outcomes and fee note using fixed templates/components, Core-supplied computed values and accepted wording only. Embed `docs/design/assets/report-renderer/**` and `docs/design/brand/**` from `Pegasus.Infrastructure.csproj` with stable logical names, assert the complete expected resource set, use normal/default density with clean page flow, and fail closed on unknown family/outcome, placeholder content, absent/mismatched signature, untrusted attachment or incomplete request. Unsupported workspace catalogue entries never enter application discovery or dispatch.
5. **Compose the adapter in the existing Web boundary.** Register the renderer and Core use case through existing `Pegasus.Infrastructure.DependencyInjection` and `Pegasus.Web` composition conventions, with one safe reusable Chromium lifetime and disposal boundary. Add no HTTP, Razor, MCP, CLI or background trigger surface. Keep Worker unchanged; PLAT-007 later extends the existing Web image/runtime and DOCS-001 later calls the use case automatically.
6. **Retire the standalone workspace boundary.** Remove `workspaces/report-renderer/` after all required source/tests/history references are accounted for; retire CollisionRenderer.Api/Cli/Mcp/MCPB/Dockerfile and host-specific tests, update `workspaces/README.md` without altering provenance, remove only the renderer job from `.github/workflows/workspaces.yml`, and update `Pegasus.slnx`/architecture tests to assert the four production projects, Core port, single Infrastructure implementation and absence of workspace/standalone host references. Preserve workspace ADR history through Git and any necessary contextual citations rather than a second live implementation.
7. **Reconcile repository build and dependency conventions.** Add only Scriban, Playwright and PDFsharp dependencies actually used by Infrastructure/tests; inherit root latest-recommended analysis and warnings-as-errors; fix surfaced warnings rather than retain the workspace policy; regenerate the existing Pegasus project `packages.lock.json` files and use locked restore. Add the Chromium installation/test setup to the main CI path that exercises renders, without importing retired host dependencies.
8. **Migrate focused tests and prove the composed boundary.** Re-home/adapt useful workspace unit tests for validation, formatting, resource completeness, HTML composition, browser launch and PDF generation; remove tests whose only subject was standalone transport/authoring. Add Core tests for the closed contract and Infrastructure/integration tests that resolve the use case from Web composition, render at least one approved rendererref1 assessment/fee-note fixture through real Chromium, assert PDF signature/page count/hash/template+engine metadata and no placeholders, and prove unknown/unsupported families, local paths and mismatched signatures fail closed. Add deterministic fake-engine tests for fast coverage; retain real-PDF evidence under `artifacts/`, never `reference/`.
9. **Run the required simplification and documentation pass.** Review the branch diff through reuse, simplification, efficiency and altitude lenses; remove duplicate vocabularies/wrappers/copies, apply behaviour-preserving findings, and append a dated “Simplification pass” section with dispositions to this plan. Update `docs/current-architecture.md` to the as-built local source/composition state and `docs/operations.md` only to state accurately that the integrated source/caller is not yet deployed; leave Azure runtime/deployment claims to PLAT-007. Write the post-implementation report with exact files, deviations and evidence.

## Verification

The post-implementation report will record:

- `dotnet restore --locked-mode`
- `dotnet build --configuration Release --no-restore`
- focused Core, Infrastructure/integration and architecture tests for Reports, dependency direction, resources and Web composition
- `dotnet test --configuration Release --no-build` (full solution)
- the repository documentation-link/placement checks required by the runbook/CI
- a real Chromium render through the Web-composed Core use case using an approved rendererref1 fixture, with retained artifact path, SHA-256, page count, engine/template identity, placeholder scan and representative comparison notes
- negative proof that legacy/unknown templates, arbitrary local attachment paths, missing/mismatched signatures and incomplete payloads are rejected
- `rg`/architecture-test proof that no application or build path retains `workspaces/report-renderer`, CollisionRenderer.Api, CollisionRenderer.Mcp/MCPB or a fifth production project
- package-lock/advisory output and resource-manifest completeness
- simplification findings and dispositions

This is implementation/local-caller evidence only. Proof of the automatic accepted-assessment trigger, durable report identity/custody/retry/correction is DOCS-001; container/IaC/deployed Chromium/health/telemetry/capacity proof is PLAT-007 and requires exact-target cloud approval.

## Risks / open questions

- **Cross-ticket overlap:** TICK-092/093/094/RPT-02 and DOCS-001 own data readiness and durable workflow. Mitigation: the Core render use case consumes an immutable approved snapshot contract and does not create a second editable assessment/report record; merge/rebase against those owners and stop on conflicting ownership.
- **Workspace removal losing useful code/history:** migrate by explicit inventory, keep Git history and contextual ADR citations, and verify every retained resource/test before deletion.
- **Chromium lifecycle/resource use:** reuse the existing `IPdfEngine` seam, serialize/bound access where required, dispose with the host, and prove locally; deployed capacity remains PLAT-007.
- **Visual parity:** compare representative approved fixtures and test stable structural/wording/resource invariants; do not rely only on byte equality where PDF metadata can vary.
- **Scope creep from 12 legacy templates:** application dispatch is a closed assessment/fee-note operation; unsupported families are omitted, not dormant.
- **No unresolved operator question:** all SIMPLI-014 questions are ticked. Audit is explicitly deferred, MCP/MCPB retired, Web is the selected existing execution boundary, normal density applies, root analyzers/locks govern, and exact supplied wording/signatures are accepted under fail-closed matching and human approval.

## Simplification pass — 2026-08-19

Two independent agents reviewed the branch diff through reuse, simplification, efficiency, and altitude lenses before the PR. All findings were applied:

- moved the closed accepted engineer tuple and every outcome settlement/cap calculation from Infrastructure into the single Core policy owner; Infrastructure now formats an already-approved presentation only;
- made assessment and fee-note artifacts explicit typed result slots instead of a list plus repeated magic family strings and scans;
- made `AssessmentReportContract.TemplateVersion` the single version owner shared by Core, Infrastructure, and tests;
- replaced template/signature wildcards with the exact active assessment/fee-note templates, stylesheet, logo, and Andy Patterson signature; Ed Mawdsley and Neil O'Reilly remain governed source assets but are not shipped or selectable while their exact qualifications remain absent;
- restored the predecessor's immutable-resource/template caching pattern and retained one serialized reusable Chromium lifetime;
- renamed the real-browser test to its actual application-composition evidence tier and made artifact writes opt-in through `PEGASUS_RENDER_EVIDENCE`, so ordinary CI asserts output without unrelated filesystem effects.

No findings were deferred. The evidence conflict discovered during the pass was resolved fail-closed: `rendererref1` explicitly marks Ed/Neil qualifications “to be confirmed”, so no value was invented despite general operator approval.

A final composition check also found that registering the renderer inside the shared `AddPegasusInfrastructure` method would have composed it in Worker as well as Web. The registration was moved to the explicit `AddPegasusReportRendering` extension called only by `Pegasus.Web`, preserving ADR-0028 and leaving Worker unchanged.

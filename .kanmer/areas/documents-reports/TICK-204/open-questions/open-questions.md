# Open questions — TICK-204

- [ ] Operator: confirm that the canonical assessment-report outcome vocabulary is exactly `total_loss | repairable | cash_in_lieu | contract_repair`, with contract repair a distinct fourth variant using the dedicated title/badge and capped agreed-repair wording shown in `reference/rendererref1`, despite `DESIGN_SPEC.md` also containing stale “three outcomes” and three-value “complete set” sentences. Recommendation: **confirm the four-value set** because `RPT-02` already requires four variants and the supplied table, schema, JSON fixtures, and PDFs all independently include contract repair.

## Parked (explicitly deferred)

None.

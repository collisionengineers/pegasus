# Open questions

No operator-only question remains for this ticket.

The production execution choice is constrained by the operator's no-separate-system direction and current supported runtime facts: use the existing Pegasus Web Container App. Resource sizing is an implementation measurement, not a product decision.

## Parked (explicitly deferred)

- A future detached Container Apps Job or other renderer deployment may be reconsidered only if measured reliability or capacity evidence proves the existing Web boundary cannot carry rendering; it would require a new accepted ADR and explicit cloud approval.

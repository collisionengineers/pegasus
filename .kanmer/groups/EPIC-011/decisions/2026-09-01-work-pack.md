# Supplied work-pack decisions — 2026-09-01

This record binds the EPIC-011 reconciliation against the local planning pack at `C:/Users/PGUSER/Documents/github/pegasus-work-pack`. The pack is evidence and a planning aid; repository PRDs, FRDs, ADRs, operator notes, and ticket-specific pipeline documents remain authoritative in their documented scopes.

## Committed prototype scope

Prototype functionality is committed for ticket planning except for these four retained exclusions:

1. autonomous outbound mail;
2. a standalone Images list;
3. runtime-managed templates;
4. direct Glass's or Audatex service-launch controls.

Staff-initiated outbound delivery remains in scope. Glass's and Audatex file imports remain in scope.

## Evidence and reporting decisions

- All eight supplied PDFs are mandatory parser fixtures.
- The visually valid YL69 sample has an unusable embedded character map and requires the Azure Document Intelligence path.
- Import assessment-relevant fields only; source addresses and diagrams remain source evidence and are not new case-data requirements.
- Report images require two distinct selected images: Close-up first and Overview second. Supporting images are optional.
- Image preparation is non-destructive: preserve the retained source and store normalized crop/selection output through the existing custody and versioning conventions.
- Administrator password reset uses an administrator-entered temporary password and the existing forced-change state; no reset email is required.

## Allocated follow-up tickets

- [[ENG-031]] — evidence-image preparation and report-image selection.
- [[ENG-032]] — Audatex full-report variant reconciliation.
- [[PLAT-064]] — administrator-initiated password reset.
- [[MAIL-030]] — staff-initiated outbound delivery policy and activity.
- [[MAIL-031]] — mailbox synchronisation and message-state policy.
- [[PLAT-065]] — Azure Document Intelligence provisioning and activation.
- [[MAIL-032]] — selected Inbox preview persistence.
- [[MAIL-033]] — sparse Graph delta cursor advancement.
- [[KANMER-010]] — Kanmer setup-drift reconciliation.

[[KANMER-009]] owns only organization of the supplied work pack and this board reconciliation; it does not implement product capabilities.

## Interface decisions confirmed binding on 2026-09-01

The operator confirmed on 2026-09-01 (evening) that the "Required interface changes" and "Locked decisions" sections of the work pack's orchestration plan are binding operator decisions. They are numbered D15–D28 in `context.md`. The governing documents that conflict with or are silent on them are corrected by one docs chore ticket before any dependent feature ticket leaves Backlog; the Claude Code runbook at `pegasus-work-pack/orchestration/claude/orchestration-plan.md` §11 lists the files per decision. Text as confirmed:

### Estimate rate cards and calculations

- Multiple global, versioned labour-rate cards: ID, name, non-paint hourly rate, enabled state, actor, and timestamps.
- Staff select a card for every new or amended estimate version.
- Disabling blocks future selection without changing history.
- Imported provider versions and their printed totals remain immutable.
- An Engineer successor selects a card and can become the accepted/report version.
- Non-paint labour = normalized non-paint hours × selected rate.
- Paint labour, paint materials, parts, and other costs are explicit amounts.
- VAT percentage belongs to the estimate version and applies to the whole subtotal.
- Betterment and guide codes are retained as evidence only.
- No original-versus-assessed savings feature exists.

### Raw estimate import

One shared Core command accepts Case ID, expected version, edit lease, operation key, filename, media type, bytes, and channel.

The Web caller: accepts one file dropped anywhere on the Assessment page; imports immediately with no confirmation and no visible picker; accepts only currently registered parser types; auto-detects provider/parser and fails closed on ambiguity; names Drafts by provider plus sequence; records filename, hash, provider/parser, actor, channel, and outcome.

The MCP caller reuses the same use case and accepts `case_id`, `expected_version`, `edit_lease_token`, `operation_key`, `file_name`, `media_type`, and base64 bytes. It returns Draft identity/name/status, replay state, source hash, parser/provider, and structured blockers/errors.

Same Case plus same hash is an idempotent replay. A different artifact creates the next immutable Draft.

### Triage

- `History` merges durable events and append-only attributable staff notes in chronological order.
- Corrections are new notes; edit/delete operations do not exist.
- `Files` contains retained request sources/attachments and linked vehicle images with view/download.
- No arbitrary Triage file store or upload action is added.

### Direct Case creation

Staff enter required identity and attach or record the instruction. Pegasus persists an attributable intake receipt, then reuses normal principal and Case/PO allocation policy. No parallel allocation implementation is allowed.

### Reports and signatories

- Any user in the Engineer role may issue a report.
- Reports render typed Engineer identity only; handwritten signature assets and qualification strings are not required.
- Generation remains deterministic, versioned, retained, and review-gated.
- Generation, approval, issue, sending, receipt, and Case closure remain distinct events.

### Report images

- Image preparation is non-destructive; retained source bytes and hashes never change.
- A report requires distinct images designated `Close-up` first and `Overview` second.
- Optional supporting images follow in explicit operator order.
- Crop and ordering data are normalized, versioned, attributable, and protected by expected-version/edit-lease rules.
- An issued report retains the exact curation snapshot and source hashes even after later Case-image or curation changes.

### Uploads

- A public link accepts one successful submission.
- Identical retry reconciles the same result; a different later submission, revocation, or expiry is refused without Case disclosure.
- Authenticated staff `/Upload` remains available only with durable production intake/case custody; no production-local-only store is accepted.
- File limit 100 MB; aggregate multipart request approximately 200 MB; Provider API envelope remains 30 MB.
- A grouped upload exposes one submission decision and per-file processing and outcome details beneath it.

### Mail and settings

- Mail freshness is a fixed 15 minutes.
- No historical backfill; show the genuine retention-start boundary.
- Delete means recoverable move to Deleted Items; permanent deletion is absent.
- Completeness is a versioned required/not-required set with exact blockers, never a percentage.
- Chase interval is one global whole-calendar-day value, range 1–365, default 7, calculated in Europe/London; Held preserves remaining time.
- AI target estimate is an optional 0–100% request value with no default. Its amount is derived visibly from Engineer's Value and is proposal guidance only.

### Provider seams

- Experian vehicle-history and Cazana valuation actions remain disabled future seams owned by their existing backlog tickets.
- Manual Glass's, Cazana, and Engineer valuation records remain active.
- Direct Assessment Glass's/Audatex service-launch controls are absent.

### Locked programme rules

- Adjacent work-pack HTML (`Pegasus_UI_Frontend_Design_Premium_Full_End_State.html`) is the canonical execution visual source.
- Visual conflicts pause only the affected lane/dependants.
- Production acceptance requires merged-main proof, deployment/activation, live caller evidence, and operator visual signoff.
- There is one final production release after all dev PRs (one deployment, two promotions: the release SHA and the promotion-only current-state docs SHA).
- Done debt is audited only; functional gaps create new linked tickets.
- Green independently reviewed ticket PRs may merge to `dev`; `dev` → `main` still requires explicit merge authority.
- PR #640/#641 are salvageable only after an independent adoption audit; PR #639 begins at the escalated implementer tier.
- Grouped upload uses one group decision plus per-file status detail.
- Real workflow controls use state/permission gates and real handlers.
- Triage files exclude arbitrary uploads.
- Direct Create Case uses a staff instruction receipt.
- Whole-page Assessment drop is immediate, one file, parser-autodetected, and has a documented pointer-only accessibility exception.
- No raw Azure OCR response is retained.
- OCR resource/SKU/volume/RBAC are measured and approved later, not guessed; the confidence threshold is measured then operator-approved.
- Rate cards are multiple global versioned cards selected per estimate version; they price non-paint labour only.
- VAT is estimate-version data and applies to the whole subtotal.
- Imported provider versions are preserved; accepted successor versions use a selected card.
- Estimate comparison/savings is removed.
- Betterment/guide codes are evidence only.
- Any Engineer may sign using typed identity only.
- Report images require distinct Close-up #1 and Overview #2, optional ordered supporting images, non-destructive normalized crops, and an immutable issued curation snapshot.
- Public upload links allow one successful submission.
- Staff production upload requires durable custody.
- Upload limits are 100 MB per file and approximately 200 MB per request.
- Mail freshness is fixed at 15 minutes and history is not backfilled.
- AI target percentage is optional, has no default, and is guidance only.
- Provider live integrations are approved exclusions; the 2,000-case capacity tier is recorded as not run (tier-10 cohort/soak evidence), never as passing, and its evidence spike sits outside this programme.
